#import "GeneratedPluginRegistrant.h"

// A stable application-owned C ABI symbol links the Rust static library into
// the iOS executable even when Flutter Rust Bridge emits an empty C header.
void camellia_remote_ios_link_anchor(void);
