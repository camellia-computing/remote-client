import 'package:flutter/widgets.dart';

class WebVideoSurface extends StatelessWidget {
  final String peerId;

  const WebVideoSurface({super.key, required this.peerId});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
