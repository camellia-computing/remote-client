{
  "name": "camellia-web-bridge",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "vite build && npm run check:build",
    "check:build": "node tools/check-web-build.mjs",
    "dev": "vite",
    "generate:proto": "node tools/generate-proto.mjs",
    "lint": "npm run test:security && npm run test:proto && tsc -p tsconfig.json --noEmit",
    "test:proto": "node --test test/proto_contract.test.ts",
    "test:security": "node --test test/direct_access_policy.test.ts test/secretbox_direction.test.ts"
  },
  "dependencies": {
    "fzstd": "^0.1.1",
    "protobufjs": "8.7.1",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "8.1.5"
  }
}
