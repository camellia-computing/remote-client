// Generated by `npm run generate:proto`; do not edit.
// camellia-proto-source-sha256: 82d29641020da9ee12362179ed6fcdca05e6a4a3a47e76d736d43e22df27eeca
/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $TypeError = $util.global.TypeError, $Boolean = $util.global.Boolean, $Number = $util.global.Number, $parseInt = $util.global.parseInt, $String = $util.global.String, $Array = $util.global.Array, $BigInt = $util.global.BigInt, $isFinite = $util.global.isFinite;

const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const hbb = $root.hbb = (() => {

    const hbb = {};

    hbb.EncodedVideoFrame = (function() {

        const EncodedVideoFrame = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        EncodedVideoFrame.prototype.data = $util.newBuffer([]);
        EncodedVideoFrame.prototype.key = false;
        EncodedVideoFrame.prototype.pts = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        EncodedVideoFrame.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                writer.uint32(10).bytes(message.data);
            if (message.key != null && $Object.hasOwnProperty.call(message, "key") && message.key !== false)
                writer.uint32(16).bool(message.key);
            if (message.pts != null && $Object.hasOwnProperty.call(message, "pts") && (typeof message.pts === "object" ? message.pts.low || message.pts.high : message.pts !== 0))
                writer.uint32(24).int64(message.pts);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        EncodedVideoFrame.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.EncodedVideoFrame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.key = value;
                        else
                            delete message.key;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.pts = value;
                        else
                            delete message.pts;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        EncodedVideoFrame.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.EncodedVideoFrame)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.EncodedVideoFrame: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.EncodedVideoFrame();
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            if (object.key != null)
                if (object.key)
                    message.key = $Boolean(object.key);
            if (object.pts != null)
                if (typeof object.pts === "object" ? object.pts.low || object.pts.high : $Number(object.pts) !== 0)
                    if ($util.Long)
                        message.pts = $util.Long.fromValue(object.pts, false);
                    else if (typeof object.pts === "string")
                        message.pts = $parseInt(object.pts, 10);
                    else if (typeof object.pts === "number")
                        message.pts = object.pts;
                    else if (typeof object.pts === "object")
                        message.pts = new $util.LongBits(object.pts.low >>> 0, object.pts.high >>> 0).toNumber();
            return message;
        };

        EncodedVideoFrame.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== $Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.key = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.pts = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.pts = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
            if (message.key != null && $Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.pts != null && $Object.hasOwnProperty.call(message, "pts"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.pts = typeof message.pts === "number" ? $BigInt(message.pts) : $util.Long.fromBits(message.pts.low >>> 0, message.pts.high >>> 0, false).toBigInt();
                else if (typeof message.pts === "number")
                    object.pts = options.longs === $String ? $String(message.pts) : message.pts;
                else
                    object.pts = options.longs === $String ? $util.Long.prototype.toString.call(message.pts) : options.longs === $Number ? new $util.LongBits(message.pts.low >>> 0, message.pts.high >>> 0).toNumber() : message.pts;
            return object;
        };

        EncodedVideoFrame.prototype.toJSON = function() {
            return EncodedVideoFrame.toObject(this, $protobuf.util.toJSONOptions);
        };

        return EncodedVideoFrame;
    })();

    hbb.EncodedVideoFrames = (function() {

        const EncodedVideoFrames = function (properties) {
            this.frames = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        EncodedVideoFrames.prototype.frames = $util.emptyArray;

        EncodedVideoFrames.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.frames != null && message.frames.length)
                for (let i = 0; i < message.frames.length; ++i)
                    $root.hbb.EncodedVideoFrame.encode(message.frames[i], writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        EncodedVideoFrames.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.EncodedVideoFrames();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.frames && message.frames.length))
                            message.frames = [];
                        message.frames.push($root.hbb.EncodedVideoFrame.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        EncodedVideoFrames.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.EncodedVideoFrames)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.EncodedVideoFrames: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.EncodedVideoFrames();
            if (object.frames) {
                if (!$Array.isArray(object.frames))
                    throw $TypeError(".hbb.EncodedVideoFrames.frames: array expected");
                message.frames = $Array(object.frames.length);
                for (let i = 0; i < object.frames.length; ++i) {
                    if (!$util.isObject(object.frames[i]))
                        throw $TypeError(".hbb.EncodedVideoFrames.frames: object expected");
                    message.frames[i] = $root.hbb.EncodedVideoFrame.fromObject(object.frames[i], _depth + 1);
                }
            }
            return message;
        };

        EncodedVideoFrames.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.frames = [];
            if (message.frames && message.frames.length) {
                object.frames = $Array(message.frames.length);
                for (let j = 0; j < message.frames.length; ++j)
                    object.frames[j] = $root.hbb.EncodedVideoFrame.toObject(message.frames[j], options, _depth + 1);
            }
            return object;
        };

        EncodedVideoFrames.prototype.toJSON = function() {
            return EncodedVideoFrames.toObject(this, $protobuf.util.toJSONOptions);
        };

        return EncodedVideoFrames;
    })();

    hbb.RGB = (function() {

        const RGB = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        RGB.prototype.compress = false;

        RGB.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.compress != null && $Object.hasOwnProperty.call(message, "compress") && message.compress !== false)
                writer.uint32(8).bool(message.compress);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RGB.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.RGB(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.compress = value;
                        else
                            delete message.compress;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        RGB.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.RGB)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.RGB: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.RGB();
            if (object.compress != null)
                if (object.compress)
                    message.compress = $Boolean(object.compress);
            return message;
        };

        RGB.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.compress = false;
            if (message.compress != null && $Object.hasOwnProperty.call(message, "compress"))
                object.compress = message.compress;
            return object;
        };

        RGB.prototype.toJSON = function() {
            return RGB.toObject(this, $protobuf.util.toJSONOptions);
        };

        return RGB;
    })();

    hbb.YUV = (function() {

        const YUV = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        YUV.prototype.compress = false;
        YUV.prototype.stride = 0;

        YUV.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.compress != null && $Object.hasOwnProperty.call(message, "compress") && message.compress !== false)
                writer.uint32(8).bool(message.compress);
            if (message.stride != null && $Object.hasOwnProperty.call(message, "stride") && message.stride !== 0)
                writer.uint32(16).int32(message.stride);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        YUV.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.YUV(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.compress = value;
                        else
                            delete message.compress;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.stride = value;
                        else
                            delete message.stride;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        YUV.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.YUV)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.YUV: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.YUV();
            if (object.compress != null)
                if (object.compress)
                    message.compress = $Boolean(object.compress);
            if (object.stride != null)
                if ($Number(object.stride) !== 0)
                    message.stride = object.stride | 0;
            return message;
        };

        YUV.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.compress = false;
                object.stride = 0;
            }
            if (message.compress != null && $Object.hasOwnProperty.call(message, "compress"))
                object.compress = message.compress;
            if (message.stride != null && $Object.hasOwnProperty.call(message, "stride"))
                object.stride = message.stride;
            return object;
        };

        YUV.prototype.toJSON = function() {
            return YUV.toObject(this, $protobuf.util.toJSONOptions);
        };

        return YUV;
    })();

    hbb.Chroma = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "I420"] = 0;
        values[valuesById[1] = "I444"] = 1;
        return values;
    })();

    hbb.VideoFrame = (function() {

        const VideoFrame = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        VideoFrame.prototype.vp9s = null;
        VideoFrame.prototype.rgb = null;
        VideoFrame.prototype.yuv = null;
        VideoFrame.prototype.h264s = null;
        VideoFrame.prototype.h265s = null;
        VideoFrame.prototype.vp8s = null;
        VideoFrame.prototype.av1s = null;
        VideoFrame.prototype.display = 0;

        let $oneOfFields;

        $Object.defineProperty(VideoFrame.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["vp9s", "rgb", "yuv", "h264s", "h265s", "vp8s", "av1s"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        VideoFrame.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.vp9s != null && $Object.hasOwnProperty.call(message, "vp9s"))
                $root.hbb.EncodedVideoFrames.encode(message.vp9s, writer.uint32(50).fork(), _depth + 1).ldelim();
            if (message.rgb != null && $Object.hasOwnProperty.call(message, "rgb"))
                $root.hbb.RGB.encode(message.rgb, writer.uint32(58).fork(), _depth + 1).ldelim();
            if (message.yuv != null && $Object.hasOwnProperty.call(message, "yuv"))
                $root.hbb.YUV.encode(message.yuv, writer.uint32(66).fork(), _depth + 1).ldelim();
            if (message.h264s != null && $Object.hasOwnProperty.call(message, "h264s"))
                $root.hbb.EncodedVideoFrames.encode(message.h264s, writer.uint32(82).fork(), _depth + 1).ldelim();
            if (message.h265s != null && $Object.hasOwnProperty.call(message, "h265s"))
                $root.hbb.EncodedVideoFrames.encode(message.h265s, writer.uint32(90).fork(), _depth + 1).ldelim();
            if (message.vp8s != null && $Object.hasOwnProperty.call(message, "vp8s"))
                $root.hbb.EncodedVideoFrames.encode(message.vp8s, writer.uint32(98).fork(), _depth + 1).ldelim();
            if (message.av1s != null && $Object.hasOwnProperty.call(message, "av1s"))
                $root.hbb.EncodedVideoFrames.encode(message.av1s, writer.uint32(106).fork(), _depth + 1).ldelim();
            if (message.display != null && $Object.hasOwnProperty.call(message, "display") && message.display !== 0)
                writer.uint32(112).int32(message.display);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        VideoFrame.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.VideoFrame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.vp9s = $root.hbb.EncodedVideoFrames.decode(reader, reader.uint32(), $undefined, _depth + 1, message.vp9s);
                        message.union = "vp9s";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.rgb = $root.hbb.RGB.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rgb);
                        message.union = "rgb";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.yuv = $root.hbb.YUV.decode(reader, reader.uint32(), $undefined, _depth + 1, message.yuv);
                        message.union = "yuv";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.h264s = $root.hbb.EncodedVideoFrames.decode(reader, reader.uint32(), $undefined, _depth + 1, message.h264s);
                        message.union = "h264s";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.h265s = $root.hbb.EncodedVideoFrames.decode(reader, reader.uint32(), $undefined, _depth + 1, message.h265s);
                        message.union = "h265s";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.vp8s = $root.hbb.EncodedVideoFrames.decode(reader, reader.uint32(), $undefined, _depth + 1, message.vp8s);
                        message.union = "vp8s";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.av1s = $root.hbb.EncodedVideoFrames.decode(reader, reader.uint32(), $undefined, _depth + 1, message.av1s);
                        message.union = "av1s";
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.display = value;
                        else
                            delete message.display;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        VideoFrame.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.VideoFrame)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.VideoFrame: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.VideoFrame();
            if (object.vp9s != null) {
                if (!$util.isObject(object.vp9s))
                    throw $TypeError(".hbb.VideoFrame.vp9s: object expected");
                message.vp9s = $root.hbb.EncodedVideoFrames.fromObject(object.vp9s, _depth + 1);
            }
            if (object.rgb != null) {
                if (!$util.isObject(object.rgb))
                    throw $TypeError(".hbb.VideoFrame.rgb: object expected");
                message.rgb = $root.hbb.RGB.fromObject(object.rgb, _depth + 1);
            }
            if (object.yuv != null) {
                if (!$util.isObject(object.yuv))
                    throw $TypeError(".hbb.VideoFrame.yuv: object expected");
                message.yuv = $root.hbb.YUV.fromObject(object.yuv, _depth + 1);
            }
            if (object.h264s != null) {
                if (!$util.isObject(object.h264s))
                    throw $TypeError(".hbb.VideoFrame.h264s: object expected");
                message.h264s = $root.hbb.EncodedVideoFrames.fromObject(object.h264s, _depth + 1);
            }
            if (object.h265s != null) {
                if (!$util.isObject(object.h265s))
                    throw $TypeError(".hbb.VideoFrame.h265s: object expected");
                message.h265s = $root.hbb.EncodedVideoFrames.fromObject(object.h265s, _depth + 1);
            }
            if (object.vp8s != null) {
                if (!$util.isObject(object.vp8s))
                    throw $TypeError(".hbb.VideoFrame.vp8s: object expected");
                message.vp8s = $root.hbb.EncodedVideoFrames.fromObject(object.vp8s, _depth + 1);
            }
            if (object.av1s != null) {
                if (!$util.isObject(object.av1s))
                    throw $TypeError(".hbb.VideoFrame.av1s: object expected");
                message.av1s = $root.hbb.EncodedVideoFrames.fromObject(object.av1s, _depth + 1);
            }
            if (object.display != null)
                if ($Number(object.display) !== 0)
                    message.display = object.display | 0;
            return message;
        };

        VideoFrame.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.display = 0;
            if (message.vp9s != null && $Object.hasOwnProperty.call(message, "vp9s")) {
                object.vp9s = $root.hbb.EncodedVideoFrames.toObject(message.vp9s, options, _depth + 1);
                if (options.oneofs)
                    object.union = "vp9s";
            }
            if (message.rgb != null && $Object.hasOwnProperty.call(message, "rgb")) {
                object.rgb = $root.hbb.RGB.toObject(message.rgb, options, _depth + 1);
                if (options.oneofs)
                    object.union = "rgb";
            }
            if (message.yuv != null && $Object.hasOwnProperty.call(message, "yuv")) {
                object.yuv = $root.hbb.YUV.toObject(message.yuv, options, _depth + 1);
                if (options.oneofs)
                    object.union = "yuv";
            }
            if (message.h264s != null && $Object.hasOwnProperty.call(message, "h264s")) {
                object.h264s = $root.hbb.EncodedVideoFrames.toObject(message.h264s, options, _depth + 1);
                if (options.oneofs)
                    object.union = "h264s";
            }
            if (message.h265s != null && $Object.hasOwnProperty.call(message, "h265s")) {
                object.h265s = $root.hbb.EncodedVideoFrames.toObject(message.h265s, options, _depth + 1);
                if (options.oneofs)
                    object.union = "h265s";
            }
            if (message.vp8s != null && $Object.hasOwnProperty.call(message, "vp8s")) {
                object.vp8s = $root.hbb.EncodedVideoFrames.toObject(message.vp8s, options, _depth + 1);
                if (options.oneofs)
                    object.union = "vp8s";
            }
            if (message.av1s != null && $Object.hasOwnProperty.call(message, "av1s")) {
                object.av1s = $root.hbb.EncodedVideoFrames.toObject(message.av1s, options, _depth + 1);
                if (options.oneofs)
                    object.union = "av1s";
            }
            if (message.display != null && $Object.hasOwnProperty.call(message, "display"))
                object.display = message.display;
            return object;
        };

        VideoFrame.prototype.toJSON = function() {
            return VideoFrame.toObject(this, $protobuf.util.toJSONOptions);
        };

        return VideoFrame;
    })();

    hbb.IdPk = (function() {

        const IdPk = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        IdPk.prototype.id = "";
        IdPk.prototype.pk = $util.newBuffer([]);

        IdPk.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(10).string(message.id);
            if (message.pk != null && $Object.hasOwnProperty.call(message, "pk") && message.pk.length)
                writer.uint32(18).bytes(message.pk);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        IdPk.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.IdPk(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pk = value;
                        else
                            delete message.pk;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        IdPk.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.IdPk)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.IdPk: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.IdPk();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.pk != null)
                if (object.pk.length)
                    if (typeof object.pk === "string")
                        $util.base64.decode(object.pk, message.pk = $util.newBuffer($util.base64.length(object.pk)), 0);
                    else if (object.pk.length >= 0)
                        message.pk = object.pk;
            return message;
        };

        IdPk.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                if (options.bytes === $String)
                    object.pk = "";
                else {
                    object.pk = [];
                    if (options.bytes !== $Array)
                        object.pk = $util.newBuffer(object.pk);
                }
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.pk != null && $Object.hasOwnProperty.call(message, "pk"))
                object.pk = options.bytes === $String ? $util.base64.encode(message.pk, 0, message.pk.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pk) : message.pk;
            return object;
        };

        IdPk.prototype.toJSON = function() {
            return IdPk.toObject(this, $protobuf.util.toJSONOptions);
        };

        return IdPk;
    })();

    hbb.DisplayInfo = (function() {

        const DisplayInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        DisplayInfo.prototype.x = 0;
        DisplayInfo.prototype.y = 0;
        DisplayInfo.prototype.width = 0;
        DisplayInfo.prototype.height = 0;
        DisplayInfo.prototype.name = "";
        DisplayInfo.prototype.online = false;
        DisplayInfo.prototype.cursorEmbedded = false;
        DisplayInfo.prototype.originalResolution = null;
        DisplayInfo.prototype.scale = 0;

        DisplayInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.x != null && $Object.hasOwnProperty.call(message, "x") && message.x !== 0)
                writer.uint32(8).sint32(message.x);
            if (message.y != null && $Object.hasOwnProperty.call(message, "y") && message.y !== 0)
                writer.uint32(16).sint32(message.y);
            if (message.width != null && $Object.hasOwnProperty.call(message, "width") && message.width !== 0)
                writer.uint32(24).int32(message.width);
            if (message.height != null && $Object.hasOwnProperty.call(message, "height") && message.height !== 0)
                writer.uint32(32).int32(message.height);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(42).string(message.name);
            if (message.online != null && $Object.hasOwnProperty.call(message, "online") && message.online !== false)
                writer.uint32(48).bool(message.online);
            if (message.cursorEmbedded != null && $Object.hasOwnProperty.call(message, "cursorEmbedded") && message.cursorEmbedded !== false)
                writer.uint32(56).bool(message.cursorEmbedded);
            if (message.originalResolution != null && $Object.hasOwnProperty.call(message, "originalResolution"))
                $root.hbb.Resolution.encode(message.originalResolution, writer.uint32(66).fork(), _depth + 1).ldelim();
            if (message.scale != null && $Object.hasOwnProperty.call(message, "scale") && !$Object.is(message.scale, 0))
                writer.uint32(73).double(message.scale);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        DisplayInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.DisplayInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.x = value;
                        else
                            delete message.x;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.y = value;
                        else
                            delete message.y;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.width = value;
                        else
                            delete message.width;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.height = value;
                        else
                            delete message.height;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.online = value;
                        else
                            delete message.online;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.cursorEmbedded = value;
                        else
                            delete message.cursorEmbedded;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.originalResolution = $root.hbb.Resolution.decode(reader, reader.uint32(), $undefined, _depth + 1, message.originalResolution);
                        continue;
                    }
                case 9: {
                        if (wireType !== 1)
                            break;
                        if (!$Object.is(value = reader.double(), 0))
                            message.scale = value;
                        else
                            delete message.scale;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        DisplayInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.DisplayInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.DisplayInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.DisplayInfo();
            if (object.x != null)
                if ($Number(object.x) !== 0)
                    message.x = object.x | 0;
            if (object.y != null)
                if ($Number(object.y) !== 0)
                    message.y = object.y | 0;
            if (object.width != null)
                if ($Number(object.width) !== 0)
                    message.width = object.width | 0;
            if (object.height != null)
                if ($Number(object.height) !== 0)
                    message.height = object.height | 0;
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.online != null)
                if (object.online)
                    message.online = $Boolean(object.online);
            if (object.cursorEmbedded != null)
                if (object.cursorEmbedded)
                    message.cursorEmbedded = $Boolean(object.cursorEmbedded);
            if (object.originalResolution != null) {
                if (!$util.isObject(object.originalResolution))
                    throw $TypeError(".hbb.DisplayInfo.originalResolution: object expected");
                message.originalResolution = $root.hbb.Resolution.fromObject(object.originalResolution, _depth + 1);
            }
            if (object.scale != null)
                if (!$Object.is($Number(object.scale), 0))
                    message.scale = $Number(object.scale);
            return message;
        };

        DisplayInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.x = 0;
                object.y = 0;
                object.width = 0;
                object.height = 0;
                object.name = "";
                object.online = false;
                object.cursorEmbedded = false;
                object.originalResolution = null;
                object.scale = 0;
            }
            if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                object.x = message.x;
            if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                object.y = message.y;
            if (message.width != null && $Object.hasOwnProperty.call(message, "width"))
                object.width = message.width;
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.online != null && $Object.hasOwnProperty.call(message, "online"))
                object.online = message.online;
            if (message.cursorEmbedded != null && $Object.hasOwnProperty.call(message, "cursorEmbedded"))
                object.cursorEmbedded = message.cursorEmbedded;
            if (message.originalResolution != null && $Object.hasOwnProperty.call(message, "originalResolution"))
                object.originalResolution = $root.hbb.Resolution.toObject(message.originalResolution, options, _depth + 1);
            if (message.scale != null && $Object.hasOwnProperty.call(message, "scale"))
                object.scale = options.json && !$isFinite(message.scale) ? $String(message.scale) : message.scale;
            return object;
        };

        DisplayInfo.prototype.toJSON = function() {
            return DisplayInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        return DisplayInfo;
    })();

    hbb.PortForward = (function() {

        const PortForward = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PortForward.prototype.host = "";
        PortForward.prototype.port = 0;

        PortForward.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.host != null && $Object.hasOwnProperty.call(message, "host") && message.host !== "")
                writer.uint32(10).string(message.host);
            if (message.port != null && $Object.hasOwnProperty.call(message, "port") && message.port !== 0)
                writer.uint32(16).int32(message.port);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PortForward.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PortForward(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.host = value;
                        else
                            delete message.host;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.port = value;
                        else
                            delete message.port;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PortForward.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PortForward)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PortForward: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PortForward();
            if (object.host != null)
                if (typeof object.host !== "string" || object.host.length)
                    message.host = $String(object.host);
            if (object.port != null)
                if ($Number(object.port) !== 0)
                    message.port = object.port | 0;
            return message;
        };

        PortForward.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.host = "";
                object.port = 0;
            }
            if (message.host != null && $Object.hasOwnProperty.call(message, "host"))
                object.host = message.host;
            if (message.port != null && $Object.hasOwnProperty.call(message, "port"))
                object.port = message.port;
            return object;
        };

        PortForward.prototype.toJSON = function() {
            return PortForward.toObject(this, $protobuf.util.toJSONOptions);
        };

        return PortForward;
    })();

    hbb.FileTransfer = (function() {

        const FileTransfer = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileTransfer.prototype.dir = "";
        FileTransfer.prototype.showHidden = false;

        FileTransfer.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.dir != null && $Object.hasOwnProperty.call(message, "dir") && message.dir !== "")
                writer.uint32(10).string(message.dir);
            if (message.showHidden != null && $Object.hasOwnProperty.call(message, "showHidden") && message.showHidden !== false)
                writer.uint32(16).bool(message.showHidden);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileTransfer.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileTransfer(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.dir = value;
                        else
                            delete message.dir;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.showHidden = value;
                        else
                            delete message.showHidden;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileTransfer.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileTransfer)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileTransfer: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileTransfer();
            if (object.dir != null)
                if (typeof object.dir !== "string" || object.dir.length)
                    message.dir = $String(object.dir);
            if (object.showHidden != null)
                if (object.showHidden)
                    message.showHidden = $Boolean(object.showHidden);
            return message;
        };

        FileTransfer.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.dir = "";
                object.showHidden = false;
            }
            if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                object.dir = message.dir;
            if (message.showHidden != null && $Object.hasOwnProperty.call(message, "showHidden"))
                object.showHidden = message.showHidden;
            return object;
        };

        FileTransfer.prototype.toJSON = function() {
            return FileTransfer.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileTransfer;
    })();

    hbb.ViewCamera = (function() {

        const ViewCamera = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ViewCamera.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ViewCamera.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ViewCamera();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ViewCamera.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ViewCamera)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ViewCamera: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.hbb.ViewCamera();
        };

        ViewCamera.toObject = function () {
            return {};
        };

        ViewCamera.prototype.toJSON = function() {
            return ViewCamera.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ViewCamera;
    })();

    hbb.OSLogin = (function() {

        const OSLogin = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        OSLogin.prototype.username = "";
        OSLogin.prototype.password = "";

        OSLogin.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.username != null && $Object.hasOwnProperty.call(message, "username") && message.username !== "")
                writer.uint32(10).string(message.username);
            if (message.password != null && $Object.hasOwnProperty.call(message, "password") && message.password !== "")
                writer.uint32(18).string(message.password);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        OSLogin.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.OSLogin(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.username = value;
                        else
                            delete message.username;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        OSLogin.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.OSLogin)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.OSLogin: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.OSLogin();
            if (object.username != null)
                if (typeof object.username !== "string" || object.username.length)
                    message.username = $String(object.username);
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = $String(object.password);
            return message;
        };

        OSLogin.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                object.password = "";
            }
            if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                object.password = message.password;
            return object;
        };

        OSLogin.prototype.toJSON = function() {
            return OSLogin.toObject(this, $protobuf.util.toJSONOptions);
        };

        return OSLogin;
    })();

    hbb.LoginRequest = (function() {

        const LoginRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        LoginRequest.prototype.username = "";
        LoginRequest.prototype.password = $util.newBuffer([]);
        LoginRequest.prototype.myId = "";
        LoginRequest.prototype.myName = "";
        LoginRequest.prototype.option = null;
        LoginRequest.prototype.fileTransfer = null;
        LoginRequest.prototype.portForward = null;
        LoginRequest.prototype.viewCamera = null;
        LoginRequest.prototype.terminal = null;
        LoginRequest.prototype.videoAckRequired = false;
        LoginRequest.prototype.sessionId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        LoginRequest.prototype.version = "";
        LoginRequest.prototype.osLogin = null;
        LoginRequest.prototype.myPlatform = "";
        LoginRequest.prototype.hwid = $util.newBuffer([]);
        LoginRequest.prototype.avatar = "";
        LoginRequest.prototype.fileTransferProtocolVersion = 0;

        let $oneOfFields;

        $Object.defineProperty(LoginRequest.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["fileTransfer", "portForward", "viewCamera", "terminal"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LoginRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.username != null && $Object.hasOwnProperty.call(message, "username") && message.username !== "")
                writer.uint32(10).string(message.username);
            if (message.password != null && $Object.hasOwnProperty.call(message, "password") && message.password.length)
                writer.uint32(18).bytes(message.password);
            if (message.myId != null && $Object.hasOwnProperty.call(message, "myId") && message.myId !== "")
                writer.uint32(34).string(message.myId);
            if (message.myName != null && $Object.hasOwnProperty.call(message, "myName") && message.myName !== "")
                writer.uint32(42).string(message.myName);
            if (message.option != null && $Object.hasOwnProperty.call(message, "option"))
                $root.hbb.OptionMessage.encode(message.option, writer.uint32(50).fork(), _depth + 1).ldelim();
            if (message.fileTransfer != null && $Object.hasOwnProperty.call(message, "fileTransfer"))
                $root.hbb.FileTransfer.encode(message.fileTransfer, writer.uint32(58).fork(), _depth + 1).ldelim();
            if (message.portForward != null && $Object.hasOwnProperty.call(message, "portForward"))
                $root.hbb.PortForward.encode(message.portForward, writer.uint32(66).fork(), _depth + 1).ldelim();
            if (message.videoAckRequired != null && $Object.hasOwnProperty.call(message, "videoAckRequired") && message.videoAckRequired !== false)
                writer.uint32(72).bool(message.videoAckRequired);
            if (message.sessionId != null && $Object.hasOwnProperty.call(message, "sessionId") && (typeof message.sessionId === "object" ? message.sessionId.low || message.sessionId.high : message.sessionId !== 0))
                writer.uint32(80).uint64(message.sessionId);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
                writer.uint32(90).string(message.version);
            if (message.osLogin != null && $Object.hasOwnProperty.call(message, "osLogin"))
                $root.hbb.OSLogin.encode(message.osLogin, writer.uint32(98).fork(), _depth + 1).ldelim();
            if (message.myPlatform != null && $Object.hasOwnProperty.call(message, "myPlatform") && message.myPlatform !== "")
                writer.uint32(106).string(message.myPlatform);
            if (message.hwid != null && $Object.hasOwnProperty.call(message, "hwid") && message.hwid.length)
                writer.uint32(114).bytes(message.hwid);
            if (message.viewCamera != null && $Object.hasOwnProperty.call(message, "viewCamera"))
                $root.hbb.ViewCamera.encode(message.viewCamera, writer.uint32(122).fork(), _depth + 1).ldelim();
            if (message.terminal != null && $Object.hasOwnProperty.call(message, "terminal"))
                $root.hbb.Terminal.encode(message.terminal, writer.uint32(130).fork(), _depth + 1).ldelim();
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar") && message.avatar !== "")
                writer.uint32(138).string(message.avatar);
            if (message.fileTransferProtocolVersion != null && $Object.hasOwnProperty.call(message, "fileTransferProtocolVersion") && message.fileTransferProtocolVersion !== 0)
                writer.uint32(144).uint32(message.fileTransferProtocolVersion);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        LoginRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.LoginRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.username = value;
                        else
                            delete message.username;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.myId = value;
                        else
                            delete message.myId;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.myName = value;
                        else
                            delete message.myName;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.option = $root.hbb.OptionMessage.decode(reader, reader.uint32(), $undefined, _depth + 1, message.option);
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.fileTransfer = $root.hbb.FileTransfer.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileTransfer);
                        message.union = "fileTransfer";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.portForward = $root.hbb.PortForward.decode(reader, reader.uint32(), $undefined, _depth + 1, message.portForward);
                        message.union = "portForward";
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.viewCamera = $root.hbb.ViewCamera.decode(reader, reader.uint32(), $undefined, _depth + 1, message.viewCamera);
                        message.union = "viewCamera";
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.terminal = $root.hbb.Terminal.decode(reader, reader.uint32(), $undefined, _depth + 1, message.terminal);
                        message.union = "terminal";
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.videoAckRequired = value;
                        else
                            delete message.videoAckRequired;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.sessionId = value;
                        else
                            delete message.sessionId;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.osLogin = $root.hbb.OSLogin.decode(reader, reader.uint32(), $undefined, _depth + 1, message.osLogin);
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.myPlatform = value;
                        else
                            delete message.myPlatform;
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.hwid = value;
                        else
                            delete message.hwid;
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.avatar = value;
                        else
                            delete message.avatar;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fileTransferProtocolVersion = value;
                        else
                            delete message.fileTransferProtocolVersion;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        LoginRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.LoginRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.LoginRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.LoginRequest();
            if (object.username != null)
                if (typeof object.username !== "string" || object.username.length)
                    message.username = $String(object.username);
            if (object.password != null)
                if (object.password.length)
                    if (typeof object.password === "string")
                        $util.base64.decode(object.password, message.password = $util.newBuffer($util.base64.length(object.password)), 0);
                    else if (object.password.length >= 0)
                        message.password = object.password;
            if (object.myId != null)
                if (typeof object.myId !== "string" || object.myId.length)
                    message.myId = $String(object.myId);
            if (object.myName != null)
                if (typeof object.myName !== "string" || object.myName.length)
                    message.myName = $String(object.myName);
            if (object.option != null) {
                if (!$util.isObject(object.option))
                    throw $TypeError(".hbb.LoginRequest.option: object expected");
                message.option = $root.hbb.OptionMessage.fromObject(object.option, _depth + 1);
            }
            if (object.fileTransfer != null) {
                if (!$util.isObject(object.fileTransfer))
                    throw $TypeError(".hbb.LoginRequest.fileTransfer: object expected");
                message.fileTransfer = $root.hbb.FileTransfer.fromObject(object.fileTransfer, _depth + 1);
            }
            if (object.portForward != null) {
                if (!$util.isObject(object.portForward))
                    throw $TypeError(".hbb.LoginRequest.portForward: object expected");
                message.portForward = $root.hbb.PortForward.fromObject(object.portForward, _depth + 1);
            }
            if (object.viewCamera != null) {
                if (!$util.isObject(object.viewCamera))
                    throw $TypeError(".hbb.LoginRequest.viewCamera: object expected");
                message.viewCamera = $root.hbb.ViewCamera.fromObject(object.viewCamera, _depth + 1);
            }
            if (object.terminal != null) {
                if (!$util.isObject(object.terminal))
                    throw $TypeError(".hbb.LoginRequest.terminal: object expected");
                message.terminal = $root.hbb.Terminal.fromObject(object.terminal, _depth + 1);
            }
            if (object.videoAckRequired != null)
                if (object.videoAckRequired)
                    message.videoAckRequired = $Boolean(object.videoAckRequired);
            if (object.sessionId != null)
                if (typeof object.sessionId === "object" ? object.sessionId.low || object.sessionId.high : $Number(object.sessionId) !== 0)
                    if ($util.Long)
                        message.sessionId = $util.Long.fromValue(object.sessionId, true);
                    else if (typeof object.sessionId === "string")
                        message.sessionId = $parseInt(object.sessionId, 10);
                    else if (typeof object.sessionId === "number")
                        message.sessionId = object.sessionId;
                    else if (typeof object.sessionId === "object")
                        message.sessionId = new $util.LongBits(object.sessionId.low >>> 0, object.sessionId.high >>> 0).toNumber(true);
            if (object.version != null)
                if (typeof object.version !== "string" || object.version.length)
                    message.version = $String(object.version);
            if (object.osLogin != null) {
                if (!$util.isObject(object.osLogin))
                    throw $TypeError(".hbb.LoginRequest.osLogin: object expected");
                message.osLogin = $root.hbb.OSLogin.fromObject(object.osLogin, _depth + 1);
            }
            if (object.myPlatform != null)
                if (typeof object.myPlatform !== "string" || object.myPlatform.length)
                    message.myPlatform = $String(object.myPlatform);
            if (object.hwid != null)
                if (object.hwid.length)
                    if (typeof object.hwid === "string")
                        $util.base64.decode(object.hwid, message.hwid = $util.newBuffer($util.base64.length(object.hwid)), 0);
                    else if (object.hwid.length >= 0)
                        message.hwid = object.hwid;
            if (object.avatar != null)
                if (typeof object.avatar !== "string" || object.avatar.length)
                    message.avatar = $String(object.avatar);
            if (object.fileTransferProtocolVersion != null)
                if ($Number(object.fileTransferProtocolVersion) !== 0)
                    message.fileTransferProtocolVersion = object.fileTransferProtocolVersion >>> 0;
            return message;
        };

        LoginRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                if (options.bytes === $String)
                    object.password = "";
                else {
                    object.password = [];
                    if (options.bytes !== $Array)
                        object.password = $util.newBuffer(object.password);
                }
                object.myId = "";
                object.myName = "";
                object.option = null;
                object.videoAckRequired = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.sessionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.sessionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.version = "";
                object.osLogin = null;
                object.myPlatform = "";
                if (options.bytes === $String)
                    object.hwid = "";
                else {
                    object.hwid = [];
                    if (options.bytes !== $Array)
                        object.hwid = $util.newBuffer(object.hwid);
                }
                object.avatar = "";
                object.fileTransferProtocolVersion = 0;
            }
            if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                object.password = options.bytes === $String ? $util.base64.encode(message.password, 0, message.password.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.password) : message.password;
            if (message.myId != null && $Object.hasOwnProperty.call(message, "myId"))
                object.myId = message.myId;
            if (message.myName != null && $Object.hasOwnProperty.call(message, "myName"))
                object.myName = message.myName;
            if (message.option != null && $Object.hasOwnProperty.call(message, "option"))
                object.option = $root.hbb.OptionMessage.toObject(message.option, options, _depth + 1);
            if (message.fileTransfer != null && $Object.hasOwnProperty.call(message, "fileTransfer")) {
                object.fileTransfer = $root.hbb.FileTransfer.toObject(message.fileTransfer, options, _depth + 1);
                if (options.oneofs)
                    object.union = "fileTransfer";
            }
            if (message.portForward != null && $Object.hasOwnProperty.call(message, "portForward")) {
                object.portForward = $root.hbb.PortForward.toObject(message.portForward, options, _depth + 1);
                if (options.oneofs)
                    object.union = "portForward";
            }
            if (message.videoAckRequired != null && $Object.hasOwnProperty.call(message, "videoAckRequired"))
                object.videoAckRequired = message.videoAckRequired;
            if (message.sessionId != null && $Object.hasOwnProperty.call(message, "sessionId"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.sessionId = typeof message.sessionId === "number" ? $BigInt(message.sessionId) : $util.Long.fromBits(message.sessionId.low >>> 0, message.sessionId.high >>> 0, true).toBigInt();
                else if (typeof message.sessionId === "number")
                    object.sessionId = options.longs === $String ? $String(message.sessionId) : message.sessionId;
                else
                    object.sessionId = options.longs === $String ? $util.Long.prototype.toString.call(message.sessionId) : options.longs === $Number ? new $util.LongBits(message.sessionId.low >>> 0, message.sessionId.high >>> 0).toNumber(true) : message.sessionId;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.osLogin != null && $Object.hasOwnProperty.call(message, "osLogin"))
                object.osLogin = $root.hbb.OSLogin.toObject(message.osLogin, options, _depth + 1);
            if (message.myPlatform != null && $Object.hasOwnProperty.call(message, "myPlatform"))
                object.myPlatform = message.myPlatform;
            if (message.hwid != null && $Object.hasOwnProperty.call(message, "hwid"))
                object.hwid = options.bytes === $String ? $util.base64.encode(message.hwid, 0, message.hwid.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hwid) : message.hwid;
            if (message.viewCamera != null && $Object.hasOwnProperty.call(message, "viewCamera")) {
                object.viewCamera = $root.hbb.ViewCamera.toObject(message.viewCamera, options, _depth + 1);
                if (options.oneofs)
                    object.union = "viewCamera";
            }
            if (message.terminal != null && $Object.hasOwnProperty.call(message, "terminal")) {
                object.terminal = $root.hbb.Terminal.toObject(message.terminal, options, _depth + 1);
                if (options.oneofs)
                    object.union = "terminal";
            }
            if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
                object.avatar = message.avatar;
            if (message.fileTransferProtocolVersion != null && $Object.hasOwnProperty.call(message, "fileTransferProtocolVersion"))
                object.fileTransferProtocolVersion = message.fileTransferProtocolVersion;
            return object;
        };

        LoginRequest.prototype.toJSON = function() {
            return LoginRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return LoginRequest;
    })();

    hbb.Terminal = (function() {

        const Terminal = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        Terminal.prototype.serviceId = "";

        Terminal.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.serviceId != null && $Object.hasOwnProperty.call(message, "serviceId") && message.serviceId !== "")
                writer.uint32(10).string(message.serviceId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Terminal.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.Terminal(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.serviceId = value;
                        else
                            delete message.serviceId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        Terminal.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.Terminal)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.Terminal: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.Terminal();
            if (object.serviceId != null)
                if (typeof object.serviceId !== "string" || object.serviceId.length)
                    message.serviceId = $String(object.serviceId);
            return message;
        };

        Terminal.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.serviceId = "";
            if (message.serviceId != null && $Object.hasOwnProperty.call(message, "serviceId"))
                object.serviceId = message.serviceId;
            return object;
        };

        Terminal.prototype.toJSON = function() {
            return Terminal.toObject(this, $protobuf.util.toJSONOptions);
        };

        return Terminal;
    })();

    hbb.Auth2FA = (function() {

        const Auth2FA = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        Auth2FA.prototype.code = "";
        Auth2FA.prototype.hwid = $util.newBuffer([]);

        Auth2FA.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code !== "")
                writer.uint32(10).string(message.code);
            if (message.hwid != null && $Object.hasOwnProperty.call(message, "hwid") && message.hwid.length)
                writer.uint32(18).bytes(message.hwid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Auth2FA.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.Auth2FA(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.code = value;
                        else
                            delete message.code;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.hwid = value;
                        else
                            delete message.hwid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        Auth2FA.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.Auth2FA)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.Auth2FA: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.Auth2FA();
            if (object.code != null)
                if (typeof object.code !== "string" || object.code.length)
                    message.code = $String(object.code);
            if (object.hwid != null)
                if (object.hwid.length)
                    if (typeof object.hwid === "string")
                        $util.base64.decode(object.hwid, message.hwid = $util.newBuffer($util.base64.length(object.hwid)), 0);
                    else if (object.hwid.length >= 0)
                        message.hwid = object.hwid;
            return message;
        };

        Auth2FA.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.code = "";
                if (options.bytes === $String)
                    object.hwid = "";
                else {
                    object.hwid = [];
                    if (options.bytes !== $Array)
                        object.hwid = $util.newBuffer(object.hwid);
                }
            }
            if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
                object.code = message.code;
            if (message.hwid != null && $Object.hasOwnProperty.call(message, "hwid"))
                object.hwid = options.bytes === $String ? $util.base64.encode(message.hwid, 0, message.hwid.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hwid) : message.hwid;
            return object;
        };

        Auth2FA.prototype.toJSON = function() {
            return Auth2FA.toObject(this, $protobuf.util.toJSONOptions);
        };

        return Auth2FA;
    })();

    hbb.ChatMessage = (function() {

        const ChatMessage = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ChatMessage.prototype.text = "";

        ChatMessage.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.text != null && $Object.hasOwnProperty.call(message, "text") && message.text !== "")
                writer.uint32(10).string(message.text);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ChatMessage.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ChatMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.text = value;
                        else
                            delete message.text;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ChatMessage.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ChatMessage)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ChatMessage: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ChatMessage();
            if (object.text != null)
                if (typeof object.text !== "string" || object.text.length)
                    message.text = $String(object.text);
            return message;
        };

        ChatMessage.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.text = "";
            if (message.text != null && $Object.hasOwnProperty.call(message, "text"))
                object.text = message.text;
            return object;
        };

        ChatMessage.prototype.toJSON = function() {
            return ChatMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ChatMessage;
    })();

    hbb.Features = (function() {

        const Features = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        Features.prototype.privacyMode = false;
        Features.prototype.terminal = false;

        Features.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.privacyMode != null && $Object.hasOwnProperty.call(message, "privacyMode") && message.privacyMode !== false)
                writer.uint32(8).bool(message.privacyMode);
            if (message.terminal != null && $Object.hasOwnProperty.call(message, "terminal") && message.terminal !== false)
                writer.uint32(16).bool(message.terminal);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Features.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.Features(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.privacyMode = value;
                        else
                            delete message.privacyMode;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.terminal = value;
                        else
                            delete message.terminal;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        Features.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.Features)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.Features: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.Features();
            if (object.privacyMode != null)
                if (object.privacyMode)
                    message.privacyMode = $Boolean(object.privacyMode);
            if (object.terminal != null)
                if (object.terminal)
                    message.terminal = $Boolean(object.terminal);
            return message;
        };

        Features.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.privacyMode = false;
                object.terminal = false;
            }
            if (message.privacyMode != null && $Object.hasOwnProperty.call(message, "privacyMode"))
                object.privacyMode = message.privacyMode;
            if (message.terminal != null && $Object.hasOwnProperty.call(message, "terminal"))
                object.terminal = message.terminal;
            return object;
        };

        Features.prototype.toJSON = function() {
            return Features.toObject(this, $protobuf.util.toJSONOptions);
        };

        return Features;
    })();

    hbb.CodecAbility = (function() {

        const CodecAbility = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CodecAbility.prototype.vp8 = false;
        CodecAbility.prototype.vp9 = false;
        CodecAbility.prototype.av1 = false;
        CodecAbility.prototype.h264 = false;
        CodecAbility.prototype.h265 = false;

        CodecAbility.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.vp8 != null && $Object.hasOwnProperty.call(message, "vp8") && message.vp8 !== false)
                writer.uint32(8).bool(message.vp8);
            if (message.vp9 != null && $Object.hasOwnProperty.call(message, "vp9") && message.vp9 !== false)
                writer.uint32(16).bool(message.vp9);
            if (message.av1 != null && $Object.hasOwnProperty.call(message, "av1") && message.av1 !== false)
                writer.uint32(24).bool(message.av1);
            if (message.h264 != null && $Object.hasOwnProperty.call(message, "h264") && message.h264 !== false)
                writer.uint32(32).bool(message.h264);
            if (message.h265 != null && $Object.hasOwnProperty.call(message, "h265") && message.h265 !== false)
                writer.uint32(40).bool(message.h265);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CodecAbility.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CodecAbility(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.vp8 = value;
                        else
                            delete message.vp8;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.vp9 = value;
                        else
                            delete message.vp9;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.av1 = value;
                        else
                            delete message.av1;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.h264 = value;
                        else
                            delete message.h264;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.h265 = value;
                        else
                            delete message.h265;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CodecAbility.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CodecAbility)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CodecAbility: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CodecAbility();
            if (object.vp8 != null)
                if (object.vp8)
                    message.vp8 = $Boolean(object.vp8);
            if (object.vp9 != null)
                if (object.vp9)
                    message.vp9 = $Boolean(object.vp9);
            if (object.av1 != null)
                if (object.av1)
                    message.av1 = $Boolean(object.av1);
            if (object.h264 != null)
                if (object.h264)
                    message.h264 = $Boolean(object.h264);
            if (object.h265 != null)
                if (object.h265)
                    message.h265 = $Boolean(object.h265);
            return message;
        };

        CodecAbility.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.vp8 = false;
                object.vp9 = false;
                object.av1 = false;
                object.h264 = false;
                object.h265 = false;
            }
            if (message.vp8 != null && $Object.hasOwnProperty.call(message, "vp8"))
                object.vp8 = message.vp8;
            if (message.vp9 != null && $Object.hasOwnProperty.call(message, "vp9"))
                object.vp9 = message.vp9;
            if (message.av1 != null && $Object.hasOwnProperty.call(message, "av1"))
                object.av1 = message.av1;
            if (message.h264 != null && $Object.hasOwnProperty.call(message, "h264"))
                object.h264 = message.h264;
            if (message.h265 != null && $Object.hasOwnProperty.call(message, "h265"))
                object.h265 = message.h265;
            return object;
        };

        CodecAbility.prototype.toJSON = function() {
            return CodecAbility.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CodecAbility;
    })();

    hbb.SupportedEncoding = (function() {

        const SupportedEncoding = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        SupportedEncoding.prototype.h264 = false;
        SupportedEncoding.prototype.h265 = false;
        SupportedEncoding.prototype.vp8 = false;
        SupportedEncoding.prototype.av1 = false;
        SupportedEncoding.prototype.i444 = null;

        SupportedEncoding.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.h264 != null && $Object.hasOwnProperty.call(message, "h264") && message.h264 !== false)
                writer.uint32(8).bool(message.h264);
            if (message.h265 != null && $Object.hasOwnProperty.call(message, "h265") && message.h265 !== false)
                writer.uint32(16).bool(message.h265);
            if (message.vp8 != null && $Object.hasOwnProperty.call(message, "vp8") && message.vp8 !== false)
                writer.uint32(24).bool(message.vp8);
            if (message.av1 != null && $Object.hasOwnProperty.call(message, "av1") && message.av1 !== false)
                writer.uint32(32).bool(message.av1);
            if (message.i444 != null && $Object.hasOwnProperty.call(message, "i444"))
                $root.hbb.CodecAbility.encode(message.i444, writer.uint32(42).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SupportedEncoding.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.SupportedEncoding(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.h264 = value;
                        else
                            delete message.h264;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.h265 = value;
                        else
                            delete message.h265;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.vp8 = value;
                        else
                            delete message.vp8;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.av1 = value;
                        else
                            delete message.av1;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.i444 = $root.hbb.CodecAbility.decode(reader, reader.uint32(), $undefined, _depth + 1, message.i444);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        SupportedEncoding.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.SupportedEncoding)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.SupportedEncoding: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.SupportedEncoding();
            if (object.h264 != null)
                if (object.h264)
                    message.h264 = $Boolean(object.h264);
            if (object.h265 != null)
                if (object.h265)
                    message.h265 = $Boolean(object.h265);
            if (object.vp8 != null)
                if (object.vp8)
                    message.vp8 = $Boolean(object.vp8);
            if (object.av1 != null)
                if (object.av1)
                    message.av1 = $Boolean(object.av1);
            if (object.i444 != null) {
                if (!$util.isObject(object.i444))
                    throw $TypeError(".hbb.SupportedEncoding.i444: object expected");
                message.i444 = $root.hbb.CodecAbility.fromObject(object.i444, _depth + 1);
            }
            return message;
        };

        SupportedEncoding.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.h264 = false;
                object.h265 = false;
                object.vp8 = false;
                object.av1 = false;
                object.i444 = null;
            }
            if (message.h264 != null && $Object.hasOwnProperty.call(message, "h264"))
                object.h264 = message.h264;
            if (message.h265 != null && $Object.hasOwnProperty.call(message, "h265"))
                object.h265 = message.h265;
            if (message.vp8 != null && $Object.hasOwnProperty.call(message, "vp8"))
                object.vp8 = message.vp8;
            if (message.av1 != null && $Object.hasOwnProperty.call(message, "av1"))
                object.av1 = message.av1;
            if (message.i444 != null && $Object.hasOwnProperty.call(message, "i444"))
                object.i444 = $root.hbb.CodecAbility.toObject(message.i444, options, _depth + 1);
            return object;
        };

        SupportedEncoding.prototype.toJSON = function() {
            return SupportedEncoding.toObject(this, $protobuf.util.toJSONOptions);
        };

        return SupportedEncoding;
    })();

    hbb.PeerInfo = (function() {

        const PeerInfo = function (properties) {
            this.displays = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PeerInfo.prototype.username = "";
        PeerInfo.prototype.hostname = "";
        PeerInfo.prototype.platform = "";
        PeerInfo.prototype.displays = $util.emptyArray;
        PeerInfo.prototype.currentDisplay = 0;
        PeerInfo.prototype.sasEnabled = false;
        PeerInfo.prototype.version = "";
        PeerInfo.prototype.features = null;
        PeerInfo.prototype.encoding = null;
        PeerInfo.prototype.resolutions = null;
        PeerInfo.prototype.platformAdditions = "";
        PeerInfo.prototype.windowsSessions = null;
        PeerInfo.prototype.fileTransferProtocolVersion = 0;

        PeerInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.username != null && $Object.hasOwnProperty.call(message, "username") && message.username !== "")
                writer.uint32(10).string(message.username);
            if (message.hostname != null && $Object.hasOwnProperty.call(message, "hostname") && message.hostname !== "")
                writer.uint32(18).string(message.hostname);
            if (message.platform != null && $Object.hasOwnProperty.call(message, "platform") && message.platform !== "")
                writer.uint32(26).string(message.platform);
            if (message.displays != null && message.displays.length)
                for (let i = 0; i < message.displays.length; ++i)
                    $root.hbb.DisplayInfo.encode(message.displays[i], writer.uint32(34).fork(), _depth + 1).ldelim();
            if (message.currentDisplay != null && $Object.hasOwnProperty.call(message, "currentDisplay") && message.currentDisplay !== 0)
                writer.uint32(40).int32(message.currentDisplay);
            if (message.sasEnabled != null && $Object.hasOwnProperty.call(message, "sasEnabled") && message.sasEnabled !== false)
                writer.uint32(48).bool(message.sasEnabled);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
                writer.uint32(58).string(message.version);
            if (message.features != null && $Object.hasOwnProperty.call(message, "features"))
                $root.hbb.Features.encode(message.features, writer.uint32(74).fork(), _depth + 1).ldelim();
            if (message.encoding != null && $Object.hasOwnProperty.call(message, "encoding"))
                $root.hbb.SupportedEncoding.encode(message.encoding, writer.uint32(82).fork(), _depth + 1).ldelim();
            if (message.resolutions != null && $Object.hasOwnProperty.call(message, "resolutions"))
                $root.hbb.SupportedResolutions.encode(message.resolutions, writer.uint32(90).fork(), _depth + 1).ldelim();
            if (message.platformAdditions != null && $Object.hasOwnProperty.call(message, "platformAdditions") && message.platformAdditions !== "")
                writer.uint32(98).string(message.platformAdditions);
            if (message.windowsSessions != null && $Object.hasOwnProperty.call(message, "windowsSessions"))
                $root.hbb.WindowsSessions.encode(message.windowsSessions, writer.uint32(106).fork(), _depth + 1).ldelim();
            if (message.fileTransferProtocolVersion != null && $Object.hasOwnProperty.call(message, "fileTransferProtocolVersion") && message.fileTransferProtocolVersion !== 0)
                writer.uint32(112).uint32(message.fileTransferProtocolVersion);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PeerInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PeerInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.username = value;
                        else
                            delete message.username;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.hostname = value;
                        else
                            delete message.hostname;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.platform = value;
                        else
                            delete message.platform;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if (!(message.displays && message.displays.length))
                            message.displays = [];
                        message.displays.push($root.hbb.DisplayInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.currentDisplay = value;
                        else
                            delete message.currentDisplay;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.sasEnabled = value;
                        else
                            delete message.sasEnabled;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.features = $root.hbb.Features.decode(reader, reader.uint32(), $undefined, _depth + 1, message.features);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.encoding = $root.hbb.SupportedEncoding.decode(reader, reader.uint32(), $undefined, _depth + 1, message.encoding);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.resolutions = $root.hbb.SupportedResolutions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.resolutions);
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.platformAdditions = value;
                        else
                            delete message.platformAdditions;
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.windowsSessions = $root.hbb.WindowsSessions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.windowsSessions);
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.fileTransferProtocolVersion = value;
                        else
                            delete message.fileTransferProtocolVersion;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PeerInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PeerInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PeerInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PeerInfo();
            if (object.username != null)
                if (typeof object.username !== "string" || object.username.length)
                    message.username = $String(object.username);
            if (object.hostname != null)
                if (typeof object.hostname !== "string" || object.hostname.length)
                    message.hostname = $String(object.hostname);
            if (object.platform != null)
                if (typeof object.platform !== "string" || object.platform.length)
                    message.platform = $String(object.platform);
            if (object.displays) {
                if (!$Array.isArray(object.displays))
                    throw $TypeError(".hbb.PeerInfo.displays: array expected");
                message.displays = $Array(object.displays.length);
                for (let i = 0; i < object.displays.length; ++i) {
                    if (!$util.isObject(object.displays[i]))
                        throw $TypeError(".hbb.PeerInfo.displays: object expected");
                    message.displays[i] = $root.hbb.DisplayInfo.fromObject(object.displays[i], _depth + 1);
                }
            }
            if (object.currentDisplay != null)
                if ($Number(object.currentDisplay) !== 0)
                    message.currentDisplay = object.currentDisplay | 0;
            if (object.sasEnabled != null)
                if (object.sasEnabled)
                    message.sasEnabled = $Boolean(object.sasEnabled);
            if (object.version != null)
                if (typeof object.version !== "string" || object.version.length)
                    message.version = $String(object.version);
            if (object.features != null) {
                if (!$util.isObject(object.features))
                    throw $TypeError(".hbb.PeerInfo.features: object expected");
                message.features = $root.hbb.Features.fromObject(object.features, _depth + 1);
            }
            if (object.encoding != null) {
                if (!$util.isObject(object.encoding))
                    throw $TypeError(".hbb.PeerInfo.encoding: object expected");
                message.encoding = $root.hbb.SupportedEncoding.fromObject(object.encoding, _depth + 1);
            }
            if (object.resolutions != null) {
                if (!$util.isObject(object.resolutions))
                    throw $TypeError(".hbb.PeerInfo.resolutions: object expected");
                message.resolutions = $root.hbb.SupportedResolutions.fromObject(object.resolutions, _depth + 1);
            }
            if (object.platformAdditions != null)
                if (typeof object.platformAdditions !== "string" || object.platformAdditions.length)
                    message.platformAdditions = $String(object.platformAdditions);
            if (object.windowsSessions != null) {
                if (!$util.isObject(object.windowsSessions))
                    throw $TypeError(".hbb.PeerInfo.windowsSessions: object expected");
                message.windowsSessions = $root.hbb.WindowsSessions.fromObject(object.windowsSessions, _depth + 1);
            }
            if (object.fileTransferProtocolVersion != null)
                if ($Number(object.fileTransferProtocolVersion) !== 0)
                    message.fileTransferProtocolVersion = object.fileTransferProtocolVersion >>> 0;
            return message;
        };

        PeerInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.displays = [];
            if (options.defaults) {
                object.username = "";
                object.hostname = "";
                object.platform = "";
                object.currentDisplay = 0;
                object.sasEnabled = false;
                object.version = "";
                object.features = null;
                object.encoding = null;
                object.resolutions = null;
                object.platformAdditions = "";
                object.windowsSessions = null;
                object.fileTransferProtocolVersion = 0;
            }
            if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.hostname != null && $Object.hasOwnProperty.call(message, "hostname"))
                object.hostname = message.hostname;
            if (message.platform != null && $Object.hasOwnProperty.call(message, "platform"))
                object.platform = message.platform;
            if (message.displays && message.displays.length) {
                object.displays = $Array(message.displays.length);
                for (let j = 0; j < message.displays.length; ++j)
                    object.displays[j] = $root.hbb.DisplayInfo.toObject(message.displays[j], options, _depth + 1);
            }
            if (message.currentDisplay != null && $Object.hasOwnProperty.call(message, "currentDisplay"))
                object.currentDisplay = message.currentDisplay;
            if (message.sasEnabled != null && $Object.hasOwnProperty.call(message, "sasEnabled"))
                object.sasEnabled = message.sasEnabled;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.features != null && $Object.hasOwnProperty.call(message, "features"))
                object.features = $root.hbb.Features.toObject(message.features, options, _depth + 1);
            if (message.encoding != null && $Object.hasOwnProperty.call(message, "encoding"))
                object.encoding = $root.hbb.SupportedEncoding.toObject(message.encoding, options, _depth + 1);
            if (message.resolutions != null && $Object.hasOwnProperty.call(message, "resolutions"))
                object.resolutions = $root.hbb.SupportedResolutions.toObject(message.resolutions, options, _depth + 1);
            if (message.platformAdditions != null && $Object.hasOwnProperty.call(message, "platformAdditions"))
                object.platformAdditions = message.platformAdditions;
            if (message.windowsSessions != null && $Object.hasOwnProperty.call(message, "windowsSessions"))
                object.windowsSessions = $root.hbb.WindowsSessions.toObject(message.windowsSessions, options, _depth + 1);
            if (message.fileTransferProtocolVersion != null && $Object.hasOwnProperty.call(message, "fileTransferProtocolVersion"))
                object.fileTransferProtocolVersion = message.fileTransferProtocolVersion;
            return object;
        };

        PeerInfo.prototype.toJSON = function() {
            return PeerInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        return PeerInfo;
    })();

    hbb.WindowsSession = (function() {

        const WindowsSession = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        WindowsSession.prototype.sid = 0;
        WindowsSession.prototype.name = "";

        WindowsSession.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sid != null && $Object.hasOwnProperty.call(message, "sid") && message.sid !== 0)
                writer.uint32(8).uint32(message.sid);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(18).string(message.name);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        WindowsSession.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.WindowsSession(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sid = value;
                        else
                            delete message.sid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        WindowsSession.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.WindowsSession)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.WindowsSession: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.WindowsSession();
            if (object.sid != null)
                if ($Number(object.sid) !== 0)
                    message.sid = object.sid >>> 0;
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            return message;
        };

        WindowsSession.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.sid = 0;
                object.name = "";
            }
            if (message.sid != null && $Object.hasOwnProperty.call(message, "sid"))
                object.sid = message.sid;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            return object;
        };

        WindowsSession.prototype.toJSON = function() {
            return WindowsSession.toObject(this, $protobuf.util.toJSONOptions);
        };

        return WindowsSession;
    })();

    hbb.LoginResponse = (function() {

        const LoginResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        LoginResponse.prototype.error = null;
        LoginResponse.prototype.peerInfo = null;
        LoginResponse.prototype.enableTrustedDevices = false;

        let $oneOfFields;

        $Object.defineProperty(LoginResponse.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["error", "peerInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LoginResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                writer.uint32(10).string(message.error);
            if (message.peerInfo != null && $Object.hasOwnProperty.call(message, "peerInfo"))
                $root.hbb.PeerInfo.encode(message.peerInfo, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.enableTrustedDevices != null && $Object.hasOwnProperty.call(message, "enableTrustedDevices") && message.enableTrustedDevices !== false)
                writer.uint32(24).bool(message.enableTrustedDevices);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        LoginResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.LoginResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.error = reader.stringVerify();
                        message.union = "error";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.peerInfo = $root.hbb.PeerInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.peerInfo);
                        message.union = "peerInfo";
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enableTrustedDevices = value;
                        else
                            delete message.enableTrustedDevices;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        LoginResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.LoginResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.LoginResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.LoginResponse();
            if (object.error != null)
                message.error = $String(object.error);
            if (object.peerInfo != null) {
                if (!$util.isObject(object.peerInfo))
                    throw $TypeError(".hbb.LoginResponse.peerInfo: object expected");
                message.peerInfo = $root.hbb.PeerInfo.fromObject(object.peerInfo, _depth + 1);
            }
            if (object.enableTrustedDevices != null)
                if (object.enableTrustedDevices)
                    message.enableTrustedDevices = $Boolean(object.enableTrustedDevices);
            return message;
        };

        LoginResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.enableTrustedDevices = false;
            if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                object.error = message.error;
                if (options.oneofs)
                    object.union = "error";
            }
            if (message.peerInfo != null && $Object.hasOwnProperty.call(message, "peerInfo")) {
                object.peerInfo = $root.hbb.PeerInfo.toObject(message.peerInfo, options, _depth + 1);
                if (options.oneofs)
                    object.union = "peerInfo";
            }
            if (message.enableTrustedDevices != null && $Object.hasOwnProperty.call(message, "enableTrustedDevices"))
                object.enableTrustedDevices = message.enableTrustedDevices;
            return object;
        };

        LoginResponse.prototype.toJSON = function() {
            return LoginResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return LoginResponse;
    })();

    hbb.TouchScaleUpdate = (function() {

        const TouchScaleUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TouchScaleUpdate.prototype.scale = 0;

        TouchScaleUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.scale != null && $Object.hasOwnProperty.call(message, "scale") && message.scale !== 0)
                writer.uint32(8).int32(message.scale);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TouchScaleUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TouchScaleUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.scale = value;
                        else
                            delete message.scale;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TouchScaleUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TouchScaleUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TouchScaleUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TouchScaleUpdate();
            if (object.scale != null)
                if ($Number(object.scale) !== 0)
                    message.scale = object.scale | 0;
            return message;
        };

        TouchScaleUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.scale = 0;
            if (message.scale != null && $Object.hasOwnProperty.call(message, "scale"))
                object.scale = message.scale;
            return object;
        };

        TouchScaleUpdate.prototype.toJSON = function() {
            return TouchScaleUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TouchScaleUpdate;
    })();

    hbb.TouchPanStart = (function() {

        const TouchPanStart = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TouchPanStart.prototype.x = 0;
        TouchPanStart.prototype.y = 0;

        TouchPanStart.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.x != null && $Object.hasOwnProperty.call(message, "x") && message.x !== 0)
                writer.uint32(8).int32(message.x);
            if (message.y != null && $Object.hasOwnProperty.call(message, "y") && message.y !== 0)
                writer.uint32(16).int32(message.y);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TouchPanStart.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TouchPanStart(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.x = value;
                        else
                            delete message.x;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.y = value;
                        else
                            delete message.y;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TouchPanStart.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TouchPanStart)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TouchPanStart: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TouchPanStart();
            if (object.x != null)
                if ($Number(object.x) !== 0)
                    message.x = object.x | 0;
            if (object.y != null)
                if ($Number(object.y) !== 0)
                    message.y = object.y | 0;
            return message;
        };

        TouchPanStart.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.x = 0;
                object.y = 0;
            }
            if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                object.x = message.x;
            if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                object.y = message.y;
            return object;
        };

        TouchPanStart.prototype.toJSON = function() {
            return TouchPanStart.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TouchPanStart;
    })();

    hbb.TouchPanUpdate = (function() {

        const TouchPanUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TouchPanUpdate.prototype.x = 0;
        TouchPanUpdate.prototype.y = 0;

        TouchPanUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.x != null && $Object.hasOwnProperty.call(message, "x") && message.x !== 0)
                writer.uint32(8).int32(message.x);
            if (message.y != null && $Object.hasOwnProperty.call(message, "y") && message.y !== 0)
                writer.uint32(16).int32(message.y);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TouchPanUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TouchPanUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.x = value;
                        else
                            delete message.x;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.y = value;
                        else
                            delete message.y;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TouchPanUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TouchPanUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TouchPanUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TouchPanUpdate();
            if (object.x != null)
                if ($Number(object.x) !== 0)
                    message.x = object.x | 0;
            if (object.y != null)
                if ($Number(object.y) !== 0)
                    message.y = object.y | 0;
            return message;
        };

        TouchPanUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.x = 0;
                object.y = 0;
            }
            if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                object.x = message.x;
            if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                object.y = message.y;
            return object;
        };

        TouchPanUpdate.prototype.toJSON = function() {
            return TouchPanUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TouchPanUpdate;
    })();

    hbb.TouchPanEnd = (function() {

        const TouchPanEnd = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TouchPanEnd.prototype.x = 0;
        TouchPanEnd.prototype.y = 0;

        TouchPanEnd.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.x != null && $Object.hasOwnProperty.call(message, "x") && message.x !== 0)
                writer.uint32(8).int32(message.x);
            if (message.y != null && $Object.hasOwnProperty.call(message, "y") && message.y !== 0)
                writer.uint32(16).int32(message.y);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TouchPanEnd.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TouchPanEnd(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.x = value;
                        else
                            delete message.x;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.y = value;
                        else
                            delete message.y;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TouchPanEnd.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TouchPanEnd)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TouchPanEnd: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TouchPanEnd();
            if (object.x != null)
                if ($Number(object.x) !== 0)
                    message.x = object.x | 0;
            if (object.y != null)
                if ($Number(object.y) !== 0)
                    message.y = object.y | 0;
            return message;
        };

        TouchPanEnd.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.x = 0;
                object.y = 0;
            }
            if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                object.x = message.x;
            if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                object.y = message.y;
            return object;
        };

        TouchPanEnd.prototype.toJSON = function() {
            return TouchPanEnd.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TouchPanEnd;
    })();

    hbb.TouchEvent = (function() {

        const TouchEvent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TouchEvent.prototype.scaleUpdate = null;
        TouchEvent.prototype.panStart = null;
        TouchEvent.prototype.panUpdate = null;
        TouchEvent.prototype.panEnd = null;

        let $oneOfFields;

        $Object.defineProperty(TouchEvent.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["scaleUpdate", "panStart", "panUpdate", "panEnd"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        TouchEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.scaleUpdate != null && $Object.hasOwnProperty.call(message, "scaleUpdate"))
                $root.hbb.TouchScaleUpdate.encode(message.scaleUpdate, writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.panStart != null && $Object.hasOwnProperty.call(message, "panStart"))
                $root.hbb.TouchPanStart.encode(message.panStart, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.panUpdate != null && $Object.hasOwnProperty.call(message, "panUpdate"))
                $root.hbb.TouchPanUpdate.encode(message.panUpdate, writer.uint32(26).fork(), _depth + 1).ldelim();
            if (message.panEnd != null && $Object.hasOwnProperty.call(message, "panEnd"))
                $root.hbb.TouchPanEnd.encode(message.panEnd, writer.uint32(34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TouchEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TouchEvent();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.scaleUpdate = $root.hbb.TouchScaleUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.scaleUpdate);
                        message.union = "scaleUpdate";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.panStart = $root.hbb.TouchPanStart.decode(reader, reader.uint32(), $undefined, _depth + 1, message.panStart);
                        message.union = "panStart";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.panUpdate = $root.hbb.TouchPanUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.panUpdate);
                        message.union = "panUpdate";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.panEnd = $root.hbb.TouchPanEnd.decode(reader, reader.uint32(), $undefined, _depth + 1, message.panEnd);
                        message.union = "panEnd";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TouchEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TouchEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TouchEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TouchEvent();
            if (object.scaleUpdate != null) {
                if (!$util.isObject(object.scaleUpdate))
                    throw $TypeError(".hbb.TouchEvent.scaleUpdate: object expected");
                message.scaleUpdate = $root.hbb.TouchScaleUpdate.fromObject(object.scaleUpdate, _depth + 1);
            }
            if (object.panStart != null) {
                if (!$util.isObject(object.panStart))
                    throw $TypeError(".hbb.TouchEvent.panStart: object expected");
                message.panStart = $root.hbb.TouchPanStart.fromObject(object.panStart, _depth + 1);
            }
            if (object.panUpdate != null) {
                if (!$util.isObject(object.panUpdate))
                    throw $TypeError(".hbb.TouchEvent.panUpdate: object expected");
                message.panUpdate = $root.hbb.TouchPanUpdate.fromObject(object.panUpdate, _depth + 1);
            }
            if (object.panEnd != null) {
                if (!$util.isObject(object.panEnd))
                    throw $TypeError(".hbb.TouchEvent.panEnd: object expected");
                message.panEnd = $root.hbb.TouchPanEnd.fromObject(object.panEnd, _depth + 1);
            }
            return message;
        };

        TouchEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.scaleUpdate != null && $Object.hasOwnProperty.call(message, "scaleUpdate")) {
                object.scaleUpdate = $root.hbb.TouchScaleUpdate.toObject(message.scaleUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.union = "scaleUpdate";
            }
            if (message.panStart != null && $Object.hasOwnProperty.call(message, "panStart")) {
                object.panStart = $root.hbb.TouchPanStart.toObject(message.panStart, options, _depth + 1);
                if (options.oneofs)
                    object.union = "panStart";
            }
            if (message.panUpdate != null && $Object.hasOwnProperty.call(message, "panUpdate")) {
                object.panUpdate = $root.hbb.TouchPanUpdate.toObject(message.panUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.union = "panUpdate";
            }
            if (message.panEnd != null && $Object.hasOwnProperty.call(message, "panEnd")) {
                object.panEnd = $root.hbb.TouchPanEnd.toObject(message.panEnd, options, _depth + 1);
                if (options.oneofs)
                    object.union = "panEnd";
            }
            return object;
        };

        TouchEvent.prototype.toJSON = function() {
            return TouchEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TouchEvent;
    })();

    hbb.PointerDeviceEvent = (function() {

        const PointerDeviceEvent = function (properties) {
            this.modifiers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PointerDeviceEvent.prototype.touchEvent = null;
        PointerDeviceEvent.prototype.modifiers = $util.emptyArray;

        let $oneOfFields;

        $Object.defineProperty(PointerDeviceEvent.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["touchEvent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PointerDeviceEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.touchEvent != null && $Object.hasOwnProperty.call(message, "touchEvent"))
                $root.hbb.TouchEvent.encode(message.touchEvent, writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.modifiers != null && message.modifiers.length)
                writer.uint32(18).int32s(message.modifiers);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PointerDeviceEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PointerDeviceEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.touchEvent = $root.hbb.TouchEvent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.touchEvent);
                        message.union = "touchEvent";
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.modifiers && message.modifiers.length))
                                message.modifiers = [];
                            reader.int32s(message.modifiers);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.modifiers && message.modifiers.length))
                            message.modifiers = [];
                        message.modifiers.push(reader.int32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PointerDeviceEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PointerDeviceEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PointerDeviceEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PointerDeviceEvent();
            if (object.touchEvent != null) {
                if (!$util.isObject(object.touchEvent))
                    throw $TypeError(".hbb.PointerDeviceEvent.touchEvent: object expected");
                message.touchEvent = $root.hbb.TouchEvent.fromObject(object.touchEvent, _depth + 1);
            }
            if (object.modifiers) {
                if (!$Array.isArray(object.modifiers))
                    throw $TypeError(".hbb.PointerDeviceEvent.modifiers: array expected");
                message.modifiers = [];
                for (let i = 0; i < object.modifiers.length; ++i)
                    switch (object.modifiers[i]) {
                    case "Unknown":
                    case 0:
                        message.modifiers[message.modifiers.length] = 0;
                        break;
                    case "Alt":
                    case 1:
                        message.modifiers[message.modifiers.length] = 1;
                        break;
                    case "Backspace":
                    case 2:
                        message.modifiers[message.modifiers.length] = 2;
                        break;
                    case "CapsLock":
                    case 3:
                        message.modifiers[message.modifiers.length] = 3;
                        break;
                    case "Control":
                    case 4:
                        message.modifiers[message.modifiers.length] = 4;
                        break;
                    case "Delete":
                    case 5:
                        message.modifiers[message.modifiers.length] = 5;
                        break;
                    case "DownArrow":
                    case 6:
                        message.modifiers[message.modifiers.length] = 6;
                        break;
                    case "End":
                    case 7:
                        message.modifiers[message.modifiers.length] = 7;
                        break;
                    case "Escape":
                    case 8:
                        message.modifiers[message.modifiers.length] = 8;
                        break;
                    case "F1":
                    case 9:
                        message.modifiers[message.modifiers.length] = 9;
                        break;
                    case "F10":
                    case 10:
                        message.modifiers[message.modifiers.length] = 10;
                        break;
                    case "F11":
                    case 11:
                        message.modifiers[message.modifiers.length] = 11;
                        break;
                    case "F12":
                    case 12:
                        message.modifiers[message.modifiers.length] = 12;
                        break;
                    case "F2":
                    case 13:
                        message.modifiers[message.modifiers.length] = 13;
                        break;
                    case "F3":
                    case 14:
                        message.modifiers[message.modifiers.length] = 14;
                        break;
                    case "F4":
                    case 15:
                        message.modifiers[message.modifiers.length] = 15;
                        break;
                    case "F5":
                    case 16:
                        message.modifiers[message.modifiers.length] = 16;
                        break;
                    case "F6":
                    case 17:
                        message.modifiers[message.modifiers.length] = 17;
                        break;
                    case "F7":
                    case 18:
                        message.modifiers[message.modifiers.length] = 18;
                        break;
                    case "F8":
                    case 19:
                        message.modifiers[message.modifiers.length] = 19;
                        break;
                    case "F9":
                    case 20:
                        message.modifiers[message.modifiers.length] = 20;
                        break;
                    case "Home":
                    case 21:
                        message.modifiers[message.modifiers.length] = 21;
                        break;
                    case "LeftArrow":
                    case 22:
                        message.modifiers[message.modifiers.length] = 22;
                        break;
                    case "Meta":
                    case 23:
                        message.modifiers[message.modifiers.length] = 23;
                        break;
                    case "Option":
                    case 24:
                        message.modifiers[message.modifiers.length] = 24;
                        break;
                    case "PageDown":
                    case 25:
                        message.modifiers[message.modifiers.length] = 25;
                        break;
                    case "PageUp":
                    case 26:
                        message.modifiers[message.modifiers.length] = 26;
                        break;
                    case "Return":
                    case 27:
                        message.modifiers[message.modifiers.length] = 27;
                        break;
                    case "RightArrow":
                    case 28:
                        message.modifiers[message.modifiers.length] = 28;
                        break;
                    case "Shift":
                    case 29:
                        message.modifiers[message.modifiers.length] = 29;
                        break;
                    case "Space":
                    case 30:
                        message.modifiers[message.modifiers.length] = 30;
                        break;
                    case "Tab":
                    case 31:
                        message.modifiers[message.modifiers.length] = 31;
                        break;
                    case "UpArrow":
                    case 32:
                        message.modifiers[message.modifiers.length] = 32;
                        break;
                    case "Numpad0":
                    case 33:
                        message.modifiers[message.modifiers.length] = 33;
                        break;
                    case "Numpad1":
                    case 34:
                        message.modifiers[message.modifiers.length] = 34;
                        break;
                    case "Numpad2":
                    case 35:
                        message.modifiers[message.modifiers.length] = 35;
                        break;
                    case "Numpad3":
                    case 36:
                        message.modifiers[message.modifiers.length] = 36;
                        break;
                    case "Numpad4":
                    case 37:
                        message.modifiers[message.modifiers.length] = 37;
                        break;
                    case "Numpad5":
                    case 38:
                        message.modifiers[message.modifiers.length] = 38;
                        break;
                    case "Numpad6":
                    case 39:
                        message.modifiers[message.modifiers.length] = 39;
                        break;
                    case "Numpad7":
                    case 40:
                        message.modifiers[message.modifiers.length] = 40;
                        break;
                    case "Numpad8":
                    case 41:
                        message.modifiers[message.modifiers.length] = 41;
                        break;
                    case "Numpad9":
                    case 42:
                        message.modifiers[message.modifiers.length] = 42;
                        break;
                    case "Cancel":
                    case 43:
                        message.modifiers[message.modifiers.length] = 43;
                        break;
                    case "Clear":
                    case 44:
                        message.modifiers[message.modifiers.length] = 44;
                        break;
                    case "Menu":
                    case 45:
                        message.modifiers[message.modifiers.length] = 45;
                        break;
                    case "Pause":
                    case 46:
                        message.modifiers[message.modifiers.length] = 46;
                        break;
                    case "Kana":
                    case 47:
                        message.modifiers[message.modifiers.length] = 47;
                        break;
                    case "Hangul":
                    case 48:
                        message.modifiers[message.modifiers.length] = 48;
                        break;
                    case "Junja":
                    case 49:
                        message.modifiers[message.modifiers.length] = 49;
                        break;
                    case "Final":
                    case 50:
                        message.modifiers[message.modifiers.length] = 50;
                        break;
                    case "Hanja":
                    case 51:
                        message.modifiers[message.modifiers.length] = 51;
                        break;
                    case "Kanji":
                    case 52:
                        message.modifiers[message.modifiers.length] = 52;
                        break;
                    case "Convert":
                    case 53:
                        message.modifiers[message.modifiers.length] = 53;
                        break;
                    case "Select":
                    case 54:
                        message.modifiers[message.modifiers.length] = 54;
                        break;
                    case "Print":
                    case 55:
                        message.modifiers[message.modifiers.length] = 55;
                        break;
                    case "Execute":
                    case 56:
                        message.modifiers[message.modifiers.length] = 56;
                        break;
                    case "Snapshot":
                    case 57:
                        message.modifiers[message.modifiers.length] = 57;
                        break;
                    case "Insert":
                    case 58:
                        message.modifiers[message.modifiers.length] = 58;
                        break;
                    case "Help":
                    case 59:
                        message.modifiers[message.modifiers.length] = 59;
                        break;
                    case "Sleep":
                    case 60:
                        message.modifiers[message.modifiers.length] = 60;
                        break;
                    case "Separator":
                    case 61:
                        message.modifiers[message.modifiers.length] = 61;
                        break;
                    case "Scroll":
                    case 62:
                        message.modifiers[message.modifiers.length] = 62;
                        break;
                    case "NumLock":
                    case 63:
                        message.modifiers[message.modifiers.length] = 63;
                        break;
                    case "RWin":
                    case 64:
                        message.modifiers[message.modifiers.length] = 64;
                        break;
                    case "Apps":
                    case 65:
                        message.modifiers[message.modifiers.length] = 65;
                        break;
                    case "Multiply":
                    case 66:
                        message.modifiers[message.modifiers.length] = 66;
                        break;
                    case "Add":
                    case 67:
                        message.modifiers[message.modifiers.length] = 67;
                        break;
                    case "Subtract":
                    case 68:
                        message.modifiers[message.modifiers.length] = 68;
                        break;
                    case "Decimal":
                    case 69:
                        message.modifiers[message.modifiers.length] = 69;
                        break;
                    case "Divide":
                    case 70:
                        message.modifiers[message.modifiers.length] = 70;
                        break;
                    case "Equals":
                    case 71:
                        message.modifiers[message.modifiers.length] = 71;
                        break;
                    case "NumpadEnter":
                    case 72:
                        message.modifiers[message.modifiers.length] = 72;
                        break;
                    case "RShift":
                    case 73:
                        message.modifiers[message.modifiers.length] = 73;
                        break;
                    case "RControl":
                    case 74:
                        message.modifiers[message.modifiers.length] = 74;
                        break;
                    case "RAlt":
                    case 75:
                        message.modifiers[message.modifiers.length] = 75;
                        break;
                    case "VolumeMute":
                    case 76:
                        message.modifiers[message.modifiers.length] = 76;
                        break;
                    case "VolumeUp":
                    case 77:
                        message.modifiers[message.modifiers.length] = 77;
                        break;
                    case "VolumeDown":
                    case 78:
                        message.modifiers[message.modifiers.length] = 78;
                        break;
                    case "Power":
                    case 79:
                        message.modifiers[message.modifiers.length] = 79;
                        break;
                    case "CtrlAltDel":
                    case 100:
                        message.modifiers[message.modifiers.length] = 100;
                        break;
                    case "LockScreen":
                    case 101:
                        message.modifiers[message.modifiers.length] = 101;
                        break;
                    default:
                        if (typeof object.modifiers[i] === "number" && (object.modifiers[i] | 0) === object.modifiers[i])
                            message.modifiers[message.modifiers.length] = object.modifiers[i];
                    }
            }
            return message;
        };

        PointerDeviceEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.modifiers = [];
            if (message.touchEvent != null && $Object.hasOwnProperty.call(message, "touchEvent")) {
                object.touchEvent = $root.hbb.TouchEvent.toObject(message.touchEvent, options, _depth + 1);
                if (options.oneofs)
                    object.union = "touchEvent";
            }
            if (message.modifiers && message.modifiers.length) {
                object.modifiers = $Array(message.modifiers.length);
                for (let j = 0; j < message.modifiers.length; ++j)
                    object.modifiers[j] = options.enums === $String ? $root.hbb.ControlKey[message.modifiers[j]] === $undefined ? message.modifiers[j] : $root.hbb.ControlKey[message.modifiers[j]] : message.modifiers[j];
            }
            return object;
        };

        PointerDeviceEvent.prototype.toJSON = function() {
            return PointerDeviceEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        return PointerDeviceEvent;
    })();

    hbb.MouseEvent = (function() {

        const MouseEvent = function (properties) {
            this.modifiers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        MouseEvent.prototype.mask = 0;
        MouseEvent.prototype.x = 0;
        MouseEvent.prototype.y = 0;
        MouseEvent.prototype.modifiers = $util.emptyArray;

        MouseEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.mask != null && $Object.hasOwnProperty.call(message, "mask") && message.mask !== 0)
                writer.uint32(8).int32(message.mask);
            if (message.x != null && $Object.hasOwnProperty.call(message, "x") && message.x !== 0)
                writer.uint32(16).sint32(message.x);
            if (message.y != null && $Object.hasOwnProperty.call(message, "y") && message.y !== 0)
                writer.uint32(24).sint32(message.y);
            if (message.modifiers != null && message.modifiers.length)
                writer.uint32(34).int32s(message.modifiers);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MouseEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.MouseEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.mask = value;
                        else
                            delete message.mask;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.x = value;
                        else
                            delete message.x;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.y = value;
                        else
                            delete message.y;
                        continue;
                    }
                case 4: {
                        if (wireType === 2) {
                            if (!(message.modifiers && message.modifiers.length))
                                message.modifiers = [];
                            reader.int32s(message.modifiers);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.modifiers && message.modifiers.length))
                            message.modifiers = [];
                        message.modifiers.push(reader.int32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        MouseEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.MouseEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.MouseEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.MouseEvent();
            if (object.mask != null)
                if ($Number(object.mask) !== 0)
                    message.mask = object.mask | 0;
            if (object.x != null)
                if ($Number(object.x) !== 0)
                    message.x = object.x | 0;
            if (object.y != null)
                if ($Number(object.y) !== 0)
                    message.y = object.y | 0;
            if (object.modifiers) {
                if (!$Array.isArray(object.modifiers))
                    throw $TypeError(".hbb.MouseEvent.modifiers: array expected");
                message.modifiers = [];
                for (let i = 0; i < object.modifiers.length; ++i)
                    switch (object.modifiers[i]) {
                    case "Unknown":
                    case 0:
                        message.modifiers[message.modifiers.length] = 0;
                        break;
                    case "Alt":
                    case 1:
                        message.modifiers[message.modifiers.length] = 1;
                        break;
                    case "Backspace":
                    case 2:
                        message.modifiers[message.modifiers.length] = 2;
                        break;
                    case "CapsLock":
                    case 3:
                        message.modifiers[message.modifiers.length] = 3;
                        break;
                    case "Control":
                    case 4:
                        message.modifiers[message.modifiers.length] = 4;
                        break;
                    case "Delete":
                    case 5:
                        message.modifiers[message.modifiers.length] = 5;
                        break;
                    case "DownArrow":
                    case 6:
                        message.modifiers[message.modifiers.length] = 6;
                        break;
                    case "End":
                    case 7:
                        message.modifiers[message.modifiers.length] = 7;
                        break;
                    case "Escape":
                    case 8:
                        message.modifiers[message.modifiers.length] = 8;
                        break;
                    case "F1":
                    case 9:
                        message.modifiers[message.modifiers.length] = 9;
                        break;
                    case "F10":
                    case 10:
                        message.modifiers[message.modifiers.length] = 10;
                        break;
                    case "F11":
                    case 11:
                        message.modifiers[message.modifiers.length] = 11;
                        break;
                    case "F12":
                    case 12:
                        message.modifiers[message.modifiers.length] = 12;
                        break;
                    case "F2":
                    case 13:
                        message.modifiers[message.modifiers.length] = 13;
                        break;
                    case "F3":
                    case 14:
                        message.modifiers[message.modifiers.length] = 14;
                        break;
                    case "F4":
                    case 15:
                        message.modifiers[message.modifiers.length] = 15;
                        break;
                    case "F5":
                    case 16:
                        message.modifiers[message.modifiers.length] = 16;
                        break;
                    case "F6":
                    case 17:
                        message.modifiers[message.modifiers.length] = 17;
                        break;
                    case "F7":
                    case 18:
                        message.modifiers[message.modifiers.length] = 18;
                        break;
                    case "F8":
                    case 19:
                        message.modifiers[message.modifiers.length] = 19;
                        break;
                    case "F9":
                    case 20:
                        message.modifiers[message.modifiers.length] = 20;
                        break;
                    case "Home":
                    case 21:
                        message.modifiers[message.modifiers.length] = 21;
                        break;
                    case "LeftArrow":
                    case 22:
                        message.modifiers[message.modifiers.length] = 22;
                        break;
                    case "Meta":
                    case 23:
                        message.modifiers[message.modifiers.length] = 23;
                        break;
                    case "Option":
                    case 24:
                        message.modifiers[message.modifiers.length] = 24;
                        break;
                    case "PageDown":
                    case 25:
                        message.modifiers[message.modifiers.length] = 25;
                        break;
                    case "PageUp":
                    case 26:
                        message.modifiers[message.modifiers.length] = 26;
                        break;
                    case "Return":
                    case 27:
                        message.modifiers[message.modifiers.length] = 27;
                        break;
                    case "RightArrow":
                    case 28:
                        message.modifiers[message.modifiers.length] = 28;
                        break;
                    case "Shift":
                    case 29:
                        message.modifiers[message.modifiers.length] = 29;
                        break;
                    case "Space":
                    case 30:
                        message.modifiers[message.modifiers.length] = 30;
                        break;
                    case "Tab":
                    case 31:
                        message.modifiers[message.modifiers.length] = 31;
                        break;
                    case "UpArrow":
                    case 32:
                        message.modifiers[message.modifiers.length] = 32;
                        break;
                    case "Numpad0":
                    case 33:
                        message.modifiers[message.modifiers.length] = 33;
                        break;
                    case "Numpad1":
                    case 34:
                        message.modifiers[message.modifiers.length] = 34;
                        break;
                    case "Numpad2":
                    case 35:
                        message.modifiers[message.modifiers.length] = 35;
                        break;
                    case "Numpad3":
                    case 36:
                        message.modifiers[message.modifiers.length] = 36;
                        break;
                    case "Numpad4":
                    case 37:
                        message.modifiers[message.modifiers.length] = 37;
                        break;
                    case "Numpad5":
                    case 38:
                        message.modifiers[message.modifiers.length] = 38;
                        break;
                    case "Numpad6":
                    case 39:
                        message.modifiers[message.modifiers.length] = 39;
                        break;
                    case "Numpad7":
                    case 40:
                        message.modifiers[message.modifiers.length] = 40;
                        break;
                    case "Numpad8":
                    case 41:
                        message.modifiers[message.modifiers.length] = 41;
                        break;
                    case "Numpad9":
                    case 42:
                        message.modifiers[message.modifiers.length] = 42;
                        break;
                    case "Cancel":
                    case 43:
                        message.modifiers[message.modifiers.length] = 43;
                        break;
                    case "Clear":
                    case 44:
                        message.modifiers[message.modifiers.length] = 44;
                        break;
                    case "Menu":
                    case 45:
                        message.modifiers[message.modifiers.length] = 45;
                        break;
                    case "Pause":
                    case 46:
                        message.modifiers[message.modifiers.length] = 46;
                        break;
                    case "Kana":
                    case 47:
                        message.modifiers[message.modifiers.length] = 47;
                        break;
                    case "Hangul":
                    case 48:
                        message.modifiers[message.modifiers.length] = 48;
                        break;
                    case "Junja":
                    case 49:
                        message.modifiers[message.modifiers.length] = 49;
                        break;
                    case "Final":
                    case 50:
                        message.modifiers[message.modifiers.length] = 50;
                        break;
                    case "Hanja":
                    case 51:
                        message.modifiers[message.modifiers.length] = 51;
                        break;
                    case "Kanji":
                    case 52:
                        message.modifiers[message.modifiers.length] = 52;
                        break;
                    case "Convert":
                    case 53:
                        message.modifiers[message.modifiers.length] = 53;
                        break;
                    case "Select":
                    case 54:
                        message.modifiers[message.modifiers.length] = 54;
                        break;
                    case "Print":
                    case 55:
                        message.modifiers[message.modifiers.length] = 55;
                        break;
                    case "Execute":
                    case 56:
                        message.modifiers[message.modifiers.length] = 56;
                        break;
                    case "Snapshot":
                    case 57:
                        message.modifiers[message.modifiers.length] = 57;
                        break;
                    case "Insert":
                    case 58:
                        message.modifiers[message.modifiers.length] = 58;
                        break;
                    case "Help":
                    case 59:
                        message.modifiers[message.modifiers.length] = 59;
                        break;
                    case "Sleep":
                    case 60:
                        message.modifiers[message.modifiers.length] = 60;
                        break;
                    case "Separator":
                    case 61:
                        message.modifiers[message.modifiers.length] = 61;
                        break;
                    case "Scroll":
                    case 62:
                        message.modifiers[message.modifiers.length] = 62;
                        break;
                    case "NumLock":
                    case 63:
                        message.modifiers[message.modifiers.length] = 63;
                        break;
                    case "RWin":
                    case 64:
                        message.modifiers[message.modifiers.length] = 64;
                        break;
                    case "Apps":
                    case 65:
                        message.modifiers[message.modifiers.length] = 65;
                        break;
                    case "Multiply":
                    case 66:
                        message.modifiers[message.modifiers.length] = 66;
                        break;
                    case "Add":
                    case 67:
                        message.modifiers[message.modifiers.length] = 67;
                        break;
                    case "Subtract":
                    case 68:
                        message.modifiers[message.modifiers.length] = 68;
                        break;
                    case "Decimal":
                    case 69:
                        message.modifiers[message.modifiers.length] = 69;
                        break;
                    case "Divide":
                    case 70:
                        message.modifiers[message.modifiers.length] = 70;
                        break;
                    case "Equals":
                    case 71:
                        message.modifiers[message.modifiers.length] = 71;
                        break;
                    case "NumpadEnter":
                    case 72:
                        message.modifiers[message.modifiers.length] = 72;
                        break;
                    case "RShift":
                    case 73:
                        message.modifiers[message.modifiers.length] = 73;
                        break;
                    case "RControl":
                    case 74:
                        message.modifiers[message.modifiers.length] = 74;
                        break;
                    case "RAlt":
                    case 75:
                        message.modifiers[message.modifiers.length] = 75;
                        break;
                    case "VolumeMute":
                    case 76:
                        message.modifiers[message.modifiers.length] = 76;
                        break;
                    case "VolumeUp":
                    case 77:
                        message.modifiers[message.modifiers.length] = 77;
                        break;
                    case "VolumeDown":
                    case 78:
                        message.modifiers[message.modifiers.length] = 78;
                        break;
                    case "Power":
                    case 79:
                        message.modifiers[message.modifiers.length] = 79;
                        break;
                    case "CtrlAltDel":
                    case 100:
                        message.modifiers[message.modifiers.length] = 100;
                        break;
                    case "LockScreen":
                    case 101:
                        message.modifiers[message.modifiers.length] = 101;
                        break;
                    default:
                        if (typeof object.modifiers[i] === "number" && (object.modifiers[i] | 0) === object.modifiers[i])
                            message.modifiers[message.modifiers.length] = object.modifiers[i];
                    }
            }
            return message;
        };

        MouseEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.modifiers = [];
            if (options.defaults) {
                object.mask = 0;
                object.x = 0;
                object.y = 0;
            }
            if (message.mask != null && $Object.hasOwnProperty.call(message, "mask"))
                object.mask = message.mask;
            if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                object.x = message.x;
            if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                object.y = message.y;
            if (message.modifiers && message.modifiers.length) {
                object.modifiers = $Array(message.modifiers.length);
                for (let j = 0; j < message.modifiers.length; ++j)
                    object.modifiers[j] = options.enums === $String ? $root.hbb.ControlKey[message.modifiers[j]] === $undefined ? message.modifiers[j] : $root.hbb.ControlKey[message.modifiers[j]] : message.modifiers[j];
            }
            return object;
        };

        MouseEvent.prototype.toJSON = function() {
            return MouseEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        return MouseEvent;
    })();

    hbb.KeyboardMode = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "Legacy"] = 0;
        values[valuesById[1] = "Map"] = 1;
        values[valuesById[2] = "Translate"] = 2;
        values[valuesById[3] = "Auto"] = 3;
        return values;
    })();

    hbb.ControlKey = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "Unknown"] = 0;
        values[valuesById[1] = "Alt"] = 1;
        values[valuesById[2] = "Backspace"] = 2;
        values[valuesById[3] = "CapsLock"] = 3;
        values[valuesById[4] = "Control"] = 4;
        values[valuesById[5] = "Delete"] = 5;
        values[valuesById[6] = "DownArrow"] = 6;
        values[valuesById[7] = "End"] = 7;
        values[valuesById[8] = "Escape"] = 8;
        values[valuesById[9] = "F1"] = 9;
        values[valuesById[10] = "F10"] = 10;
        values[valuesById[11] = "F11"] = 11;
        values[valuesById[12] = "F12"] = 12;
        values[valuesById[13] = "F2"] = 13;
        values[valuesById[14] = "F3"] = 14;
        values[valuesById[15] = "F4"] = 15;
        values[valuesById[16] = "F5"] = 16;
        values[valuesById[17] = "F6"] = 17;
        values[valuesById[18] = "F7"] = 18;
        values[valuesById[19] = "F8"] = 19;
        values[valuesById[20] = "F9"] = 20;
        values[valuesById[21] = "Home"] = 21;
        values[valuesById[22] = "LeftArrow"] = 22;
        values[valuesById[23] = "Meta"] = 23;
        values[valuesById[24] = "Option"] = 24;
        values[valuesById[25] = "PageDown"] = 25;
        values[valuesById[26] = "PageUp"] = 26;
        values[valuesById[27] = "Return"] = 27;
        values[valuesById[28] = "RightArrow"] = 28;
        values[valuesById[29] = "Shift"] = 29;
        values[valuesById[30] = "Space"] = 30;
        values[valuesById[31] = "Tab"] = 31;
        values[valuesById[32] = "UpArrow"] = 32;
        values[valuesById[33] = "Numpad0"] = 33;
        values[valuesById[34] = "Numpad1"] = 34;
        values[valuesById[35] = "Numpad2"] = 35;
        values[valuesById[36] = "Numpad3"] = 36;
        values[valuesById[37] = "Numpad4"] = 37;
        values[valuesById[38] = "Numpad5"] = 38;
        values[valuesById[39] = "Numpad6"] = 39;
        values[valuesById[40] = "Numpad7"] = 40;
        values[valuesById[41] = "Numpad8"] = 41;
        values[valuesById[42] = "Numpad9"] = 42;
        values[valuesById[43] = "Cancel"] = 43;
        values[valuesById[44] = "Clear"] = 44;
        values[valuesById[45] = "Menu"] = 45;
        values[valuesById[46] = "Pause"] = 46;
        values[valuesById[47] = "Kana"] = 47;
        values[valuesById[48] = "Hangul"] = 48;
        values[valuesById[49] = "Junja"] = 49;
        values[valuesById[50] = "Final"] = 50;
        values[valuesById[51] = "Hanja"] = 51;
        values[valuesById[52] = "Kanji"] = 52;
        values[valuesById[53] = "Convert"] = 53;
        values[valuesById[54] = "Select"] = 54;
        values[valuesById[55] = "Print"] = 55;
        values[valuesById[56] = "Execute"] = 56;
        values[valuesById[57] = "Snapshot"] = 57;
        values[valuesById[58] = "Insert"] = 58;
        values[valuesById[59] = "Help"] = 59;
        values[valuesById[60] = "Sleep"] = 60;
        values[valuesById[61] = "Separator"] = 61;
        values[valuesById[62] = "Scroll"] = 62;
        values[valuesById[63] = "NumLock"] = 63;
        values[valuesById[64] = "RWin"] = 64;
        values[valuesById[65] = "Apps"] = 65;
        values[valuesById[66] = "Multiply"] = 66;
        values[valuesById[67] = "Add"] = 67;
        values[valuesById[68] = "Subtract"] = 68;
        values[valuesById[69] = "Decimal"] = 69;
        values[valuesById[70] = "Divide"] = 70;
        values[valuesById[71] = "Equals"] = 71;
        values[valuesById[72] = "NumpadEnter"] = 72;
        values[valuesById[73] = "RShift"] = 73;
        values[valuesById[74] = "RControl"] = 74;
        values[valuesById[75] = "RAlt"] = 75;
        values[valuesById[76] = "VolumeMute"] = 76;
        values[valuesById[77] = "VolumeUp"] = 77;
        values[valuesById[78] = "VolumeDown"] = 78;
        values[valuesById[79] = "Power"] = 79;
        values[valuesById[100] = "CtrlAltDel"] = 100;
        values[valuesById[101] = "LockScreen"] = 101;
        return values;
    })();

    hbb.KeyEvent = (function() {

        const KeyEvent = function (properties) {
            this.modifiers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        KeyEvent.prototype.down = false;
        KeyEvent.prototype.press = false;
        KeyEvent.prototype.controlKey = null;
        KeyEvent.prototype.chr = null;
        KeyEvent.prototype.unicode = null;
        KeyEvent.prototype.seq = null;
        KeyEvent.prototype.win2winHotkey = null;
        KeyEvent.prototype.modifiers = $util.emptyArray;
        KeyEvent.prototype.mode = 0;

        let $oneOfFields;

        $Object.defineProperty(KeyEvent.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["controlKey", "chr", "unicode", "seq", "win2winHotkey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        KeyEvent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.down != null && $Object.hasOwnProperty.call(message, "down") && message.down !== false)
                writer.uint32(8).bool(message.down);
            if (message.press != null && $Object.hasOwnProperty.call(message, "press") && message.press !== false)
                writer.uint32(16).bool(message.press);
            if (message.controlKey != null && $Object.hasOwnProperty.call(message, "controlKey"))
                writer.uint32(24).int32(message.controlKey);
            if (message.chr != null && $Object.hasOwnProperty.call(message, "chr"))
                writer.uint32(32).uint32(message.chr);
            if (message.unicode != null && $Object.hasOwnProperty.call(message, "unicode"))
                writer.uint32(40).uint32(message.unicode);
            if (message.seq != null && $Object.hasOwnProperty.call(message, "seq"))
                writer.uint32(50).string(message.seq);
            if (message.win2winHotkey != null && $Object.hasOwnProperty.call(message, "win2winHotkey"))
                writer.uint32(56).uint32(message.win2winHotkey);
            if (message.modifiers != null && message.modifiers.length)
                writer.uint32(66).int32s(message.modifiers);
            if (message.mode != null && $Object.hasOwnProperty.call(message, "mode") && message.mode !== 0)
                writer.uint32(72).int32(message.mode);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        KeyEvent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.KeyEvent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.down = value;
                        else
                            delete message.down;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.press = value;
                        else
                            delete message.press;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        message.controlKey = reader.int32();
                        message.union = "controlKey";
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        message.chr = reader.uint32();
                        message.union = "chr";
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        message.unicode = reader.uint32();
                        message.union = "unicode";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.seq = reader.stringVerify();
                        message.union = "seq";
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        message.win2winHotkey = reader.uint32();
                        message.union = "win2winHotkey";
                        continue;
                    }
                case 8: {
                        if (wireType === 2) {
                            if (!(message.modifiers && message.modifiers.length))
                                message.modifiers = [];
                            reader.int32s(message.modifiers);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.modifiers && message.modifiers.length))
                            message.modifiers = [];
                        message.modifiers.push(reader.int32());
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.mode = value;
                        else
                            delete message.mode;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        KeyEvent.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.KeyEvent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.KeyEvent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.KeyEvent();
            if (object.down != null)
                if (object.down)
                    message.down = $Boolean(object.down);
            if (object.press != null)
                if (object.press)
                    message.press = $Boolean(object.press);
            switch (object.controlKey) {
            case "Unknown":
            case 0:
                message.controlKey = 0;
                break;
            case "Alt":
            case 1:
                message.controlKey = 1;
                break;
            case "Backspace":
            case 2:
                message.controlKey = 2;
                break;
            case "CapsLock":
            case 3:
                message.controlKey = 3;
                break;
            case "Control":
            case 4:
                message.controlKey = 4;
                break;
            case "Delete":
            case 5:
                message.controlKey = 5;
                break;
            case "DownArrow":
            case 6:
                message.controlKey = 6;
                break;
            case "End":
            case 7:
                message.controlKey = 7;
                break;
            case "Escape":
            case 8:
                message.controlKey = 8;
                break;
            case "F1":
            case 9:
                message.controlKey = 9;
                break;
            case "F10":
            case 10:
                message.controlKey = 10;
                break;
            case "F11":
            case 11:
                message.controlKey = 11;
                break;
            case "F12":
            case 12:
                message.controlKey = 12;
                break;
            case "F2":
            case 13:
                message.controlKey = 13;
                break;
            case "F3":
            case 14:
                message.controlKey = 14;
                break;
            case "F4":
            case 15:
                message.controlKey = 15;
                break;
            case "F5":
            case 16:
                message.controlKey = 16;
                break;
            case "F6":
            case 17:
                message.controlKey = 17;
                break;
            case "F7":
            case 18:
                message.controlKey = 18;
                break;
            case "F8":
            case 19:
                message.controlKey = 19;
                break;
            case "F9":
            case 20:
                message.controlKey = 20;
                break;
            case "Home":
            case 21:
                message.controlKey = 21;
                break;
            case "LeftArrow":
            case 22:
                message.controlKey = 22;
                break;
            case "Meta":
            case 23:
                message.controlKey = 23;
                break;
            case "Option":
            case 24:
                message.controlKey = 24;
                break;
            case "PageDown":
            case 25:
                message.controlKey = 25;
                break;
            case "PageUp":
            case 26:
                message.controlKey = 26;
                break;
            case "Return":
            case 27:
                message.controlKey = 27;
                break;
            case "RightArrow":
            case 28:
                message.controlKey = 28;
                break;
            case "Shift":
            case 29:
                message.controlKey = 29;
                break;
            case "Space":
            case 30:
                message.controlKey = 30;
                break;
            case "Tab":
            case 31:
                message.controlKey = 31;
                break;
            case "UpArrow":
            case 32:
                message.controlKey = 32;
                break;
            case "Numpad0":
            case 33:
                message.controlKey = 33;
                break;
            case "Numpad1":
            case 34:
                message.controlKey = 34;
                break;
            case "Numpad2":
            case 35:
                message.controlKey = 35;
                break;
            case "Numpad3":
            case 36:
                message.controlKey = 36;
                break;
            case "Numpad4":
            case 37:
                message.controlKey = 37;
                break;
            case "Numpad5":
            case 38:
                message.controlKey = 38;
                break;
            case "Numpad6":
            case 39:
                message.controlKey = 39;
                break;
            case "Numpad7":
            case 40:
                message.controlKey = 40;
                break;
            case "Numpad8":
            case 41:
                message.controlKey = 41;
                break;
            case "Numpad9":
            case 42:
                message.controlKey = 42;
                break;
            case "Cancel":
            case 43:
                message.controlKey = 43;
                break;
            case "Clear":
            case 44:
                message.controlKey = 44;
                break;
            case "Menu":
            case 45:
                message.controlKey = 45;
                break;
            case "Pause":
            case 46:
                message.controlKey = 46;
                break;
            case "Kana":
            case 47:
                message.controlKey = 47;
                break;
            case "Hangul":
            case 48:
                message.controlKey = 48;
                break;
            case "Junja":
            case 49:
                message.controlKey = 49;
                break;
            case "Final":
            case 50:
                message.controlKey = 50;
                break;
            case "Hanja":
            case 51:
                message.controlKey = 51;
                break;
            case "Kanji":
            case 52:
                message.controlKey = 52;
                break;
            case "Convert":
            case 53:
                message.controlKey = 53;
                break;
            case "Select":
            case 54:
                message.controlKey = 54;
                break;
            case "Print":
            case 55:
                message.controlKey = 55;
                break;
            case "Execute":
            case 56:
                message.controlKey = 56;
                break;
            case "Snapshot":
            case 57:
                message.controlKey = 57;
                break;
            case "Insert":
            case 58:
                message.controlKey = 58;
                break;
            case "Help":
            case 59:
                message.controlKey = 59;
                break;
            case "Sleep":
            case 60:
                message.controlKey = 60;
                break;
            case "Separator":
            case 61:
                message.controlKey = 61;
                break;
            case "Scroll":
            case 62:
                message.controlKey = 62;
                break;
            case "NumLock":
            case 63:
                message.controlKey = 63;
                break;
            case "RWin":
            case 64:
                message.controlKey = 64;
                break;
            case "Apps":
            case 65:
                message.controlKey = 65;
                break;
            case "Multiply":
            case 66:
                message.controlKey = 66;
                break;
            case "Add":
            case 67:
                message.controlKey = 67;
                break;
            case "Subtract":
            case 68:
                message.controlKey = 68;
                break;
            case "Decimal":
            case 69:
                message.controlKey = 69;
                break;
            case "Divide":
            case 70:
                message.controlKey = 70;
                break;
            case "Equals":
            case 71:
                message.controlKey = 71;
                break;
            case "NumpadEnter":
            case 72:
                message.controlKey = 72;
                break;
            case "RShift":
            case 73:
                message.controlKey = 73;
                break;
            case "RControl":
            case 74:
                message.controlKey = 74;
                break;
            case "RAlt":
            case 75:
                message.controlKey = 75;
                break;
            case "VolumeMute":
            case 76:
                message.controlKey = 76;
                break;
            case "VolumeUp":
            case 77:
                message.controlKey = 77;
                break;
            case "VolumeDown":
            case 78:
                message.controlKey = 78;
                break;
            case "Power":
            case 79:
                message.controlKey = 79;
                break;
            case "CtrlAltDel":
            case 100:
                message.controlKey = 100;
                break;
            case "LockScreen":
            case 101:
                message.controlKey = 101;
                break;
            default:
                if (typeof object.controlKey === "number" && (object.controlKey | 0) === object.controlKey)
                    message.controlKey = object.controlKey;
            }
            if (object.chr != null)
                message.chr = object.chr >>> 0;
            if (object.unicode != null)
                message.unicode = object.unicode >>> 0;
            if (object.seq != null)
                message.seq = $String(object.seq);
            if (object.win2winHotkey != null)
                message.win2winHotkey = object.win2winHotkey >>> 0;
            if (object.modifiers) {
                if (!$Array.isArray(object.modifiers))
                    throw $TypeError(".hbb.KeyEvent.modifiers: array expected");
                message.modifiers = [];
                for (let i = 0; i < object.modifiers.length; ++i)
                    switch (object.modifiers[i]) {
                    case "Unknown":
                    case 0:
                        message.modifiers[message.modifiers.length] = 0;
                        break;
                    case "Alt":
                    case 1:
                        message.modifiers[message.modifiers.length] = 1;
                        break;
                    case "Backspace":
                    case 2:
                        message.modifiers[message.modifiers.length] = 2;
                        break;
                    case "CapsLock":
                    case 3:
                        message.modifiers[message.modifiers.length] = 3;
                        break;
                    case "Control":
                    case 4:
                        message.modifiers[message.modifiers.length] = 4;
                        break;
                    case "Delete":
                    case 5:
                        message.modifiers[message.modifiers.length] = 5;
                        break;
                    case "DownArrow":
                    case 6:
                        message.modifiers[message.modifiers.length] = 6;
                        break;
                    case "End":
                    case 7:
                        message.modifiers[message.modifiers.length] = 7;
                        break;
                    case "Escape":
                    case 8:
                        message.modifiers[message.modifiers.length] = 8;
                        break;
                    case "F1":
                    case 9:
                        message.modifiers[message.modifiers.length] = 9;
                        break;
                    case "F10":
                    case 10:
                        message.modifiers[message.modifiers.length] = 10;
                        break;
                    case "F11":
                    case 11:
                        message.modifiers[message.modifiers.length] = 11;
                        break;
                    case "F12":
                    case 12:
                        message.modifiers[message.modifiers.length] = 12;
                        break;
                    case "F2":
                    case 13:
                        message.modifiers[message.modifiers.length] = 13;
                        break;
                    case "F3":
                    case 14:
                        message.modifiers[message.modifiers.length] = 14;
                        break;
                    case "F4":
                    case 15:
                        message.modifiers[message.modifiers.length] = 15;
                        break;
                    case "F5":
                    case 16:
                        message.modifiers[message.modifiers.length] = 16;
                        break;
                    case "F6":
                    case 17:
                        message.modifiers[message.modifiers.length] = 17;
                        break;
                    case "F7":
                    case 18:
                        message.modifiers[message.modifiers.length] = 18;
                        break;
                    case "F8":
                    case 19:
                        message.modifiers[message.modifiers.length] = 19;
                        break;
                    case "F9":
                    case 20:
                        message.modifiers[message.modifiers.length] = 20;
                        break;
                    case "Home":
                    case 21:
                        message.modifiers[message.modifiers.length] = 21;
                        break;
                    case "LeftArrow":
                    case 22:
                        message.modifiers[message.modifiers.length] = 22;
                        break;
                    case "Meta":
                    case 23:
                        message.modifiers[message.modifiers.length] = 23;
                        break;
                    case "Option":
                    case 24:
                        message.modifiers[message.modifiers.length] = 24;
                        break;
                    case "PageDown":
                    case 25:
                        message.modifiers[message.modifiers.length] = 25;
                        break;
                    case "PageUp":
                    case 26:
                        message.modifiers[message.modifiers.length] = 26;
                        break;
                    case "Return":
                    case 27:
                        message.modifiers[message.modifiers.length] = 27;
                        break;
                    case "RightArrow":
                    case 28:
                        message.modifiers[message.modifiers.length] = 28;
                        break;
                    case "Shift":
                    case 29:
                        message.modifiers[message.modifiers.length] = 29;
                        break;
                    case "Space":
                    case 30:
                        message.modifiers[message.modifiers.length] = 30;
                        break;
                    case "Tab":
                    case 31:
                        message.modifiers[message.modifiers.length] = 31;
                        break;
                    case "UpArrow":
                    case 32:
                        message.modifiers[message.modifiers.length] = 32;
                        break;
                    case "Numpad0":
                    case 33:
                        message.modifiers[message.modifiers.length] = 33;
                        break;
                    case "Numpad1":
                    case 34:
                        message.modifiers[message.modifiers.length] = 34;
                        break;
                    case "Numpad2":
                    case 35:
                        message.modifiers[message.modifiers.length] = 35;
                        break;
                    case "Numpad3":
                    case 36:
                        message.modifiers[message.modifiers.length] = 36;
                        break;
                    case "Numpad4":
                    case 37:
                        message.modifiers[message.modifiers.length] = 37;
                        break;
                    case "Numpad5":
                    case 38:
                        message.modifiers[message.modifiers.length] = 38;
                        break;
                    case "Numpad6":
                    case 39:
                        message.modifiers[message.modifiers.length] = 39;
                        break;
                    case "Numpad7":
                    case 40:
                        message.modifiers[message.modifiers.length] = 40;
                        break;
                    case "Numpad8":
                    case 41:
                        message.modifiers[message.modifiers.length] = 41;
                        break;
                    case "Numpad9":
                    case 42:
                        message.modifiers[message.modifiers.length] = 42;
                        break;
                    case "Cancel":
                    case 43:
                        message.modifiers[message.modifiers.length] = 43;
                        break;
                    case "Clear":
                    case 44:
                        message.modifiers[message.modifiers.length] = 44;
                        break;
                    case "Menu":
                    case 45:
                        message.modifiers[message.modifiers.length] = 45;
                        break;
                    case "Pause":
                    case 46:
                        message.modifiers[message.modifiers.length] = 46;
                        break;
                    case "Kana":
                    case 47:
                        message.modifiers[message.modifiers.length] = 47;
                        break;
                    case "Hangul":
                    case 48:
                        message.modifiers[message.modifiers.length] = 48;
                        break;
                    case "Junja":
                    case 49:
                        message.modifiers[message.modifiers.length] = 49;
                        break;
                    case "Final":
                    case 50:
                        message.modifiers[message.modifiers.length] = 50;
                        break;
                    case "Hanja":
                    case 51:
                        message.modifiers[message.modifiers.length] = 51;
                        break;
                    case "Kanji":
                    case 52:
                        message.modifiers[message.modifiers.length] = 52;
                        break;
                    case "Convert":
                    case 53:
                        message.modifiers[message.modifiers.length] = 53;
                        break;
                    case "Select":
                    case 54:
                        message.modifiers[message.modifiers.length] = 54;
                        break;
                    case "Print":
                    case 55:
                        message.modifiers[message.modifiers.length] = 55;
                        break;
                    case "Execute":
                    case 56:
                        message.modifiers[message.modifiers.length] = 56;
                        break;
                    case "Snapshot":
                    case 57:
                        message.modifiers[message.modifiers.length] = 57;
                        break;
                    case "Insert":
                    case 58:
                        message.modifiers[message.modifiers.length] = 58;
                        break;
                    case "Help":
                    case 59:
                        message.modifiers[message.modifiers.length] = 59;
                        break;
                    case "Sleep":
                    case 60:
                        message.modifiers[message.modifiers.length] = 60;
                        break;
                    case "Separator":
                    case 61:
                        message.modifiers[message.modifiers.length] = 61;
                        break;
                    case "Scroll":
                    case 62:
                        message.modifiers[message.modifiers.length] = 62;
                        break;
                    case "NumLock":
                    case 63:
                        message.modifiers[message.modifiers.length] = 63;
                        break;
                    case "RWin":
                    case 64:
                        message.modifiers[message.modifiers.length] = 64;
                        break;
                    case "Apps":
                    case 65:
                        message.modifiers[message.modifiers.length] = 65;
                        break;
                    case "Multiply":
                    case 66:
                        message.modifiers[message.modifiers.length] = 66;
                        break;
                    case "Add":
                    case 67:
                        message.modifiers[message.modifiers.length] = 67;
                        break;
                    case "Subtract":
                    case 68:
                        message.modifiers[message.modifiers.length] = 68;
                        break;
                    case "Decimal":
                    case 69:
                        message.modifiers[message.modifiers.length] = 69;
                        break;
                    case "Divide":
                    case 70:
                        message.modifiers[message.modifiers.length] = 70;
                        break;
                    case "Equals":
                    case 71:
                        message.modifiers[message.modifiers.length] = 71;
                        break;
                    case "NumpadEnter":
                    case 72:
                        message.modifiers[message.modifiers.length] = 72;
                        break;
                    case "RShift":
                    case 73:
                        message.modifiers[message.modifiers.length] = 73;
                        break;
                    case "RControl":
                    case 74:
                        message.modifiers[message.modifiers.length] = 74;
                        break;
                    case "RAlt":
                    case 75:
                        message.modifiers[message.modifiers.length] = 75;
                        break;
                    case "VolumeMute":
                    case 76:
                        message.modifiers[message.modifiers.length] = 76;
                        break;
                    case "VolumeUp":
                    case 77:
                        message.modifiers[message.modifiers.length] = 77;
                        break;
                    case "VolumeDown":
                    case 78:
                        message.modifiers[message.modifiers.length] = 78;
                        break;
                    case "Power":
                    case 79:
                        message.modifiers[message.modifiers.length] = 79;
                        break;
                    case "CtrlAltDel":
                    case 100:
                        message.modifiers[message.modifiers.length] = 100;
                        break;
                    case "LockScreen":
                    case 101:
                        message.modifiers[message.modifiers.length] = 101;
                        break;
                    default:
                        if (typeof object.modifiers[i] === "number" && (object.modifiers[i] | 0) === object.modifiers[i])
                            message.modifiers[message.modifiers.length] = object.modifiers[i];
                    }
            }
            if (object.mode !== 0 && (typeof object.mode !== "string" || $root.hbb.KeyboardMode[object.mode] !== 0))
                switch (object.mode) {
                case "Legacy":
                case 0:
                    message.mode = 0;
                    break;
                case "Map":
                case 1:
                    message.mode = 1;
                    break;
                case "Translate":
                case 2:
                    message.mode = 2;
                    break;
                case "Auto":
                case 3:
                    message.mode = 3;
                    break;
                default:
                    if (typeof object.mode === "number" && (object.mode | 0) === object.mode)
                        message.mode = object.mode;
                }
            return message;
        };

        KeyEvent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.modifiers = [];
            if (options.defaults) {
                object.down = false;
                object.press = false;
                object.mode = options.enums === $String ? "Legacy" : 0;
            }
            if (message.down != null && $Object.hasOwnProperty.call(message, "down"))
                object.down = message.down;
            if (message.press != null && $Object.hasOwnProperty.call(message, "press"))
                object.press = message.press;
            if (message.controlKey != null && $Object.hasOwnProperty.call(message, "controlKey")) {
                object.controlKey = options.enums === $String ? $root.hbb.ControlKey[message.controlKey] === $undefined ? message.controlKey : $root.hbb.ControlKey[message.controlKey] : message.controlKey;
                if (options.oneofs)
                    object.union = "controlKey";
            }
            if (message.chr != null && $Object.hasOwnProperty.call(message, "chr")) {
                object.chr = message.chr;
                if (options.oneofs)
                    object.union = "chr";
            }
            if (message.unicode != null && $Object.hasOwnProperty.call(message, "unicode")) {
                object.unicode = message.unicode;
                if (options.oneofs)
                    object.union = "unicode";
            }
            if (message.seq != null && $Object.hasOwnProperty.call(message, "seq")) {
                object.seq = message.seq;
                if (options.oneofs)
                    object.union = "seq";
            }
            if (message.win2winHotkey != null && $Object.hasOwnProperty.call(message, "win2winHotkey")) {
                object.win2winHotkey = message.win2winHotkey;
                if (options.oneofs)
                    object.union = "win2winHotkey";
            }
            if (message.modifiers && message.modifiers.length) {
                object.modifiers = $Array(message.modifiers.length);
                for (let j = 0; j < message.modifiers.length; ++j)
                    object.modifiers[j] = options.enums === $String ? $root.hbb.ControlKey[message.modifiers[j]] === $undefined ? message.modifiers[j] : $root.hbb.ControlKey[message.modifiers[j]] : message.modifiers[j];
            }
            if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
                object.mode = options.enums === $String ? $root.hbb.KeyboardMode[message.mode] === $undefined ? message.mode : $root.hbb.KeyboardMode[message.mode] : message.mode;
            return object;
        };

        KeyEvent.prototype.toJSON = function() {
            return KeyEvent.toObject(this, $protobuf.util.toJSONOptions);
        };

        return KeyEvent;
    })();

    hbb.CursorData = (function() {

        const CursorData = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CursorData.prototype.id = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        CursorData.prototype.hotx = 0;
        CursorData.prototype.hoty = 0;
        CursorData.prototype.width = 0;
        CursorData.prototype.height = 0;
        CursorData.prototype.colors = $util.newBuffer([]);

        CursorData.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && (typeof message.id === "object" ? message.id.low || message.id.high : message.id !== 0))
                writer.uint32(8).uint64(message.id);
            if (message.hotx != null && $Object.hasOwnProperty.call(message, "hotx") && message.hotx !== 0)
                writer.uint32(16).sint32(message.hotx);
            if (message.hoty != null && $Object.hasOwnProperty.call(message, "hoty") && message.hoty !== 0)
                writer.uint32(24).sint32(message.hoty);
            if (message.width != null && $Object.hasOwnProperty.call(message, "width") && message.width !== 0)
                writer.uint32(32).int32(message.width);
            if (message.height != null && $Object.hasOwnProperty.call(message, "height") && message.height !== 0)
                writer.uint32(40).int32(message.height);
            if (message.colors != null && $Object.hasOwnProperty.call(message, "colors") && message.colors.length)
                writer.uint32(50).bytes(message.colors);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CursorData.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CursorData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.hotx = value;
                        else
                            delete message.hotx;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.hoty = value;
                        else
                            delete message.hoty;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.width = value;
                        else
                            delete message.width;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.height = value;
                        else
                            delete message.height;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.colors = value;
                        else
                            delete message.colors;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CursorData.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CursorData)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CursorData: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CursorData();
            if (object.id != null)
                if (typeof object.id === "object" ? object.id.low || object.id.high : $Number(object.id) !== 0)
                    if ($util.Long)
                        message.id = $util.Long.fromValue(object.id, true);
                    else if (typeof object.id === "string")
                        message.id = $parseInt(object.id, 10);
                    else if (typeof object.id === "number")
                        message.id = object.id;
                    else if (typeof object.id === "object")
                        message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber(true);
            if (object.hotx != null)
                if ($Number(object.hotx) !== 0)
                    message.hotx = object.hotx | 0;
            if (object.hoty != null)
                if ($Number(object.hoty) !== 0)
                    message.hoty = object.hoty | 0;
            if (object.width != null)
                if ($Number(object.width) !== 0)
                    message.width = object.width | 0;
            if (object.height != null)
                if ($Number(object.height) !== 0)
                    message.height = object.height | 0;
            if (object.colors != null)
                if (object.colors.length)
                    if (typeof object.colors === "string")
                        $util.base64.decode(object.colors, message.colors = $util.newBuffer($util.base64.length(object.colors)), 0);
                    else if (object.colors.length >= 0)
                        message.colors = object.colors;
            return message;
        };

        CursorData.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.hotx = 0;
                object.hoty = 0;
                object.width = 0;
                object.height = 0;
                if (options.bytes === $String)
                    object.colors = "";
                else {
                    object.colors = [];
                    if (options.bytes !== $Array)
                        object.colors = $util.newBuffer(object.colors);
                }
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.id = typeof message.id === "number" ? $BigInt(message.id) : $util.Long.fromBits(message.id.low >>> 0, message.id.high >>> 0, true).toBigInt();
                else if (typeof message.id === "number")
                    object.id = options.longs === $String ? $String(message.id) : message.id;
                else
                    object.id = options.longs === $String ? $util.Long.prototype.toString.call(message.id) : options.longs === $Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber(true) : message.id;
            if (message.hotx != null && $Object.hasOwnProperty.call(message, "hotx"))
                object.hotx = message.hotx;
            if (message.hoty != null && $Object.hasOwnProperty.call(message, "hoty"))
                object.hoty = message.hoty;
            if (message.width != null && $Object.hasOwnProperty.call(message, "width"))
                object.width = message.width;
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            if (message.colors != null && $Object.hasOwnProperty.call(message, "colors"))
                object.colors = options.bytes === $String ? $util.base64.encode(message.colors, 0, message.colors.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.colors) : message.colors;
            return object;
        };

        CursorData.prototype.toJSON = function() {
            return CursorData.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CursorData;
    })();

    hbb.CursorPosition = (function() {

        const CursorPosition = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CursorPosition.prototype.x = 0;
        CursorPosition.prototype.y = 0;

        CursorPosition.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.x != null && $Object.hasOwnProperty.call(message, "x") && message.x !== 0)
                writer.uint32(8).sint32(message.x);
            if (message.y != null && $Object.hasOwnProperty.call(message, "y") && message.y !== 0)
                writer.uint32(16).sint32(message.y);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CursorPosition.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CursorPosition(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.x = value;
                        else
                            delete message.x;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.y = value;
                        else
                            delete message.y;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CursorPosition.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CursorPosition)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CursorPosition: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CursorPosition();
            if (object.x != null)
                if ($Number(object.x) !== 0)
                    message.x = object.x | 0;
            if (object.y != null)
                if ($Number(object.y) !== 0)
                    message.y = object.y | 0;
            return message;
        };

        CursorPosition.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.x = 0;
                object.y = 0;
            }
            if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                object.x = message.x;
            if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                object.y = message.y;
            return object;
        };

        CursorPosition.prototype.toJSON = function() {
            return CursorPosition.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CursorPosition;
    })();

    hbb.Hash = (function() {

        const Hash = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        Hash.prototype.salt = "";
        Hash.prototype.challenge = "";

        Hash.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.salt != null && $Object.hasOwnProperty.call(message, "salt") && message.salt !== "")
                writer.uint32(10).string(message.salt);
            if (message.challenge != null && $Object.hasOwnProperty.call(message, "challenge") && message.challenge !== "")
                writer.uint32(18).string(message.challenge);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Hash.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.Hash(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.salt = value;
                        else
                            delete message.salt;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.challenge = value;
                        else
                            delete message.challenge;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        Hash.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.Hash)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.Hash: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.Hash();
            if (object.salt != null)
                if (typeof object.salt !== "string" || object.salt.length)
                    message.salt = $String(object.salt);
            if (object.challenge != null)
                if (typeof object.challenge !== "string" || object.challenge.length)
                    message.challenge = $String(object.challenge);
            return message;
        };

        Hash.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.salt = "";
                object.challenge = "";
            }
            if (message.salt != null && $Object.hasOwnProperty.call(message, "salt"))
                object.salt = message.salt;
            if (message.challenge != null && $Object.hasOwnProperty.call(message, "challenge"))
                object.challenge = message.challenge;
            return object;
        };

        Hash.prototype.toJSON = function() {
            return Hash.toObject(this, $protobuf.util.toJSONOptions);
        };

        return Hash;
    })();

    hbb.ClipboardFormat = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "Text"] = 0;
        values[valuesById[1] = "Rtf"] = 1;
        values[valuesById[2] = "Html"] = 2;
        values[valuesById[21] = "ImageRgba"] = 21;
        values[valuesById[22] = "ImagePng"] = 22;
        values[valuesById[23] = "ImageSvg"] = 23;
        values[valuesById[31] = "Special"] = 31;
        return values;
    })();

    hbb.Clipboard = (function() {

        const Clipboard = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        Clipboard.prototype.compress = false;
        Clipboard.prototype.content = $util.newBuffer([]);
        Clipboard.prototype.width = 0;
        Clipboard.prototype.height = 0;
        Clipboard.prototype.format = 0;
        Clipboard.prototype.specialName = "";

        Clipboard.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.compress != null && $Object.hasOwnProperty.call(message, "compress") && message.compress !== false)
                writer.uint32(8).bool(message.compress);
            if (message.content != null && $Object.hasOwnProperty.call(message, "content") && message.content.length)
                writer.uint32(18).bytes(message.content);
            if (message.width != null && $Object.hasOwnProperty.call(message, "width") && message.width !== 0)
                writer.uint32(24).int32(message.width);
            if (message.height != null && $Object.hasOwnProperty.call(message, "height") && message.height !== 0)
                writer.uint32(32).int32(message.height);
            if (message.format != null && $Object.hasOwnProperty.call(message, "format") && message.format !== 0)
                writer.uint32(40).int32(message.format);
            if (message.specialName != null && $Object.hasOwnProperty.call(message, "specialName") && message.specialName !== "")
                writer.uint32(50).string(message.specialName);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Clipboard.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.Clipboard(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.compress = value;
                        else
                            delete message.compress;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.width = value;
                        else
                            delete message.width;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.height = value;
                        else
                            delete message.height;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.format = value;
                        else
                            delete message.format;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.specialName = value;
                        else
                            delete message.specialName;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        Clipboard.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.Clipboard)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.Clipboard: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.Clipboard();
            if (object.compress != null)
                if (object.compress)
                    message.compress = $Boolean(object.compress);
            if (object.content != null)
                if (object.content.length)
                    if (typeof object.content === "string")
                        $util.base64.decode(object.content, message.content = $util.newBuffer($util.base64.length(object.content)), 0);
                    else if (object.content.length >= 0)
                        message.content = object.content;
            if (object.width != null)
                if ($Number(object.width) !== 0)
                    message.width = object.width | 0;
            if (object.height != null)
                if ($Number(object.height) !== 0)
                    message.height = object.height | 0;
            if (object.format !== 0 && (typeof object.format !== "string" || $root.hbb.ClipboardFormat[object.format] !== 0))
                switch (object.format) {
                case "Text":
                case 0:
                    message.format = 0;
                    break;
                case "Rtf":
                case 1:
                    message.format = 1;
                    break;
                case "Html":
                case 2:
                    message.format = 2;
                    break;
                case "ImageRgba":
                case 21:
                    message.format = 21;
                    break;
                case "ImagePng":
                case 22:
                    message.format = 22;
                    break;
                case "ImageSvg":
                case 23:
                    message.format = 23;
                    break;
                case "Special":
                case 31:
                    message.format = 31;
                    break;
                default:
                    if (typeof object.format === "number" && (object.format | 0) === object.format)
                        message.format = object.format;
                }
            if (object.specialName != null)
                if (typeof object.specialName !== "string" || object.specialName.length)
                    message.specialName = $String(object.specialName);
            return message;
        };

        Clipboard.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.compress = false;
                if (options.bytes === $String)
                    object.content = "";
                else {
                    object.content = [];
                    if (options.bytes !== $Array)
                        object.content = $util.newBuffer(object.content);
                }
                object.width = 0;
                object.height = 0;
                object.format = options.enums === $String ? "Text" : 0;
                object.specialName = "";
            }
            if (message.compress != null && $Object.hasOwnProperty.call(message, "compress"))
                object.compress = message.compress;
            if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                object.content = options.bytes === $String ? $util.base64.encode(message.content, 0, message.content.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.content) : message.content;
            if (message.width != null && $Object.hasOwnProperty.call(message, "width"))
                object.width = message.width;
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            if (message.format != null && $Object.hasOwnProperty.call(message, "format"))
                object.format = options.enums === $String ? $root.hbb.ClipboardFormat[message.format] === $undefined ? message.format : $root.hbb.ClipboardFormat[message.format] : message.format;
            if (message.specialName != null && $Object.hasOwnProperty.call(message, "specialName"))
                object.specialName = message.specialName;
            return object;
        };

        Clipboard.prototype.toJSON = function() {
            return Clipboard.toObject(this, $protobuf.util.toJSONOptions);
        };

        return Clipboard;
    })();

    hbb.MultiClipboards = (function() {

        const MultiClipboards = function (properties) {
            this.clipboards = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        MultiClipboards.prototype.clipboards = $util.emptyArray;

        MultiClipboards.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.clipboards != null && message.clipboards.length)
                for (let i = 0; i < message.clipboards.length; ++i)
                    $root.hbb.Clipboard.encode(message.clipboards[i], writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MultiClipboards.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.MultiClipboards();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.clipboards && message.clipboards.length))
                            message.clipboards = [];
                        message.clipboards.push($root.hbb.Clipboard.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        MultiClipboards.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.MultiClipboards)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.MultiClipboards: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.MultiClipboards();
            if (object.clipboards) {
                if (!$Array.isArray(object.clipboards))
                    throw $TypeError(".hbb.MultiClipboards.clipboards: array expected");
                message.clipboards = $Array(object.clipboards.length);
                for (let i = 0; i < object.clipboards.length; ++i) {
                    if (!$util.isObject(object.clipboards[i]))
                        throw $TypeError(".hbb.MultiClipboards.clipboards: object expected");
                    message.clipboards[i] = $root.hbb.Clipboard.fromObject(object.clipboards[i], _depth + 1);
                }
            }
            return message;
        };

        MultiClipboards.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.clipboards = [];
            if (message.clipboards && message.clipboards.length) {
                object.clipboards = $Array(message.clipboards.length);
                for (let j = 0; j < message.clipboards.length; ++j)
                    object.clipboards[j] = $root.hbb.Clipboard.toObject(message.clipboards[j], options, _depth + 1);
            }
            return object;
        };

        MultiClipboards.prototype.toJSON = function() {
            return MultiClipboards.toObject(this, $protobuf.util.toJSONOptions);
        };

        return MultiClipboards;
    })();

    hbb.FileType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "Dir"] = 0;
        values[valuesById[2] = "DirLink"] = 2;
        values[valuesById[3] = "DirDrive"] = 3;
        values[valuesById[4] = "File"] = 4;
        values[valuesById[5] = "FileLink"] = 5;
        return values;
    })();

    hbb.FileEntry = (function() {

        const FileEntry = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileEntry.prototype.entryType = 0;
        FileEntry.prototype.name = "";
        FileEntry.prototype.isHidden = false;
        FileEntry.prototype.size = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        FileEntry.prototype.modifiedTime = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        FileEntry.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.entryType != null && $Object.hasOwnProperty.call(message, "entryType") && message.entryType !== 0)
                writer.uint32(8).int32(message.entryType);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(18).string(message.name);
            if (message.isHidden != null && $Object.hasOwnProperty.call(message, "isHidden") && message.isHidden !== false)
                writer.uint32(24).bool(message.isHidden);
            if (message.size != null && $Object.hasOwnProperty.call(message, "size") && (typeof message.size === "object" ? message.size.low || message.size.high : message.size !== 0))
                writer.uint32(32).uint64(message.size);
            if (message.modifiedTime != null && $Object.hasOwnProperty.call(message, "modifiedTime") && (typeof message.modifiedTime === "object" ? message.modifiedTime.low || message.modifiedTime.high : message.modifiedTime !== 0))
                writer.uint32(40).uint64(message.modifiedTime);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileEntry.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileEntry(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.entryType = value;
                        else
                            delete message.entryType;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isHidden = value;
                        else
                            delete message.isHidden;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.size = value;
                        else
                            delete message.size;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.modifiedTime = value;
                        else
                            delete message.modifiedTime;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileEntry.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileEntry)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileEntry: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileEntry();
            if (object.entryType !== 0 && (typeof object.entryType !== "string" || $root.hbb.FileType[object.entryType] !== 0))
                switch (object.entryType) {
                case "Dir":
                case 0:
                    message.entryType = 0;
                    break;
                case "DirLink":
                case 2:
                    message.entryType = 2;
                    break;
                case "DirDrive":
                case 3:
                    message.entryType = 3;
                    break;
                case "File":
                case 4:
                    message.entryType = 4;
                    break;
                case "FileLink":
                case 5:
                    message.entryType = 5;
                    break;
                default:
                    if (typeof object.entryType === "number" && (object.entryType | 0) === object.entryType)
                        message.entryType = object.entryType;
                }
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.isHidden != null)
                if (object.isHidden)
                    message.isHidden = $Boolean(object.isHidden);
            if (object.size != null)
                if (typeof object.size === "object" ? object.size.low || object.size.high : $Number(object.size) !== 0)
                    if ($util.Long)
                        message.size = $util.Long.fromValue(object.size, true);
                    else if (typeof object.size === "string")
                        message.size = $parseInt(object.size, 10);
                    else if (typeof object.size === "number")
                        message.size = object.size;
                    else if (typeof object.size === "object")
                        message.size = new $util.LongBits(object.size.low >>> 0, object.size.high >>> 0).toNumber(true);
            if (object.modifiedTime != null)
                if (typeof object.modifiedTime === "object" ? object.modifiedTime.low || object.modifiedTime.high : $Number(object.modifiedTime) !== 0)
                    if ($util.Long)
                        message.modifiedTime = $util.Long.fromValue(object.modifiedTime, true);
                    else if (typeof object.modifiedTime === "string")
                        message.modifiedTime = $parseInt(object.modifiedTime, 10);
                    else if (typeof object.modifiedTime === "number")
                        message.modifiedTime = object.modifiedTime;
                    else if (typeof object.modifiedTime === "object")
                        message.modifiedTime = new $util.LongBits(object.modifiedTime.low >>> 0, object.modifiedTime.high >>> 0).toNumber(true);
            return message;
        };

        FileEntry.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.entryType = options.enums === $String ? "Dir" : 0;
                object.name = "";
                object.isHidden = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.size = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.size = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.modifiedTime = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.modifiedTime = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.entryType != null && $Object.hasOwnProperty.call(message, "entryType"))
                object.entryType = options.enums === $String ? $root.hbb.FileType[message.entryType] === $undefined ? message.entryType : $root.hbb.FileType[message.entryType] : message.entryType;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.isHidden != null && $Object.hasOwnProperty.call(message, "isHidden"))
                object.isHidden = message.isHidden;
            if (message.size != null && $Object.hasOwnProperty.call(message, "size"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.size = typeof message.size === "number" ? $BigInt(message.size) : $util.Long.fromBits(message.size.low >>> 0, message.size.high >>> 0, true).toBigInt();
                else if (typeof message.size === "number")
                    object.size = options.longs === $String ? $String(message.size) : message.size;
                else
                    object.size = options.longs === $String ? $util.Long.prototype.toString.call(message.size) : options.longs === $Number ? new $util.LongBits(message.size.low >>> 0, message.size.high >>> 0).toNumber(true) : message.size;
            if (message.modifiedTime != null && $Object.hasOwnProperty.call(message, "modifiedTime"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.modifiedTime = typeof message.modifiedTime === "number" ? $BigInt(message.modifiedTime) : $util.Long.fromBits(message.modifiedTime.low >>> 0, message.modifiedTime.high >>> 0, true).toBigInt();
                else if (typeof message.modifiedTime === "number")
                    object.modifiedTime = options.longs === $String ? $String(message.modifiedTime) : message.modifiedTime;
                else
                    object.modifiedTime = options.longs === $String ? $util.Long.prototype.toString.call(message.modifiedTime) : options.longs === $Number ? new $util.LongBits(message.modifiedTime.low >>> 0, message.modifiedTime.high >>> 0).toNumber(true) : message.modifiedTime;
            return object;
        };

        FileEntry.prototype.toJSON = function() {
            return FileEntry.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileEntry;
    })();

    hbb.FileDirectory = (function() {

        const FileDirectory = function (properties) {
            this.entries = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileDirectory.prototype.id = 0;
        FileDirectory.prototype.path = "";
        FileDirectory.prototype.entries = $util.emptyArray;

        FileDirectory.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(18).string(message.path);
            if (message.entries != null && message.entries.length)
                for (let i = 0; i < message.entries.length; ++i)
                    $root.hbb.FileEntry.encode(message.entries[i], writer.uint32(26).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileDirectory.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileDirectory(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.entries && message.entries.length))
                            message.entries = [];
                        message.entries.push($root.hbb.FileEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileDirectory.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileDirectory)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileDirectory: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileDirectory();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.entries) {
                if (!$Array.isArray(object.entries))
                    throw $TypeError(".hbb.FileDirectory.entries: array expected");
                message.entries = $Array(object.entries.length);
                for (let i = 0; i < object.entries.length; ++i) {
                    if (!$util.isObject(object.entries[i]))
                        throw $TypeError(".hbb.FileDirectory.entries: object expected");
                    message.entries[i] = $root.hbb.FileEntry.fromObject(object.entries[i], _depth + 1);
                }
            }
            return message;
        };

        FileDirectory.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.entries = [];
            if (options.defaults) {
                object.id = 0;
                object.path = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.entries && message.entries.length) {
                object.entries = $Array(message.entries.length);
                for (let j = 0; j < message.entries.length; ++j)
                    object.entries[j] = $root.hbb.FileEntry.toObject(message.entries[j], options, _depth + 1);
            }
            return object;
        };

        FileDirectory.prototype.toJSON = function() {
            return FileDirectory.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileDirectory;
    })();

    hbb.ReadDir = (function() {

        const ReadDir = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ReadDir.prototype.path = "";
        ReadDir.prototype.includeHidden = false;

        ReadDir.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(10).string(message.path);
            if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden") && message.includeHidden !== false)
                writer.uint32(16).bool(message.includeHidden);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReadDir.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ReadDir(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.includeHidden = value;
                        else
                            delete message.includeHidden;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ReadDir.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ReadDir)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ReadDir: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ReadDir();
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.includeHidden != null)
                if (object.includeHidden)
                    message.includeHidden = $Boolean(object.includeHidden);
            return message;
        };

        ReadDir.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.path = "";
                object.includeHidden = false;
            }
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden"))
                object.includeHidden = message.includeHidden;
            return object;
        };

        ReadDir.prototype.toJSON = function() {
            return ReadDir.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ReadDir;
    })();

    hbb.ReadEmptyDirs = (function() {

        const ReadEmptyDirs = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ReadEmptyDirs.prototype.path = "";
        ReadEmptyDirs.prototype.includeHidden = false;

        ReadEmptyDirs.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(10).string(message.path);
            if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden") && message.includeHidden !== false)
                writer.uint32(16).bool(message.includeHidden);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReadEmptyDirs.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ReadEmptyDirs(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.includeHidden = value;
                        else
                            delete message.includeHidden;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ReadEmptyDirs.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ReadEmptyDirs)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ReadEmptyDirs: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ReadEmptyDirs();
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.includeHidden != null)
                if (object.includeHidden)
                    message.includeHidden = $Boolean(object.includeHidden);
            return message;
        };

        ReadEmptyDirs.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.path = "";
                object.includeHidden = false;
            }
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden"))
                object.includeHidden = message.includeHidden;
            return object;
        };

        ReadEmptyDirs.prototype.toJSON = function() {
            return ReadEmptyDirs.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ReadEmptyDirs;
    })();

    hbb.ReadEmptyDirsResponse = (function() {

        const ReadEmptyDirsResponse = function (properties) {
            this.emptyDirs = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ReadEmptyDirsResponse.prototype.path = "";
        ReadEmptyDirsResponse.prototype.emptyDirs = $util.emptyArray;

        ReadEmptyDirsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(10).string(message.path);
            if (message.emptyDirs != null && message.emptyDirs.length)
                for (let i = 0; i < message.emptyDirs.length; ++i)
                    $root.hbb.FileDirectory.encode(message.emptyDirs[i], writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReadEmptyDirsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ReadEmptyDirsResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.emptyDirs && message.emptyDirs.length))
                            message.emptyDirs = [];
                        message.emptyDirs.push($root.hbb.FileDirectory.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ReadEmptyDirsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ReadEmptyDirsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ReadEmptyDirsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ReadEmptyDirsResponse();
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.emptyDirs) {
                if (!$Array.isArray(object.emptyDirs))
                    throw $TypeError(".hbb.ReadEmptyDirsResponse.emptyDirs: array expected");
                message.emptyDirs = $Array(object.emptyDirs.length);
                for (let i = 0; i < object.emptyDirs.length; ++i) {
                    if (!$util.isObject(object.emptyDirs[i]))
                        throw $TypeError(".hbb.ReadEmptyDirsResponse.emptyDirs: object expected");
                    message.emptyDirs[i] = $root.hbb.FileDirectory.fromObject(object.emptyDirs[i], _depth + 1);
                }
            }
            return message;
        };

        ReadEmptyDirsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.emptyDirs = [];
            if (options.defaults)
                object.path = "";
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.emptyDirs && message.emptyDirs.length) {
                object.emptyDirs = $Array(message.emptyDirs.length);
                for (let j = 0; j < message.emptyDirs.length; ++j)
                    object.emptyDirs[j] = $root.hbb.FileDirectory.toObject(message.emptyDirs[j], options, _depth + 1);
            }
            return object;
        };

        ReadEmptyDirsResponse.prototype.toJSON = function() {
            return ReadEmptyDirsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ReadEmptyDirsResponse;
    })();

    hbb.ReadAllFiles = (function() {

        const ReadAllFiles = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ReadAllFiles.prototype.id = 0;
        ReadAllFiles.prototype.path = "";
        ReadAllFiles.prototype.includeHidden = false;

        ReadAllFiles.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(18).string(message.path);
            if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden") && message.includeHidden !== false)
                writer.uint32(24).bool(message.includeHidden);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ReadAllFiles.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ReadAllFiles(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.includeHidden = value;
                        else
                            delete message.includeHidden;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ReadAllFiles.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ReadAllFiles)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ReadAllFiles: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ReadAllFiles();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.includeHidden != null)
                if (object.includeHidden)
                    message.includeHidden = $Boolean(object.includeHidden);
            return message;
        };

        ReadAllFiles.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.path = "";
                object.includeHidden = false;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden"))
                object.includeHidden = message.includeHidden;
            return object;
        };

        ReadAllFiles.prototype.toJSON = function() {
            return ReadAllFiles.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ReadAllFiles;
    })();

    hbb.FileRename = (function() {

        const FileRename = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileRename.prototype.id = 0;
        FileRename.prototype.path = "";
        FileRename.prototype.newName = "";

        FileRename.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(18).string(message.path);
            if (message.newName != null && $Object.hasOwnProperty.call(message, "newName") && message.newName !== "")
                writer.uint32(26).string(message.newName);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileRename.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileRename(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.newName = value;
                        else
                            delete message.newName;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileRename.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileRename)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileRename: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileRename();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.newName != null)
                if (typeof object.newName !== "string" || object.newName.length)
                    message.newName = $String(object.newName);
            return message;
        };

        FileRename.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.path = "";
                object.newName = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.newName != null && $Object.hasOwnProperty.call(message, "newName"))
                object.newName = message.newName;
            return object;
        };

        FileRename.prototype.toJSON = function() {
            return FileRename.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileRename;
    })();

    hbb.FileAction = (function() {

        const FileAction = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileAction.prototype.readDir = null;
        FileAction.prototype.send = null;
        FileAction.prototype.receive = null;
        FileAction.prototype.create = null;
        FileAction.prototype.removeDir = null;
        FileAction.prototype.removeFile = null;
        FileAction.prototype.allFiles = null;
        FileAction.prototype.cancel = null;
        FileAction.prototype.sendConfirm = null;
        FileAction.prototype.rename = null;
        FileAction.prototype.readEmptyDirs = null;

        let $oneOfFields;

        $Object.defineProperty(FileAction.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["readDir", "send", "receive", "create", "removeDir", "removeFile", "allFiles", "cancel", "sendConfirm", "rename", "readEmptyDirs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        FileAction.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.readDir != null && $Object.hasOwnProperty.call(message, "readDir"))
                $root.hbb.ReadDir.encode(message.readDir, writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.send != null && $Object.hasOwnProperty.call(message, "send"))
                $root.hbb.FileTransferSendRequest.encode(message.send, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.receive != null && $Object.hasOwnProperty.call(message, "receive"))
                $root.hbb.FileTransferReceiveRequest.encode(message.receive, writer.uint32(26).fork(), _depth + 1).ldelim();
            if (message.create != null && $Object.hasOwnProperty.call(message, "create"))
                $root.hbb.FileDirCreate.encode(message.create, writer.uint32(34).fork(), _depth + 1).ldelim();
            if (message.removeDir != null && $Object.hasOwnProperty.call(message, "removeDir"))
                $root.hbb.FileRemoveDir.encode(message.removeDir, writer.uint32(42).fork(), _depth + 1).ldelim();
            if (message.removeFile != null && $Object.hasOwnProperty.call(message, "removeFile"))
                $root.hbb.FileRemoveFile.encode(message.removeFile, writer.uint32(50).fork(), _depth + 1).ldelim();
            if (message.allFiles != null && $Object.hasOwnProperty.call(message, "allFiles"))
                $root.hbb.ReadAllFiles.encode(message.allFiles, writer.uint32(58).fork(), _depth + 1).ldelim();
            if (message.cancel != null && $Object.hasOwnProperty.call(message, "cancel"))
                $root.hbb.FileTransferCancel.encode(message.cancel, writer.uint32(66).fork(), _depth + 1).ldelim();
            if (message.sendConfirm != null && $Object.hasOwnProperty.call(message, "sendConfirm"))
                $root.hbb.FileTransferSendConfirmRequest.encode(message.sendConfirm, writer.uint32(74).fork(), _depth + 1).ldelim();
            if (message.rename != null && $Object.hasOwnProperty.call(message, "rename"))
                $root.hbb.FileRename.encode(message.rename, writer.uint32(82).fork(), _depth + 1).ldelim();
            if (message.readEmptyDirs != null && $Object.hasOwnProperty.call(message, "readEmptyDirs"))
                $root.hbb.ReadEmptyDirs.encode(message.readEmptyDirs, writer.uint32(90).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileAction.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileAction();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.readDir = $root.hbb.ReadDir.decode(reader, reader.uint32(), $undefined, _depth + 1, message.readDir);
                        message.union = "readDir";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.send = $root.hbb.FileTransferSendRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.send);
                        message.union = "send";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.receive = $root.hbb.FileTransferReceiveRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.receive);
                        message.union = "receive";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.create = $root.hbb.FileDirCreate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.create);
                        message.union = "create";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.removeDir = $root.hbb.FileRemoveDir.decode(reader, reader.uint32(), $undefined, _depth + 1, message.removeDir);
                        message.union = "removeDir";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.removeFile = $root.hbb.FileRemoveFile.decode(reader, reader.uint32(), $undefined, _depth + 1, message.removeFile);
                        message.union = "removeFile";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.allFiles = $root.hbb.ReadAllFiles.decode(reader, reader.uint32(), $undefined, _depth + 1, message.allFiles);
                        message.union = "allFiles";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.cancel = $root.hbb.FileTransferCancel.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cancel);
                        message.union = "cancel";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.sendConfirm = $root.hbb.FileTransferSendConfirmRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.sendConfirm);
                        message.union = "sendConfirm";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.rename = $root.hbb.FileRename.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rename);
                        message.union = "rename";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.readEmptyDirs = $root.hbb.ReadEmptyDirs.decode(reader, reader.uint32(), $undefined, _depth + 1, message.readEmptyDirs);
                        message.union = "readEmptyDirs";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileAction.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileAction)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileAction: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileAction();
            if (object.readDir != null) {
                if (!$util.isObject(object.readDir))
                    throw $TypeError(".hbb.FileAction.readDir: object expected");
                message.readDir = $root.hbb.ReadDir.fromObject(object.readDir, _depth + 1);
            }
            if (object.send != null) {
                if (!$util.isObject(object.send))
                    throw $TypeError(".hbb.FileAction.send: object expected");
                message.send = $root.hbb.FileTransferSendRequest.fromObject(object.send, _depth + 1);
            }
            if (object.receive != null) {
                if (!$util.isObject(object.receive))
                    throw $TypeError(".hbb.FileAction.receive: object expected");
                message.receive = $root.hbb.FileTransferReceiveRequest.fromObject(object.receive, _depth + 1);
            }
            if (object.create != null) {
                if (!$util.isObject(object.create))
                    throw $TypeError(".hbb.FileAction.create: object expected");
                message.create = $root.hbb.FileDirCreate.fromObject(object.create, _depth + 1);
            }
            if (object.removeDir != null) {
                if (!$util.isObject(object.removeDir))
                    throw $TypeError(".hbb.FileAction.removeDir: object expected");
                message.removeDir = $root.hbb.FileRemoveDir.fromObject(object.removeDir, _depth + 1);
            }
            if (object.removeFile != null) {
                if (!$util.isObject(object.removeFile))
                    throw $TypeError(".hbb.FileAction.removeFile: object expected");
                message.removeFile = $root.hbb.FileRemoveFile.fromObject(object.removeFile, _depth + 1);
            }
            if (object.allFiles != null) {
                if (!$util.isObject(object.allFiles))
                    throw $TypeError(".hbb.FileAction.allFiles: object expected");
                message.allFiles = $root.hbb.ReadAllFiles.fromObject(object.allFiles, _depth + 1);
            }
            if (object.cancel != null) {
                if (!$util.isObject(object.cancel))
                    throw $TypeError(".hbb.FileAction.cancel: object expected");
                message.cancel = $root.hbb.FileTransferCancel.fromObject(object.cancel, _depth + 1);
            }
            if (object.sendConfirm != null) {
                if (!$util.isObject(object.sendConfirm))
                    throw $TypeError(".hbb.FileAction.sendConfirm: object expected");
                message.sendConfirm = $root.hbb.FileTransferSendConfirmRequest.fromObject(object.sendConfirm, _depth + 1);
            }
            if (object.rename != null) {
                if (!$util.isObject(object.rename))
                    throw $TypeError(".hbb.FileAction.rename: object expected");
                message.rename = $root.hbb.FileRename.fromObject(object.rename, _depth + 1);
            }
            if (object.readEmptyDirs != null) {
                if (!$util.isObject(object.readEmptyDirs))
                    throw $TypeError(".hbb.FileAction.readEmptyDirs: object expected");
                message.readEmptyDirs = $root.hbb.ReadEmptyDirs.fromObject(object.readEmptyDirs, _depth + 1);
            }
            return message;
        };

        FileAction.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.readDir != null && $Object.hasOwnProperty.call(message, "readDir")) {
                object.readDir = $root.hbb.ReadDir.toObject(message.readDir, options, _depth + 1);
                if (options.oneofs)
                    object.union = "readDir";
            }
            if (message.send != null && $Object.hasOwnProperty.call(message, "send")) {
                object.send = $root.hbb.FileTransferSendRequest.toObject(message.send, options, _depth + 1);
                if (options.oneofs)
                    object.union = "send";
            }
            if (message.receive != null && $Object.hasOwnProperty.call(message, "receive")) {
                object.receive = $root.hbb.FileTransferReceiveRequest.toObject(message.receive, options, _depth + 1);
                if (options.oneofs)
                    object.union = "receive";
            }
            if (message.create != null && $Object.hasOwnProperty.call(message, "create")) {
                object.create = $root.hbb.FileDirCreate.toObject(message.create, options, _depth + 1);
                if (options.oneofs)
                    object.union = "create";
            }
            if (message.removeDir != null && $Object.hasOwnProperty.call(message, "removeDir")) {
                object.removeDir = $root.hbb.FileRemoveDir.toObject(message.removeDir, options, _depth + 1);
                if (options.oneofs)
                    object.union = "removeDir";
            }
            if (message.removeFile != null && $Object.hasOwnProperty.call(message, "removeFile")) {
                object.removeFile = $root.hbb.FileRemoveFile.toObject(message.removeFile, options, _depth + 1);
                if (options.oneofs)
                    object.union = "removeFile";
            }
            if (message.allFiles != null && $Object.hasOwnProperty.call(message, "allFiles")) {
                object.allFiles = $root.hbb.ReadAllFiles.toObject(message.allFiles, options, _depth + 1);
                if (options.oneofs)
                    object.union = "allFiles";
            }
            if (message.cancel != null && $Object.hasOwnProperty.call(message, "cancel")) {
                object.cancel = $root.hbb.FileTransferCancel.toObject(message.cancel, options, _depth + 1);
                if (options.oneofs)
                    object.union = "cancel";
            }
            if (message.sendConfirm != null && $Object.hasOwnProperty.call(message, "sendConfirm")) {
                object.sendConfirm = $root.hbb.FileTransferSendConfirmRequest.toObject(message.sendConfirm, options, _depth + 1);
                if (options.oneofs)
                    object.union = "sendConfirm";
            }
            if (message.rename != null && $Object.hasOwnProperty.call(message, "rename")) {
                object.rename = $root.hbb.FileRename.toObject(message.rename, options, _depth + 1);
                if (options.oneofs)
                    object.union = "rename";
            }
            if (message.readEmptyDirs != null && $Object.hasOwnProperty.call(message, "readEmptyDirs")) {
                object.readEmptyDirs = $root.hbb.ReadEmptyDirs.toObject(message.readEmptyDirs, options, _depth + 1);
                if (options.oneofs)
                    object.union = "readEmptyDirs";
            }
            return object;
        };

        FileAction.prototype.toJSON = function() {
            return FileAction.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileAction;
    })();

    hbb.FileTransferCancel = (function() {

        const FileTransferCancel = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileTransferCancel.prototype.id = 0;

        FileTransferCancel.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileTransferCancel.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileTransferCancel(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileTransferCancel.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileTransferCancel)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileTransferCancel: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileTransferCancel();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            return message;
        };

        FileTransferCancel.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.id = 0;
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            return object;
        };

        FileTransferCancel.prototype.toJSON = function() {
            return FileTransferCancel.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileTransferCancel;
    })();

    hbb.FileResponse = (function() {

        const FileResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileResponse.prototype.dir = null;
        FileResponse.prototype.block = null;
        FileResponse.prototype.error = null;
        FileResponse.prototype.done = null;
        FileResponse.prototype.digest = null;
        FileResponse.prototype.emptyDirs = null;

        let $oneOfFields;

        $Object.defineProperty(FileResponse.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["dir", "block", "error", "done", "digest", "emptyDirs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        FileResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.dir != null && $Object.hasOwnProperty.call(message, "dir"))
                $root.hbb.FileDirectory.encode(message.dir, writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.block != null && $Object.hasOwnProperty.call(message, "block"))
                $root.hbb.FileTransferBlock.encode(message.block, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                $root.hbb.FileTransferError.encode(message.error, writer.uint32(26).fork(), _depth + 1).ldelim();
            if (message.done != null && $Object.hasOwnProperty.call(message, "done"))
                $root.hbb.FileTransferDone.encode(message.done, writer.uint32(34).fork(), _depth + 1).ldelim();
            if (message.digest != null && $Object.hasOwnProperty.call(message, "digest"))
                $root.hbb.FileTransferDigest.encode(message.digest, writer.uint32(42).fork(), _depth + 1).ldelim();
            if (message.emptyDirs != null && $Object.hasOwnProperty.call(message, "emptyDirs"))
                $root.hbb.ReadEmptyDirsResponse.encode(message.emptyDirs, writer.uint32(50).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.dir = $root.hbb.FileDirectory.decode(reader, reader.uint32(), $undefined, _depth + 1, message.dir);
                        message.union = "dir";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.block = $root.hbb.FileTransferBlock.decode(reader, reader.uint32(), $undefined, _depth + 1, message.block);
                        message.union = "block";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.hbb.FileTransferError.decode(reader, reader.uint32(), $undefined, _depth + 1, message.error);
                        message.union = "error";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.done = $root.hbb.FileTransferDone.decode(reader, reader.uint32(), $undefined, _depth + 1, message.done);
                        message.union = "done";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.digest = $root.hbb.FileTransferDigest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.digest);
                        message.union = "digest";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.emptyDirs = $root.hbb.ReadEmptyDirsResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.emptyDirs);
                        message.union = "emptyDirs";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileResponse();
            if (object.dir != null) {
                if (!$util.isObject(object.dir))
                    throw $TypeError(".hbb.FileResponse.dir: object expected");
                message.dir = $root.hbb.FileDirectory.fromObject(object.dir, _depth + 1);
            }
            if (object.block != null) {
                if (!$util.isObject(object.block))
                    throw $TypeError(".hbb.FileResponse.block: object expected");
                message.block = $root.hbb.FileTransferBlock.fromObject(object.block, _depth + 1);
            }
            if (object.error != null) {
                if (!$util.isObject(object.error))
                    throw $TypeError(".hbb.FileResponse.error: object expected");
                message.error = $root.hbb.FileTransferError.fromObject(object.error, _depth + 1);
            }
            if (object.done != null) {
                if (!$util.isObject(object.done))
                    throw $TypeError(".hbb.FileResponse.done: object expected");
                message.done = $root.hbb.FileTransferDone.fromObject(object.done, _depth + 1);
            }
            if (object.digest != null) {
                if (!$util.isObject(object.digest))
                    throw $TypeError(".hbb.FileResponse.digest: object expected");
                message.digest = $root.hbb.FileTransferDigest.fromObject(object.digest, _depth + 1);
            }
            if (object.emptyDirs != null) {
                if (!$util.isObject(object.emptyDirs))
                    throw $TypeError(".hbb.FileResponse.emptyDirs: object expected");
                message.emptyDirs = $root.hbb.ReadEmptyDirsResponse.fromObject(object.emptyDirs, _depth + 1);
            }
            return message;
        };

        FileResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.dir != null && $Object.hasOwnProperty.call(message, "dir")) {
                object.dir = $root.hbb.FileDirectory.toObject(message.dir, options, _depth + 1);
                if (options.oneofs)
                    object.union = "dir";
            }
            if (message.block != null && $Object.hasOwnProperty.call(message, "block")) {
                object.block = $root.hbb.FileTransferBlock.toObject(message.block, options, _depth + 1);
                if (options.oneofs)
                    object.union = "block";
            }
            if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                object.error = $root.hbb.FileTransferError.toObject(message.error, options, _depth + 1);
                if (options.oneofs)
                    object.union = "error";
            }
            if (message.done != null && $Object.hasOwnProperty.call(message, "done")) {
                object.done = $root.hbb.FileTransferDone.toObject(message.done, options, _depth + 1);
                if (options.oneofs)
                    object.union = "done";
            }
            if (message.digest != null && $Object.hasOwnProperty.call(message, "digest")) {
                object.digest = $root.hbb.FileTransferDigest.toObject(message.digest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "digest";
            }
            if (message.emptyDirs != null && $Object.hasOwnProperty.call(message, "emptyDirs")) {
                object.emptyDirs = $root.hbb.ReadEmptyDirsResponse.toObject(message.emptyDirs, options, _depth + 1);
                if (options.oneofs)
                    object.union = "emptyDirs";
            }
            return object;
        };

        FileResponse.prototype.toJSON = function() {
            return FileResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileResponse;
    })();

    hbb.FileTransferDigest = (function() {

        const FileTransferDigest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileTransferDigest.prototype.id = 0;
        FileTransferDigest.prototype.fileNum = 0;
        FileTransferDigest.prototype.lastModified = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        FileTransferDigest.prototype.fileSize = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        FileTransferDigest.prototype.isUpload = false;
        FileTransferDigest.prototype.isIdentical = false;
        FileTransferDigest.prototype.transferredSize = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        FileTransferDigest.prototype.isResume = false;

        FileTransferDigest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum") && message.fileNum !== 0)
                writer.uint32(16).sint32(message.fileNum);
            if (message.lastModified != null && $Object.hasOwnProperty.call(message, "lastModified") && (typeof message.lastModified === "object" ? message.lastModified.low || message.lastModified.high : message.lastModified !== 0))
                writer.uint32(24).uint64(message.lastModified);
            if (message.fileSize != null && $Object.hasOwnProperty.call(message, "fileSize") && (typeof message.fileSize === "object" ? message.fileSize.low || message.fileSize.high : message.fileSize !== 0))
                writer.uint32(32).uint64(message.fileSize);
            if (message.isUpload != null && $Object.hasOwnProperty.call(message, "isUpload") && message.isUpload !== false)
                writer.uint32(40).bool(message.isUpload);
            if (message.isIdentical != null && $Object.hasOwnProperty.call(message, "isIdentical") && message.isIdentical !== false)
                writer.uint32(48).bool(message.isIdentical);
            if (message.transferredSize != null && $Object.hasOwnProperty.call(message, "transferredSize") && (typeof message.transferredSize === "object" ? message.transferredSize.low || message.transferredSize.high : message.transferredSize !== 0))
                writer.uint32(56).uint64(message.transferredSize);
            if (message.isResume != null && $Object.hasOwnProperty.call(message, "isResume") && message.isResume !== false)
                writer.uint32(64).bool(message.isResume);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileTransferDigest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileTransferDigest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.fileNum = value;
                        else
                            delete message.fileNum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.lastModified = value;
                        else
                            delete message.lastModified;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.fileSize = value;
                        else
                            delete message.fileSize;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isUpload = value;
                        else
                            delete message.isUpload;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isIdentical = value;
                        else
                            delete message.isIdentical;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.transferredSize = value;
                        else
                            delete message.transferredSize;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isResume = value;
                        else
                            delete message.isResume;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileTransferDigest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileTransferDigest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileTransferDigest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileTransferDigest();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.fileNum != null)
                if ($Number(object.fileNum) !== 0)
                    message.fileNum = object.fileNum | 0;
            if (object.lastModified != null)
                if (typeof object.lastModified === "object" ? object.lastModified.low || object.lastModified.high : $Number(object.lastModified) !== 0)
                    if ($util.Long)
                        message.lastModified = $util.Long.fromValue(object.lastModified, true);
                    else if (typeof object.lastModified === "string")
                        message.lastModified = $parseInt(object.lastModified, 10);
                    else if (typeof object.lastModified === "number")
                        message.lastModified = object.lastModified;
                    else if (typeof object.lastModified === "object")
                        message.lastModified = new $util.LongBits(object.lastModified.low >>> 0, object.lastModified.high >>> 0).toNumber(true);
            if (object.fileSize != null)
                if (typeof object.fileSize === "object" ? object.fileSize.low || object.fileSize.high : $Number(object.fileSize) !== 0)
                    if ($util.Long)
                        message.fileSize = $util.Long.fromValue(object.fileSize, true);
                    else if (typeof object.fileSize === "string")
                        message.fileSize = $parseInt(object.fileSize, 10);
                    else if (typeof object.fileSize === "number")
                        message.fileSize = object.fileSize;
                    else if (typeof object.fileSize === "object")
                        message.fileSize = new $util.LongBits(object.fileSize.low >>> 0, object.fileSize.high >>> 0).toNumber(true);
            if (object.isUpload != null)
                if (object.isUpload)
                    message.isUpload = $Boolean(object.isUpload);
            if (object.isIdentical != null)
                if (object.isIdentical)
                    message.isIdentical = $Boolean(object.isIdentical);
            if (object.transferredSize != null)
                if (typeof object.transferredSize === "object" ? object.transferredSize.low || object.transferredSize.high : $Number(object.transferredSize) !== 0)
                    if ($util.Long)
                        message.transferredSize = $util.Long.fromValue(object.transferredSize, true);
                    else if (typeof object.transferredSize === "string")
                        message.transferredSize = $parseInt(object.transferredSize, 10);
                    else if (typeof object.transferredSize === "number")
                        message.transferredSize = object.transferredSize;
                    else if (typeof object.transferredSize === "object")
                        message.transferredSize = new $util.LongBits(object.transferredSize.low >>> 0, object.transferredSize.high >>> 0).toNumber(true);
            if (object.isResume != null)
                if (object.isResume)
                    message.isResume = $Boolean(object.isResume);
            return message;
        };

        FileTransferDigest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.fileNum = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.lastModified = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.lastModified = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.fileSize = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.fileSize = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.isUpload = false;
                object.isIdentical = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.transferredSize = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.transferredSize = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.isResume = false;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum"))
                object.fileNum = message.fileNum;
            if (message.lastModified != null && $Object.hasOwnProperty.call(message, "lastModified"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.lastModified = typeof message.lastModified === "number" ? $BigInt(message.lastModified) : $util.Long.fromBits(message.lastModified.low >>> 0, message.lastModified.high >>> 0, true).toBigInt();
                else if (typeof message.lastModified === "number")
                    object.lastModified = options.longs === $String ? $String(message.lastModified) : message.lastModified;
                else
                    object.lastModified = options.longs === $String ? $util.Long.prototype.toString.call(message.lastModified) : options.longs === $Number ? new $util.LongBits(message.lastModified.low >>> 0, message.lastModified.high >>> 0).toNumber(true) : message.lastModified;
            if (message.fileSize != null && $Object.hasOwnProperty.call(message, "fileSize"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.fileSize = typeof message.fileSize === "number" ? $BigInt(message.fileSize) : $util.Long.fromBits(message.fileSize.low >>> 0, message.fileSize.high >>> 0, true).toBigInt();
                else if (typeof message.fileSize === "number")
                    object.fileSize = options.longs === $String ? $String(message.fileSize) : message.fileSize;
                else
                    object.fileSize = options.longs === $String ? $util.Long.prototype.toString.call(message.fileSize) : options.longs === $Number ? new $util.LongBits(message.fileSize.low >>> 0, message.fileSize.high >>> 0).toNumber(true) : message.fileSize;
            if (message.isUpload != null && $Object.hasOwnProperty.call(message, "isUpload"))
                object.isUpload = message.isUpload;
            if (message.isIdentical != null && $Object.hasOwnProperty.call(message, "isIdentical"))
                object.isIdentical = message.isIdentical;
            if (message.transferredSize != null && $Object.hasOwnProperty.call(message, "transferredSize"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.transferredSize = typeof message.transferredSize === "number" ? $BigInt(message.transferredSize) : $util.Long.fromBits(message.transferredSize.low >>> 0, message.transferredSize.high >>> 0, true).toBigInt();
                else if (typeof message.transferredSize === "number")
                    object.transferredSize = options.longs === $String ? $String(message.transferredSize) : message.transferredSize;
                else
                    object.transferredSize = options.longs === $String ? $util.Long.prototype.toString.call(message.transferredSize) : options.longs === $Number ? new $util.LongBits(message.transferredSize.low >>> 0, message.transferredSize.high >>> 0).toNumber(true) : message.transferredSize;
            if (message.isResume != null && $Object.hasOwnProperty.call(message, "isResume"))
                object.isResume = message.isResume;
            return object;
        };

        FileTransferDigest.prototype.toJSON = function() {
            return FileTransferDigest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileTransferDigest;
    })();

    hbb.FileTransferBlock = (function() {

        const FileTransferBlock = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileTransferBlock.prototype.id = 0;
        FileTransferBlock.prototype.fileNum = 0;
        FileTransferBlock.prototype.data = $util.newBuffer([]);
        FileTransferBlock.prototype.compressed = false;
        FileTransferBlock.prototype.blkId = 0;

        FileTransferBlock.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum") && message.fileNum !== 0)
                writer.uint32(16).sint32(message.fileNum);
            if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                writer.uint32(26).bytes(message.data);
            if (message.compressed != null && $Object.hasOwnProperty.call(message, "compressed") && message.compressed !== false)
                writer.uint32(32).bool(message.compressed);
            if (message.blkId != null && $Object.hasOwnProperty.call(message, "blkId") && message.blkId !== 0)
                writer.uint32(40).uint32(message.blkId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileTransferBlock.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileTransferBlock(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.fileNum = value;
                        else
                            delete message.fileNum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.compressed = value;
                        else
                            delete message.compressed;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.blkId = value;
                        else
                            delete message.blkId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileTransferBlock.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileTransferBlock)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileTransferBlock: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileTransferBlock();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.fileNum != null)
                if ($Number(object.fileNum) !== 0)
                    message.fileNum = object.fileNum | 0;
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            if (object.compressed != null)
                if (object.compressed)
                    message.compressed = $Boolean(object.compressed);
            if (object.blkId != null)
                if ($Number(object.blkId) !== 0)
                    message.blkId = object.blkId >>> 0;
            return message;
        };

        FileTransferBlock.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.fileNum = 0;
                if (options.bytes === $String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== $Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.compressed = false;
                object.blkId = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum"))
                object.fileNum = message.fileNum;
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
            if (message.compressed != null && $Object.hasOwnProperty.call(message, "compressed"))
                object.compressed = message.compressed;
            if (message.blkId != null && $Object.hasOwnProperty.call(message, "blkId"))
                object.blkId = message.blkId;
            return object;
        };

        FileTransferBlock.prototype.toJSON = function() {
            return FileTransferBlock.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileTransferBlock;
    })();

    hbb.FileTransferError = (function() {

        const FileTransferError = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileTransferError.prototype.id = 0;
        FileTransferError.prototype.error = "";
        FileTransferError.prototype.fileNum = 0;
        FileTransferError.prototype.path = "";
        FileTransferError.prototype.operation = 0;

        FileTransferError.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.error != null && $Object.hasOwnProperty.call(message, "error") && message.error !== "")
                writer.uint32(18).string(message.error);
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum") && message.fileNum !== 0)
                writer.uint32(24).sint32(message.fileNum);
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(34).string(message.path);
            if (message.operation != null && $Object.hasOwnProperty.call(message, "operation") && message.operation !== 0)
                writer.uint32(40).int32(message.operation);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileTransferError.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileTransferError(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.error = value;
                        else
                            delete message.error;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.fileNum = value;
                        else
                            delete message.fileNum;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.operation = value;
                        else
                            delete message.operation;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileTransferError.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileTransferError)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileTransferError: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileTransferError();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.error != null)
                if (typeof object.error !== "string" || object.error.length)
                    message.error = $String(object.error);
            if (object.fileNum != null)
                if ($Number(object.fileNum) !== 0)
                    message.fileNum = object.fileNum | 0;
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.operation !== 0 && (typeof object.operation !== "string" || $root.hbb.FileErrorOperation[object.operation] !== 0))
                switch (object.operation) {
                case "Transfer":
                case 0:
                    message.operation = 0;
                    break;
                case "ReadDir":
                case 1:
                    message.operation = 1;
                    break;
                case "ReadEmptyDirs":
                case 2:
                    message.operation = 2;
                    break;
                default:
                    if (typeof object.operation === "number" && (object.operation | 0) === object.operation)
                        message.operation = object.operation;
                }
            return message;
        };

        FileTransferError.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.error = "";
                object.fileNum = 0;
                object.path = "";
                object.operation = options.enums === $String ? "Transfer" : 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                object.error = message.error;
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum"))
                object.fileNum = message.fileNum;
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.operation != null && $Object.hasOwnProperty.call(message, "operation"))
                object.operation = options.enums === $String ? $root.hbb.FileErrorOperation[message.operation] === $undefined ? message.operation : $root.hbb.FileErrorOperation[message.operation] : message.operation;
            return object;
        };

        FileTransferError.prototype.toJSON = function() {
            return FileTransferError.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileTransferError;
    })();

    hbb.FileErrorOperation = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "Transfer"] = 0;
        values[valuesById[1] = "ReadDir"] = 1;
        values[valuesById[2] = "ReadEmptyDirs"] = 2;
        return values;
    })();

    hbb.FileTransferSendRequest = (function() {

        const FileTransferSendRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileTransferSendRequest.prototype.id = 0;
        FileTransferSendRequest.prototype.path = "";
        FileTransferSendRequest.prototype.includeHidden = false;
        FileTransferSendRequest.prototype.fileNum = 0;
        FileTransferSendRequest.prototype.fileType = 0;

        FileTransferSendRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(18).string(message.path);
            if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden") && message.includeHidden !== false)
                writer.uint32(24).bool(message.includeHidden);
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum") && message.fileNum !== 0)
                writer.uint32(32).int32(message.fileNum);
            if (message.fileType != null && $Object.hasOwnProperty.call(message, "fileType") && message.fileType !== 0)
                writer.uint32(40).int32(message.fileType);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileTransferSendRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileTransferSendRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.includeHidden = value;
                        else
                            delete message.includeHidden;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.fileNum = value;
                        else
                            delete message.fileNum;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.fileType = value;
                        else
                            delete message.fileType;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileTransferSendRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileTransferSendRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileTransferSendRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileTransferSendRequest();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.includeHidden != null)
                if (object.includeHidden)
                    message.includeHidden = $Boolean(object.includeHidden);
            if (object.fileNum != null)
                if ($Number(object.fileNum) !== 0)
                    message.fileNum = object.fileNum | 0;
            if (object.fileType !== 0 && (typeof object.fileType !== "string" || $root.hbb.FileTransferSendRequest.FileType[object.fileType] !== 0))
                switch (object.fileType) {
                case "Generic":
                case 0:
                    message.fileType = 0;
                    break;
                case "Printer":
                case 1:
                    message.fileType = 1;
                    break;
                default:
                    if (typeof object.fileType === "number" && (object.fileType | 0) === object.fileType)
                        message.fileType = object.fileType;
                }
            return message;
        };

        FileTransferSendRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.path = "";
                object.includeHidden = false;
                object.fileNum = 0;
                object.fileType = options.enums === $String ? "Generic" : 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden"))
                object.includeHidden = message.includeHidden;
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum"))
                object.fileNum = message.fileNum;
            if (message.fileType != null && $Object.hasOwnProperty.call(message, "fileType"))
                object.fileType = options.enums === $String ? $root.hbb.FileTransferSendRequest.FileType[message.fileType] === $undefined ? message.fileType : $root.hbb.FileTransferSendRequest.FileType[message.fileType] : message.fileType;
            return object;
        };

        FileTransferSendRequest.prototype.toJSON = function() {
            return FileTransferSendRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        FileTransferSendRequest.FileType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "Generic"] = 0;
            values[valuesById[1] = "Printer"] = 1;
            return values;
        })();

        return FileTransferSendRequest;
    })();

    hbb.FileTransferSendConfirmRequest = (function() {

        const FileTransferSendConfirmRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileTransferSendConfirmRequest.prototype.id = 0;
        FileTransferSendConfirmRequest.prototype.fileNum = 0;
        FileTransferSendConfirmRequest.prototype.skip = null;
        FileTransferSendConfirmRequest.prototype.offsetBytes = null;

        let $oneOfFields;

        $Object.defineProperty(FileTransferSendConfirmRequest.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["skip", "offsetBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        FileTransferSendConfirmRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum") && message.fileNum !== 0)
                writer.uint32(16).sint32(message.fileNum);
            if (message.skip != null && $Object.hasOwnProperty.call(message, "skip"))
                writer.uint32(24).bool(message.skip);
            if (message.offsetBytes != null && $Object.hasOwnProperty.call(message, "offsetBytes"))
                writer.uint32(32).uint64(message.offsetBytes);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileTransferSendConfirmRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileTransferSendConfirmRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.fileNum = value;
                        else
                            delete message.fileNum;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        message.skip = reader.bool();
                        message.union = "skip";
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        message.offsetBytes = reader.uint64();
                        message.union = "offsetBytes";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileTransferSendConfirmRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileTransferSendConfirmRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileTransferSendConfirmRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileTransferSendConfirmRequest();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.fileNum != null)
                if ($Number(object.fileNum) !== 0)
                    message.fileNum = object.fileNum | 0;
            if (object.skip != null)
                message.skip = $Boolean(object.skip);
            if (object.offsetBytes != null)
                if ($util.Long)
                    message.offsetBytes = $util.Long.fromValue(object.offsetBytes, true);
                else if (typeof object.offsetBytes === "string")
                    message.offsetBytes = $parseInt(object.offsetBytes, 10);
                else if (typeof object.offsetBytes === "number")
                    message.offsetBytes = object.offsetBytes;
                else if (typeof object.offsetBytes === "object")
                    message.offsetBytes = new $util.LongBits(object.offsetBytes.low >>> 0, object.offsetBytes.high >>> 0).toNumber(true);
            return message;
        };

        FileTransferSendConfirmRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.fileNum = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum"))
                object.fileNum = message.fileNum;
            if (message.skip != null && $Object.hasOwnProperty.call(message, "skip")) {
                object.skip = message.skip;
                if (options.oneofs)
                    object.union = "skip";
            }
            if (message.offsetBytes != null && $Object.hasOwnProperty.call(message, "offsetBytes")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.offsetBytes = typeof message.offsetBytes === "number" ? $BigInt(message.offsetBytes) : $util.Long.fromBits(message.offsetBytes.low >>> 0, message.offsetBytes.high >>> 0, true).toBigInt();
                else if (typeof message.offsetBytes === "number")
                    object.offsetBytes = options.longs === $String ? $String(message.offsetBytes) : message.offsetBytes;
                else
                    object.offsetBytes = options.longs === $String ? $util.Long.prototype.toString.call(message.offsetBytes) : options.longs === $Number ? new $util.LongBits(message.offsetBytes.low >>> 0, message.offsetBytes.high >>> 0).toNumber(true) : message.offsetBytes;
                if (options.oneofs)
                    object.union = "offsetBytes";
            }
            return object;
        };

        FileTransferSendConfirmRequest.prototype.toJSON = function() {
            return FileTransferSendConfirmRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileTransferSendConfirmRequest;
    })();

    hbb.FileTransferDone = (function() {

        const FileTransferDone = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileTransferDone.prototype.id = 0;
        FileTransferDone.prototype.fileNum = 0;

        FileTransferDone.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum") && message.fileNum !== 0)
                writer.uint32(16).sint32(message.fileNum);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileTransferDone.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileTransferDone(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.fileNum = value;
                        else
                            delete message.fileNum;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileTransferDone.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileTransferDone)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileTransferDone: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileTransferDone();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.fileNum != null)
                if ($Number(object.fileNum) !== 0)
                    message.fileNum = object.fileNum | 0;
            return message;
        };

        FileTransferDone.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.fileNum = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum"))
                object.fileNum = message.fileNum;
            return object;
        };

        FileTransferDone.prototype.toJSON = function() {
            return FileTransferDone.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileTransferDone;
    })();

    hbb.FileTransferReceiveRequest = (function() {

        const FileTransferReceiveRequest = function (properties) {
            this.files = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileTransferReceiveRequest.prototype.id = 0;
        FileTransferReceiveRequest.prototype.path = "";
        FileTransferReceiveRequest.prototype.files = $util.emptyArray;
        FileTransferReceiveRequest.prototype.fileNum = 0;
        FileTransferReceiveRequest.prototype.totalSize = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        FileTransferReceiveRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(18).string(message.path);
            if (message.files != null && message.files.length)
                for (let i = 0; i < message.files.length; ++i)
                    $root.hbb.FileEntry.encode(message.files[i], writer.uint32(26).fork(), _depth + 1).ldelim();
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum") && message.fileNum !== 0)
                writer.uint32(32).int32(message.fileNum);
            if (message.totalSize != null && $Object.hasOwnProperty.call(message, "totalSize") && (typeof message.totalSize === "object" ? message.totalSize.low || message.totalSize.high : message.totalSize !== 0))
                writer.uint32(40).uint64(message.totalSize);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileTransferReceiveRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileTransferReceiveRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.files && message.files.length))
                            message.files = [];
                        message.files.push($root.hbb.FileEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.fileNum = value;
                        else
                            delete message.fileNum;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.totalSize = value;
                        else
                            delete message.totalSize;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileTransferReceiveRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileTransferReceiveRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileTransferReceiveRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileTransferReceiveRequest();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.files) {
                if (!$Array.isArray(object.files))
                    throw $TypeError(".hbb.FileTransferReceiveRequest.files: array expected");
                message.files = $Array(object.files.length);
                for (let i = 0; i < object.files.length; ++i) {
                    if (!$util.isObject(object.files[i]))
                        throw $TypeError(".hbb.FileTransferReceiveRequest.files: object expected");
                    message.files[i] = $root.hbb.FileEntry.fromObject(object.files[i], _depth + 1);
                }
            }
            if (object.fileNum != null)
                if ($Number(object.fileNum) !== 0)
                    message.fileNum = object.fileNum | 0;
            if (object.totalSize != null)
                if (typeof object.totalSize === "object" ? object.totalSize.low || object.totalSize.high : $Number(object.totalSize) !== 0)
                    if ($util.Long)
                        message.totalSize = $util.Long.fromValue(object.totalSize, true);
                    else if (typeof object.totalSize === "string")
                        message.totalSize = $parseInt(object.totalSize, 10);
                    else if (typeof object.totalSize === "number")
                        message.totalSize = object.totalSize;
                    else if (typeof object.totalSize === "object")
                        message.totalSize = new $util.LongBits(object.totalSize.low >>> 0, object.totalSize.high >>> 0).toNumber(true);
            return message;
        };

        FileTransferReceiveRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.files = [];
            if (options.defaults) {
                object.id = 0;
                object.path = "";
                object.fileNum = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.totalSize = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.totalSize = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.files && message.files.length) {
                object.files = $Array(message.files.length);
                for (let j = 0; j < message.files.length; ++j)
                    object.files[j] = $root.hbb.FileEntry.toObject(message.files[j], options, _depth + 1);
            }
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum"))
                object.fileNum = message.fileNum;
            if (message.totalSize != null && $Object.hasOwnProperty.call(message, "totalSize"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.totalSize = typeof message.totalSize === "number" ? $BigInt(message.totalSize) : $util.Long.fromBits(message.totalSize.low >>> 0, message.totalSize.high >>> 0, true).toBigInt();
                else if (typeof message.totalSize === "number")
                    object.totalSize = options.longs === $String ? $String(message.totalSize) : message.totalSize;
                else
                    object.totalSize = options.longs === $String ? $util.Long.prototype.toString.call(message.totalSize) : options.longs === $Number ? new $util.LongBits(message.totalSize.low >>> 0, message.totalSize.high >>> 0).toNumber(true) : message.totalSize;
            return object;
        };

        FileTransferReceiveRequest.prototype.toJSON = function() {
            return FileTransferReceiveRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileTransferReceiveRequest;
    })();

    hbb.FileRemoveDir = (function() {

        const FileRemoveDir = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileRemoveDir.prototype.id = 0;
        FileRemoveDir.prototype.path = "";
        FileRemoveDir.prototype.recursive = false;

        FileRemoveDir.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(18).string(message.path);
            if (message.recursive != null && $Object.hasOwnProperty.call(message, "recursive") && message.recursive !== false)
                writer.uint32(24).bool(message.recursive);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileRemoveDir.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileRemoveDir(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.recursive = value;
                        else
                            delete message.recursive;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileRemoveDir.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileRemoveDir)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileRemoveDir: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileRemoveDir();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.recursive != null)
                if (object.recursive)
                    message.recursive = $Boolean(object.recursive);
            return message;
        };

        FileRemoveDir.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.path = "";
                object.recursive = false;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.recursive != null && $Object.hasOwnProperty.call(message, "recursive"))
                object.recursive = message.recursive;
            return object;
        };

        FileRemoveDir.prototype.toJSON = function() {
            return FileRemoveDir.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileRemoveDir;
    })();

    hbb.FileRemoveFile = (function() {

        const FileRemoveFile = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileRemoveFile.prototype.id = 0;
        FileRemoveFile.prototype.path = "";
        FileRemoveFile.prototype.fileNum = 0;

        FileRemoveFile.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(18).string(message.path);
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum") && message.fileNum !== 0)
                writer.uint32(24).sint32(message.fileNum);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileRemoveFile.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileRemoveFile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.fileNum = value;
                        else
                            delete message.fileNum;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileRemoveFile.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileRemoveFile)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileRemoveFile: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileRemoveFile();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.fileNum != null)
                if ($Number(object.fileNum) !== 0)
                    message.fileNum = object.fileNum | 0;
            return message;
        };

        FileRemoveFile.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.path = "";
                object.fileNum = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.fileNum != null && $Object.hasOwnProperty.call(message, "fileNum"))
                object.fileNum = message.fileNum;
            return object;
        };

        FileRemoveFile.prototype.toJSON = function() {
            return FileRemoveFile.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileRemoveFile;
    })();

    hbb.FileDirCreate = (function() {

        const FileDirCreate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FileDirCreate.prototype.id = 0;
        FileDirCreate.prototype.path = "";

        FileDirCreate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(8).int32(message.id);
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(18).string(message.path);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FileDirCreate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FileDirCreate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FileDirCreate.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FileDirCreate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FileDirCreate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FileDirCreate();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            return message;
        };

        FileDirCreate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.path = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            return object;
        };

        FileDirCreate.prototype.toJSON = function() {
            return FileDirCreate.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FileDirCreate;
    })();

    hbb.CliprdrMonitorReady = (function() {

        const CliprdrMonitorReady = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrMonitorReady.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrMonitorReady.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrMonitorReady();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrMonitorReady.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrMonitorReady)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrMonitorReady: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.hbb.CliprdrMonitorReady();
        };

        CliprdrMonitorReady.toObject = function () {
            return {};
        };

        CliprdrMonitorReady.prototype.toJSON = function() {
            return CliprdrMonitorReady.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrMonitorReady;
    })();

    hbb.CliprdrFormat = (function() {

        const CliprdrFormat = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrFormat.prototype.id = 0;
        CliprdrFormat.prototype.format = "";

        CliprdrFormat.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== 0)
                writer.uint32(16).int32(message.id);
            if (message.format != null && $Object.hasOwnProperty.call(message, "format") && message.format !== "")
                writer.uint32(26).string(message.format);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrFormat.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrFormat(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.format = value;
                        else
                            delete message.format;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrFormat.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrFormat)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrFormat: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CliprdrFormat();
            if (object.id != null)
                if ($Number(object.id) !== 0)
                    message.id = object.id | 0;
            if (object.format != null)
                if (typeof object.format !== "string" || object.format.length)
                    message.format = $String(object.format);
            return message;
        };

        CliprdrFormat.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = 0;
                object.format = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.format != null && $Object.hasOwnProperty.call(message, "format"))
                object.format = message.format;
            return object;
        };

        CliprdrFormat.prototype.toJSON = function() {
            return CliprdrFormat.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrFormat;
    })();

    hbb.CliprdrServerFormatList = (function() {

        const CliprdrServerFormatList = function (properties) {
            this.formats = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrServerFormatList.prototype.formats = $util.emptyArray;

        CliprdrServerFormatList.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.formats != null && message.formats.length)
                for (let i = 0; i < message.formats.length; ++i)
                    $root.hbb.CliprdrFormat.encode(message.formats[i], writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrServerFormatList.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrServerFormatList();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.formats && message.formats.length))
                            message.formats = [];
                        message.formats.push($root.hbb.CliprdrFormat.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrServerFormatList.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrServerFormatList)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrServerFormatList: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CliprdrServerFormatList();
            if (object.formats) {
                if (!$Array.isArray(object.formats))
                    throw $TypeError(".hbb.CliprdrServerFormatList.formats: array expected");
                message.formats = $Array(object.formats.length);
                for (let i = 0; i < object.formats.length; ++i) {
                    if (!$util.isObject(object.formats[i]))
                        throw $TypeError(".hbb.CliprdrServerFormatList.formats: object expected");
                    message.formats[i] = $root.hbb.CliprdrFormat.fromObject(object.formats[i], _depth + 1);
                }
            }
            return message;
        };

        CliprdrServerFormatList.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.formats = [];
            if (message.formats && message.formats.length) {
                object.formats = $Array(message.formats.length);
                for (let j = 0; j < message.formats.length; ++j)
                    object.formats[j] = $root.hbb.CliprdrFormat.toObject(message.formats[j], options, _depth + 1);
            }
            return object;
        };

        CliprdrServerFormatList.prototype.toJSON = function() {
            return CliprdrServerFormatList.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrServerFormatList;
    })();

    hbb.CliprdrServerFormatListResponse = (function() {

        const CliprdrServerFormatListResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrServerFormatListResponse.prototype.msgFlags = 0;

        CliprdrServerFormatListResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.msgFlags != null && $Object.hasOwnProperty.call(message, "msgFlags") && message.msgFlags !== 0)
                writer.uint32(16).int32(message.msgFlags);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrServerFormatListResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrServerFormatListResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.msgFlags = value;
                        else
                            delete message.msgFlags;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrServerFormatListResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrServerFormatListResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrServerFormatListResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CliprdrServerFormatListResponse();
            if (object.msgFlags != null)
                if ($Number(object.msgFlags) !== 0)
                    message.msgFlags = object.msgFlags | 0;
            return message;
        };

        CliprdrServerFormatListResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.msgFlags = 0;
            if (message.msgFlags != null && $Object.hasOwnProperty.call(message, "msgFlags"))
                object.msgFlags = message.msgFlags;
            return object;
        };

        CliprdrServerFormatListResponse.prototype.toJSON = function() {
            return CliprdrServerFormatListResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrServerFormatListResponse;
    })();

    hbb.CliprdrServerFormatDataRequest = (function() {

        const CliprdrServerFormatDataRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrServerFormatDataRequest.prototype.requestedFormatId = 0;

        CliprdrServerFormatDataRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.requestedFormatId != null && $Object.hasOwnProperty.call(message, "requestedFormatId") && message.requestedFormatId !== 0)
                writer.uint32(16).int32(message.requestedFormatId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrServerFormatDataRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrServerFormatDataRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.requestedFormatId = value;
                        else
                            delete message.requestedFormatId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrServerFormatDataRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrServerFormatDataRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrServerFormatDataRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CliprdrServerFormatDataRequest();
            if (object.requestedFormatId != null)
                if ($Number(object.requestedFormatId) !== 0)
                    message.requestedFormatId = object.requestedFormatId | 0;
            return message;
        };

        CliprdrServerFormatDataRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.requestedFormatId = 0;
            if (message.requestedFormatId != null && $Object.hasOwnProperty.call(message, "requestedFormatId"))
                object.requestedFormatId = message.requestedFormatId;
            return object;
        };

        CliprdrServerFormatDataRequest.prototype.toJSON = function() {
            return CliprdrServerFormatDataRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrServerFormatDataRequest;
    })();

    hbb.CliprdrServerFormatDataResponse = (function() {

        const CliprdrServerFormatDataResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrServerFormatDataResponse.prototype.msgFlags = 0;
        CliprdrServerFormatDataResponse.prototype.formatData = $util.newBuffer([]);

        CliprdrServerFormatDataResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.msgFlags != null && $Object.hasOwnProperty.call(message, "msgFlags") && message.msgFlags !== 0)
                writer.uint32(16).int32(message.msgFlags);
            if (message.formatData != null && $Object.hasOwnProperty.call(message, "formatData") && message.formatData.length)
                writer.uint32(26).bytes(message.formatData);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrServerFormatDataResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrServerFormatDataResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.msgFlags = value;
                        else
                            delete message.msgFlags;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.formatData = value;
                        else
                            delete message.formatData;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrServerFormatDataResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrServerFormatDataResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrServerFormatDataResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CliprdrServerFormatDataResponse();
            if (object.msgFlags != null)
                if ($Number(object.msgFlags) !== 0)
                    message.msgFlags = object.msgFlags | 0;
            if (object.formatData != null)
                if (object.formatData.length)
                    if (typeof object.formatData === "string")
                        $util.base64.decode(object.formatData, message.formatData = $util.newBuffer($util.base64.length(object.formatData)), 0);
                    else if (object.formatData.length >= 0)
                        message.formatData = object.formatData;
            return message;
        };

        CliprdrServerFormatDataResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.msgFlags = 0;
                if (options.bytes === $String)
                    object.formatData = "";
                else {
                    object.formatData = [];
                    if (options.bytes !== $Array)
                        object.formatData = $util.newBuffer(object.formatData);
                }
            }
            if (message.msgFlags != null && $Object.hasOwnProperty.call(message, "msgFlags"))
                object.msgFlags = message.msgFlags;
            if (message.formatData != null && $Object.hasOwnProperty.call(message, "formatData"))
                object.formatData = options.bytes === $String ? $util.base64.encode(message.formatData, 0, message.formatData.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.formatData) : message.formatData;
            return object;
        };

        CliprdrServerFormatDataResponse.prototype.toJSON = function() {
            return CliprdrServerFormatDataResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrServerFormatDataResponse;
    })();

    hbb.CliprdrFileContentsRequest = (function() {

        const CliprdrFileContentsRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrFileContentsRequest.prototype.streamId = 0;
        CliprdrFileContentsRequest.prototype.listIndex = 0;
        CliprdrFileContentsRequest.prototype.dwFlags = 0;
        CliprdrFileContentsRequest.prototype.nPositionLow = 0;
        CliprdrFileContentsRequest.prototype.nPositionHigh = 0;
        CliprdrFileContentsRequest.prototype.cbRequested = 0;
        CliprdrFileContentsRequest.prototype.haveClipDataId = false;
        CliprdrFileContentsRequest.prototype.clipDataId = 0;

        CliprdrFileContentsRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.streamId != null && $Object.hasOwnProperty.call(message, "streamId") && message.streamId !== 0)
                writer.uint32(16).uint32(message.streamId);
            if (message.listIndex != null && $Object.hasOwnProperty.call(message, "listIndex") && message.listIndex !== 0)
                writer.uint32(24).uint32(message.listIndex);
            if (message.dwFlags != null && $Object.hasOwnProperty.call(message, "dwFlags") && message.dwFlags !== 0)
                writer.uint32(32).uint32(message.dwFlags);
            if (message.nPositionLow != null && $Object.hasOwnProperty.call(message, "nPositionLow") && message.nPositionLow !== 0)
                writer.uint32(40).uint32(message.nPositionLow);
            if (message.nPositionHigh != null && $Object.hasOwnProperty.call(message, "nPositionHigh") && message.nPositionHigh !== 0)
                writer.uint32(48).uint32(message.nPositionHigh);
            if (message.cbRequested != null && $Object.hasOwnProperty.call(message, "cbRequested") && message.cbRequested !== 0)
                writer.uint32(56).uint32(message.cbRequested);
            if (message.haveClipDataId != null && $Object.hasOwnProperty.call(message, "haveClipDataId") && message.haveClipDataId !== false)
                writer.uint32(64).bool(message.haveClipDataId);
            if (message.clipDataId != null && $Object.hasOwnProperty.call(message, "clipDataId") && message.clipDataId !== 0)
                writer.uint32(72).uint32(message.clipDataId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrFileContentsRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrFileContentsRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.streamId = value;
                        else
                            delete message.streamId;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.listIndex = value;
                        else
                            delete message.listIndex;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.dwFlags = value;
                        else
                            delete message.dwFlags;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.nPositionLow = value;
                        else
                            delete message.nPositionLow;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.nPositionHigh = value;
                        else
                            delete message.nPositionHigh;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.cbRequested = value;
                        else
                            delete message.cbRequested;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.haveClipDataId = value;
                        else
                            delete message.haveClipDataId;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.clipDataId = value;
                        else
                            delete message.clipDataId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrFileContentsRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrFileContentsRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrFileContentsRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CliprdrFileContentsRequest();
            if (object.streamId != null)
                if ($Number(object.streamId) !== 0)
                    message.streamId = object.streamId >>> 0;
            if (object.listIndex != null)
                if ($Number(object.listIndex) !== 0)
                    message.listIndex = object.listIndex >>> 0;
            if (object.dwFlags != null)
                if ($Number(object.dwFlags) !== 0)
                    message.dwFlags = object.dwFlags >>> 0;
            if (object.nPositionLow != null)
                if ($Number(object.nPositionLow) !== 0)
                    message.nPositionLow = object.nPositionLow >>> 0;
            if (object.nPositionHigh != null)
                if ($Number(object.nPositionHigh) !== 0)
                    message.nPositionHigh = object.nPositionHigh >>> 0;
            if (object.cbRequested != null)
                if ($Number(object.cbRequested) !== 0)
                    message.cbRequested = object.cbRequested >>> 0;
            if (object.haveClipDataId != null)
                if (object.haveClipDataId)
                    message.haveClipDataId = $Boolean(object.haveClipDataId);
            if (object.clipDataId != null)
                if ($Number(object.clipDataId) !== 0)
                    message.clipDataId = object.clipDataId >>> 0;
            return message;
        };

        CliprdrFileContentsRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.streamId = 0;
                object.listIndex = 0;
                object.dwFlags = 0;
                object.nPositionLow = 0;
                object.nPositionHigh = 0;
                object.cbRequested = 0;
                object.haveClipDataId = false;
                object.clipDataId = 0;
            }
            if (message.streamId != null && $Object.hasOwnProperty.call(message, "streamId"))
                object.streamId = message.streamId;
            if (message.listIndex != null && $Object.hasOwnProperty.call(message, "listIndex"))
                object.listIndex = message.listIndex;
            if (message.dwFlags != null && $Object.hasOwnProperty.call(message, "dwFlags"))
                object.dwFlags = message.dwFlags;
            if (message.nPositionLow != null && $Object.hasOwnProperty.call(message, "nPositionLow"))
                object.nPositionLow = message.nPositionLow;
            if (message.nPositionHigh != null && $Object.hasOwnProperty.call(message, "nPositionHigh"))
                object.nPositionHigh = message.nPositionHigh;
            if (message.cbRequested != null && $Object.hasOwnProperty.call(message, "cbRequested"))
                object.cbRequested = message.cbRequested;
            if (message.haveClipDataId != null && $Object.hasOwnProperty.call(message, "haveClipDataId"))
                object.haveClipDataId = message.haveClipDataId;
            if (message.clipDataId != null && $Object.hasOwnProperty.call(message, "clipDataId"))
                object.clipDataId = message.clipDataId;
            return object;
        };

        CliprdrFileContentsRequest.prototype.toJSON = function() {
            return CliprdrFileContentsRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrFileContentsRequest;
    })();

    hbb.CliprdrFileContentsResponse = (function() {

        const CliprdrFileContentsResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrFileContentsResponse.prototype.msgFlags = 0;
        CliprdrFileContentsResponse.prototype.streamId = 0;
        CliprdrFileContentsResponse.prototype.requestedData = $util.newBuffer([]);

        CliprdrFileContentsResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.msgFlags != null && $Object.hasOwnProperty.call(message, "msgFlags") && message.msgFlags !== 0)
                writer.uint32(24).uint32(message.msgFlags);
            if (message.streamId != null && $Object.hasOwnProperty.call(message, "streamId") && message.streamId !== 0)
                writer.uint32(32).uint32(message.streamId);
            if (message.requestedData != null && $Object.hasOwnProperty.call(message, "requestedData") && message.requestedData.length)
                writer.uint32(42).bytes(message.requestedData);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrFileContentsResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrFileContentsResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.msgFlags = value;
                        else
                            delete message.msgFlags;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.streamId = value;
                        else
                            delete message.streamId;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.requestedData = value;
                        else
                            delete message.requestedData;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrFileContentsResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrFileContentsResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrFileContentsResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CliprdrFileContentsResponse();
            if (object.msgFlags != null)
                if ($Number(object.msgFlags) !== 0)
                    message.msgFlags = object.msgFlags >>> 0;
            if (object.streamId != null)
                if ($Number(object.streamId) !== 0)
                    message.streamId = object.streamId >>> 0;
            if (object.requestedData != null)
                if (object.requestedData.length)
                    if (typeof object.requestedData === "string")
                        $util.base64.decode(object.requestedData, message.requestedData = $util.newBuffer($util.base64.length(object.requestedData)), 0);
                    else if (object.requestedData.length >= 0)
                        message.requestedData = object.requestedData;
            return message;
        };

        CliprdrFileContentsResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.msgFlags = 0;
                object.streamId = 0;
                if (options.bytes === $String)
                    object.requestedData = "";
                else {
                    object.requestedData = [];
                    if (options.bytes !== $Array)
                        object.requestedData = $util.newBuffer(object.requestedData);
                }
            }
            if (message.msgFlags != null && $Object.hasOwnProperty.call(message, "msgFlags"))
                object.msgFlags = message.msgFlags;
            if (message.streamId != null && $Object.hasOwnProperty.call(message, "streamId"))
                object.streamId = message.streamId;
            if (message.requestedData != null && $Object.hasOwnProperty.call(message, "requestedData"))
                object.requestedData = options.bytes === $String ? $util.base64.encode(message.requestedData, 0, message.requestedData.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.requestedData) : message.requestedData;
            return object;
        };

        CliprdrFileContentsResponse.prototype.toJSON = function() {
            return CliprdrFileContentsResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrFileContentsResponse;
    })();

    hbb.CliprdrTryEmpty = (function() {

        const CliprdrTryEmpty = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrTryEmpty.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrTryEmpty.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrTryEmpty();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrTryEmpty.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrTryEmpty)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrTryEmpty: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.hbb.CliprdrTryEmpty();
        };

        CliprdrTryEmpty.toObject = function () {
            return {};
        };

        CliprdrTryEmpty.prototype.toJSON = function() {
            return CliprdrTryEmpty.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrTryEmpty;
    })();

    hbb.CliprdrFile = (function() {

        const CliprdrFile = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrFile.prototype.name = "";
        CliprdrFile.prototype.size = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        CliprdrFile.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(10).string(message.name);
            if (message.size != null && $Object.hasOwnProperty.call(message, "size") && (typeof message.size === "object" ? message.size.low || message.size.high : message.size !== 0))
                writer.uint32(16).uint64(message.size);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrFile.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrFile(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.size = value;
                        else
                            delete message.size;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrFile.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrFile)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrFile: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CliprdrFile();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.size != null)
                if (typeof object.size === "object" ? object.size.low || object.size.high : $Number(object.size) !== 0)
                    if ($util.Long)
                        message.size = $util.Long.fromValue(object.size, true);
                    else if (typeof object.size === "string")
                        message.size = $parseInt(object.size, 10);
                    else if (typeof object.size === "number")
                        message.size = object.size;
                    else if (typeof object.size === "object")
                        message.size = new $util.LongBits(object.size.low >>> 0, object.size.high >>> 0).toNumber(true);
            return message;
        };

        CliprdrFile.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.size = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.size = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.size != null && $Object.hasOwnProperty.call(message, "size"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.size = typeof message.size === "number" ? $BigInt(message.size) : $util.Long.fromBits(message.size.low >>> 0, message.size.high >>> 0, true).toBigInt();
                else if (typeof message.size === "number")
                    object.size = options.longs === $String ? $String(message.size) : message.size;
                else
                    object.size = options.longs === $String ? $util.Long.prototype.toString.call(message.size) : options.longs === $Number ? new $util.LongBits(message.size.low >>> 0, message.size.high >>> 0).toNumber(true) : message.size;
            return object;
        };

        CliprdrFile.prototype.toJSON = function() {
            return CliprdrFile.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrFile;
    })();

    hbb.CliprdrFiles = (function() {

        const CliprdrFiles = function (properties) {
            this.files = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CliprdrFiles.prototype.files = $util.emptyArray;

        CliprdrFiles.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.files != null && message.files.length)
                for (let i = 0; i < message.files.length; ++i)
                    $root.hbb.CliprdrFile.encode(message.files[i], writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CliprdrFiles.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CliprdrFiles();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.files && message.files.length))
                            message.files = [];
                        message.files.push($root.hbb.CliprdrFile.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CliprdrFiles.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CliprdrFiles)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CliprdrFiles: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CliprdrFiles();
            if (object.files) {
                if (!$Array.isArray(object.files))
                    throw $TypeError(".hbb.CliprdrFiles.files: array expected");
                message.files = $Array(object.files.length);
                for (let i = 0; i < object.files.length; ++i) {
                    if (!$util.isObject(object.files[i]))
                        throw $TypeError(".hbb.CliprdrFiles.files: object expected");
                    message.files[i] = $root.hbb.CliprdrFile.fromObject(object.files[i], _depth + 1);
                }
            }
            return message;
        };

        CliprdrFiles.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.files = [];
            if (message.files && message.files.length) {
                object.files = $Array(message.files.length);
                for (let j = 0; j < message.files.length; ++j)
                    object.files[j] = $root.hbb.CliprdrFile.toObject(message.files[j], options, _depth + 1);
            }
            return object;
        };

        CliprdrFiles.prototype.toJSON = function() {
            return CliprdrFiles.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CliprdrFiles;
    })();

    hbb.Cliprdr = (function() {

        const Cliprdr = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        Cliprdr.prototype.ready = null;
        Cliprdr.prototype.formatList = null;
        Cliprdr.prototype.formatListResponse = null;
        Cliprdr.prototype.formatDataRequest = null;
        Cliprdr.prototype.formatDataResponse = null;
        Cliprdr.prototype.fileContentsRequest = null;
        Cliprdr.prototype.fileContentsResponse = null;
        Cliprdr.prototype.tryEmpty = null;
        Cliprdr.prototype.files = null;

        let $oneOfFields;

        $Object.defineProperty(Cliprdr.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["ready", "formatList", "formatListResponse", "formatDataRequest", "formatDataResponse", "fileContentsRequest", "fileContentsResponse", "tryEmpty", "files"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Cliprdr.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.ready != null && $Object.hasOwnProperty.call(message, "ready"))
                $root.hbb.CliprdrMonitorReady.encode(message.ready, writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.formatList != null && $Object.hasOwnProperty.call(message, "formatList"))
                $root.hbb.CliprdrServerFormatList.encode(message.formatList, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.formatListResponse != null && $Object.hasOwnProperty.call(message, "formatListResponse"))
                $root.hbb.CliprdrServerFormatListResponse.encode(message.formatListResponse, writer.uint32(26).fork(), _depth + 1).ldelim();
            if (message.formatDataRequest != null && $Object.hasOwnProperty.call(message, "formatDataRequest"))
                $root.hbb.CliprdrServerFormatDataRequest.encode(message.formatDataRequest, writer.uint32(34).fork(), _depth + 1).ldelim();
            if (message.formatDataResponse != null && $Object.hasOwnProperty.call(message, "formatDataResponse"))
                $root.hbb.CliprdrServerFormatDataResponse.encode(message.formatDataResponse, writer.uint32(42).fork(), _depth + 1).ldelim();
            if (message.fileContentsRequest != null && $Object.hasOwnProperty.call(message, "fileContentsRequest"))
                $root.hbb.CliprdrFileContentsRequest.encode(message.fileContentsRequest, writer.uint32(50).fork(), _depth + 1).ldelim();
            if (message.fileContentsResponse != null && $Object.hasOwnProperty.call(message, "fileContentsResponse"))
                $root.hbb.CliprdrFileContentsResponse.encode(message.fileContentsResponse, writer.uint32(58).fork(), _depth + 1).ldelim();
            if (message.tryEmpty != null && $Object.hasOwnProperty.call(message, "tryEmpty"))
                $root.hbb.CliprdrTryEmpty.encode(message.tryEmpty, writer.uint32(66).fork(), _depth + 1).ldelim();
            if (message.files != null && $Object.hasOwnProperty.call(message, "files"))
                $root.hbb.CliprdrFiles.encode(message.files, writer.uint32(74).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Cliprdr.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.Cliprdr();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.ready = $root.hbb.CliprdrMonitorReady.decode(reader, reader.uint32(), $undefined, _depth + 1, message.ready);
                        message.union = "ready";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.formatList = $root.hbb.CliprdrServerFormatList.decode(reader, reader.uint32(), $undefined, _depth + 1, message.formatList);
                        message.union = "formatList";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.formatListResponse = $root.hbb.CliprdrServerFormatListResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.formatListResponse);
                        message.union = "formatListResponse";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.formatDataRequest = $root.hbb.CliprdrServerFormatDataRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.formatDataRequest);
                        message.union = "formatDataRequest";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.formatDataResponse = $root.hbb.CliprdrServerFormatDataResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.formatDataResponse);
                        message.union = "formatDataResponse";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.fileContentsRequest = $root.hbb.CliprdrFileContentsRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileContentsRequest);
                        message.union = "fileContentsRequest";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.fileContentsResponse = $root.hbb.CliprdrFileContentsResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileContentsResponse);
                        message.union = "fileContentsResponse";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.tryEmpty = $root.hbb.CliprdrTryEmpty.decode(reader, reader.uint32(), $undefined, _depth + 1, message.tryEmpty);
                        message.union = "tryEmpty";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.files = $root.hbb.CliprdrFiles.decode(reader, reader.uint32(), $undefined, _depth + 1, message.files);
                        message.union = "files";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        Cliprdr.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.Cliprdr)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.Cliprdr: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.Cliprdr();
            if (object.ready != null) {
                if (!$util.isObject(object.ready))
                    throw $TypeError(".hbb.Cliprdr.ready: object expected");
                message.ready = $root.hbb.CliprdrMonitorReady.fromObject(object.ready, _depth + 1);
            }
            if (object.formatList != null) {
                if (!$util.isObject(object.formatList))
                    throw $TypeError(".hbb.Cliprdr.formatList: object expected");
                message.formatList = $root.hbb.CliprdrServerFormatList.fromObject(object.formatList, _depth + 1);
            }
            if (object.formatListResponse != null) {
                if (!$util.isObject(object.formatListResponse))
                    throw $TypeError(".hbb.Cliprdr.formatListResponse: object expected");
                message.formatListResponse = $root.hbb.CliprdrServerFormatListResponse.fromObject(object.formatListResponse, _depth + 1);
            }
            if (object.formatDataRequest != null) {
                if (!$util.isObject(object.formatDataRequest))
                    throw $TypeError(".hbb.Cliprdr.formatDataRequest: object expected");
                message.formatDataRequest = $root.hbb.CliprdrServerFormatDataRequest.fromObject(object.formatDataRequest, _depth + 1);
            }
            if (object.formatDataResponse != null) {
                if (!$util.isObject(object.formatDataResponse))
                    throw $TypeError(".hbb.Cliprdr.formatDataResponse: object expected");
                message.formatDataResponse = $root.hbb.CliprdrServerFormatDataResponse.fromObject(object.formatDataResponse, _depth + 1);
            }
            if (object.fileContentsRequest != null) {
                if (!$util.isObject(object.fileContentsRequest))
                    throw $TypeError(".hbb.Cliprdr.fileContentsRequest: object expected");
                message.fileContentsRequest = $root.hbb.CliprdrFileContentsRequest.fromObject(object.fileContentsRequest, _depth + 1);
            }
            if (object.fileContentsResponse != null) {
                if (!$util.isObject(object.fileContentsResponse))
                    throw $TypeError(".hbb.Cliprdr.fileContentsResponse: object expected");
                message.fileContentsResponse = $root.hbb.CliprdrFileContentsResponse.fromObject(object.fileContentsResponse, _depth + 1);
            }
            if (object.tryEmpty != null) {
                if (!$util.isObject(object.tryEmpty))
                    throw $TypeError(".hbb.Cliprdr.tryEmpty: object expected");
                message.tryEmpty = $root.hbb.CliprdrTryEmpty.fromObject(object.tryEmpty, _depth + 1);
            }
            if (object.files != null) {
                if (!$util.isObject(object.files))
                    throw $TypeError(".hbb.Cliprdr.files: object expected");
                message.files = $root.hbb.CliprdrFiles.fromObject(object.files, _depth + 1);
            }
            return message;
        };

        Cliprdr.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.ready != null && $Object.hasOwnProperty.call(message, "ready")) {
                object.ready = $root.hbb.CliprdrMonitorReady.toObject(message.ready, options, _depth + 1);
                if (options.oneofs)
                    object.union = "ready";
            }
            if (message.formatList != null && $Object.hasOwnProperty.call(message, "formatList")) {
                object.formatList = $root.hbb.CliprdrServerFormatList.toObject(message.formatList, options, _depth + 1);
                if (options.oneofs)
                    object.union = "formatList";
            }
            if (message.formatListResponse != null && $Object.hasOwnProperty.call(message, "formatListResponse")) {
                object.formatListResponse = $root.hbb.CliprdrServerFormatListResponse.toObject(message.formatListResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "formatListResponse";
            }
            if (message.formatDataRequest != null && $Object.hasOwnProperty.call(message, "formatDataRequest")) {
                object.formatDataRequest = $root.hbb.CliprdrServerFormatDataRequest.toObject(message.formatDataRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "formatDataRequest";
            }
            if (message.formatDataResponse != null && $Object.hasOwnProperty.call(message, "formatDataResponse")) {
                object.formatDataResponse = $root.hbb.CliprdrServerFormatDataResponse.toObject(message.formatDataResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "formatDataResponse";
            }
            if (message.fileContentsRequest != null && $Object.hasOwnProperty.call(message, "fileContentsRequest")) {
                object.fileContentsRequest = $root.hbb.CliprdrFileContentsRequest.toObject(message.fileContentsRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "fileContentsRequest";
            }
            if (message.fileContentsResponse != null && $Object.hasOwnProperty.call(message, "fileContentsResponse")) {
                object.fileContentsResponse = $root.hbb.CliprdrFileContentsResponse.toObject(message.fileContentsResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "fileContentsResponse";
            }
            if (message.tryEmpty != null && $Object.hasOwnProperty.call(message, "tryEmpty")) {
                object.tryEmpty = $root.hbb.CliprdrTryEmpty.toObject(message.tryEmpty, options, _depth + 1);
                if (options.oneofs)
                    object.union = "tryEmpty";
            }
            if (message.files != null && $Object.hasOwnProperty.call(message, "files")) {
                object.files = $root.hbb.CliprdrFiles.toObject(message.files, options, _depth + 1);
                if (options.oneofs)
                    object.union = "files";
            }
            return object;
        };

        Cliprdr.prototype.toJSON = function() {
            return Cliprdr.toObject(this, $protobuf.util.toJSONOptions);
        };

        return Cliprdr;
    })();

    hbb.Resolution = (function() {

        const Resolution = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        Resolution.prototype.width = 0;
        Resolution.prototype.height = 0;

        Resolution.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.width != null && $Object.hasOwnProperty.call(message, "width") && message.width !== 0)
                writer.uint32(8).int32(message.width);
            if (message.height != null && $Object.hasOwnProperty.call(message, "height") && message.height !== 0)
                writer.uint32(16).int32(message.height);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Resolution.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.Resolution(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.width = value;
                        else
                            delete message.width;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.height = value;
                        else
                            delete message.height;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        Resolution.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.Resolution)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.Resolution: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.Resolution();
            if (object.width != null)
                if ($Number(object.width) !== 0)
                    message.width = object.width | 0;
            if (object.height != null)
                if ($Number(object.height) !== 0)
                    message.height = object.height | 0;
            return message;
        };

        Resolution.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.width = 0;
                object.height = 0;
            }
            if (message.width != null && $Object.hasOwnProperty.call(message, "width"))
                object.width = message.width;
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            return object;
        };

        Resolution.prototype.toJSON = function() {
            return Resolution.toObject(this, $protobuf.util.toJSONOptions);
        };

        return Resolution;
    })();

    hbb.DisplayResolution = (function() {

        const DisplayResolution = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        DisplayResolution.prototype.display = 0;
        DisplayResolution.prototype.resolution = null;

        DisplayResolution.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.display != null && $Object.hasOwnProperty.call(message, "display") && message.display !== 0)
                writer.uint32(8).int32(message.display);
            if (message.resolution != null && $Object.hasOwnProperty.call(message, "resolution"))
                $root.hbb.Resolution.encode(message.resolution, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        DisplayResolution.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.DisplayResolution(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.display = value;
                        else
                            delete message.display;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.resolution = $root.hbb.Resolution.decode(reader, reader.uint32(), $undefined, _depth + 1, message.resolution);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        DisplayResolution.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.DisplayResolution)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.DisplayResolution: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.DisplayResolution();
            if (object.display != null)
                if ($Number(object.display) !== 0)
                    message.display = object.display | 0;
            if (object.resolution != null) {
                if (!$util.isObject(object.resolution))
                    throw $TypeError(".hbb.DisplayResolution.resolution: object expected");
                message.resolution = $root.hbb.Resolution.fromObject(object.resolution, _depth + 1);
            }
            return message;
        };

        DisplayResolution.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.display = 0;
                object.resolution = null;
            }
            if (message.display != null && $Object.hasOwnProperty.call(message, "display"))
                object.display = message.display;
            if (message.resolution != null && $Object.hasOwnProperty.call(message, "resolution"))
                object.resolution = $root.hbb.Resolution.toObject(message.resolution, options, _depth + 1);
            return object;
        };

        DisplayResolution.prototype.toJSON = function() {
            return DisplayResolution.toObject(this, $protobuf.util.toJSONOptions);
        };

        return DisplayResolution;
    })();

    hbb.SupportedResolutions = (function() {

        const SupportedResolutions = function (properties) {
            this.resolutions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        SupportedResolutions.prototype.resolutions = $util.emptyArray;

        SupportedResolutions.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.resolutions != null && message.resolutions.length)
                for (let i = 0; i < message.resolutions.length; ++i)
                    $root.hbb.Resolution.encode(message.resolutions[i], writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SupportedResolutions.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.SupportedResolutions();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.resolutions && message.resolutions.length))
                            message.resolutions = [];
                        message.resolutions.push($root.hbb.Resolution.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        SupportedResolutions.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.SupportedResolutions)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.SupportedResolutions: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.SupportedResolutions();
            if (object.resolutions) {
                if (!$Array.isArray(object.resolutions))
                    throw $TypeError(".hbb.SupportedResolutions.resolutions: array expected");
                message.resolutions = $Array(object.resolutions.length);
                for (let i = 0; i < object.resolutions.length; ++i) {
                    if (!$util.isObject(object.resolutions[i]))
                        throw $TypeError(".hbb.SupportedResolutions.resolutions: object expected");
                    message.resolutions[i] = $root.hbb.Resolution.fromObject(object.resolutions[i], _depth + 1);
                }
            }
            return message;
        };

        SupportedResolutions.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.resolutions = [];
            if (message.resolutions && message.resolutions.length) {
                object.resolutions = $Array(message.resolutions.length);
                for (let j = 0; j < message.resolutions.length; ++j)
                    object.resolutions[j] = $root.hbb.Resolution.toObject(message.resolutions[j], options, _depth + 1);
            }
            return object;
        };

        SupportedResolutions.prototype.toJSON = function() {
            return SupportedResolutions.toObject(this, $protobuf.util.toJSONOptions);
        };

        return SupportedResolutions;
    })();

    hbb.SwitchDisplay = (function() {

        const SwitchDisplay = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        SwitchDisplay.prototype.display = 0;
        SwitchDisplay.prototype.x = 0;
        SwitchDisplay.prototype.y = 0;
        SwitchDisplay.prototype.width = 0;
        SwitchDisplay.prototype.height = 0;
        SwitchDisplay.prototype.cursorEmbedded = false;
        SwitchDisplay.prototype.resolutions = null;
        SwitchDisplay.prototype.originalResolution = null;

        SwitchDisplay.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.display != null && $Object.hasOwnProperty.call(message, "display") && message.display !== 0)
                writer.uint32(8).int32(message.display);
            if (message.x != null && $Object.hasOwnProperty.call(message, "x") && message.x !== 0)
                writer.uint32(16).sint32(message.x);
            if (message.y != null && $Object.hasOwnProperty.call(message, "y") && message.y !== 0)
                writer.uint32(24).sint32(message.y);
            if (message.width != null && $Object.hasOwnProperty.call(message, "width") && message.width !== 0)
                writer.uint32(32).int32(message.width);
            if (message.height != null && $Object.hasOwnProperty.call(message, "height") && message.height !== 0)
                writer.uint32(40).int32(message.height);
            if (message.cursorEmbedded != null && $Object.hasOwnProperty.call(message, "cursorEmbedded") && message.cursorEmbedded !== false)
                writer.uint32(48).bool(message.cursorEmbedded);
            if (message.resolutions != null && $Object.hasOwnProperty.call(message, "resolutions"))
                $root.hbb.SupportedResolutions.encode(message.resolutions, writer.uint32(58).fork(), _depth + 1).ldelim();
            if (message.originalResolution != null && $Object.hasOwnProperty.call(message, "originalResolution"))
                $root.hbb.Resolution.encode(message.originalResolution, writer.uint32(66).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SwitchDisplay.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.SwitchDisplay(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.display = value;
                        else
                            delete message.display;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.x = value;
                        else
                            delete message.x;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.sint32())
                            message.y = value;
                        else
                            delete message.y;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.width = value;
                        else
                            delete message.width;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.height = value;
                        else
                            delete message.height;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.cursorEmbedded = value;
                        else
                            delete message.cursorEmbedded;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.resolutions = $root.hbb.SupportedResolutions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.resolutions);
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.originalResolution = $root.hbb.Resolution.decode(reader, reader.uint32(), $undefined, _depth + 1, message.originalResolution);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        SwitchDisplay.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.SwitchDisplay)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.SwitchDisplay: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.SwitchDisplay();
            if (object.display != null)
                if ($Number(object.display) !== 0)
                    message.display = object.display | 0;
            if (object.x != null)
                if ($Number(object.x) !== 0)
                    message.x = object.x | 0;
            if (object.y != null)
                if ($Number(object.y) !== 0)
                    message.y = object.y | 0;
            if (object.width != null)
                if ($Number(object.width) !== 0)
                    message.width = object.width | 0;
            if (object.height != null)
                if ($Number(object.height) !== 0)
                    message.height = object.height | 0;
            if (object.cursorEmbedded != null)
                if (object.cursorEmbedded)
                    message.cursorEmbedded = $Boolean(object.cursorEmbedded);
            if (object.resolutions != null) {
                if (!$util.isObject(object.resolutions))
                    throw $TypeError(".hbb.SwitchDisplay.resolutions: object expected");
                message.resolutions = $root.hbb.SupportedResolutions.fromObject(object.resolutions, _depth + 1);
            }
            if (object.originalResolution != null) {
                if (!$util.isObject(object.originalResolution))
                    throw $TypeError(".hbb.SwitchDisplay.originalResolution: object expected");
                message.originalResolution = $root.hbb.Resolution.fromObject(object.originalResolution, _depth + 1);
            }
            return message;
        };

        SwitchDisplay.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.display = 0;
                object.x = 0;
                object.y = 0;
                object.width = 0;
                object.height = 0;
                object.cursorEmbedded = false;
                object.resolutions = null;
                object.originalResolution = null;
            }
            if (message.display != null && $Object.hasOwnProperty.call(message, "display"))
                object.display = message.display;
            if (message.x != null && $Object.hasOwnProperty.call(message, "x"))
                object.x = message.x;
            if (message.y != null && $Object.hasOwnProperty.call(message, "y"))
                object.y = message.y;
            if (message.width != null && $Object.hasOwnProperty.call(message, "width"))
                object.width = message.width;
            if (message.height != null && $Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            if (message.cursorEmbedded != null && $Object.hasOwnProperty.call(message, "cursorEmbedded"))
                object.cursorEmbedded = message.cursorEmbedded;
            if (message.resolutions != null && $Object.hasOwnProperty.call(message, "resolutions"))
                object.resolutions = $root.hbb.SupportedResolutions.toObject(message.resolutions, options, _depth + 1);
            if (message.originalResolution != null && $Object.hasOwnProperty.call(message, "originalResolution"))
                object.originalResolution = $root.hbb.Resolution.toObject(message.originalResolution, options, _depth + 1);
            return object;
        };

        SwitchDisplay.prototype.toJSON = function() {
            return SwitchDisplay.toObject(this, $protobuf.util.toJSONOptions);
        };

        return SwitchDisplay;
    })();

    hbb.CaptureDisplays = (function() {

        const CaptureDisplays = function (properties) {
            this.add = [];
            this.sub = [];
            this.set = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CaptureDisplays.prototype.add = $util.emptyArray;
        CaptureDisplays.prototype.sub = $util.emptyArray;
        CaptureDisplays.prototype.set = $util.emptyArray;

        CaptureDisplays.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.add != null && message.add.length)
                writer.uint32(10).int32s(message.add);
            if (message.sub != null && message.sub.length)
                writer.uint32(18).int32s(message.sub);
            if (message.set != null && message.set.length)
                writer.uint32(26).int32s(message.set);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CaptureDisplays.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CaptureDisplays();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType === 2) {
                            if (!(message.add && message.add.length))
                                message.add = [];
                            reader.int32s(message.add);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.add && message.add.length))
                            message.add = [];
                        message.add.push(reader.int32());
                        continue;
                    }
                case 2: {
                        if (wireType === 2) {
                            if (!(message.sub && message.sub.length))
                                message.sub = [];
                            reader.int32s(message.sub);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.sub && message.sub.length))
                            message.sub = [];
                        message.sub.push(reader.int32());
                        continue;
                    }
                case 3: {
                        if (wireType === 2) {
                            if (!(message.set && message.set.length))
                                message.set = [];
                            reader.int32s(message.set);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.set && message.set.length))
                            message.set = [];
                        message.set.push(reader.int32());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CaptureDisplays.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CaptureDisplays)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CaptureDisplays: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CaptureDisplays();
            if (object.add) {
                if (!$Array.isArray(object.add))
                    throw $TypeError(".hbb.CaptureDisplays.add: array expected");
                message.add = $Array(object.add.length);
                for (let i = 0; i < object.add.length; ++i)
                    message.add[i] = object.add[i] | 0;
            }
            if (object.sub) {
                if (!$Array.isArray(object.sub))
                    throw $TypeError(".hbb.CaptureDisplays.sub: array expected");
                message.sub = $Array(object.sub.length);
                for (let i = 0; i < object.sub.length; ++i)
                    message.sub[i] = object.sub[i] | 0;
            }
            if (object.set) {
                if (!$Array.isArray(object.set))
                    throw $TypeError(".hbb.CaptureDisplays.set: array expected");
                message.set = $Array(object.set.length);
                for (let i = 0; i < object.set.length; ++i)
                    message.set[i] = object.set[i] | 0;
            }
            return message;
        };

        CaptureDisplays.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.add = [];
                object.sub = [];
                object.set = [];
            }
            if (message.add && message.add.length) {
                object.add = $Array(message.add.length);
                for (let j = 0; j < message.add.length; ++j)
                    object.add[j] = message.add[j];
            }
            if (message.sub && message.sub.length) {
                object.sub = $Array(message.sub.length);
                for (let j = 0; j < message.sub.length; ++j)
                    object.sub[j] = message.sub[j];
            }
            if (message.set && message.set.length) {
                object.set = $Array(message.set.length);
                for (let j = 0; j < message.set.length; ++j)
                    object.set[j] = message.set[j];
            }
            return object;
        };

        CaptureDisplays.prototype.toJSON = function() {
            return CaptureDisplays.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CaptureDisplays;
    })();

    hbb.ToggleVirtualDisplay = (function() {

        const ToggleVirtualDisplay = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ToggleVirtualDisplay.prototype.display = 0;
        ToggleVirtualDisplay.prototype.on = false;

        ToggleVirtualDisplay.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.display != null && $Object.hasOwnProperty.call(message, "display") && message.display !== 0)
                writer.uint32(8).int32(message.display);
            if (message.on != null && $Object.hasOwnProperty.call(message, "on") && message.on !== false)
                writer.uint32(16).bool(message.on);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ToggleVirtualDisplay.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ToggleVirtualDisplay(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.display = value;
                        else
                            delete message.display;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.on = value;
                        else
                            delete message.on;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ToggleVirtualDisplay.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ToggleVirtualDisplay)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ToggleVirtualDisplay: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ToggleVirtualDisplay();
            if (object.display != null)
                if ($Number(object.display) !== 0)
                    message.display = object.display | 0;
            if (object.on != null)
                if (object.on)
                    message.on = $Boolean(object.on);
            return message;
        };

        ToggleVirtualDisplay.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.display = 0;
                object.on = false;
            }
            if (message.display != null && $Object.hasOwnProperty.call(message, "display"))
                object.display = message.display;
            if (message.on != null && $Object.hasOwnProperty.call(message, "on"))
                object.on = message.on;
            return object;
        };

        ToggleVirtualDisplay.prototype.toJSON = function() {
            return ToggleVirtualDisplay.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ToggleVirtualDisplay;
    })();

    hbb.TogglePrivacyMode = (function() {

        const TogglePrivacyMode = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TogglePrivacyMode.prototype.implKey = "";
        TogglePrivacyMode.prototype.on = false;

        TogglePrivacyMode.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.implKey != null && $Object.hasOwnProperty.call(message, "implKey") && message.implKey !== "")
                writer.uint32(10).string(message.implKey);
            if (message.on != null && $Object.hasOwnProperty.call(message, "on") && message.on !== false)
                writer.uint32(16).bool(message.on);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TogglePrivacyMode.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TogglePrivacyMode(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.implKey = value;
                        else
                            delete message.implKey;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.on = value;
                        else
                            delete message.on;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TogglePrivacyMode.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TogglePrivacyMode)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TogglePrivacyMode: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TogglePrivacyMode();
            if (object.implKey != null)
                if (typeof object.implKey !== "string" || object.implKey.length)
                    message.implKey = $String(object.implKey);
            if (object.on != null)
                if (object.on)
                    message.on = $Boolean(object.on);
            return message;
        };

        TogglePrivacyMode.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.implKey = "";
                object.on = false;
            }
            if (message.implKey != null && $Object.hasOwnProperty.call(message, "implKey"))
                object.implKey = message.implKey;
            if (message.on != null && $Object.hasOwnProperty.call(message, "on"))
                object.on = message.on;
            return object;
        };

        TogglePrivacyMode.prototype.toJSON = function() {
            return TogglePrivacyMode.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TogglePrivacyMode;
    })();

    hbb.PermissionInfo = (function() {

        const PermissionInfo = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PermissionInfo.prototype.permission = 0;
        PermissionInfo.prototype.enabled = false;

        PermissionInfo.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.permission != null && $Object.hasOwnProperty.call(message, "permission") && message.permission !== 0)
                writer.uint32(8).int32(message.permission);
            if (message.enabled != null && $Object.hasOwnProperty.call(message, "enabled") && message.enabled !== false)
                writer.uint32(16).bool(message.enabled);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PermissionInfo.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PermissionInfo(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.permission = value;
                        else
                            delete message.permission;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.enabled = value;
                        else
                            delete message.enabled;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PermissionInfo.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PermissionInfo)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PermissionInfo: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PermissionInfo();
            if (object.permission !== 0 && (typeof object.permission !== "string" || $root.hbb.PermissionInfo.Permission[object.permission] !== 0))
                switch (object.permission) {
                case "Keyboard":
                case 0:
                    message.permission = 0;
                    break;
                case "Clipboard":
                case 2:
                    message.permission = 2;
                    break;
                case "Audio":
                case 3:
                    message.permission = 3;
                    break;
                case "File":
                case 4:
                    message.permission = 4;
                    break;
                case "Restart":
                case 5:
                    message.permission = 5;
                    break;
                case "Recording":
                case 6:
                    message.permission = 6;
                    break;
                case "BlockInput":
                case 7:
                    message.permission = 7;
                    break;
                case "PrivacyMode":
                case 8:
                    message.permission = 8;
                    break;
                default:
                    if (typeof object.permission === "number" && (object.permission | 0) === object.permission)
                        message.permission = object.permission;
                }
            if (object.enabled != null)
                if (object.enabled)
                    message.enabled = $Boolean(object.enabled);
            return message;
        };

        PermissionInfo.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.permission = options.enums === $String ? "Keyboard" : 0;
                object.enabled = false;
            }
            if (message.permission != null && $Object.hasOwnProperty.call(message, "permission"))
                object.permission = options.enums === $String ? $root.hbb.PermissionInfo.Permission[message.permission] === $undefined ? message.permission : $root.hbb.PermissionInfo.Permission[message.permission] : message.permission;
            if (message.enabled != null && $Object.hasOwnProperty.call(message, "enabled"))
                object.enabled = message.enabled;
            return object;
        };

        PermissionInfo.prototype.toJSON = function() {
            return PermissionInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        PermissionInfo.Permission = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "Keyboard"] = 0;
            values[valuesById[2] = "Clipboard"] = 2;
            values[valuesById[3] = "Audio"] = 3;
            values[valuesById[4] = "File"] = 4;
            values[valuesById[5] = "Restart"] = 5;
            values[valuesById[6] = "Recording"] = 6;
            values[valuesById[7] = "BlockInput"] = 7;
            values[valuesById[8] = "PrivacyMode"] = 8;
            return values;
        })();

        return PermissionInfo;
    })();

    hbb.ImageQuality = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "NotSet"] = 0;
        values[valuesById[2] = "Low"] = 2;
        values[valuesById[3] = "Balanced"] = 3;
        values[valuesById[4] = "Best"] = 4;
        return values;
    })();

    hbb.SupportedDecoding = (function() {

        const SupportedDecoding = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        SupportedDecoding.prototype.abilityVp9 = 0;
        SupportedDecoding.prototype.abilityH264 = 0;
        SupportedDecoding.prototype.abilityH265 = 0;
        SupportedDecoding.prototype.prefer = 0;
        SupportedDecoding.prototype.abilityVp8 = 0;
        SupportedDecoding.prototype.abilityAv1 = 0;
        SupportedDecoding.prototype.i444 = null;
        SupportedDecoding.prototype.preferChroma = 0;

        SupportedDecoding.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.abilityVp9 != null && $Object.hasOwnProperty.call(message, "abilityVp9") && message.abilityVp9 !== 0)
                writer.uint32(8).int32(message.abilityVp9);
            if (message.abilityH264 != null && $Object.hasOwnProperty.call(message, "abilityH264") && message.abilityH264 !== 0)
                writer.uint32(16).int32(message.abilityH264);
            if (message.abilityH265 != null && $Object.hasOwnProperty.call(message, "abilityH265") && message.abilityH265 !== 0)
                writer.uint32(24).int32(message.abilityH265);
            if (message.prefer != null && $Object.hasOwnProperty.call(message, "prefer") && message.prefer !== 0)
                writer.uint32(32).int32(message.prefer);
            if (message.abilityVp8 != null && $Object.hasOwnProperty.call(message, "abilityVp8") && message.abilityVp8 !== 0)
                writer.uint32(40).int32(message.abilityVp8);
            if (message.abilityAv1 != null && $Object.hasOwnProperty.call(message, "abilityAv1") && message.abilityAv1 !== 0)
                writer.uint32(48).int32(message.abilityAv1);
            if (message.i444 != null && $Object.hasOwnProperty.call(message, "i444"))
                $root.hbb.CodecAbility.encode(message.i444, writer.uint32(58).fork(), _depth + 1).ldelim();
            if (message.preferChroma != null && $Object.hasOwnProperty.call(message, "preferChroma") && message.preferChroma !== 0)
                writer.uint32(64).int32(message.preferChroma);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SupportedDecoding.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.SupportedDecoding(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.abilityVp9 = value;
                        else
                            delete message.abilityVp9;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.abilityH264 = value;
                        else
                            delete message.abilityH264;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.abilityH265 = value;
                        else
                            delete message.abilityH265;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.prefer = value;
                        else
                            delete message.prefer;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.abilityVp8 = value;
                        else
                            delete message.abilityVp8;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.abilityAv1 = value;
                        else
                            delete message.abilityAv1;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.i444 = $root.hbb.CodecAbility.decode(reader, reader.uint32(), $undefined, _depth + 1, message.i444);
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.preferChroma = value;
                        else
                            delete message.preferChroma;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        SupportedDecoding.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.SupportedDecoding)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.SupportedDecoding: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.SupportedDecoding();
            if (object.abilityVp9 != null)
                if ($Number(object.abilityVp9) !== 0)
                    message.abilityVp9 = object.abilityVp9 | 0;
            if (object.abilityH264 != null)
                if ($Number(object.abilityH264) !== 0)
                    message.abilityH264 = object.abilityH264 | 0;
            if (object.abilityH265 != null)
                if ($Number(object.abilityH265) !== 0)
                    message.abilityH265 = object.abilityH265 | 0;
            if (object.prefer !== 0 && (typeof object.prefer !== "string" || $root.hbb.SupportedDecoding.PreferCodec[object.prefer] !== 0))
                switch (object.prefer) {
                case "Auto":
                case 0:
                    message.prefer = 0;
                    break;
                case "VP9":
                case 1:
                    message.prefer = 1;
                    break;
                case "H264":
                case 2:
                    message.prefer = 2;
                    break;
                case "H265":
                case 3:
                    message.prefer = 3;
                    break;
                case "VP8":
                case 4:
                    message.prefer = 4;
                    break;
                case "AV1":
                case 5:
                    message.prefer = 5;
                    break;
                default:
                    if (typeof object.prefer === "number" && (object.prefer | 0) === object.prefer)
                        message.prefer = object.prefer;
                }
            if (object.abilityVp8 != null)
                if ($Number(object.abilityVp8) !== 0)
                    message.abilityVp8 = object.abilityVp8 | 0;
            if (object.abilityAv1 != null)
                if ($Number(object.abilityAv1) !== 0)
                    message.abilityAv1 = object.abilityAv1 | 0;
            if (object.i444 != null) {
                if (!$util.isObject(object.i444))
                    throw $TypeError(".hbb.SupportedDecoding.i444: object expected");
                message.i444 = $root.hbb.CodecAbility.fromObject(object.i444, _depth + 1);
            }
            if (object.preferChroma !== 0 && (typeof object.preferChroma !== "string" || $root.hbb.Chroma[object.preferChroma] !== 0))
                switch (object.preferChroma) {
                case "I420":
                case 0:
                    message.preferChroma = 0;
                    break;
                case "I444":
                case 1:
                    message.preferChroma = 1;
                    break;
                default:
                    if (typeof object.preferChroma === "number" && (object.preferChroma | 0) === object.preferChroma)
                        message.preferChroma = object.preferChroma;
                }
            return message;
        };

        SupportedDecoding.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.abilityVp9 = 0;
                object.abilityH264 = 0;
                object.abilityH265 = 0;
                object.prefer = options.enums === $String ? "Auto" : 0;
                object.abilityVp8 = 0;
                object.abilityAv1 = 0;
                object.i444 = null;
                object.preferChroma = options.enums === $String ? "I420" : 0;
            }
            if (message.abilityVp9 != null && $Object.hasOwnProperty.call(message, "abilityVp9"))
                object.abilityVp9 = message.abilityVp9;
            if (message.abilityH264 != null && $Object.hasOwnProperty.call(message, "abilityH264"))
                object.abilityH264 = message.abilityH264;
            if (message.abilityH265 != null && $Object.hasOwnProperty.call(message, "abilityH265"))
                object.abilityH265 = message.abilityH265;
            if (message.prefer != null && $Object.hasOwnProperty.call(message, "prefer"))
                object.prefer = options.enums === $String ? $root.hbb.SupportedDecoding.PreferCodec[message.prefer] === $undefined ? message.prefer : $root.hbb.SupportedDecoding.PreferCodec[message.prefer] : message.prefer;
            if (message.abilityVp8 != null && $Object.hasOwnProperty.call(message, "abilityVp8"))
                object.abilityVp8 = message.abilityVp8;
            if (message.abilityAv1 != null && $Object.hasOwnProperty.call(message, "abilityAv1"))
                object.abilityAv1 = message.abilityAv1;
            if (message.i444 != null && $Object.hasOwnProperty.call(message, "i444"))
                object.i444 = $root.hbb.CodecAbility.toObject(message.i444, options, _depth + 1);
            if (message.preferChroma != null && $Object.hasOwnProperty.call(message, "preferChroma"))
                object.preferChroma = options.enums === $String ? $root.hbb.Chroma[message.preferChroma] === $undefined ? message.preferChroma : $root.hbb.Chroma[message.preferChroma] : message.preferChroma;
            return object;
        };

        SupportedDecoding.prototype.toJSON = function() {
            return SupportedDecoding.toObject(this, $protobuf.util.toJSONOptions);
        };

        SupportedDecoding.PreferCodec = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "Auto"] = 0;
            values[valuesById[1] = "VP9"] = 1;
            values[valuesById[2] = "H264"] = 2;
            values[valuesById[3] = "H265"] = 3;
            values[valuesById[4] = "VP8"] = 4;
            values[valuesById[5] = "AV1"] = 5;
            return values;
        })();

        return SupportedDecoding;
    })();

    hbb.OptionMessage = (function() {

        const OptionMessage = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        OptionMessage.prototype.imageQuality = 0;
        OptionMessage.prototype.lockAfterSessionEnd = 0;
        OptionMessage.prototype.showRemoteCursor = 0;
        OptionMessage.prototype.privacyMode = 0;
        OptionMessage.prototype.blockInput = 0;
        OptionMessage.prototype.customImageQuality = 0;
        OptionMessage.prototype.disableAudio = 0;
        OptionMessage.prototype.disableClipboard = 0;
        OptionMessage.prototype.enableFileTransfer = 0;
        OptionMessage.prototype.supportedDecoding = null;
        OptionMessage.prototype.customFps = 0;
        OptionMessage.prototype.disableKeyboard = 0;
        OptionMessage.prototype.followRemoteCursor = 0;
        OptionMessage.prototype.followRemoteWindow = 0;
        OptionMessage.prototype.disableCamera = 0;
        OptionMessage.prototype.terminalPersistent = 0;
        OptionMessage.prototype.showMyCursor = 0;

        OptionMessage.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.imageQuality != null && $Object.hasOwnProperty.call(message, "imageQuality") && message.imageQuality !== 0)
                writer.uint32(8).int32(message.imageQuality);
            if (message.lockAfterSessionEnd != null && $Object.hasOwnProperty.call(message, "lockAfterSessionEnd") && message.lockAfterSessionEnd !== 0)
                writer.uint32(16).int32(message.lockAfterSessionEnd);
            if (message.showRemoteCursor != null && $Object.hasOwnProperty.call(message, "showRemoteCursor") && message.showRemoteCursor !== 0)
                writer.uint32(24).int32(message.showRemoteCursor);
            if (message.privacyMode != null && $Object.hasOwnProperty.call(message, "privacyMode") && message.privacyMode !== 0)
                writer.uint32(32).int32(message.privacyMode);
            if (message.blockInput != null && $Object.hasOwnProperty.call(message, "blockInput") && message.blockInput !== 0)
                writer.uint32(40).int32(message.blockInput);
            if (message.customImageQuality != null && $Object.hasOwnProperty.call(message, "customImageQuality") && message.customImageQuality !== 0)
                writer.uint32(48).int32(message.customImageQuality);
            if (message.disableAudio != null && $Object.hasOwnProperty.call(message, "disableAudio") && message.disableAudio !== 0)
                writer.uint32(56).int32(message.disableAudio);
            if (message.disableClipboard != null && $Object.hasOwnProperty.call(message, "disableClipboard") && message.disableClipboard !== 0)
                writer.uint32(64).int32(message.disableClipboard);
            if (message.enableFileTransfer != null && $Object.hasOwnProperty.call(message, "enableFileTransfer") && message.enableFileTransfer !== 0)
                writer.uint32(72).int32(message.enableFileTransfer);
            if (message.supportedDecoding != null && $Object.hasOwnProperty.call(message, "supportedDecoding"))
                $root.hbb.SupportedDecoding.encode(message.supportedDecoding, writer.uint32(82).fork(), _depth + 1).ldelim();
            if (message.customFps != null && $Object.hasOwnProperty.call(message, "customFps") && message.customFps !== 0)
                writer.uint32(88).int32(message.customFps);
            if (message.disableKeyboard != null && $Object.hasOwnProperty.call(message, "disableKeyboard") && message.disableKeyboard !== 0)
                writer.uint32(96).int32(message.disableKeyboard);
            if (message.followRemoteCursor != null && $Object.hasOwnProperty.call(message, "followRemoteCursor") && message.followRemoteCursor !== 0)
                writer.uint32(120).int32(message.followRemoteCursor);
            if (message.followRemoteWindow != null && $Object.hasOwnProperty.call(message, "followRemoteWindow") && message.followRemoteWindow !== 0)
                writer.uint32(128).int32(message.followRemoteWindow);
            if (message.disableCamera != null && $Object.hasOwnProperty.call(message, "disableCamera") && message.disableCamera !== 0)
                writer.uint32(136).int32(message.disableCamera);
            if (message.terminalPersistent != null && $Object.hasOwnProperty.call(message, "terminalPersistent") && message.terminalPersistent !== 0)
                writer.uint32(144).int32(message.terminalPersistent);
            if (message.showMyCursor != null && $Object.hasOwnProperty.call(message, "showMyCursor") && message.showMyCursor !== 0)
                writer.uint32(152).int32(message.showMyCursor);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        OptionMessage.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.OptionMessage(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.imageQuality = value;
                        else
                            delete message.imageQuality;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.lockAfterSessionEnd = value;
                        else
                            delete message.lockAfterSessionEnd;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.showRemoteCursor = value;
                        else
                            delete message.showRemoteCursor;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.privacyMode = value;
                        else
                            delete message.privacyMode;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.blockInput = value;
                        else
                            delete message.blockInput;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.customImageQuality = value;
                        else
                            delete message.customImageQuality;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.disableAudio = value;
                        else
                            delete message.disableAudio;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.disableClipboard = value;
                        else
                            delete message.disableClipboard;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.enableFileTransfer = value;
                        else
                            delete message.enableFileTransfer;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.supportedDecoding = $root.hbb.SupportedDecoding.decode(reader, reader.uint32(), $undefined, _depth + 1, message.supportedDecoding);
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.customFps = value;
                        else
                            delete message.customFps;
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.disableKeyboard = value;
                        else
                            delete message.disableKeyboard;
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.followRemoteCursor = value;
                        else
                            delete message.followRemoteCursor;
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.followRemoteWindow = value;
                        else
                            delete message.followRemoteWindow;
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.disableCamera = value;
                        else
                            delete message.disableCamera;
                        continue;
                    }
                case 18: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.terminalPersistent = value;
                        else
                            delete message.terminalPersistent;
                        continue;
                    }
                case 19: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.showMyCursor = value;
                        else
                            delete message.showMyCursor;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        OptionMessage.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.OptionMessage)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.OptionMessage: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.OptionMessage();
            if (object.imageQuality !== 0 && (typeof object.imageQuality !== "string" || $root.hbb.ImageQuality[object.imageQuality] !== 0))
                switch (object.imageQuality) {
                case "NotSet":
                case 0:
                    message.imageQuality = 0;
                    break;
                case "Low":
                case 2:
                    message.imageQuality = 2;
                    break;
                case "Balanced":
                case 3:
                    message.imageQuality = 3;
                    break;
                case "Best":
                case 4:
                    message.imageQuality = 4;
                    break;
                default:
                    if (typeof object.imageQuality === "number" && (object.imageQuality | 0) === object.imageQuality)
                        message.imageQuality = object.imageQuality;
                }
            if (object.lockAfterSessionEnd !== 0 && (typeof object.lockAfterSessionEnd !== "string" || $root.hbb.OptionMessage.BoolOption[object.lockAfterSessionEnd] !== 0))
                switch (object.lockAfterSessionEnd) {
                case "NotSet":
                case 0:
                    message.lockAfterSessionEnd = 0;
                    break;
                case "No":
                case 1:
                    message.lockAfterSessionEnd = 1;
                    break;
                case "Yes":
                case 2:
                    message.lockAfterSessionEnd = 2;
                    break;
                default:
                    if (typeof object.lockAfterSessionEnd === "number" && (object.lockAfterSessionEnd | 0) === object.lockAfterSessionEnd)
                        message.lockAfterSessionEnd = object.lockAfterSessionEnd;
                }
            if (object.showRemoteCursor !== 0 && (typeof object.showRemoteCursor !== "string" || $root.hbb.OptionMessage.BoolOption[object.showRemoteCursor] !== 0))
                switch (object.showRemoteCursor) {
                case "NotSet":
                case 0:
                    message.showRemoteCursor = 0;
                    break;
                case "No":
                case 1:
                    message.showRemoteCursor = 1;
                    break;
                case "Yes":
                case 2:
                    message.showRemoteCursor = 2;
                    break;
                default:
                    if (typeof object.showRemoteCursor === "number" && (object.showRemoteCursor | 0) === object.showRemoteCursor)
                        message.showRemoteCursor = object.showRemoteCursor;
                }
            if (object.privacyMode !== 0 && (typeof object.privacyMode !== "string" || $root.hbb.OptionMessage.BoolOption[object.privacyMode] !== 0))
                switch (object.privacyMode) {
                case "NotSet":
                case 0:
                    message.privacyMode = 0;
                    break;
                case "No":
                case 1:
                    message.privacyMode = 1;
                    break;
                case "Yes":
                case 2:
                    message.privacyMode = 2;
                    break;
                default:
                    if (typeof object.privacyMode === "number" && (object.privacyMode | 0) === object.privacyMode)
                        message.privacyMode = object.privacyMode;
                }
            if (object.blockInput !== 0 && (typeof object.blockInput !== "string" || $root.hbb.OptionMessage.BoolOption[object.blockInput] !== 0))
                switch (object.blockInput) {
                case "NotSet":
                case 0:
                    message.blockInput = 0;
                    break;
                case "No":
                case 1:
                    message.blockInput = 1;
                    break;
                case "Yes":
                case 2:
                    message.blockInput = 2;
                    break;
                default:
                    if (typeof object.blockInput === "number" && (object.blockInput | 0) === object.blockInput)
                        message.blockInput = object.blockInput;
                }
            if (object.customImageQuality != null)
                if ($Number(object.customImageQuality) !== 0)
                    message.customImageQuality = object.customImageQuality | 0;
            if (object.disableAudio !== 0 && (typeof object.disableAudio !== "string" || $root.hbb.OptionMessage.BoolOption[object.disableAudio] !== 0))
                switch (object.disableAudio) {
                case "NotSet":
                case 0:
                    message.disableAudio = 0;
                    break;
                case "No":
                case 1:
                    message.disableAudio = 1;
                    break;
                case "Yes":
                case 2:
                    message.disableAudio = 2;
                    break;
                default:
                    if (typeof object.disableAudio === "number" && (object.disableAudio | 0) === object.disableAudio)
                        message.disableAudio = object.disableAudio;
                }
            if (object.disableClipboard !== 0 && (typeof object.disableClipboard !== "string" || $root.hbb.OptionMessage.BoolOption[object.disableClipboard] !== 0))
                switch (object.disableClipboard) {
                case "NotSet":
                case 0:
                    message.disableClipboard = 0;
                    break;
                case "No":
                case 1:
                    message.disableClipboard = 1;
                    break;
                case "Yes":
                case 2:
                    message.disableClipboard = 2;
                    break;
                default:
                    if (typeof object.disableClipboard === "number" && (object.disableClipboard | 0) === object.disableClipboard)
                        message.disableClipboard = object.disableClipboard;
                }
            if (object.enableFileTransfer !== 0 && (typeof object.enableFileTransfer !== "string" || $root.hbb.OptionMessage.BoolOption[object.enableFileTransfer] !== 0))
                switch (object.enableFileTransfer) {
                case "NotSet":
                case 0:
                    message.enableFileTransfer = 0;
                    break;
                case "No":
                case 1:
                    message.enableFileTransfer = 1;
                    break;
                case "Yes":
                case 2:
                    message.enableFileTransfer = 2;
                    break;
                default:
                    if (typeof object.enableFileTransfer === "number" && (object.enableFileTransfer | 0) === object.enableFileTransfer)
                        message.enableFileTransfer = object.enableFileTransfer;
                }
            if (object.supportedDecoding != null) {
                if (!$util.isObject(object.supportedDecoding))
                    throw $TypeError(".hbb.OptionMessage.supportedDecoding: object expected");
                message.supportedDecoding = $root.hbb.SupportedDecoding.fromObject(object.supportedDecoding, _depth + 1);
            }
            if (object.customFps != null)
                if ($Number(object.customFps) !== 0)
                    message.customFps = object.customFps | 0;
            if (object.disableKeyboard !== 0 && (typeof object.disableKeyboard !== "string" || $root.hbb.OptionMessage.BoolOption[object.disableKeyboard] !== 0))
                switch (object.disableKeyboard) {
                case "NotSet":
                case 0:
                    message.disableKeyboard = 0;
                    break;
                case "No":
                case 1:
                    message.disableKeyboard = 1;
                    break;
                case "Yes":
                case 2:
                    message.disableKeyboard = 2;
                    break;
                default:
                    if (typeof object.disableKeyboard === "number" && (object.disableKeyboard | 0) === object.disableKeyboard)
                        message.disableKeyboard = object.disableKeyboard;
                }
            if (object.followRemoteCursor !== 0 && (typeof object.followRemoteCursor !== "string" || $root.hbb.OptionMessage.BoolOption[object.followRemoteCursor] !== 0))
                switch (object.followRemoteCursor) {
                case "NotSet":
                case 0:
                    message.followRemoteCursor = 0;
                    break;
                case "No":
                case 1:
                    message.followRemoteCursor = 1;
                    break;
                case "Yes":
                case 2:
                    message.followRemoteCursor = 2;
                    break;
                default:
                    if (typeof object.followRemoteCursor === "number" && (object.followRemoteCursor | 0) === object.followRemoteCursor)
                        message.followRemoteCursor = object.followRemoteCursor;
                }
            if (object.followRemoteWindow !== 0 && (typeof object.followRemoteWindow !== "string" || $root.hbb.OptionMessage.BoolOption[object.followRemoteWindow] !== 0))
                switch (object.followRemoteWindow) {
                case "NotSet":
                case 0:
                    message.followRemoteWindow = 0;
                    break;
                case "No":
                case 1:
                    message.followRemoteWindow = 1;
                    break;
                case "Yes":
                case 2:
                    message.followRemoteWindow = 2;
                    break;
                default:
                    if (typeof object.followRemoteWindow === "number" && (object.followRemoteWindow | 0) === object.followRemoteWindow)
                        message.followRemoteWindow = object.followRemoteWindow;
                }
            if (object.disableCamera !== 0 && (typeof object.disableCamera !== "string" || $root.hbb.OptionMessage.BoolOption[object.disableCamera] !== 0))
                switch (object.disableCamera) {
                case "NotSet":
                case 0:
                    message.disableCamera = 0;
                    break;
                case "No":
                case 1:
                    message.disableCamera = 1;
                    break;
                case "Yes":
                case 2:
                    message.disableCamera = 2;
                    break;
                default:
                    if (typeof object.disableCamera === "number" && (object.disableCamera | 0) === object.disableCamera)
                        message.disableCamera = object.disableCamera;
                }
            if (object.terminalPersistent !== 0 && (typeof object.terminalPersistent !== "string" || $root.hbb.OptionMessage.BoolOption[object.terminalPersistent] !== 0))
                switch (object.terminalPersistent) {
                case "NotSet":
                case 0:
                    message.terminalPersistent = 0;
                    break;
                case "No":
                case 1:
                    message.terminalPersistent = 1;
                    break;
                case "Yes":
                case 2:
                    message.terminalPersistent = 2;
                    break;
                default:
                    if (typeof object.terminalPersistent === "number" && (object.terminalPersistent | 0) === object.terminalPersistent)
                        message.terminalPersistent = object.terminalPersistent;
                }
            if (object.showMyCursor !== 0 && (typeof object.showMyCursor !== "string" || $root.hbb.OptionMessage.BoolOption[object.showMyCursor] !== 0))
                switch (object.showMyCursor) {
                case "NotSet":
                case 0:
                    message.showMyCursor = 0;
                    break;
                case "No":
                case 1:
                    message.showMyCursor = 1;
                    break;
                case "Yes":
                case 2:
                    message.showMyCursor = 2;
                    break;
                default:
                    if (typeof object.showMyCursor === "number" && (object.showMyCursor | 0) === object.showMyCursor)
                        message.showMyCursor = object.showMyCursor;
                }
            return message;
        };

        OptionMessage.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.imageQuality = options.enums === $String ? "NotSet" : 0;
                object.lockAfterSessionEnd = options.enums === $String ? "NotSet" : 0;
                object.showRemoteCursor = options.enums === $String ? "NotSet" : 0;
                object.privacyMode = options.enums === $String ? "NotSet" : 0;
                object.blockInput = options.enums === $String ? "NotSet" : 0;
                object.customImageQuality = 0;
                object.disableAudio = options.enums === $String ? "NotSet" : 0;
                object.disableClipboard = options.enums === $String ? "NotSet" : 0;
                object.enableFileTransfer = options.enums === $String ? "NotSet" : 0;
                object.supportedDecoding = null;
                object.customFps = 0;
                object.disableKeyboard = options.enums === $String ? "NotSet" : 0;
                object.followRemoteCursor = options.enums === $String ? "NotSet" : 0;
                object.followRemoteWindow = options.enums === $String ? "NotSet" : 0;
                object.disableCamera = options.enums === $String ? "NotSet" : 0;
                object.terminalPersistent = options.enums === $String ? "NotSet" : 0;
                object.showMyCursor = options.enums === $String ? "NotSet" : 0;
            }
            if (message.imageQuality != null && $Object.hasOwnProperty.call(message, "imageQuality"))
                object.imageQuality = options.enums === $String ? $root.hbb.ImageQuality[message.imageQuality] === $undefined ? message.imageQuality : $root.hbb.ImageQuality[message.imageQuality] : message.imageQuality;
            if (message.lockAfterSessionEnd != null && $Object.hasOwnProperty.call(message, "lockAfterSessionEnd"))
                object.lockAfterSessionEnd = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.lockAfterSessionEnd] === $undefined ? message.lockAfterSessionEnd : $root.hbb.OptionMessage.BoolOption[message.lockAfterSessionEnd] : message.lockAfterSessionEnd;
            if (message.showRemoteCursor != null && $Object.hasOwnProperty.call(message, "showRemoteCursor"))
                object.showRemoteCursor = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.showRemoteCursor] === $undefined ? message.showRemoteCursor : $root.hbb.OptionMessage.BoolOption[message.showRemoteCursor] : message.showRemoteCursor;
            if (message.privacyMode != null && $Object.hasOwnProperty.call(message, "privacyMode"))
                object.privacyMode = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.privacyMode] === $undefined ? message.privacyMode : $root.hbb.OptionMessage.BoolOption[message.privacyMode] : message.privacyMode;
            if (message.blockInput != null && $Object.hasOwnProperty.call(message, "blockInput"))
                object.blockInput = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.blockInput] === $undefined ? message.blockInput : $root.hbb.OptionMessage.BoolOption[message.blockInput] : message.blockInput;
            if (message.customImageQuality != null && $Object.hasOwnProperty.call(message, "customImageQuality"))
                object.customImageQuality = message.customImageQuality;
            if (message.disableAudio != null && $Object.hasOwnProperty.call(message, "disableAudio"))
                object.disableAudio = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.disableAudio] === $undefined ? message.disableAudio : $root.hbb.OptionMessage.BoolOption[message.disableAudio] : message.disableAudio;
            if (message.disableClipboard != null && $Object.hasOwnProperty.call(message, "disableClipboard"))
                object.disableClipboard = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.disableClipboard] === $undefined ? message.disableClipboard : $root.hbb.OptionMessage.BoolOption[message.disableClipboard] : message.disableClipboard;
            if (message.enableFileTransfer != null && $Object.hasOwnProperty.call(message, "enableFileTransfer"))
                object.enableFileTransfer = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.enableFileTransfer] === $undefined ? message.enableFileTransfer : $root.hbb.OptionMessage.BoolOption[message.enableFileTransfer] : message.enableFileTransfer;
            if (message.supportedDecoding != null && $Object.hasOwnProperty.call(message, "supportedDecoding"))
                object.supportedDecoding = $root.hbb.SupportedDecoding.toObject(message.supportedDecoding, options, _depth + 1);
            if (message.customFps != null && $Object.hasOwnProperty.call(message, "customFps"))
                object.customFps = message.customFps;
            if (message.disableKeyboard != null && $Object.hasOwnProperty.call(message, "disableKeyboard"))
                object.disableKeyboard = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.disableKeyboard] === $undefined ? message.disableKeyboard : $root.hbb.OptionMessage.BoolOption[message.disableKeyboard] : message.disableKeyboard;
            if (message.followRemoteCursor != null && $Object.hasOwnProperty.call(message, "followRemoteCursor"))
                object.followRemoteCursor = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.followRemoteCursor] === $undefined ? message.followRemoteCursor : $root.hbb.OptionMessage.BoolOption[message.followRemoteCursor] : message.followRemoteCursor;
            if (message.followRemoteWindow != null && $Object.hasOwnProperty.call(message, "followRemoteWindow"))
                object.followRemoteWindow = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.followRemoteWindow] === $undefined ? message.followRemoteWindow : $root.hbb.OptionMessage.BoolOption[message.followRemoteWindow] : message.followRemoteWindow;
            if (message.disableCamera != null && $Object.hasOwnProperty.call(message, "disableCamera"))
                object.disableCamera = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.disableCamera] === $undefined ? message.disableCamera : $root.hbb.OptionMessage.BoolOption[message.disableCamera] : message.disableCamera;
            if (message.terminalPersistent != null && $Object.hasOwnProperty.call(message, "terminalPersistent"))
                object.terminalPersistent = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.terminalPersistent] === $undefined ? message.terminalPersistent : $root.hbb.OptionMessage.BoolOption[message.terminalPersistent] : message.terminalPersistent;
            if (message.showMyCursor != null && $Object.hasOwnProperty.call(message, "showMyCursor"))
                object.showMyCursor = options.enums === $String ? $root.hbb.OptionMessage.BoolOption[message.showMyCursor] === $undefined ? message.showMyCursor : $root.hbb.OptionMessage.BoolOption[message.showMyCursor] : message.showMyCursor;
            return object;
        };

        OptionMessage.prototype.toJSON = function() {
            return OptionMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        OptionMessage.BoolOption = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NotSet"] = 0;
            values[valuesById[1] = "No"] = 1;
            values[valuesById[2] = "Yes"] = 2;
            return values;
        })();

        return OptionMessage;
    })();

    hbb.TestDelay = (function() {

        const TestDelay = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TestDelay.prototype.time = $util.Long ? $util.Long.fromBits(0,0,false) : 0;
        TestDelay.prototype.fromClient = false;
        TestDelay.prototype.lastDelay = 0;
        TestDelay.prototype.targetBitrate = 0;

        TestDelay.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.time != null && $Object.hasOwnProperty.call(message, "time") && (typeof message.time === "object" ? message.time.low || message.time.high : message.time !== 0))
                writer.uint32(8).int64(message.time);
            if (message.fromClient != null && $Object.hasOwnProperty.call(message, "fromClient") && message.fromClient !== false)
                writer.uint32(16).bool(message.fromClient);
            if (message.lastDelay != null && $Object.hasOwnProperty.call(message, "lastDelay") && message.lastDelay !== 0)
                writer.uint32(24).uint32(message.lastDelay);
            if (message.targetBitrate != null && $Object.hasOwnProperty.call(message, "targetBitrate") && message.targetBitrate !== 0)
                writer.uint32(32).uint32(message.targetBitrate);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TestDelay.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TestDelay(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.time = value;
                        else
                            delete message.time;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.fromClient = value;
                        else
                            delete message.fromClient;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.lastDelay = value;
                        else
                            delete message.lastDelay;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.targetBitrate = value;
                        else
                            delete message.targetBitrate;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TestDelay.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TestDelay)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TestDelay: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TestDelay();
            if (object.time != null)
                if (typeof object.time === "object" ? object.time.low || object.time.high : $Number(object.time) !== 0)
                    if ($util.Long)
                        message.time = $util.Long.fromValue(object.time, false);
                    else if (typeof object.time === "string")
                        message.time = $parseInt(object.time, 10);
                    else if (typeof object.time === "number")
                        message.time = object.time;
                    else if (typeof object.time === "object")
                        message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
            if (object.fromClient != null)
                if (object.fromClient)
                    message.fromClient = $Boolean(object.fromClient);
            if (object.lastDelay != null)
                if ($Number(object.lastDelay) !== 0)
                    message.lastDelay = object.lastDelay >>> 0;
            if (object.targetBitrate != null)
                if ($Number(object.targetBitrate) !== 0)
                    message.targetBitrate = object.targetBitrate >>> 0;
            return message;
        };

        TestDelay.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.time = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.time = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.fromClient = false;
                object.lastDelay = 0;
                object.targetBitrate = 0;
            }
            if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.time = typeof message.time === "number" ? $BigInt(message.time) : $util.Long.fromBits(message.time.low >>> 0, message.time.high >>> 0, false).toBigInt();
                else if (typeof message.time === "number")
                    object.time = options.longs === $String ? $String(message.time) : message.time;
                else
                    object.time = options.longs === $String ? $util.Long.prototype.toString.call(message.time) : options.longs === $Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
            if (message.fromClient != null && $Object.hasOwnProperty.call(message, "fromClient"))
                object.fromClient = message.fromClient;
            if (message.lastDelay != null && $Object.hasOwnProperty.call(message, "lastDelay"))
                object.lastDelay = message.lastDelay;
            if (message.targetBitrate != null && $Object.hasOwnProperty.call(message, "targetBitrate"))
                object.targetBitrate = message.targetBitrate;
            return object;
        };

        TestDelay.prototype.toJSON = function() {
            return TestDelay.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TestDelay;
    })();

    hbb.PublicKey = (function() {

        const PublicKey = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PublicKey.prototype.asymmetricValue = $util.newBuffer([]);
        PublicKey.prototype.symmetricValue = $util.newBuffer([]);

        PublicKey.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.asymmetricValue != null && $Object.hasOwnProperty.call(message, "asymmetricValue") && message.asymmetricValue.length)
                writer.uint32(10).bytes(message.asymmetricValue);
            if (message.symmetricValue != null && $Object.hasOwnProperty.call(message, "symmetricValue") && message.symmetricValue.length)
                writer.uint32(18).bytes(message.symmetricValue);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PublicKey.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PublicKey(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.asymmetricValue = value;
                        else
                            delete message.asymmetricValue;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.symmetricValue = value;
                        else
                            delete message.symmetricValue;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PublicKey.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PublicKey)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PublicKey: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PublicKey();
            if (object.asymmetricValue != null)
                if (object.asymmetricValue.length)
                    if (typeof object.asymmetricValue === "string")
                        $util.base64.decode(object.asymmetricValue, message.asymmetricValue = $util.newBuffer($util.base64.length(object.asymmetricValue)), 0);
                    else if (object.asymmetricValue.length >= 0)
                        message.asymmetricValue = object.asymmetricValue;
            if (object.symmetricValue != null)
                if (object.symmetricValue.length)
                    if (typeof object.symmetricValue === "string")
                        $util.base64.decode(object.symmetricValue, message.symmetricValue = $util.newBuffer($util.base64.length(object.symmetricValue)), 0);
                    else if (object.symmetricValue.length >= 0)
                        message.symmetricValue = object.symmetricValue;
            return message;
        };

        PublicKey.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.asymmetricValue = "";
                else {
                    object.asymmetricValue = [];
                    if (options.bytes !== $Array)
                        object.asymmetricValue = $util.newBuffer(object.asymmetricValue);
                }
                if (options.bytes === $String)
                    object.symmetricValue = "";
                else {
                    object.symmetricValue = [];
                    if (options.bytes !== $Array)
                        object.symmetricValue = $util.newBuffer(object.symmetricValue);
                }
            }
            if (message.asymmetricValue != null && $Object.hasOwnProperty.call(message, "asymmetricValue"))
                object.asymmetricValue = options.bytes === $String ? $util.base64.encode(message.asymmetricValue, 0, message.asymmetricValue.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.asymmetricValue) : message.asymmetricValue;
            if (message.symmetricValue != null && $Object.hasOwnProperty.call(message, "symmetricValue"))
                object.symmetricValue = options.bytes === $String ? $util.base64.encode(message.symmetricValue, 0, message.symmetricValue.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.symmetricValue) : message.symmetricValue;
            return object;
        };

        PublicKey.prototype.toJSON = function() {
            return PublicKey.toObject(this, $protobuf.util.toJSONOptions);
        };

        return PublicKey;
    })();

    hbb.SignedId = (function() {

        const SignedId = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        SignedId.prototype.id = $util.newBuffer([]);

        SignedId.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id.length)
                writer.uint32(10).bytes(message.id);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SignedId.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.SignedId(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        SignedId.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.SignedId)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.SignedId: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.SignedId();
            if (object.id != null)
                if (object.id.length)
                    if (typeof object.id === "string")
                        $util.base64.decode(object.id, message.id = $util.newBuffer($util.base64.length(object.id)), 0);
                    else if (object.id.length >= 0)
                        message.id = object.id;
            return message;
        };

        SignedId.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.id = "";
                else {
                    object.id = [];
                    if (options.bytes !== $Array)
                        object.id = $util.newBuffer(object.id);
                }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = options.bytes === $String ? $util.base64.encode(message.id, 0, message.id.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.id) : message.id;
            return object;
        };

        SignedId.prototype.toJSON = function() {
            return SignedId.toObject(this, $protobuf.util.toJSONOptions);
        };

        return SignedId;
    })();

    hbb.AudioFormat = (function() {

        const AudioFormat = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        AudioFormat.prototype.sampleRate = 0;
        AudioFormat.prototype.channels = 0;

        AudioFormat.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sampleRate != null && $Object.hasOwnProperty.call(message, "sampleRate") && message.sampleRate !== 0)
                writer.uint32(8).uint32(message.sampleRate);
            if (message.channels != null && $Object.hasOwnProperty.call(message, "channels") && message.channels !== 0)
                writer.uint32(16).uint32(message.channels);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AudioFormat.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.AudioFormat(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.sampleRate = value;
                        else
                            delete message.sampleRate;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.channels = value;
                        else
                            delete message.channels;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        AudioFormat.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.AudioFormat)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.AudioFormat: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.AudioFormat();
            if (object.sampleRate != null)
                if ($Number(object.sampleRate) !== 0)
                    message.sampleRate = object.sampleRate >>> 0;
            if (object.channels != null)
                if ($Number(object.channels) !== 0)
                    message.channels = object.channels >>> 0;
            return message;
        };

        AudioFormat.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.sampleRate = 0;
                object.channels = 0;
            }
            if (message.sampleRate != null && $Object.hasOwnProperty.call(message, "sampleRate"))
                object.sampleRate = message.sampleRate;
            if (message.channels != null && $Object.hasOwnProperty.call(message, "channels"))
                object.channels = message.channels;
            return object;
        };

        AudioFormat.prototype.toJSON = function() {
            return AudioFormat.toObject(this, $protobuf.util.toJSONOptions);
        };

        return AudioFormat;
    })();

    hbb.AudioFrame = (function() {

        const AudioFrame = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        AudioFrame.prototype.data = $util.newBuffer([]);

        AudioFrame.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                writer.uint32(10).bytes(message.data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        AudioFrame.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.AudioFrame(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        AudioFrame.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.AudioFrame)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.AudioFrame: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.AudioFrame();
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            return message;
        };

        AudioFrame.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== $Array)
                        object.data = $util.newBuffer(object.data);
                }
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        AudioFrame.prototype.toJSON = function() {
            return AudioFrame.toObject(this, $protobuf.util.toJSONOptions);
        };

        return AudioFrame;
    })();

    hbb.MessageBox = (function() {

        const MessageBox = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        MessageBox.prototype.msgtype = "";
        MessageBox.prototype.title = "";
        MessageBox.prototype.text = "";
        MessageBox.prototype.link = "";

        MessageBox.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.msgtype != null && $Object.hasOwnProperty.call(message, "msgtype") && message.msgtype !== "")
                writer.uint32(10).string(message.msgtype);
            if (message.title != null && $Object.hasOwnProperty.call(message, "title") && message.title !== "")
                writer.uint32(18).string(message.title);
            if (message.text != null && $Object.hasOwnProperty.call(message, "text") && message.text !== "")
                writer.uint32(26).string(message.text);
            if (message.link != null && $Object.hasOwnProperty.call(message, "link") && message.link !== "")
                writer.uint32(34).string(message.link);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MessageBox.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.MessageBox(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.msgtype = value;
                        else
                            delete message.msgtype;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.title = value;
                        else
                            delete message.title;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.text = value;
                        else
                            delete message.text;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.link = value;
                        else
                            delete message.link;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        MessageBox.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.MessageBox)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.MessageBox: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.MessageBox();
            if (object.msgtype != null)
                if (typeof object.msgtype !== "string" || object.msgtype.length)
                    message.msgtype = $String(object.msgtype);
            if (object.title != null)
                if (typeof object.title !== "string" || object.title.length)
                    message.title = $String(object.title);
            if (object.text != null)
                if (typeof object.text !== "string" || object.text.length)
                    message.text = $String(object.text);
            if (object.link != null)
                if (typeof object.link !== "string" || object.link.length)
                    message.link = $String(object.link);
            return message;
        };

        MessageBox.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.msgtype = "";
                object.title = "";
                object.text = "";
                object.link = "";
            }
            if (message.msgtype != null && $Object.hasOwnProperty.call(message, "msgtype"))
                object.msgtype = message.msgtype;
            if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
                object.title = message.title;
            if (message.text != null && $Object.hasOwnProperty.call(message, "text"))
                object.text = message.text;
            if (message.link != null && $Object.hasOwnProperty.call(message, "link"))
                object.link = message.link;
            return object;
        };

        MessageBox.prototype.toJSON = function() {
            return MessageBox.toObject(this, $protobuf.util.toJSONOptions);
        };

        return MessageBox;
    })();

    hbb.BackNotification = (function() {

        const BackNotification = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        BackNotification.prototype.privacyModeState = null;
        BackNotification.prototype.blockInputState = null;
        BackNotification.prototype.details = "";
        BackNotification.prototype.implKey = "";

        let $oneOfFields;

        $Object.defineProperty(BackNotification.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["privacyModeState", "blockInputState"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BackNotification.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.privacyModeState != null && $Object.hasOwnProperty.call(message, "privacyModeState"))
                writer.uint32(8).int32(message.privacyModeState);
            if (message.blockInputState != null && $Object.hasOwnProperty.call(message, "blockInputState"))
                writer.uint32(16).int32(message.blockInputState);
            if (message.details != null && $Object.hasOwnProperty.call(message, "details") && message.details !== "")
                writer.uint32(26).string(message.details);
            if (message.implKey != null && $Object.hasOwnProperty.call(message, "implKey") && message.implKey !== "")
                writer.uint32(34).string(message.implKey);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        BackNotification.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.BackNotification(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        message.privacyModeState = reader.int32();
                        message.union = "privacyModeState";
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        message.blockInputState = reader.int32();
                        message.union = "blockInputState";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.details = value;
                        else
                            delete message.details;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.implKey = value;
                        else
                            delete message.implKey;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        BackNotification.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.BackNotification)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.BackNotification: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.BackNotification();
            switch (object.privacyModeState) {
            case "PrvStateUnknown":
            case 0:
                message.privacyModeState = 0;
                break;
            case "PrvOnByOther":
            case 2:
                message.privacyModeState = 2;
                break;
            case "PrvNotSupported":
            case 3:
                message.privacyModeState = 3;
                break;
            case "PrvOnSucceeded":
            case 4:
                message.privacyModeState = 4;
                break;
            case "PrvOnFailedDenied":
            case 5:
                message.privacyModeState = 5;
                break;
            case "PrvOnFailedPlugin":
            case 6:
                message.privacyModeState = 6;
                break;
            case "PrvOnFailed":
            case 7:
                message.privacyModeState = 7;
                break;
            case "PrvOffSucceeded":
            case 8:
                message.privacyModeState = 8;
                break;
            case "PrvOffByPeer":
            case 9:
                message.privacyModeState = 9;
                break;
            case "PrvOffFailed":
            case 10:
                message.privacyModeState = 10;
                break;
            case "PrvOffUnknown":
            case 11:
                message.privacyModeState = 11;
                break;
            default:
                if (typeof object.privacyModeState === "number" && (object.privacyModeState | 0) === object.privacyModeState)
                    message.privacyModeState = object.privacyModeState;
            }
            switch (object.blockInputState) {
            case "BlkStateUnknown":
            case 0:
                message.blockInputState = 0;
                break;
            case "BlkOnSucceeded":
            case 2:
                message.blockInputState = 2;
                break;
            case "BlkOnFailed":
            case 3:
                message.blockInputState = 3;
                break;
            case "BlkOffSucceeded":
            case 4:
                message.blockInputState = 4;
                break;
            case "BlkOffFailed":
            case 5:
                message.blockInputState = 5;
                break;
            default:
                if (typeof object.blockInputState === "number" && (object.blockInputState | 0) === object.blockInputState)
                    message.blockInputState = object.blockInputState;
            }
            if (object.details != null)
                if (typeof object.details !== "string" || object.details.length)
                    message.details = $String(object.details);
            if (object.implKey != null)
                if (typeof object.implKey !== "string" || object.implKey.length)
                    message.implKey = $String(object.implKey);
            return message;
        };

        BackNotification.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.details = "";
                object.implKey = "";
            }
            if (message.privacyModeState != null && $Object.hasOwnProperty.call(message, "privacyModeState")) {
                object.privacyModeState = options.enums === $String ? $root.hbb.BackNotification.PrivacyModeState[message.privacyModeState] === $undefined ? message.privacyModeState : $root.hbb.BackNotification.PrivacyModeState[message.privacyModeState] : message.privacyModeState;
                if (options.oneofs)
                    object.union = "privacyModeState";
            }
            if (message.blockInputState != null && $Object.hasOwnProperty.call(message, "blockInputState")) {
                object.blockInputState = options.enums === $String ? $root.hbb.BackNotification.BlockInputState[message.blockInputState] === $undefined ? message.blockInputState : $root.hbb.BackNotification.BlockInputState[message.blockInputState] : message.blockInputState;
                if (options.oneofs)
                    object.union = "blockInputState";
            }
            if (message.details != null && $Object.hasOwnProperty.call(message, "details"))
                object.details = message.details;
            if (message.implKey != null && $Object.hasOwnProperty.call(message, "implKey"))
                object.implKey = message.implKey;
            return object;
        };

        BackNotification.prototype.toJSON = function() {
            return BackNotification.toObject(this, $protobuf.util.toJSONOptions);
        };

        BackNotification.BlockInputState = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "BlkStateUnknown"] = 0;
            values[valuesById[2] = "BlkOnSucceeded"] = 2;
            values[valuesById[3] = "BlkOnFailed"] = 3;
            values[valuesById[4] = "BlkOffSucceeded"] = 4;
            values[valuesById[5] = "BlkOffFailed"] = 5;
            return values;
        })();

        BackNotification.PrivacyModeState = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "PrvStateUnknown"] = 0;
            values[valuesById[2] = "PrvOnByOther"] = 2;
            values[valuesById[3] = "PrvNotSupported"] = 3;
            values[valuesById[4] = "PrvOnSucceeded"] = 4;
            values[valuesById[5] = "PrvOnFailedDenied"] = 5;
            values[valuesById[6] = "PrvOnFailedPlugin"] = 6;
            values[valuesById[7] = "PrvOnFailed"] = 7;
            values[valuesById[8] = "PrvOffSucceeded"] = 8;
            values[valuesById[9] = "PrvOffByPeer"] = 9;
            values[valuesById[10] = "PrvOffFailed"] = 10;
            values[valuesById[11] = "PrvOffUnknown"] = 11;
            return values;
        })();

        return BackNotification;
    })();

    hbb.ElevationRequestWithLogon = (function() {

        const ElevationRequestWithLogon = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ElevationRequestWithLogon.prototype.username = "";
        ElevationRequestWithLogon.prototype.password = "";

        ElevationRequestWithLogon.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.username != null && $Object.hasOwnProperty.call(message, "username") && message.username !== "")
                writer.uint32(10).string(message.username);
            if (message.password != null && $Object.hasOwnProperty.call(message, "password") && message.password !== "")
                writer.uint32(18).string(message.password);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ElevationRequestWithLogon.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ElevationRequestWithLogon(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.username = value;
                        else
                            delete message.username;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.password = value;
                        else
                            delete message.password;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ElevationRequestWithLogon.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ElevationRequestWithLogon)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ElevationRequestWithLogon: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ElevationRequestWithLogon();
            if (object.username != null)
                if (typeof object.username !== "string" || object.username.length)
                    message.username = $String(object.username);
            if (object.password != null)
                if (typeof object.password !== "string" || object.password.length)
                    message.password = $String(object.password);
            return message;
        };

        ElevationRequestWithLogon.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                object.password = "";
            }
            if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
                object.password = message.password;
            return object;
        };

        ElevationRequestWithLogon.prototype.toJSON = function() {
            return ElevationRequestWithLogon.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ElevationRequestWithLogon;
    })();

    hbb.ElevationRequest = (function() {

        const ElevationRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ElevationRequest.prototype.direct = null;
        ElevationRequest.prototype.logon = null;

        let $oneOfFields;

        $Object.defineProperty(ElevationRequest.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["direct", "logon"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ElevationRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.direct != null && $Object.hasOwnProperty.call(message, "direct"))
                writer.uint32(8).bool(message.direct);
            if (message.logon != null && $Object.hasOwnProperty.call(message, "logon"))
                $root.hbb.ElevationRequestWithLogon.encode(message.logon, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ElevationRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ElevationRequest();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        message.direct = reader.bool();
                        message.union = "direct";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.logon = $root.hbb.ElevationRequestWithLogon.decode(reader, reader.uint32(), $undefined, _depth + 1, message.logon);
                        message.union = "logon";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ElevationRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ElevationRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ElevationRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ElevationRequest();
            if (object.direct != null)
                message.direct = $Boolean(object.direct);
            if (object.logon != null) {
                if (!$util.isObject(object.logon))
                    throw $TypeError(".hbb.ElevationRequest.logon: object expected");
                message.logon = $root.hbb.ElevationRequestWithLogon.fromObject(object.logon, _depth + 1);
            }
            return message;
        };

        ElevationRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.direct != null && $Object.hasOwnProperty.call(message, "direct")) {
                object.direct = message.direct;
                if (options.oneofs)
                    object.union = "direct";
            }
            if (message.logon != null && $Object.hasOwnProperty.call(message, "logon")) {
                object.logon = $root.hbb.ElevationRequestWithLogon.toObject(message.logon, options, _depth + 1);
                if (options.oneofs)
                    object.union = "logon";
            }
            return object;
        };

        ElevationRequest.prototype.toJSON = function() {
            return ElevationRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ElevationRequest;
    })();

    hbb.SwitchSidesRequest = (function() {

        const SwitchSidesRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        SwitchSidesRequest.prototype.uuid = $util.newBuffer([]);

        SwitchSidesRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.uuid != null && $Object.hasOwnProperty.call(message, "uuid") && message.uuid.length)
                writer.uint32(10).bytes(message.uuid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SwitchSidesRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.SwitchSidesRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        SwitchSidesRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.SwitchSidesRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.SwitchSidesRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.SwitchSidesRequest();
            if (object.uuid != null)
                if (object.uuid.length)
                    if (typeof object.uuid === "string")
                        $util.base64.decode(object.uuid, message.uuid = $util.newBuffer($util.base64.length(object.uuid)), 0);
                    else if (object.uuid.length >= 0)
                        message.uuid = object.uuid;
            return message;
        };

        SwitchSidesRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.uuid = "";
                else {
                    object.uuid = [];
                    if (options.bytes !== $Array)
                        object.uuid = $util.newBuffer(object.uuid);
                }
            if (message.uuid != null && $Object.hasOwnProperty.call(message, "uuid"))
                object.uuid = options.bytes === $String ? $util.base64.encode(message.uuid, 0, message.uuid.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.uuid) : message.uuid;
            return object;
        };

        SwitchSidesRequest.prototype.toJSON = function() {
            return SwitchSidesRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return SwitchSidesRequest;
    })();

    hbb.SwitchSidesResponse = (function() {

        const SwitchSidesResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        SwitchSidesResponse.prototype.uuid = $util.newBuffer([]);
        SwitchSidesResponse.prototype.lr = null;

        SwitchSidesResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.uuid != null && $Object.hasOwnProperty.call(message, "uuid") && message.uuid.length)
                writer.uint32(10).bytes(message.uuid);
            if (message.lr != null && $Object.hasOwnProperty.call(message, "lr"))
                $root.hbb.LoginRequest.encode(message.lr, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SwitchSidesResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.SwitchSidesResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.lr = $root.hbb.LoginRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lr);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        SwitchSidesResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.SwitchSidesResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.SwitchSidesResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.SwitchSidesResponse();
            if (object.uuid != null)
                if (object.uuid.length)
                    if (typeof object.uuid === "string")
                        $util.base64.decode(object.uuid, message.uuid = $util.newBuffer($util.base64.length(object.uuid)), 0);
                    else if (object.uuid.length >= 0)
                        message.uuid = object.uuid;
            if (object.lr != null) {
                if (!$util.isObject(object.lr))
                    throw $TypeError(".hbb.SwitchSidesResponse.lr: object expected");
                message.lr = $root.hbb.LoginRequest.fromObject(object.lr, _depth + 1);
            }
            return message;
        };

        SwitchSidesResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.uuid = "";
                else {
                    object.uuid = [];
                    if (options.bytes !== $Array)
                        object.uuid = $util.newBuffer(object.uuid);
                }
                object.lr = null;
            }
            if (message.uuid != null && $Object.hasOwnProperty.call(message, "uuid"))
                object.uuid = options.bytes === $String ? $util.base64.encode(message.uuid, 0, message.uuid.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.uuid) : message.uuid;
            if (message.lr != null && $Object.hasOwnProperty.call(message, "lr"))
                object.lr = $root.hbb.LoginRequest.toObject(message.lr, options, _depth + 1);
            return object;
        };

        SwitchSidesResponse.prototype.toJSON = function() {
            return SwitchSidesResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return SwitchSidesResponse;
    })();

    hbb.SwitchBack = (function() {

        const SwitchBack = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        SwitchBack.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SwitchBack.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.SwitchBack();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                reader.skipType(tag & 7, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        SwitchBack.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.SwitchBack)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.SwitchBack: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.hbb.SwitchBack();
        };

        SwitchBack.toObject = function () {
            return {};
        };

        SwitchBack.prototype.toJSON = function() {
            return SwitchBack.toObject(this, $protobuf.util.toJSONOptions);
        };

        return SwitchBack;
    })();

    hbb.PluginRequest = (function() {

        const PluginRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PluginRequest.prototype.id = "";
        PluginRequest.prototype.content = $util.newBuffer([]);

        PluginRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(10).string(message.id);
            if (message.content != null && $Object.hasOwnProperty.call(message, "content") && message.content.length)
                writer.uint32(18).bytes(message.content);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PluginRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PluginRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.content = value;
                        else
                            delete message.content;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PluginRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PluginRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PluginRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PluginRequest();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.content != null)
                if (object.content.length)
                    if (typeof object.content === "string")
                        $util.base64.decode(object.content, message.content = $util.newBuffer($util.base64.length(object.content)), 0);
                    else if (object.content.length >= 0)
                        message.content = object.content;
            return message;
        };

        PluginRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                if (options.bytes === $String)
                    object.content = "";
                else {
                    object.content = [];
                    if (options.bytes !== $Array)
                        object.content = $util.newBuffer(object.content);
                }
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
                object.content = options.bytes === $String ? $util.base64.encode(message.content, 0, message.content.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.content) : message.content;
            return object;
        };

        PluginRequest.prototype.toJSON = function() {
            return PluginRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return PluginRequest;
    })();

    hbb.PluginFailure = (function() {

        const PluginFailure = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PluginFailure.prototype.id = "";
        PluginFailure.prototype.name = "";
        PluginFailure.prototype.msg = "";

        PluginFailure.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(10).string(message.id);
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(18).string(message.name);
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg") && message.msg !== "")
                writer.uint32(26).string(message.msg);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PluginFailure.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PluginFailure(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.msg = value;
                        else
                            delete message.msg;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PluginFailure.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PluginFailure)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PluginFailure: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PluginFailure();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.msg != null)
                if (typeof object.msg !== "string" || object.msg.length)
                    message.msg = $String(object.msg);
            return message;
        };

        PluginFailure.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.name = "";
                object.msg = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                object.msg = message.msg;
            return object;
        };

        PluginFailure.prototype.toJSON = function() {
            return PluginFailure.toObject(this, $protobuf.util.toJSONOptions);
        };

        return PluginFailure;
    })();

    hbb.WindowsSessions = (function() {

        const WindowsSessions = function (properties) {
            this.sessions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        WindowsSessions.prototype.sessions = $util.emptyArray;
        WindowsSessions.prototype.currentSid = 0;

        WindowsSessions.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sessions != null && message.sessions.length)
                for (let i = 0; i < message.sessions.length; ++i)
                    $root.hbb.WindowsSession.encode(message.sessions[i], writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.currentSid != null && $Object.hasOwnProperty.call(message, "currentSid") && message.currentSid !== 0)
                writer.uint32(16).uint32(message.currentSid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        WindowsSessions.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.WindowsSessions(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.sessions && message.sessions.length))
                            message.sessions = [];
                        message.sessions.push($root.hbb.WindowsSession.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.currentSid = value;
                        else
                            delete message.currentSid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        WindowsSessions.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.WindowsSessions)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.WindowsSessions: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.WindowsSessions();
            if (object.sessions) {
                if (!$Array.isArray(object.sessions))
                    throw $TypeError(".hbb.WindowsSessions.sessions: array expected");
                message.sessions = $Array(object.sessions.length);
                for (let i = 0; i < object.sessions.length; ++i) {
                    if (!$util.isObject(object.sessions[i]))
                        throw $TypeError(".hbb.WindowsSessions.sessions: object expected");
                    message.sessions[i] = $root.hbb.WindowsSession.fromObject(object.sessions[i], _depth + 1);
                }
            }
            if (object.currentSid != null)
                if ($Number(object.currentSid) !== 0)
                    message.currentSid = object.currentSid >>> 0;
            return message;
        };

        WindowsSessions.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.sessions = [];
            if (options.defaults)
                object.currentSid = 0;
            if (message.sessions && message.sessions.length) {
                object.sessions = $Array(message.sessions.length);
                for (let j = 0; j < message.sessions.length; ++j)
                    object.sessions[j] = $root.hbb.WindowsSession.toObject(message.sessions[j], options, _depth + 1);
            }
            if (message.currentSid != null && $Object.hasOwnProperty.call(message, "currentSid"))
                object.currentSid = message.currentSid;
            return object;
        };

        WindowsSessions.prototype.toJSON = function() {
            return WindowsSessions.toObject(this, $protobuf.util.toJSONOptions);
        };

        return WindowsSessions;
    })();

    hbb.MessageQuery = (function() {

        const MessageQuery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        MessageQuery.prototype.switchDisplay = 0;

        MessageQuery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.switchDisplay != null && $Object.hasOwnProperty.call(message, "switchDisplay") && message.switchDisplay !== 0)
                writer.uint32(8).int32(message.switchDisplay);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        MessageQuery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.MessageQuery(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.switchDisplay = value;
                        else
                            delete message.switchDisplay;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        MessageQuery.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.MessageQuery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.MessageQuery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.MessageQuery();
            if (object.switchDisplay != null)
                if ($Number(object.switchDisplay) !== 0)
                    message.switchDisplay = object.switchDisplay | 0;
            return message;
        };

        MessageQuery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.switchDisplay = 0;
            if (message.switchDisplay != null && $Object.hasOwnProperty.call(message, "switchDisplay"))
                object.switchDisplay = message.switchDisplay;
            return object;
        };

        MessageQuery.prototype.toJSON = function() {
            return MessageQuery.toObject(this, $protobuf.util.toJSONOptions);
        };

        return MessageQuery;
    })();

    hbb.Misc = (function() {

        const Misc = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        Misc.prototype.chatMessage = null;
        Misc.prototype.switchDisplay = null;
        Misc.prototype.permissionInfo = null;
        Misc.prototype.option = null;
        Misc.prototype.audioFormat = null;
        Misc.prototype.closeReason = null;
        Misc.prototype.refreshVideo = null;
        Misc.prototype.videoReceived = null;
        Misc.prototype.backNotification = null;
        Misc.prototype.restartRemoteDevice = null;
        Misc.prototype.uac = null;
        Misc.prototype.foregroundWindowElevated = null;
        Misc.prototype.stopService = null;
        Misc.prototype.elevationRequest = null;
        Misc.prototype.elevationResponse = null;
        Misc.prototype.portableServiceRunning = null;
        Misc.prototype.switchSidesRequest = null;
        Misc.prototype.switchBack = null;
        Misc.prototype.changeResolution = null;
        Misc.prototype.pluginRequest = null;
        Misc.prototype.pluginFailure = null;
        Misc.prototype.fullSpeedFps = null;
        Misc.prototype.autoAdjustFps = null;
        Misc.prototype.clientRecordStatus = null;
        Misc.prototype.captureDisplays = null;
        Misc.prototype.refreshVideoDisplay = null;
        Misc.prototype.toggleVirtualDisplay = null;
        Misc.prototype.togglePrivacyMode = null;
        Misc.prototype.supportedEncoding = null;
        Misc.prototype.selectedSid = null;
        Misc.prototype.changeDisplayResolution = null;
        Misc.prototype.messageQuery = null;
        Misc.prototype.followCurrentDisplay = null;

        let $oneOfFields;

        $Object.defineProperty(Misc.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["chatMessage", "switchDisplay", "permissionInfo", "option", "audioFormat", "closeReason", "refreshVideo", "videoReceived", "backNotification", "restartRemoteDevice", "uac", "foregroundWindowElevated", "stopService", "elevationRequest", "elevationResponse", "portableServiceRunning", "switchSidesRequest", "switchBack", "changeResolution", "pluginRequest", "pluginFailure", "fullSpeedFps", "autoAdjustFps", "clientRecordStatus", "captureDisplays", "refreshVideoDisplay", "toggleVirtualDisplay", "togglePrivacyMode", "supportedEncoding", "selectedSid", "changeDisplayResolution", "messageQuery", "followCurrentDisplay"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Misc.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.chatMessage != null && $Object.hasOwnProperty.call(message, "chatMessage"))
                $root.hbb.ChatMessage.encode(message.chatMessage, writer.uint32(34).fork(), _depth + 1).ldelim();
            if (message.switchDisplay != null && $Object.hasOwnProperty.call(message, "switchDisplay"))
                $root.hbb.SwitchDisplay.encode(message.switchDisplay, writer.uint32(42).fork(), _depth + 1).ldelim();
            if (message.permissionInfo != null && $Object.hasOwnProperty.call(message, "permissionInfo"))
                $root.hbb.PermissionInfo.encode(message.permissionInfo, writer.uint32(50).fork(), _depth + 1).ldelim();
            if (message.option != null && $Object.hasOwnProperty.call(message, "option"))
                $root.hbb.OptionMessage.encode(message.option, writer.uint32(58).fork(), _depth + 1).ldelim();
            if (message.audioFormat != null && $Object.hasOwnProperty.call(message, "audioFormat"))
                $root.hbb.AudioFormat.encode(message.audioFormat, writer.uint32(66).fork(), _depth + 1).ldelim();
            if (message.closeReason != null && $Object.hasOwnProperty.call(message, "closeReason"))
                writer.uint32(74).string(message.closeReason);
            if (message.refreshVideo != null && $Object.hasOwnProperty.call(message, "refreshVideo"))
                writer.uint32(80).bool(message.refreshVideo);
            if (message.videoReceived != null && $Object.hasOwnProperty.call(message, "videoReceived"))
                writer.uint32(96).bool(message.videoReceived);
            if (message.backNotification != null && $Object.hasOwnProperty.call(message, "backNotification"))
                $root.hbb.BackNotification.encode(message.backNotification, writer.uint32(106).fork(), _depth + 1).ldelim();
            if (message.restartRemoteDevice != null && $Object.hasOwnProperty.call(message, "restartRemoteDevice"))
                writer.uint32(112).bool(message.restartRemoteDevice);
            if (message.uac != null && $Object.hasOwnProperty.call(message, "uac"))
                writer.uint32(120).bool(message.uac);
            if (message.foregroundWindowElevated != null && $Object.hasOwnProperty.call(message, "foregroundWindowElevated"))
                writer.uint32(128).bool(message.foregroundWindowElevated);
            if (message.stopService != null && $Object.hasOwnProperty.call(message, "stopService"))
                writer.uint32(136).bool(message.stopService);
            if (message.elevationRequest != null && $Object.hasOwnProperty.call(message, "elevationRequest"))
                $root.hbb.ElevationRequest.encode(message.elevationRequest, writer.uint32(146).fork(), _depth + 1).ldelim();
            if (message.elevationResponse != null && $Object.hasOwnProperty.call(message, "elevationResponse"))
                writer.uint32(154).string(message.elevationResponse);
            if (message.portableServiceRunning != null && $Object.hasOwnProperty.call(message, "portableServiceRunning"))
                writer.uint32(160).bool(message.portableServiceRunning);
            if (message.switchSidesRequest != null && $Object.hasOwnProperty.call(message, "switchSidesRequest"))
                $root.hbb.SwitchSidesRequest.encode(message.switchSidesRequest, writer.uint32(170).fork(), _depth + 1).ldelim();
            if (message.switchBack != null && $Object.hasOwnProperty.call(message, "switchBack"))
                $root.hbb.SwitchBack.encode(message.switchBack, writer.uint32(178).fork(), _depth + 1).ldelim();
            if (message.changeResolution != null && $Object.hasOwnProperty.call(message, "changeResolution"))
                $root.hbb.Resolution.encode(message.changeResolution, writer.uint32(194).fork(), _depth + 1).ldelim();
            if (message.pluginRequest != null && $Object.hasOwnProperty.call(message, "pluginRequest"))
                $root.hbb.PluginRequest.encode(message.pluginRequest, writer.uint32(202).fork(), _depth + 1).ldelim();
            if (message.pluginFailure != null && $Object.hasOwnProperty.call(message, "pluginFailure"))
                $root.hbb.PluginFailure.encode(message.pluginFailure, writer.uint32(210).fork(), _depth + 1).ldelim();
            if (message.fullSpeedFps != null && $Object.hasOwnProperty.call(message, "fullSpeedFps"))
                writer.uint32(216).uint32(message.fullSpeedFps);
            if (message.autoAdjustFps != null && $Object.hasOwnProperty.call(message, "autoAdjustFps"))
                writer.uint32(224).uint32(message.autoAdjustFps);
            if (message.clientRecordStatus != null && $Object.hasOwnProperty.call(message, "clientRecordStatus"))
                writer.uint32(232).bool(message.clientRecordStatus);
            if (message.captureDisplays != null && $Object.hasOwnProperty.call(message, "captureDisplays"))
                $root.hbb.CaptureDisplays.encode(message.captureDisplays, writer.uint32(242).fork(), _depth + 1).ldelim();
            if (message.refreshVideoDisplay != null && $Object.hasOwnProperty.call(message, "refreshVideoDisplay"))
                writer.uint32(248).int32(message.refreshVideoDisplay);
            if (message.toggleVirtualDisplay != null && $Object.hasOwnProperty.call(message, "toggleVirtualDisplay"))
                $root.hbb.ToggleVirtualDisplay.encode(message.toggleVirtualDisplay, writer.uint32(258).fork(), _depth + 1).ldelim();
            if (message.togglePrivacyMode != null && $Object.hasOwnProperty.call(message, "togglePrivacyMode"))
                $root.hbb.TogglePrivacyMode.encode(message.togglePrivacyMode, writer.uint32(266).fork(), _depth + 1).ldelim();
            if (message.supportedEncoding != null && $Object.hasOwnProperty.call(message, "supportedEncoding"))
                $root.hbb.SupportedEncoding.encode(message.supportedEncoding, writer.uint32(274).fork(), _depth + 1).ldelim();
            if (message.selectedSid != null && $Object.hasOwnProperty.call(message, "selectedSid"))
                writer.uint32(280).uint32(message.selectedSid);
            if (message.changeDisplayResolution != null && $Object.hasOwnProperty.call(message, "changeDisplayResolution"))
                $root.hbb.DisplayResolution.encode(message.changeDisplayResolution, writer.uint32(290).fork(), _depth + 1).ldelim();
            if (message.messageQuery != null && $Object.hasOwnProperty.call(message, "messageQuery"))
                $root.hbb.MessageQuery.encode(message.messageQuery, writer.uint32(298).fork(), _depth + 1).ldelim();
            if (message.followCurrentDisplay != null && $Object.hasOwnProperty.call(message, "followCurrentDisplay"))
                writer.uint32(304).int32(message.followCurrentDisplay);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Misc.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.Misc();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.chatMessage = $root.hbb.ChatMessage.decode(reader, reader.uint32(), $undefined, _depth + 1, message.chatMessage);
                        message.union = "chatMessage";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.switchDisplay = $root.hbb.SwitchDisplay.decode(reader, reader.uint32(), $undefined, _depth + 1, message.switchDisplay);
                        message.union = "switchDisplay";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.permissionInfo = $root.hbb.PermissionInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.permissionInfo);
                        message.union = "permissionInfo";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.option = $root.hbb.OptionMessage.decode(reader, reader.uint32(), $undefined, _depth + 1, message.option);
                        message.union = "option";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.audioFormat = $root.hbb.AudioFormat.decode(reader, reader.uint32(), $undefined, _depth + 1, message.audioFormat);
                        message.union = "audioFormat";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.closeReason = reader.stringVerify();
                        message.union = "closeReason";
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        message.refreshVideo = reader.bool();
                        message.union = "refreshVideo";
                        continue;
                    }
                case 12: {
                        if (wireType !== 0)
                            break;
                        message.videoReceived = reader.bool();
                        message.union = "videoReceived";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.backNotification = $root.hbb.BackNotification.decode(reader, reader.uint32(), $undefined, _depth + 1, message.backNotification);
                        message.union = "backNotification";
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        message.restartRemoteDevice = reader.bool();
                        message.union = "restartRemoteDevice";
                        continue;
                    }
                case 15: {
                        if (wireType !== 0)
                            break;
                        message.uac = reader.bool();
                        message.union = "uac";
                        continue;
                    }
                case 16: {
                        if (wireType !== 0)
                            break;
                        message.foregroundWindowElevated = reader.bool();
                        message.union = "foregroundWindowElevated";
                        continue;
                    }
                case 17: {
                        if (wireType !== 0)
                            break;
                        message.stopService = reader.bool();
                        message.union = "stopService";
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.elevationRequest = $root.hbb.ElevationRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.elevationRequest);
                        message.union = "elevationRequest";
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        message.elevationResponse = reader.stringVerify();
                        message.union = "elevationResponse";
                        continue;
                    }
                case 20: {
                        if (wireType !== 0)
                            break;
                        message.portableServiceRunning = reader.bool();
                        message.union = "portableServiceRunning";
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        message.switchSidesRequest = $root.hbb.SwitchSidesRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.switchSidesRequest);
                        message.union = "switchSidesRequest";
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.switchBack = $root.hbb.SwitchBack.decode(reader, reader.uint32(), $undefined, _depth + 1, message.switchBack);
                        message.union = "switchBack";
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        message.changeResolution = $root.hbb.Resolution.decode(reader, reader.uint32(), $undefined, _depth + 1, message.changeResolution);
                        message.union = "changeResolution";
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        message.pluginRequest = $root.hbb.PluginRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pluginRequest);
                        message.union = "pluginRequest";
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        message.pluginFailure = $root.hbb.PluginFailure.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pluginFailure);
                        message.union = "pluginFailure";
                        continue;
                    }
                case 27: {
                        if (wireType !== 0)
                            break;
                        message.fullSpeedFps = reader.uint32();
                        message.union = "fullSpeedFps";
                        continue;
                    }
                case 28: {
                        if (wireType !== 0)
                            break;
                        message.autoAdjustFps = reader.uint32();
                        message.union = "autoAdjustFps";
                        continue;
                    }
                case 29: {
                        if (wireType !== 0)
                            break;
                        message.clientRecordStatus = reader.bool();
                        message.union = "clientRecordStatus";
                        continue;
                    }
                case 30: {
                        if (wireType !== 2)
                            break;
                        message.captureDisplays = $root.hbb.CaptureDisplays.decode(reader, reader.uint32(), $undefined, _depth + 1, message.captureDisplays);
                        message.union = "captureDisplays";
                        continue;
                    }
                case 31: {
                        if (wireType !== 0)
                            break;
                        message.refreshVideoDisplay = reader.int32();
                        message.union = "refreshVideoDisplay";
                        continue;
                    }
                case 32: {
                        if (wireType !== 2)
                            break;
                        message.toggleVirtualDisplay = $root.hbb.ToggleVirtualDisplay.decode(reader, reader.uint32(), $undefined, _depth + 1, message.toggleVirtualDisplay);
                        message.union = "toggleVirtualDisplay";
                        continue;
                    }
                case 33: {
                        if (wireType !== 2)
                            break;
                        message.togglePrivacyMode = $root.hbb.TogglePrivacyMode.decode(reader, reader.uint32(), $undefined, _depth + 1, message.togglePrivacyMode);
                        message.union = "togglePrivacyMode";
                        continue;
                    }
                case 34: {
                        if (wireType !== 2)
                            break;
                        message.supportedEncoding = $root.hbb.SupportedEncoding.decode(reader, reader.uint32(), $undefined, _depth + 1, message.supportedEncoding);
                        message.union = "supportedEncoding";
                        continue;
                    }
                case 35: {
                        if (wireType !== 0)
                            break;
                        message.selectedSid = reader.uint32();
                        message.union = "selectedSid";
                        continue;
                    }
                case 36: {
                        if (wireType !== 2)
                            break;
                        message.changeDisplayResolution = $root.hbb.DisplayResolution.decode(reader, reader.uint32(), $undefined, _depth + 1, message.changeDisplayResolution);
                        message.union = "changeDisplayResolution";
                        continue;
                    }
                case 37: {
                        if (wireType !== 2)
                            break;
                        message.messageQuery = $root.hbb.MessageQuery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.messageQuery);
                        message.union = "messageQuery";
                        continue;
                    }
                case 38: {
                        if (wireType !== 0)
                            break;
                        message.followCurrentDisplay = reader.int32();
                        message.union = "followCurrentDisplay";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        Misc.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.Misc)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.Misc: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.Misc();
            if (object.chatMessage != null) {
                if (!$util.isObject(object.chatMessage))
                    throw $TypeError(".hbb.Misc.chatMessage: object expected");
                message.chatMessage = $root.hbb.ChatMessage.fromObject(object.chatMessage, _depth + 1);
            }
            if (object.switchDisplay != null) {
                if (!$util.isObject(object.switchDisplay))
                    throw $TypeError(".hbb.Misc.switchDisplay: object expected");
                message.switchDisplay = $root.hbb.SwitchDisplay.fromObject(object.switchDisplay, _depth + 1);
            }
            if (object.permissionInfo != null) {
                if (!$util.isObject(object.permissionInfo))
                    throw $TypeError(".hbb.Misc.permissionInfo: object expected");
                message.permissionInfo = $root.hbb.PermissionInfo.fromObject(object.permissionInfo, _depth + 1);
            }
            if (object.option != null) {
                if (!$util.isObject(object.option))
                    throw $TypeError(".hbb.Misc.option: object expected");
                message.option = $root.hbb.OptionMessage.fromObject(object.option, _depth + 1);
            }
            if (object.audioFormat != null) {
                if (!$util.isObject(object.audioFormat))
                    throw $TypeError(".hbb.Misc.audioFormat: object expected");
                message.audioFormat = $root.hbb.AudioFormat.fromObject(object.audioFormat, _depth + 1);
            }
            if (object.closeReason != null)
                message.closeReason = $String(object.closeReason);
            if (object.refreshVideo != null)
                message.refreshVideo = $Boolean(object.refreshVideo);
            if (object.videoReceived != null)
                message.videoReceived = $Boolean(object.videoReceived);
            if (object.backNotification != null) {
                if (!$util.isObject(object.backNotification))
                    throw $TypeError(".hbb.Misc.backNotification: object expected");
                message.backNotification = $root.hbb.BackNotification.fromObject(object.backNotification, _depth + 1);
            }
            if (object.restartRemoteDevice != null)
                message.restartRemoteDevice = $Boolean(object.restartRemoteDevice);
            if (object.uac != null)
                message.uac = $Boolean(object.uac);
            if (object.foregroundWindowElevated != null)
                message.foregroundWindowElevated = $Boolean(object.foregroundWindowElevated);
            if (object.stopService != null)
                message.stopService = $Boolean(object.stopService);
            if (object.elevationRequest != null) {
                if (!$util.isObject(object.elevationRequest))
                    throw $TypeError(".hbb.Misc.elevationRequest: object expected");
                message.elevationRequest = $root.hbb.ElevationRequest.fromObject(object.elevationRequest, _depth + 1);
            }
            if (object.elevationResponse != null)
                message.elevationResponse = $String(object.elevationResponse);
            if (object.portableServiceRunning != null)
                message.portableServiceRunning = $Boolean(object.portableServiceRunning);
            if (object.switchSidesRequest != null) {
                if (!$util.isObject(object.switchSidesRequest))
                    throw $TypeError(".hbb.Misc.switchSidesRequest: object expected");
                message.switchSidesRequest = $root.hbb.SwitchSidesRequest.fromObject(object.switchSidesRequest, _depth + 1);
            }
            if (object.switchBack != null) {
                if (!$util.isObject(object.switchBack))
                    throw $TypeError(".hbb.Misc.switchBack: object expected");
                message.switchBack = $root.hbb.SwitchBack.fromObject(object.switchBack, _depth + 1);
            }
            if (object.changeResolution != null) {
                if (!$util.isObject(object.changeResolution))
                    throw $TypeError(".hbb.Misc.changeResolution: object expected");
                message.changeResolution = $root.hbb.Resolution.fromObject(object.changeResolution, _depth + 1);
            }
            if (object.pluginRequest != null) {
                if (!$util.isObject(object.pluginRequest))
                    throw $TypeError(".hbb.Misc.pluginRequest: object expected");
                message.pluginRequest = $root.hbb.PluginRequest.fromObject(object.pluginRequest, _depth + 1);
            }
            if (object.pluginFailure != null) {
                if (!$util.isObject(object.pluginFailure))
                    throw $TypeError(".hbb.Misc.pluginFailure: object expected");
                message.pluginFailure = $root.hbb.PluginFailure.fromObject(object.pluginFailure, _depth + 1);
            }
            if (object.fullSpeedFps != null)
                message.fullSpeedFps = object.fullSpeedFps >>> 0;
            if (object.autoAdjustFps != null)
                message.autoAdjustFps = object.autoAdjustFps >>> 0;
            if (object.clientRecordStatus != null)
                message.clientRecordStatus = $Boolean(object.clientRecordStatus);
            if (object.captureDisplays != null) {
                if (!$util.isObject(object.captureDisplays))
                    throw $TypeError(".hbb.Misc.captureDisplays: object expected");
                message.captureDisplays = $root.hbb.CaptureDisplays.fromObject(object.captureDisplays, _depth + 1);
            }
            if (object.refreshVideoDisplay != null)
                message.refreshVideoDisplay = object.refreshVideoDisplay | 0;
            if (object.toggleVirtualDisplay != null) {
                if (!$util.isObject(object.toggleVirtualDisplay))
                    throw $TypeError(".hbb.Misc.toggleVirtualDisplay: object expected");
                message.toggleVirtualDisplay = $root.hbb.ToggleVirtualDisplay.fromObject(object.toggleVirtualDisplay, _depth + 1);
            }
            if (object.togglePrivacyMode != null) {
                if (!$util.isObject(object.togglePrivacyMode))
                    throw $TypeError(".hbb.Misc.togglePrivacyMode: object expected");
                message.togglePrivacyMode = $root.hbb.TogglePrivacyMode.fromObject(object.togglePrivacyMode, _depth + 1);
            }
            if (object.supportedEncoding != null) {
                if (!$util.isObject(object.supportedEncoding))
                    throw $TypeError(".hbb.Misc.supportedEncoding: object expected");
                message.supportedEncoding = $root.hbb.SupportedEncoding.fromObject(object.supportedEncoding, _depth + 1);
            }
            if (object.selectedSid != null)
                message.selectedSid = object.selectedSid >>> 0;
            if (object.changeDisplayResolution != null) {
                if (!$util.isObject(object.changeDisplayResolution))
                    throw $TypeError(".hbb.Misc.changeDisplayResolution: object expected");
                message.changeDisplayResolution = $root.hbb.DisplayResolution.fromObject(object.changeDisplayResolution, _depth + 1);
            }
            if (object.messageQuery != null) {
                if (!$util.isObject(object.messageQuery))
                    throw $TypeError(".hbb.Misc.messageQuery: object expected");
                message.messageQuery = $root.hbb.MessageQuery.fromObject(object.messageQuery, _depth + 1);
            }
            if (object.followCurrentDisplay != null)
                message.followCurrentDisplay = object.followCurrentDisplay | 0;
            return message;
        };

        Misc.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.chatMessage != null && $Object.hasOwnProperty.call(message, "chatMessage")) {
                object.chatMessage = $root.hbb.ChatMessage.toObject(message.chatMessage, options, _depth + 1);
                if (options.oneofs)
                    object.union = "chatMessage";
            }
            if (message.switchDisplay != null && $Object.hasOwnProperty.call(message, "switchDisplay")) {
                object.switchDisplay = $root.hbb.SwitchDisplay.toObject(message.switchDisplay, options, _depth + 1);
                if (options.oneofs)
                    object.union = "switchDisplay";
            }
            if (message.permissionInfo != null && $Object.hasOwnProperty.call(message, "permissionInfo")) {
                object.permissionInfo = $root.hbb.PermissionInfo.toObject(message.permissionInfo, options, _depth + 1);
                if (options.oneofs)
                    object.union = "permissionInfo";
            }
            if (message.option != null && $Object.hasOwnProperty.call(message, "option")) {
                object.option = $root.hbb.OptionMessage.toObject(message.option, options, _depth + 1);
                if (options.oneofs)
                    object.union = "option";
            }
            if (message.audioFormat != null && $Object.hasOwnProperty.call(message, "audioFormat")) {
                object.audioFormat = $root.hbb.AudioFormat.toObject(message.audioFormat, options, _depth + 1);
                if (options.oneofs)
                    object.union = "audioFormat";
            }
            if (message.closeReason != null && $Object.hasOwnProperty.call(message, "closeReason")) {
                object.closeReason = message.closeReason;
                if (options.oneofs)
                    object.union = "closeReason";
            }
            if (message.refreshVideo != null && $Object.hasOwnProperty.call(message, "refreshVideo")) {
                object.refreshVideo = message.refreshVideo;
                if (options.oneofs)
                    object.union = "refreshVideo";
            }
            if (message.videoReceived != null && $Object.hasOwnProperty.call(message, "videoReceived")) {
                object.videoReceived = message.videoReceived;
                if (options.oneofs)
                    object.union = "videoReceived";
            }
            if (message.backNotification != null && $Object.hasOwnProperty.call(message, "backNotification")) {
                object.backNotification = $root.hbb.BackNotification.toObject(message.backNotification, options, _depth + 1);
                if (options.oneofs)
                    object.union = "backNotification";
            }
            if (message.restartRemoteDevice != null && $Object.hasOwnProperty.call(message, "restartRemoteDevice")) {
                object.restartRemoteDevice = message.restartRemoteDevice;
                if (options.oneofs)
                    object.union = "restartRemoteDevice";
            }
            if (message.uac != null && $Object.hasOwnProperty.call(message, "uac")) {
                object.uac = message.uac;
                if (options.oneofs)
                    object.union = "uac";
            }
            if (message.foregroundWindowElevated != null && $Object.hasOwnProperty.call(message, "foregroundWindowElevated")) {
                object.foregroundWindowElevated = message.foregroundWindowElevated;
                if (options.oneofs)
                    object.union = "foregroundWindowElevated";
            }
            if (message.stopService != null && $Object.hasOwnProperty.call(message, "stopService")) {
                object.stopService = message.stopService;
                if (options.oneofs)
                    object.union = "stopService";
            }
            if (message.elevationRequest != null && $Object.hasOwnProperty.call(message, "elevationRequest")) {
                object.elevationRequest = $root.hbb.ElevationRequest.toObject(message.elevationRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "elevationRequest";
            }
            if (message.elevationResponse != null && $Object.hasOwnProperty.call(message, "elevationResponse")) {
                object.elevationResponse = message.elevationResponse;
                if (options.oneofs)
                    object.union = "elevationResponse";
            }
            if (message.portableServiceRunning != null && $Object.hasOwnProperty.call(message, "portableServiceRunning")) {
                object.portableServiceRunning = message.portableServiceRunning;
                if (options.oneofs)
                    object.union = "portableServiceRunning";
            }
            if (message.switchSidesRequest != null && $Object.hasOwnProperty.call(message, "switchSidesRequest")) {
                object.switchSidesRequest = $root.hbb.SwitchSidesRequest.toObject(message.switchSidesRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "switchSidesRequest";
            }
            if (message.switchBack != null && $Object.hasOwnProperty.call(message, "switchBack")) {
                object.switchBack = $root.hbb.SwitchBack.toObject(message.switchBack, options, _depth + 1);
                if (options.oneofs)
                    object.union = "switchBack";
            }
            if (message.changeResolution != null && $Object.hasOwnProperty.call(message, "changeResolution")) {
                object.changeResolution = $root.hbb.Resolution.toObject(message.changeResolution, options, _depth + 1);
                if (options.oneofs)
                    object.union = "changeResolution";
            }
            if (message.pluginRequest != null && $Object.hasOwnProperty.call(message, "pluginRequest")) {
                object.pluginRequest = $root.hbb.PluginRequest.toObject(message.pluginRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "pluginRequest";
            }
            if (message.pluginFailure != null && $Object.hasOwnProperty.call(message, "pluginFailure")) {
                object.pluginFailure = $root.hbb.PluginFailure.toObject(message.pluginFailure, options, _depth + 1);
                if (options.oneofs)
                    object.union = "pluginFailure";
            }
            if (message.fullSpeedFps != null && $Object.hasOwnProperty.call(message, "fullSpeedFps")) {
                object.fullSpeedFps = message.fullSpeedFps;
                if (options.oneofs)
                    object.union = "fullSpeedFps";
            }
            if (message.autoAdjustFps != null && $Object.hasOwnProperty.call(message, "autoAdjustFps")) {
                object.autoAdjustFps = message.autoAdjustFps;
                if (options.oneofs)
                    object.union = "autoAdjustFps";
            }
            if (message.clientRecordStatus != null && $Object.hasOwnProperty.call(message, "clientRecordStatus")) {
                object.clientRecordStatus = message.clientRecordStatus;
                if (options.oneofs)
                    object.union = "clientRecordStatus";
            }
            if (message.captureDisplays != null && $Object.hasOwnProperty.call(message, "captureDisplays")) {
                object.captureDisplays = $root.hbb.CaptureDisplays.toObject(message.captureDisplays, options, _depth + 1);
                if (options.oneofs)
                    object.union = "captureDisplays";
            }
            if (message.refreshVideoDisplay != null && $Object.hasOwnProperty.call(message, "refreshVideoDisplay")) {
                object.refreshVideoDisplay = message.refreshVideoDisplay;
                if (options.oneofs)
                    object.union = "refreshVideoDisplay";
            }
            if (message.toggleVirtualDisplay != null && $Object.hasOwnProperty.call(message, "toggleVirtualDisplay")) {
                object.toggleVirtualDisplay = $root.hbb.ToggleVirtualDisplay.toObject(message.toggleVirtualDisplay, options, _depth + 1);
                if (options.oneofs)
                    object.union = "toggleVirtualDisplay";
            }
            if (message.togglePrivacyMode != null && $Object.hasOwnProperty.call(message, "togglePrivacyMode")) {
                object.togglePrivacyMode = $root.hbb.TogglePrivacyMode.toObject(message.togglePrivacyMode, options, _depth + 1);
                if (options.oneofs)
                    object.union = "togglePrivacyMode";
            }
            if (message.supportedEncoding != null && $Object.hasOwnProperty.call(message, "supportedEncoding")) {
                object.supportedEncoding = $root.hbb.SupportedEncoding.toObject(message.supportedEncoding, options, _depth + 1);
                if (options.oneofs)
                    object.union = "supportedEncoding";
            }
            if (message.selectedSid != null && $Object.hasOwnProperty.call(message, "selectedSid")) {
                object.selectedSid = message.selectedSid;
                if (options.oneofs)
                    object.union = "selectedSid";
            }
            if (message.changeDisplayResolution != null && $Object.hasOwnProperty.call(message, "changeDisplayResolution")) {
                object.changeDisplayResolution = $root.hbb.DisplayResolution.toObject(message.changeDisplayResolution, options, _depth + 1);
                if (options.oneofs)
                    object.union = "changeDisplayResolution";
            }
            if (message.messageQuery != null && $Object.hasOwnProperty.call(message, "messageQuery")) {
                object.messageQuery = $root.hbb.MessageQuery.toObject(message.messageQuery, options, _depth + 1);
                if (options.oneofs)
                    object.union = "messageQuery";
            }
            if (message.followCurrentDisplay != null && $Object.hasOwnProperty.call(message, "followCurrentDisplay")) {
                object.followCurrentDisplay = message.followCurrentDisplay;
                if (options.oneofs)
                    object.union = "followCurrentDisplay";
            }
            return object;
        };

        Misc.prototype.toJSON = function() {
            return Misc.toObject(this, $protobuf.util.toJSONOptions);
        };

        return Misc;
    })();

    hbb.VoiceCallRequest = (function() {

        const VoiceCallRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        VoiceCallRequest.prototype.reqTimestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;
        VoiceCallRequest.prototype.isConnect = false;

        VoiceCallRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.reqTimestamp != null && $Object.hasOwnProperty.call(message, "reqTimestamp") && (typeof message.reqTimestamp === "object" ? message.reqTimestamp.low || message.reqTimestamp.high : message.reqTimestamp !== 0))
                writer.uint32(8).int64(message.reqTimestamp);
            if (message.isConnect != null && $Object.hasOwnProperty.call(message, "isConnect") && message.isConnect !== false)
                writer.uint32(16).bool(message.isConnect);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        VoiceCallRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.VoiceCallRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.reqTimestamp = value;
                        else
                            delete message.reqTimestamp;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isConnect = value;
                        else
                            delete message.isConnect;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        VoiceCallRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.VoiceCallRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.VoiceCallRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.VoiceCallRequest();
            if (object.reqTimestamp != null)
                if (typeof object.reqTimestamp === "object" ? object.reqTimestamp.low || object.reqTimestamp.high : $Number(object.reqTimestamp) !== 0)
                    if ($util.Long)
                        message.reqTimestamp = $util.Long.fromValue(object.reqTimestamp, false);
                    else if (typeof object.reqTimestamp === "string")
                        message.reqTimestamp = $parseInt(object.reqTimestamp, 10);
                    else if (typeof object.reqTimestamp === "number")
                        message.reqTimestamp = object.reqTimestamp;
                    else if (typeof object.reqTimestamp === "object")
                        message.reqTimestamp = new $util.LongBits(object.reqTimestamp.low >>> 0, object.reqTimestamp.high >>> 0).toNumber();
            if (object.isConnect != null)
                if (object.isConnect)
                    message.isConnect = $Boolean(object.isConnect);
            return message;
        };

        VoiceCallRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.reqTimestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.reqTimestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                object.isConnect = false;
            }
            if (message.reqTimestamp != null && $Object.hasOwnProperty.call(message, "reqTimestamp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.reqTimestamp = typeof message.reqTimestamp === "number" ? $BigInt(message.reqTimestamp) : $util.Long.fromBits(message.reqTimestamp.low >>> 0, message.reqTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.reqTimestamp === "number")
                    object.reqTimestamp = options.longs === $String ? $String(message.reqTimestamp) : message.reqTimestamp;
                else
                    object.reqTimestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.reqTimestamp) : options.longs === $Number ? new $util.LongBits(message.reqTimestamp.low >>> 0, message.reqTimestamp.high >>> 0).toNumber() : message.reqTimestamp;
            if (message.isConnect != null && $Object.hasOwnProperty.call(message, "isConnect"))
                object.isConnect = message.isConnect;
            return object;
        };

        VoiceCallRequest.prototype.toJSON = function() {
            return VoiceCallRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return VoiceCallRequest;
    })();

    hbb.VoiceCallResponse = (function() {

        const VoiceCallResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        VoiceCallResponse.prototype.accepted = false;
        VoiceCallResponse.prototype.reqTimestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;
        VoiceCallResponse.prototype.ackTimestamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        VoiceCallResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.accepted != null && $Object.hasOwnProperty.call(message, "accepted") && message.accepted !== false)
                writer.uint32(8).bool(message.accepted);
            if (message.reqTimestamp != null && $Object.hasOwnProperty.call(message, "reqTimestamp") && (typeof message.reqTimestamp === "object" ? message.reqTimestamp.low || message.reqTimestamp.high : message.reqTimestamp !== 0))
                writer.uint32(16).int64(message.reqTimestamp);
            if (message.ackTimestamp != null && $Object.hasOwnProperty.call(message, "ackTimestamp") && (typeof message.ackTimestamp === "object" ? message.ackTimestamp.low || message.ackTimestamp.high : message.ackTimestamp !== 0))
                writer.uint32(24).int64(message.ackTimestamp);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        VoiceCallResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.VoiceCallResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.accepted = value;
                        else
                            delete message.accepted;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.reqTimestamp = value;
                        else
                            delete message.reqTimestamp;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                            message.ackTimestamp = value;
                        else
                            delete message.ackTimestamp;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        VoiceCallResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.VoiceCallResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.VoiceCallResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.VoiceCallResponse();
            if (object.accepted != null)
                if (object.accepted)
                    message.accepted = $Boolean(object.accepted);
            if (object.reqTimestamp != null)
                if (typeof object.reqTimestamp === "object" ? object.reqTimestamp.low || object.reqTimestamp.high : $Number(object.reqTimestamp) !== 0)
                    if ($util.Long)
                        message.reqTimestamp = $util.Long.fromValue(object.reqTimestamp, false);
                    else if (typeof object.reqTimestamp === "string")
                        message.reqTimestamp = $parseInt(object.reqTimestamp, 10);
                    else if (typeof object.reqTimestamp === "number")
                        message.reqTimestamp = object.reqTimestamp;
                    else if (typeof object.reqTimestamp === "object")
                        message.reqTimestamp = new $util.LongBits(object.reqTimestamp.low >>> 0, object.reqTimestamp.high >>> 0).toNumber();
            if (object.ackTimestamp != null)
                if (typeof object.ackTimestamp === "object" ? object.ackTimestamp.low || object.ackTimestamp.high : $Number(object.ackTimestamp) !== 0)
                    if ($util.Long)
                        message.ackTimestamp = $util.Long.fromValue(object.ackTimestamp, false);
                    else if (typeof object.ackTimestamp === "string")
                        message.ackTimestamp = $parseInt(object.ackTimestamp, 10);
                    else if (typeof object.ackTimestamp === "number")
                        message.ackTimestamp = object.ackTimestamp;
                    else if (typeof object.ackTimestamp === "object")
                        message.ackTimestamp = new $util.LongBits(object.ackTimestamp.low >>> 0, object.ackTimestamp.high >>> 0).toNumber();
            return message;
        };

        VoiceCallResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.accepted = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.reqTimestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.reqTimestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.ackTimestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.ackTimestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (message.accepted != null && $Object.hasOwnProperty.call(message, "accepted"))
                object.accepted = message.accepted;
            if (message.reqTimestamp != null && $Object.hasOwnProperty.call(message, "reqTimestamp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.reqTimestamp = typeof message.reqTimestamp === "number" ? $BigInt(message.reqTimestamp) : $util.Long.fromBits(message.reqTimestamp.low >>> 0, message.reqTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.reqTimestamp === "number")
                    object.reqTimestamp = options.longs === $String ? $String(message.reqTimestamp) : message.reqTimestamp;
                else
                    object.reqTimestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.reqTimestamp) : options.longs === $Number ? new $util.LongBits(message.reqTimestamp.low >>> 0, message.reqTimestamp.high >>> 0).toNumber() : message.reqTimestamp;
            if (message.ackTimestamp != null && $Object.hasOwnProperty.call(message, "ackTimestamp"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.ackTimestamp = typeof message.ackTimestamp === "number" ? $BigInt(message.ackTimestamp) : $util.Long.fromBits(message.ackTimestamp.low >>> 0, message.ackTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.ackTimestamp === "number")
                    object.ackTimestamp = options.longs === $String ? $String(message.ackTimestamp) : message.ackTimestamp;
                else
                    object.ackTimestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.ackTimestamp) : options.longs === $Number ? new $util.LongBits(message.ackTimestamp.low >>> 0, message.ackTimestamp.high >>> 0).toNumber() : message.ackTimestamp;
            return object;
        };

        VoiceCallResponse.prototype.toJSON = function() {
            return VoiceCallResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return VoiceCallResponse;
    })();

    hbb.ScreenshotRequest = (function() {

        const ScreenshotRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ScreenshotRequest.prototype.display = 0;
        ScreenshotRequest.prototype.sid = "";

        ScreenshotRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.display != null && $Object.hasOwnProperty.call(message, "display") && message.display !== 0)
                writer.uint32(8).int32(message.display);
            if (message.sid != null && $Object.hasOwnProperty.call(message, "sid") && message.sid !== "")
                writer.uint32(18).string(message.sid);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ScreenshotRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ScreenshotRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.display = value;
                        else
                            delete message.display;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.sid = value;
                        else
                            delete message.sid;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ScreenshotRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ScreenshotRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ScreenshotRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ScreenshotRequest();
            if (object.display != null)
                if ($Number(object.display) !== 0)
                    message.display = object.display | 0;
            if (object.sid != null)
                if (typeof object.sid !== "string" || object.sid.length)
                    message.sid = $String(object.sid);
            return message;
        };

        ScreenshotRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.display = 0;
                object.sid = "";
            }
            if (message.display != null && $Object.hasOwnProperty.call(message, "display"))
                object.display = message.display;
            if (message.sid != null && $Object.hasOwnProperty.call(message, "sid"))
                object.sid = message.sid;
            return object;
        };

        ScreenshotRequest.prototype.toJSON = function() {
            return ScreenshotRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ScreenshotRequest;
    })();

    hbb.ScreenshotResponse = (function() {

        const ScreenshotResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ScreenshotResponse.prototype.sid = "";
        ScreenshotResponse.prototype.msg = "";
        ScreenshotResponse.prototype.data = $util.newBuffer([]);

        ScreenshotResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.sid != null && $Object.hasOwnProperty.call(message, "sid") && message.sid !== "")
                writer.uint32(10).string(message.sid);
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg") && message.msg !== "")
                writer.uint32(18).string(message.msg);
            if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                writer.uint32(26).bytes(message.data);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ScreenshotResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ScreenshotResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.sid = value;
                        else
                            delete message.sid;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.msg = value;
                        else
                            delete message.msg;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ScreenshotResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ScreenshotResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ScreenshotResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ScreenshotResponse();
            if (object.sid != null)
                if (typeof object.sid !== "string" || object.sid.length)
                    message.sid = $String(object.sid);
            if (object.msg != null)
                if (typeof object.msg !== "string" || object.msg.length)
                    message.msg = $String(object.msg);
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            return message;
        };

        ScreenshotResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.sid = "";
                object.msg = "";
                if (options.bytes === $String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== $Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.sid != null && $Object.hasOwnProperty.call(message, "sid"))
                object.sid = message.sid;
            if (message.msg != null && $Object.hasOwnProperty.call(message, "msg"))
                object.msg = message.msg;
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        ScreenshotResponse.prototype.toJSON = function() {
            return ScreenshotResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ScreenshotResponse;
    })();

    hbb.OpenTerminal = (function() {

        const OpenTerminal = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        OpenTerminal.prototype.terminalId = 0;
        OpenTerminal.prototype.rows = 0;
        OpenTerminal.prototype.cols = 0;

        OpenTerminal.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId") && message.terminalId !== 0)
                writer.uint32(8).int32(message.terminalId);
            if (message.rows != null && $Object.hasOwnProperty.call(message, "rows") && message.rows !== 0)
                writer.uint32(16).uint32(message.rows);
            if (message.cols != null && $Object.hasOwnProperty.call(message, "cols") && message.cols !== 0)
                writer.uint32(24).uint32(message.cols);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        OpenTerminal.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.OpenTerminal(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.terminalId = value;
                        else
                            delete message.terminalId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rows = value;
                        else
                            delete message.rows;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.cols = value;
                        else
                            delete message.cols;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        OpenTerminal.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.OpenTerminal)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.OpenTerminal: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.OpenTerminal();
            if (object.terminalId != null)
                if ($Number(object.terminalId) !== 0)
                    message.terminalId = object.terminalId | 0;
            if (object.rows != null)
                if ($Number(object.rows) !== 0)
                    message.rows = object.rows >>> 0;
            if (object.cols != null)
                if ($Number(object.cols) !== 0)
                    message.cols = object.cols >>> 0;
            return message;
        };

        OpenTerminal.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.terminalId = 0;
                object.rows = 0;
                object.cols = 0;
            }
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId"))
                object.terminalId = message.terminalId;
            if (message.rows != null && $Object.hasOwnProperty.call(message, "rows"))
                object.rows = message.rows;
            if (message.cols != null && $Object.hasOwnProperty.call(message, "cols"))
                object.cols = message.cols;
            return object;
        };

        OpenTerminal.prototype.toJSON = function() {
            return OpenTerminal.toObject(this, $protobuf.util.toJSONOptions);
        };

        return OpenTerminal;
    })();

    hbb.ResizeTerminal = (function() {

        const ResizeTerminal = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ResizeTerminal.prototype.terminalId = 0;
        ResizeTerminal.prototype.rows = 0;
        ResizeTerminal.prototype.cols = 0;

        ResizeTerminal.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId") && message.terminalId !== 0)
                writer.uint32(8).int32(message.terminalId);
            if (message.rows != null && $Object.hasOwnProperty.call(message, "rows") && message.rows !== 0)
                writer.uint32(16).uint32(message.rows);
            if (message.cols != null && $Object.hasOwnProperty.call(message, "cols") && message.cols !== 0)
                writer.uint32(24).uint32(message.cols);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ResizeTerminal.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ResizeTerminal(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.terminalId = value;
                        else
                            delete message.terminalId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.rows = value;
                        else
                            delete message.rows;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.cols = value;
                        else
                            delete message.cols;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ResizeTerminal.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ResizeTerminal)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ResizeTerminal: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ResizeTerminal();
            if (object.terminalId != null)
                if ($Number(object.terminalId) !== 0)
                    message.terminalId = object.terminalId | 0;
            if (object.rows != null)
                if ($Number(object.rows) !== 0)
                    message.rows = object.rows >>> 0;
            if (object.cols != null)
                if ($Number(object.cols) !== 0)
                    message.cols = object.cols >>> 0;
            return message;
        };

        ResizeTerminal.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.terminalId = 0;
                object.rows = 0;
                object.cols = 0;
            }
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId"))
                object.terminalId = message.terminalId;
            if (message.rows != null && $Object.hasOwnProperty.call(message, "rows"))
                object.rows = message.rows;
            if (message.cols != null && $Object.hasOwnProperty.call(message, "cols"))
                object.cols = message.cols;
            return object;
        };

        ResizeTerminal.prototype.toJSON = function() {
            return ResizeTerminal.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ResizeTerminal;
    })();

    hbb.TerminalData = (function() {

        const TerminalData = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TerminalData.prototype.terminalId = 0;
        TerminalData.prototype.data = $util.newBuffer([]);
        TerminalData.prototype.compressed = false;

        TerminalData.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId") && message.terminalId !== 0)
                writer.uint32(8).int32(message.terminalId);
            if (message.data != null && $Object.hasOwnProperty.call(message, "data") && message.data.length)
                writer.uint32(18).bytes(message.data);
            if (message.compressed != null && $Object.hasOwnProperty.call(message, "compressed") && message.compressed !== false)
                writer.uint32(24).bool(message.compressed);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TerminalData.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TerminalData(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.terminalId = value;
                        else
                            delete message.terminalId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.data = value;
                        else
                            delete message.data;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.compressed = value;
                        else
                            delete message.compressed;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TerminalData.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TerminalData)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TerminalData: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TerminalData();
            if (object.terminalId != null)
                if ($Number(object.terminalId) !== 0)
                    message.terminalId = object.terminalId | 0;
            if (object.data != null)
                if (object.data.length)
                    if (typeof object.data === "string")
                        $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                    else if (object.data.length >= 0)
                        message.data = object.data;
            if (object.compressed != null)
                if (object.compressed)
                    message.compressed = $Boolean(object.compressed);
            return message;
        };

        TerminalData.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.terminalId = 0;
                if (options.bytes === $String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== $Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.compressed = false;
            }
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId"))
                object.terminalId = message.terminalId;
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === $String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.data) : message.data;
            if (message.compressed != null && $Object.hasOwnProperty.call(message, "compressed"))
                object.compressed = message.compressed;
            return object;
        };

        TerminalData.prototype.toJSON = function() {
            return TerminalData.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TerminalData;
    })();

    hbb.CloseTerminal = (function() {

        const CloseTerminal = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        CloseTerminal.prototype.terminalId = 0;

        CloseTerminal.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId") && message.terminalId !== 0)
                writer.uint32(8).int32(message.terminalId);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        CloseTerminal.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.CloseTerminal(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.terminalId = value;
                        else
                            delete message.terminalId;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        CloseTerminal.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.CloseTerminal)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.CloseTerminal: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.CloseTerminal();
            if (object.terminalId != null)
                if ($Number(object.terminalId) !== 0)
                    message.terminalId = object.terminalId | 0;
            return message;
        };

        CloseTerminal.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.terminalId = 0;
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId"))
                object.terminalId = message.terminalId;
            return object;
        };

        CloseTerminal.prototype.toJSON = function() {
            return CloseTerminal.toObject(this, $protobuf.util.toJSONOptions);
        };

        return CloseTerminal;
    })();

    hbb.TerminalAction = (function() {

        const TerminalAction = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TerminalAction.prototype.open = null;
        TerminalAction.prototype.data = null;
        TerminalAction.prototype.resize = null;
        TerminalAction.prototype.close = null;

        let $oneOfFields;

        $Object.defineProperty(TerminalAction.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["open", "data", "resize", "close"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        TerminalAction.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.open != null && $Object.hasOwnProperty.call(message, "open"))
                $root.hbb.OpenTerminal.encode(message.open, writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                $root.hbb.TerminalData.encode(message.data, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.resize != null && $Object.hasOwnProperty.call(message, "resize"))
                $root.hbb.ResizeTerminal.encode(message.resize, writer.uint32(26).fork(), _depth + 1).ldelim();
            if (message.close != null && $Object.hasOwnProperty.call(message, "close"))
                $root.hbb.CloseTerminal.encode(message.close, writer.uint32(34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TerminalAction.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TerminalAction();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.open = $root.hbb.OpenTerminal.decode(reader, reader.uint32(), $undefined, _depth + 1, message.open);
                        message.union = "open";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.data = $root.hbb.TerminalData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                        message.union = "data";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.resize = $root.hbb.ResizeTerminal.decode(reader, reader.uint32(), $undefined, _depth + 1, message.resize);
                        message.union = "resize";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.close = $root.hbb.CloseTerminal.decode(reader, reader.uint32(), $undefined, _depth + 1, message.close);
                        message.union = "close";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TerminalAction.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TerminalAction)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TerminalAction: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TerminalAction();
            if (object.open != null) {
                if (!$util.isObject(object.open))
                    throw $TypeError(".hbb.TerminalAction.open: object expected");
                message.open = $root.hbb.OpenTerminal.fromObject(object.open, _depth + 1);
            }
            if (object.data != null) {
                if (!$util.isObject(object.data))
                    throw $TypeError(".hbb.TerminalAction.data: object expected");
                message.data = $root.hbb.TerminalData.fromObject(object.data, _depth + 1);
            }
            if (object.resize != null) {
                if (!$util.isObject(object.resize))
                    throw $TypeError(".hbb.TerminalAction.resize: object expected");
                message.resize = $root.hbb.ResizeTerminal.fromObject(object.resize, _depth + 1);
            }
            if (object.close != null) {
                if (!$util.isObject(object.close))
                    throw $TypeError(".hbb.TerminalAction.close: object expected");
                message.close = $root.hbb.CloseTerminal.fromObject(object.close, _depth + 1);
            }
            return message;
        };

        TerminalAction.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.open != null && $Object.hasOwnProperty.call(message, "open")) {
                object.open = $root.hbb.OpenTerminal.toObject(message.open, options, _depth + 1);
                if (options.oneofs)
                    object.union = "open";
            }
            if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                object.data = $root.hbb.TerminalData.toObject(message.data, options, _depth + 1);
                if (options.oneofs)
                    object.union = "data";
            }
            if (message.resize != null && $Object.hasOwnProperty.call(message, "resize")) {
                object.resize = $root.hbb.ResizeTerminal.toObject(message.resize, options, _depth + 1);
                if (options.oneofs)
                    object.union = "resize";
            }
            if (message.close != null && $Object.hasOwnProperty.call(message, "close")) {
                object.close = $root.hbb.CloseTerminal.toObject(message.close, options, _depth + 1);
                if (options.oneofs)
                    object.union = "close";
            }
            return object;
        };

        TerminalAction.prototype.toJSON = function() {
            return TerminalAction.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TerminalAction;
    })();

    hbb.TerminalOpened = (function() {

        const TerminalOpened = function (properties) {
            this.persistentSessions = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TerminalOpened.prototype.terminalId = 0;
        TerminalOpened.prototype.success = false;
        TerminalOpened.prototype.message = "";
        TerminalOpened.prototype.pid = 0;
        TerminalOpened.prototype.serviceId = "";
        TerminalOpened.prototype.persistentSessions = $util.emptyArray;
        TerminalOpened.prototype.replayTerminalOutput = false;

        TerminalOpened.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId") && message.terminalId !== 0)
                writer.uint32(8).int32(message.terminalId);
            if (message.success != null && $Object.hasOwnProperty.call(message, "success") && message.success !== false)
                writer.uint32(16).bool(message.success);
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
                writer.uint32(26).string(message.message);
            if (message.pid != null && $Object.hasOwnProperty.call(message, "pid") && message.pid !== 0)
                writer.uint32(32).uint32(message.pid);
            if (message.serviceId != null && $Object.hasOwnProperty.call(message, "serviceId") && message.serviceId !== "")
                writer.uint32(42).string(message.serviceId);
            if (message.persistentSessions != null && message.persistentSessions.length)
                writer.uint32(50).int32s(message.persistentSessions);
            if (message.replayTerminalOutput != null && $Object.hasOwnProperty.call(message, "replayTerminalOutput") && message.replayTerminalOutput !== false)
                writer.uint32(56).bool(message.replayTerminalOutput);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TerminalOpened.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TerminalOpened(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.terminalId = value;
                        else
                            delete message.terminalId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.success = value;
                        else
                            delete message.success;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.uint32())
                            message.pid = value;
                        else
                            delete message.pid;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.serviceId = value;
                        else
                            delete message.serviceId;
                        continue;
                    }
                case 6: {
                        if (wireType === 2) {
                            if (!(message.persistentSessions && message.persistentSessions.length))
                                message.persistentSessions = [];
                            reader.int32s(message.persistentSessions);
                            continue;
                        }
                        if (wireType !== 0)
                            break;
                        if (!(message.persistentSessions && message.persistentSessions.length))
                            message.persistentSessions = [];
                        message.persistentSessions.push(reader.int32());
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.replayTerminalOutput = value;
                        else
                            delete message.replayTerminalOutput;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TerminalOpened.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TerminalOpened)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TerminalOpened: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TerminalOpened();
            if (object.terminalId != null)
                if ($Number(object.terminalId) !== 0)
                    message.terminalId = object.terminalId | 0;
            if (object.success != null)
                if (object.success)
                    message.success = $Boolean(object.success);
            if (object.message != null)
                if (typeof object.message !== "string" || object.message.length)
                    message.message = $String(object.message);
            if (object.pid != null)
                if ($Number(object.pid) !== 0)
                    message.pid = object.pid >>> 0;
            if (object.serviceId != null)
                if (typeof object.serviceId !== "string" || object.serviceId.length)
                    message.serviceId = $String(object.serviceId);
            if (object.persistentSessions) {
                if (!$Array.isArray(object.persistentSessions))
                    throw $TypeError(".hbb.TerminalOpened.persistentSessions: array expected");
                message.persistentSessions = $Array(object.persistentSessions.length);
                for (let i = 0; i < object.persistentSessions.length; ++i)
                    message.persistentSessions[i] = object.persistentSessions[i] | 0;
            }
            if (object.replayTerminalOutput != null)
                if (object.replayTerminalOutput)
                    message.replayTerminalOutput = $Boolean(object.replayTerminalOutput);
            return message;
        };

        TerminalOpened.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.persistentSessions = [];
            if (options.defaults) {
                object.terminalId = 0;
                object.success = false;
                object.message = "";
                object.pid = 0;
                object.serviceId = "";
                object.replayTerminalOutput = false;
            }
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId"))
                object.terminalId = message.terminalId;
            if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
                object.success = message.success;
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            if (message.pid != null && $Object.hasOwnProperty.call(message, "pid"))
                object.pid = message.pid;
            if (message.serviceId != null && $Object.hasOwnProperty.call(message, "serviceId"))
                object.serviceId = message.serviceId;
            if (message.persistentSessions && message.persistentSessions.length) {
                object.persistentSessions = $Array(message.persistentSessions.length);
                for (let j = 0; j < message.persistentSessions.length; ++j)
                    object.persistentSessions[j] = message.persistentSessions[j];
            }
            if (message.replayTerminalOutput != null && $Object.hasOwnProperty.call(message, "replayTerminalOutput"))
                object.replayTerminalOutput = message.replayTerminalOutput;
            return object;
        };

        TerminalOpened.prototype.toJSON = function() {
            return TerminalOpened.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TerminalOpened;
    })();

    hbb.TerminalClosed = (function() {

        const TerminalClosed = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TerminalClosed.prototype.terminalId = 0;
        TerminalClosed.prototype.exitCode = 0;

        TerminalClosed.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId") && message.terminalId !== 0)
                writer.uint32(8).int32(message.terminalId);
            if (message.exitCode != null && $Object.hasOwnProperty.call(message, "exitCode") && message.exitCode !== 0)
                writer.uint32(16).int32(message.exitCode);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TerminalClosed.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TerminalClosed(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.terminalId = value;
                        else
                            delete message.terminalId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.exitCode = value;
                        else
                            delete message.exitCode;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TerminalClosed.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TerminalClosed)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TerminalClosed: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TerminalClosed();
            if (object.terminalId != null)
                if ($Number(object.terminalId) !== 0)
                    message.terminalId = object.terminalId | 0;
            if (object.exitCode != null)
                if ($Number(object.exitCode) !== 0)
                    message.exitCode = object.exitCode | 0;
            return message;
        };

        TerminalClosed.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.terminalId = 0;
                object.exitCode = 0;
            }
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId"))
                object.terminalId = message.terminalId;
            if (message.exitCode != null && $Object.hasOwnProperty.call(message, "exitCode"))
                object.exitCode = message.exitCode;
            return object;
        };

        TerminalClosed.prototype.toJSON = function() {
            return TerminalClosed.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TerminalClosed;
    })();

    hbb.TerminalError = (function() {

        const TerminalError = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TerminalError.prototype.terminalId = 0;
        TerminalError.prototype.message = "";

        TerminalError.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId") && message.terminalId !== 0)
                writer.uint32(8).int32(message.terminalId);
            if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
                writer.uint32(18).string(message.message);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TerminalError.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TerminalError(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.terminalId = value;
                        else
                            delete message.terminalId;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.message = value;
                        else
                            delete message.message;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TerminalError.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TerminalError)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TerminalError: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TerminalError();
            if (object.terminalId != null)
                if ($Number(object.terminalId) !== 0)
                    message.terminalId = object.terminalId | 0;
            if (object.message != null)
                if (typeof object.message !== "string" || object.message.length)
                    message.message = $String(object.message);
            return message;
        };

        TerminalError.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.terminalId = 0;
                object.message = "";
            }
            if (message.terminalId != null && $Object.hasOwnProperty.call(message, "terminalId"))
                object.terminalId = message.terminalId;
            if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            return object;
        };

        TerminalError.prototype.toJSON = function() {
            return TerminalError.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TerminalError;
    })();

    hbb.TerminalResponse = (function() {

        const TerminalResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TerminalResponse.prototype.opened = null;
        TerminalResponse.prototype.data = null;
        TerminalResponse.prototype.closed = null;
        TerminalResponse.prototype.error = null;

        let $oneOfFields;

        $Object.defineProperty(TerminalResponse.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["opened", "data", "closed", "error"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        TerminalResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.opened != null && $Object.hasOwnProperty.call(message, "opened"))
                $root.hbb.TerminalOpened.encode(message.opened, writer.uint32(10).fork(), _depth + 1).ldelim();
            if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
                $root.hbb.TerminalData.encode(message.data, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.closed != null && $Object.hasOwnProperty.call(message, "closed"))
                $root.hbb.TerminalClosed.encode(message.closed, writer.uint32(26).fork(), _depth + 1).ldelim();
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                $root.hbb.TerminalError.encode(message.error, writer.uint32(34).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TerminalResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TerminalResponse();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        message.opened = $root.hbb.TerminalOpened.decode(reader, reader.uint32(), $undefined, _depth + 1, message.opened);
                        message.union = "opened";
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.data = $root.hbb.TerminalData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.data);
                        message.union = "data";
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.closed = $root.hbb.TerminalClosed.decode(reader, reader.uint32(), $undefined, _depth + 1, message.closed);
                        message.union = "closed";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.error = $root.hbb.TerminalError.decode(reader, reader.uint32(), $undefined, _depth + 1, message.error);
                        message.union = "error";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TerminalResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TerminalResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TerminalResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TerminalResponse();
            if (object.opened != null) {
                if (!$util.isObject(object.opened))
                    throw $TypeError(".hbb.TerminalResponse.opened: object expected");
                message.opened = $root.hbb.TerminalOpened.fromObject(object.opened, _depth + 1);
            }
            if (object.data != null) {
                if (!$util.isObject(object.data))
                    throw $TypeError(".hbb.TerminalResponse.data: object expected");
                message.data = $root.hbb.TerminalData.fromObject(object.data, _depth + 1);
            }
            if (object.closed != null) {
                if (!$util.isObject(object.closed))
                    throw $TypeError(".hbb.TerminalResponse.closed: object expected");
                message.closed = $root.hbb.TerminalClosed.fromObject(object.closed, _depth + 1);
            }
            if (object.error != null) {
                if (!$util.isObject(object.error))
                    throw $TypeError(".hbb.TerminalResponse.error: object expected");
                message.error = $root.hbb.TerminalError.fromObject(object.error, _depth + 1);
            }
            return message;
        };

        TerminalResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.opened != null && $Object.hasOwnProperty.call(message, "opened")) {
                object.opened = $root.hbb.TerminalOpened.toObject(message.opened, options, _depth + 1);
                if (options.oneofs)
                    object.union = "opened";
            }
            if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
                object.data = $root.hbb.TerminalData.toObject(message.data, options, _depth + 1);
                if (options.oneofs)
                    object.union = "data";
            }
            if (message.closed != null && $Object.hasOwnProperty.call(message, "closed")) {
                object.closed = $root.hbb.TerminalClosed.toObject(message.closed, options, _depth + 1);
                if (options.oneofs)
                    object.union = "closed";
            }
            if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
                object.error = $root.hbb.TerminalError.toObject(message.error, options, _depth + 1);
                if (options.oneofs)
                    object.union = "error";
            }
            return object;
        };

        TerminalResponse.prototype.toJSON = function() {
            return TerminalResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TerminalResponse;
    })();

    hbb.Message = (function() {

        const Message = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        Message.prototype.signedId = null;
        Message.prototype.publicKey = null;
        Message.prototype.testDelay = null;
        Message.prototype.videoFrame = null;
        Message.prototype.loginRequest = null;
        Message.prototype.loginResponse = null;
        Message.prototype.hash = null;
        Message.prototype.mouseEvent = null;
        Message.prototype.audioFrame = null;
        Message.prototype.cursorData = null;
        Message.prototype.cursorPosition = null;
        Message.prototype.cursorId = null;
        Message.prototype.keyEvent = null;
        Message.prototype.clipboard = null;
        Message.prototype.fileAction = null;
        Message.prototype.fileResponse = null;
        Message.prototype.misc = null;
        Message.prototype.cliprdr = null;
        Message.prototype.messageBox = null;
        Message.prototype.switchSidesResponse = null;
        Message.prototype.voiceCallRequest = null;
        Message.prototype.voiceCallResponse = null;
        Message.prototype.peerInfo = null;
        Message.prototype.pointerDeviceEvent = null;
        Message.prototype.auth_2fa = null;
        Message.prototype.multiClipboards = null;
        Message.prototype.screenshotRequest = null;
        Message.prototype.screenshotResponse = null;
        Message.prototype.terminalAction = null;
        Message.prototype.terminalResponse = null;

        let $oneOfFields;

        $Object.defineProperty(Message.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["signedId", "publicKey", "testDelay", "videoFrame", "loginRequest", "loginResponse", "hash", "mouseEvent", "audioFrame", "cursorData", "cursorPosition", "cursorId", "keyEvent", "clipboard", "fileAction", "fileResponse", "misc", "cliprdr", "messageBox", "switchSidesResponse", "voiceCallRequest", "voiceCallResponse", "peerInfo", "pointerDeviceEvent", "auth_2fa", "multiClipboards", "screenshotRequest", "screenshotResponse", "terminalAction", "terminalResponse"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Message.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.signedId != null && $Object.hasOwnProperty.call(message, "signedId"))
                $root.hbb.SignedId.encode(message.signedId, writer.uint32(26).fork(), _depth + 1).ldelim();
            if (message.publicKey != null && $Object.hasOwnProperty.call(message, "publicKey"))
                $root.hbb.PublicKey.encode(message.publicKey, writer.uint32(34).fork(), _depth + 1).ldelim();
            if (message.testDelay != null && $Object.hasOwnProperty.call(message, "testDelay"))
                $root.hbb.TestDelay.encode(message.testDelay, writer.uint32(42).fork(), _depth + 1).ldelim();
            if (message.videoFrame != null && $Object.hasOwnProperty.call(message, "videoFrame"))
                $root.hbb.VideoFrame.encode(message.videoFrame, writer.uint32(50).fork(), _depth + 1).ldelim();
            if (message.loginRequest != null && $Object.hasOwnProperty.call(message, "loginRequest"))
                $root.hbb.LoginRequest.encode(message.loginRequest, writer.uint32(58).fork(), _depth + 1).ldelim();
            if (message.loginResponse != null && $Object.hasOwnProperty.call(message, "loginResponse"))
                $root.hbb.LoginResponse.encode(message.loginResponse, writer.uint32(66).fork(), _depth + 1).ldelim();
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash"))
                $root.hbb.Hash.encode(message.hash, writer.uint32(74).fork(), _depth + 1).ldelim();
            if (message.mouseEvent != null && $Object.hasOwnProperty.call(message, "mouseEvent"))
                $root.hbb.MouseEvent.encode(message.mouseEvent, writer.uint32(82).fork(), _depth + 1).ldelim();
            if (message.audioFrame != null && $Object.hasOwnProperty.call(message, "audioFrame"))
                $root.hbb.AudioFrame.encode(message.audioFrame, writer.uint32(90).fork(), _depth + 1).ldelim();
            if (message.cursorData != null && $Object.hasOwnProperty.call(message, "cursorData"))
                $root.hbb.CursorData.encode(message.cursorData, writer.uint32(98).fork(), _depth + 1).ldelim();
            if (message.cursorPosition != null && $Object.hasOwnProperty.call(message, "cursorPosition"))
                $root.hbb.CursorPosition.encode(message.cursorPosition, writer.uint32(106).fork(), _depth + 1).ldelim();
            if (message.cursorId != null && $Object.hasOwnProperty.call(message, "cursorId"))
                writer.uint32(112).uint64(message.cursorId);
            if (message.keyEvent != null && $Object.hasOwnProperty.call(message, "keyEvent"))
                $root.hbb.KeyEvent.encode(message.keyEvent, writer.uint32(122).fork(), _depth + 1).ldelim();
            if (message.clipboard != null && $Object.hasOwnProperty.call(message, "clipboard"))
                $root.hbb.Clipboard.encode(message.clipboard, writer.uint32(130).fork(), _depth + 1).ldelim();
            if (message.fileAction != null && $Object.hasOwnProperty.call(message, "fileAction"))
                $root.hbb.FileAction.encode(message.fileAction, writer.uint32(138).fork(), _depth + 1).ldelim();
            if (message.fileResponse != null && $Object.hasOwnProperty.call(message, "fileResponse"))
                $root.hbb.FileResponse.encode(message.fileResponse, writer.uint32(146).fork(), _depth + 1).ldelim();
            if (message.misc != null && $Object.hasOwnProperty.call(message, "misc"))
                $root.hbb.Misc.encode(message.misc, writer.uint32(154).fork(), _depth + 1).ldelim();
            if (message.cliprdr != null && $Object.hasOwnProperty.call(message, "cliprdr"))
                $root.hbb.Cliprdr.encode(message.cliprdr, writer.uint32(162).fork(), _depth + 1).ldelim();
            if (message.messageBox != null && $Object.hasOwnProperty.call(message, "messageBox"))
                $root.hbb.MessageBox.encode(message.messageBox, writer.uint32(170).fork(), _depth + 1).ldelim();
            if (message.switchSidesResponse != null && $Object.hasOwnProperty.call(message, "switchSidesResponse"))
                $root.hbb.SwitchSidesResponse.encode(message.switchSidesResponse, writer.uint32(178).fork(), _depth + 1).ldelim();
            if (message.voiceCallRequest != null && $Object.hasOwnProperty.call(message, "voiceCallRequest"))
                $root.hbb.VoiceCallRequest.encode(message.voiceCallRequest, writer.uint32(186).fork(), _depth + 1).ldelim();
            if (message.voiceCallResponse != null && $Object.hasOwnProperty.call(message, "voiceCallResponse"))
                $root.hbb.VoiceCallResponse.encode(message.voiceCallResponse, writer.uint32(194).fork(), _depth + 1).ldelim();
            if (message.peerInfo != null && $Object.hasOwnProperty.call(message, "peerInfo"))
                $root.hbb.PeerInfo.encode(message.peerInfo, writer.uint32(202).fork(), _depth + 1).ldelim();
            if (message.pointerDeviceEvent != null && $Object.hasOwnProperty.call(message, "pointerDeviceEvent"))
                $root.hbb.PointerDeviceEvent.encode(message.pointerDeviceEvent, writer.uint32(210).fork(), _depth + 1).ldelim();
            if (message.auth_2fa != null && $Object.hasOwnProperty.call(message, "auth_2fa"))
                $root.hbb.Auth2FA.encode(message.auth_2fa, writer.uint32(218).fork(), _depth + 1).ldelim();
            if (message.multiClipboards != null && $Object.hasOwnProperty.call(message, "multiClipboards"))
                $root.hbb.MultiClipboards.encode(message.multiClipboards, writer.uint32(226).fork(), _depth + 1).ldelim();
            if (message.screenshotRequest != null && $Object.hasOwnProperty.call(message, "screenshotRequest"))
                $root.hbb.ScreenshotRequest.encode(message.screenshotRequest, writer.uint32(234).fork(), _depth + 1).ldelim();
            if (message.screenshotResponse != null && $Object.hasOwnProperty.call(message, "screenshotResponse"))
                $root.hbb.ScreenshotResponse.encode(message.screenshotResponse, writer.uint32(242).fork(), _depth + 1).ldelim();
            if (message.terminalAction != null && $Object.hasOwnProperty.call(message, "terminalAction"))
                $root.hbb.TerminalAction.encode(message.terminalAction, writer.uint32(250).fork(), _depth + 1).ldelim();
            if (message.terminalResponse != null && $Object.hasOwnProperty.call(message, "terminalResponse"))
                $root.hbb.TerminalResponse.encode(message.terminalResponse, writer.uint32(258).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        Message.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.Message();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 3: {
                        if (wireType !== 2)
                            break;
                        message.signedId = $root.hbb.SignedId.decode(reader, reader.uint32(), $undefined, _depth + 1, message.signedId);
                        message.union = "signedId";
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.publicKey = $root.hbb.PublicKey.decode(reader, reader.uint32(), $undefined, _depth + 1, message.publicKey);
                        message.union = "publicKey";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.testDelay = $root.hbb.TestDelay.decode(reader, reader.uint32(), $undefined, _depth + 1, message.testDelay);
                        message.union = "testDelay";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.videoFrame = $root.hbb.VideoFrame.decode(reader, reader.uint32(), $undefined, _depth + 1, message.videoFrame);
                        message.union = "videoFrame";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.loginRequest = $root.hbb.LoginRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.loginRequest);
                        message.union = "loginRequest";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.loginResponse = $root.hbb.LoginResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.loginResponse);
                        message.union = "loginResponse";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.hash = $root.hbb.Hash.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hash);
                        message.union = "hash";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.mouseEvent = $root.hbb.MouseEvent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.mouseEvent);
                        message.union = "mouseEvent";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.audioFrame = $root.hbb.AudioFrame.decode(reader, reader.uint32(), $undefined, _depth + 1, message.audioFrame);
                        message.union = "audioFrame";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.cursorData = $root.hbb.CursorData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cursorData);
                        message.union = "cursorData";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.cursorPosition = $root.hbb.CursorPosition.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cursorPosition);
                        message.union = "cursorPosition";
                        continue;
                    }
                case 14: {
                        if (wireType !== 0)
                            break;
                        message.cursorId = reader.uint64();
                        message.union = "cursorId";
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.keyEvent = $root.hbb.KeyEvent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.keyEvent);
                        message.union = "keyEvent";
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.clipboard = $root.hbb.Clipboard.decode(reader, reader.uint32(), $undefined, _depth + 1, message.clipboard);
                        message.union = "clipboard";
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.fileAction = $root.hbb.FileAction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileAction);
                        message.union = "fileAction";
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.fileResponse = $root.hbb.FileResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fileResponse);
                        message.union = "fileResponse";
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        message.misc = $root.hbb.Misc.decode(reader, reader.uint32(), $undefined, _depth + 1, message.misc);
                        message.union = "misc";
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        message.cliprdr = $root.hbb.Cliprdr.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cliprdr);
                        message.union = "cliprdr";
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        message.messageBox = $root.hbb.MessageBox.decode(reader, reader.uint32(), $undefined, _depth + 1, message.messageBox);
                        message.union = "messageBox";
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.switchSidesResponse = $root.hbb.SwitchSidesResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.switchSidesResponse);
                        message.union = "switchSidesResponse";
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        message.voiceCallRequest = $root.hbb.VoiceCallRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.voiceCallRequest);
                        message.union = "voiceCallRequest";
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        message.voiceCallResponse = $root.hbb.VoiceCallResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.voiceCallResponse);
                        message.union = "voiceCallResponse";
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        message.peerInfo = $root.hbb.PeerInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.peerInfo);
                        message.union = "peerInfo";
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        message.pointerDeviceEvent = $root.hbb.PointerDeviceEvent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.pointerDeviceEvent);
                        message.union = "pointerDeviceEvent";
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        message.auth_2fa = $root.hbb.Auth2FA.decode(reader, reader.uint32(), $undefined, _depth + 1, message.auth_2fa);
                        message.union = "auth_2fa";
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        message.multiClipboards = $root.hbb.MultiClipboards.decode(reader, reader.uint32(), $undefined, _depth + 1, message.multiClipboards);
                        message.union = "multiClipboards";
                        continue;
                    }
                case 29: {
                        if (wireType !== 2)
                            break;
                        message.screenshotRequest = $root.hbb.ScreenshotRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.screenshotRequest);
                        message.union = "screenshotRequest";
                        continue;
                    }
                case 30: {
                        if (wireType !== 2)
                            break;
                        message.screenshotResponse = $root.hbb.ScreenshotResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.screenshotResponse);
                        message.union = "screenshotResponse";
                        continue;
                    }
                case 31: {
                        if (wireType !== 2)
                            break;
                        message.terminalAction = $root.hbb.TerminalAction.decode(reader, reader.uint32(), $undefined, _depth + 1, message.terminalAction);
                        message.union = "terminalAction";
                        continue;
                    }
                case 32: {
                        if (wireType !== 2)
                            break;
                        message.terminalResponse = $root.hbb.TerminalResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.terminalResponse);
                        message.union = "terminalResponse";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        Message.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.Message)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.Message: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.Message();
            if (object.signedId != null) {
                if (!$util.isObject(object.signedId))
                    throw $TypeError(".hbb.Message.signedId: object expected");
                message.signedId = $root.hbb.SignedId.fromObject(object.signedId, _depth + 1);
            }
            if (object.publicKey != null) {
                if (!$util.isObject(object.publicKey))
                    throw $TypeError(".hbb.Message.publicKey: object expected");
                message.publicKey = $root.hbb.PublicKey.fromObject(object.publicKey, _depth + 1);
            }
            if (object.testDelay != null) {
                if (!$util.isObject(object.testDelay))
                    throw $TypeError(".hbb.Message.testDelay: object expected");
                message.testDelay = $root.hbb.TestDelay.fromObject(object.testDelay, _depth + 1);
            }
            if (object.videoFrame != null) {
                if (!$util.isObject(object.videoFrame))
                    throw $TypeError(".hbb.Message.videoFrame: object expected");
                message.videoFrame = $root.hbb.VideoFrame.fromObject(object.videoFrame, _depth + 1);
            }
            if (object.loginRequest != null) {
                if (!$util.isObject(object.loginRequest))
                    throw $TypeError(".hbb.Message.loginRequest: object expected");
                message.loginRequest = $root.hbb.LoginRequest.fromObject(object.loginRequest, _depth + 1);
            }
            if (object.loginResponse != null) {
                if (!$util.isObject(object.loginResponse))
                    throw $TypeError(".hbb.Message.loginResponse: object expected");
                message.loginResponse = $root.hbb.LoginResponse.fromObject(object.loginResponse, _depth + 1);
            }
            if (object.hash != null) {
                if (!$util.isObject(object.hash))
                    throw $TypeError(".hbb.Message.hash: object expected");
                message.hash = $root.hbb.Hash.fromObject(object.hash, _depth + 1);
            }
            if (object.mouseEvent != null) {
                if (!$util.isObject(object.mouseEvent))
                    throw $TypeError(".hbb.Message.mouseEvent: object expected");
                message.mouseEvent = $root.hbb.MouseEvent.fromObject(object.mouseEvent, _depth + 1);
            }
            if (object.audioFrame != null) {
                if (!$util.isObject(object.audioFrame))
                    throw $TypeError(".hbb.Message.audioFrame: object expected");
                message.audioFrame = $root.hbb.AudioFrame.fromObject(object.audioFrame, _depth + 1);
            }
            if (object.cursorData != null) {
                if (!$util.isObject(object.cursorData))
                    throw $TypeError(".hbb.Message.cursorData: object expected");
                message.cursorData = $root.hbb.CursorData.fromObject(object.cursorData, _depth + 1);
            }
            if (object.cursorPosition != null) {
                if (!$util.isObject(object.cursorPosition))
                    throw $TypeError(".hbb.Message.cursorPosition: object expected");
                message.cursorPosition = $root.hbb.CursorPosition.fromObject(object.cursorPosition, _depth + 1);
            }
            if (object.cursorId != null)
                if ($util.Long)
                    message.cursorId = $util.Long.fromValue(object.cursorId, true);
                else if (typeof object.cursorId === "string")
                    message.cursorId = $parseInt(object.cursorId, 10);
                else if (typeof object.cursorId === "number")
                    message.cursorId = object.cursorId;
                else if (typeof object.cursorId === "object")
                    message.cursorId = new $util.LongBits(object.cursorId.low >>> 0, object.cursorId.high >>> 0).toNumber(true);
            if (object.keyEvent != null) {
                if (!$util.isObject(object.keyEvent))
                    throw $TypeError(".hbb.Message.keyEvent: object expected");
                message.keyEvent = $root.hbb.KeyEvent.fromObject(object.keyEvent, _depth + 1);
            }
            if (object.clipboard != null) {
                if (!$util.isObject(object.clipboard))
                    throw $TypeError(".hbb.Message.clipboard: object expected");
                message.clipboard = $root.hbb.Clipboard.fromObject(object.clipboard, _depth + 1);
            }
            if (object.fileAction != null) {
                if (!$util.isObject(object.fileAction))
                    throw $TypeError(".hbb.Message.fileAction: object expected");
                message.fileAction = $root.hbb.FileAction.fromObject(object.fileAction, _depth + 1);
            }
            if (object.fileResponse != null) {
                if (!$util.isObject(object.fileResponse))
                    throw $TypeError(".hbb.Message.fileResponse: object expected");
                message.fileResponse = $root.hbb.FileResponse.fromObject(object.fileResponse, _depth + 1);
            }
            if (object.misc != null) {
                if (!$util.isObject(object.misc))
                    throw $TypeError(".hbb.Message.misc: object expected");
                message.misc = $root.hbb.Misc.fromObject(object.misc, _depth + 1);
            }
            if (object.cliprdr != null) {
                if (!$util.isObject(object.cliprdr))
                    throw $TypeError(".hbb.Message.cliprdr: object expected");
                message.cliprdr = $root.hbb.Cliprdr.fromObject(object.cliprdr, _depth + 1);
            }
            if (object.messageBox != null) {
                if (!$util.isObject(object.messageBox))
                    throw $TypeError(".hbb.Message.messageBox: object expected");
                message.messageBox = $root.hbb.MessageBox.fromObject(object.messageBox, _depth + 1);
            }
            if (object.switchSidesResponse != null) {
                if (!$util.isObject(object.switchSidesResponse))
                    throw $TypeError(".hbb.Message.switchSidesResponse: object expected");
                message.switchSidesResponse = $root.hbb.SwitchSidesResponse.fromObject(object.switchSidesResponse, _depth + 1);
            }
            if (object.voiceCallRequest != null) {
                if (!$util.isObject(object.voiceCallRequest))
                    throw $TypeError(".hbb.Message.voiceCallRequest: object expected");
                message.voiceCallRequest = $root.hbb.VoiceCallRequest.fromObject(object.voiceCallRequest, _depth + 1);
            }
            if (object.voiceCallResponse != null) {
                if (!$util.isObject(object.voiceCallResponse))
                    throw $TypeError(".hbb.Message.voiceCallResponse: object expected");
                message.voiceCallResponse = $root.hbb.VoiceCallResponse.fromObject(object.voiceCallResponse, _depth + 1);
            }
            if (object.peerInfo != null) {
                if (!$util.isObject(object.peerInfo))
                    throw $TypeError(".hbb.Message.peerInfo: object expected");
                message.peerInfo = $root.hbb.PeerInfo.fromObject(object.peerInfo, _depth + 1);
            }
            if (object.pointerDeviceEvent != null) {
                if (!$util.isObject(object.pointerDeviceEvent))
                    throw $TypeError(".hbb.Message.pointerDeviceEvent: object expected");
                message.pointerDeviceEvent = $root.hbb.PointerDeviceEvent.fromObject(object.pointerDeviceEvent, _depth + 1);
            }
            if (object.auth_2fa != null) {
                if (!$util.isObject(object.auth_2fa))
                    throw $TypeError(".hbb.Message.auth_2fa: object expected");
                message.auth_2fa = $root.hbb.Auth2FA.fromObject(object.auth_2fa, _depth + 1);
            }
            if (object.multiClipboards != null) {
                if (!$util.isObject(object.multiClipboards))
                    throw $TypeError(".hbb.Message.multiClipboards: object expected");
                message.multiClipboards = $root.hbb.MultiClipboards.fromObject(object.multiClipboards, _depth + 1);
            }
            if (object.screenshotRequest != null) {
                if (!$util.isObject(object.screenshotRequest))
                    throw $TypeError(".hbb.Message.screenshotRequest: object expected");
                message.screenshotRequest = $root.hbb.ScreenshotRequest.fromObject(object.screenshotRequest, _depth + 1);
            }
            if (object.screenshotResponse != null) {
                if (!$util.isObject(object.screenshotResponse))
                    throw $TypeError(".hbb.Message.screenshotResponse: object expected");
                message.screenshotResponse = $root.hbb.ScreenshotResponse.fromObject(object.screenshotResponse, _depth + 1);
            }
            if (object.terminalAction != null) {
                if (!$util.isObject(object.terminalAction))
                    throw $TypeError(".hbb.Message.terminalAction: object expected");
                message.terminalAction = $root.hbb.TerminalAction.fromObject(object.terminalAction, _depth + 1);
            }
            if (object.terminalResponse != null) {
                if (!$util.isObject(object.terminalResponse))
                    throw $TypeError(".hbb.Message.terminalResponse: object expected");
                message.terminalResponse = $root.hbb.TerminalResponse.fromObject(object.terminalResponse, _depth + 1);
            }
            return message;
        };

        Message.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.signedId != null && $Object.hasOwnProperty.call(message, "signedId")) {
                object.signedId = $root.hbb.SignedId.toObject(message.signedId, options, _depth + 1);
                if (options.oneofs)
                    object.union = "signedId";
            }
            if (message.publicKey != null && $Object.hasOwnProperty.call(message, "publicKey")) {
                object.publicKey = $root.hbb.PublicKey.toObject(message.publicKey, options, _depth + 1);
                if (options.oneofs)
                    object.union = "publicKey";
            }
            if (message.testDelay != null && $Object.hasOwnProperty.call(message, "testDelay")) {
                object.testDelay = $root.hbb.TestDelay.toObject(message.testDelay, options, _depth + 1);
                if (options.oneofs)
                    object.union = "testDelay";
            }
            if (message.videoFrame != null && $Object.hasOwnProperty.call(message, "videoFrame")) {
                object.videoFrame = $root.hbb.VideoFrame.toObject(message.videoFrame, options, _depth + 1);
                if (options.oneofs)
                    object.union = "videoFrame";
            }
            if (message.loginRequest != null && $Object.hasOwnProperty.call(message, "loginRequest")) {
                object.loginRequest = $root.hbb.LoginRequest.toObject(message.loginRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "loginRequest";
            }
            if (message.loginResponse != null && $Object.hasOwnProperty.call(message, "loginResponse")) {
                object.loginResponse = $root.hbb.LoginResponse.toObject(message.loginResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "loginResponse";
            }
            if (message.hash != null && $Object.hasOwnProperty.call(message, "hash")) {
                object.hash = $root.hbb.Hash.toObject(message.hash, options, _depth + 1);
                if (options.oneofs)
                    object.union = "hash";
            }
            if (message.mouseEvent != null && $Object.hasOwnProperty.call(message, "mouseEvent")) {
                object.mouseEvent = $root.hbb.MouseEvent.toObject(message.mouseEvent, options, _depth + 1);
                if (options.oneofs)
                    object.union = "mouseEvent";
            }
            if (message.audioFrame != null && $Object.hasOwnProperty.call(message, "audioFrame")) {
                object.audioFrame = $root.hbb.AudioFrame.toObject(message.audioFrame, options, _depth + 1);
                if (options.oneofs)
                    object.union = "audioFrame";
            }
            if (message.cursorData != null && $Object.hasOwnProperty.call(message, "cursorData")) {
                object.cursorData = $root.hbb.CursorData.toObject(message.cursorData, options, _depth + 1);
                if (options.oneofs)
                    object.union = "cursorData";
            }
            if (message.cursorPosition != null && $Object.hasOwnProperty.call(message, "cursorPosition")) {
                object.cursorPosition = $root.hbb.CursorPosition.toObject(message.cursorPosition, options, _depth + 1);
                if (options.oneofs)
                    object.union = "cursorPosition";
            }
            if (message.cursorId != null && $Object.hasOwnProperty.call(message, "cursorId")) {
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.cursorId = typeof message.cursorId === "number" ? $BigInt(message.cursorId) : $util.Long.fromBits(message.cursorId.low >>> 0, message.cursorId.high >>> 0, true).toBigInt();
                else if (typeof message.cursorId === "number")
                    object.cursorId = options.longs === $String ? $String(message.cursorId) : message.cursorId;
                else
                    object.cursorId = options.longs === $String ? $util.Long.prototype.toString.call(message.cursorId) : options.longs === $Number ? new $util.LongBits(message.cursorId.low >>> 0, message.cursorId.high >>> 0).toNumber(true) : message.cursorId;
                if (options.oneofs)
                    object.union = "cursorId";
            }
            if (message.keyEvent != null && $Object.hasOwnProperty.call(message, "keyEvent")) {
                object.keyEvent = $root.hbb.KeyEvent.toObject(message.keyEvent, options, _depth + 1);
                if (options.oneofs)
                    object.union = "keyEvent";
            }
            if (message.clipboard != null && $Object.hasOwnProperty.call(message, "clipboard")) {
                object.clipboard = $root.hbb.Clipboard.toObject(message.clipboard, options, _depth + 1);
                if (options.oneofs)
                    object.union = "clipboard";
            }
            if (message.fileAction != null && $Object.hasOwnProperty.call(message, "fileAction")) {
                object.fileAction = $root.hbb.FileAction.toObject(message.fileAction, options, _depth + 1);
                if (options.oneofs)
                    object.union = "fileAction";
            }
            if (message.fileResponse != null && $Object.hasOwnProperty.call(message, "fileResponse")) {
                object.fileResponse = $root.hbb.FileResponse.toObject(message.fileResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "fileResponse";
            }
            if (message.misc != null && $Object.hasOwnProperty.call(message, "misc")) {
                object.misc = $root.hbb.Misc.toObject(message.misc, options, _depth + 1);
                if (options.oneofs)
                    object.union = "misc";
            }
            if (message.cliprdr != null && $Object.hasOwnProperty.call(message, "cliprdr")) {
                object.cliprdr = $root.hbb.Cliprdr.toObject(message.cliprdr, options, _depth + 1);
                if (options.oneofs)
                    object.union = "cliprdr";
            }
            if (message.messageBox != null && $Object.hasOwnProperty.call(message, "messageBox")) {
                object.messageBox = $root.hbb.MessageBox.toObject(message.messageBox, options, _depth + 1);
                if (options.oneofs)
                    object.union = "messageBox";
            }
            if (message.switchSidesResponse != null && $Object.hasOwnProperty.call(message, "switchSidesResponse")) {
                object.switchSidesResponse = $root.hbb.SwitchSidesResponse.toObject(message.switchSidesResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "switchSidesResponse";
            }
            if (message.voiceCallRequest != null && $Object.hasOwnProperty.call(message, "voiceCallRequest")) {
                object.voiceCallRequest = $root.hbb.VoiceCallRequest.toObject(message.voiceCallRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "voiceCallRequest";
            }
            if (message.voiceCallResponse != null && $Object.hasOwnProperty.call(message, "voiceCallResponse")) {
                object.voiceCallResponse = $root.hbb.VoiceCallResponse.toObject(message.voiceCallResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "voiceCallResponse";
            }
            if (message.peerInfo != null && $Object.hasOwnProperty.call(message, "peerInfo")) {
                object.peerInfo = $root.hbb.PeerInfo.toObject(message.peerInfo, options, _depth + 1);
                if (options.oneofs)
                    object.union = "peerInfo";
            }
            if (message.pointerDeviceEvent != null && $Object.hasOwnProperty.call(message, "pointerDeviceEvent")) {
                object.pointerDeviceEvent = $root.hbb.PointerDeviceEvent.toObject(message.pointerDeviceEvent, options, _depth + 1);
                if (options.oneofs)
                    object.union = "pointerDeviceEvent";
            }
            if (message.auth_2fa != null && $Object.hasOwnProperty.call(message, "auth_2fa")) {
                object.auth_2fa = $root.hbb.Auth2FA.toObject(message.auth_2fa, options, _depth + 1);
                if (options.oneofs)
                    object.union = "auth_2fa";
            }
            if (message.multiClipboards != null && $Object.hasOwnProperty.call(message, "multiClipboards")) {
                object.multiClipboards = $root.hbb.MultiClipboards.toObject(message.multiClipboards, options, _depth + 1);
                if (options.oneofs)
                    object.union = "multiClipboards";
            }
            if (message.screenshotRequest != null && $Object.hasOwnProperty.call(message, "screenshotRequest")) {
                object.screenshotRequest = $root.hbb.ScreenshotRequest.toObject(message.screenshotRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "screenshotRequest";
            }
            if (message.screenshotResponse != null && $Object.hasOwnProperty.call(message, "screenshotResponse")) {
                object.screenshotResponse = $root.hbb.ScreenshotResponse.toObject(message.screenshotResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "screenshotResponse";
            }
            if (message.terminalAction != null && $Object.hasOwnProperty.call(message, "terminalAction")) {
                object.terminalAction = $root.hbb.TerminalAction.toObject(message.terminalAction, options, _depth + 1);
                if (options.oneofs)
                    object.union = "terminalAction";
            }
            if (message.terminalResponse != null && $Object.hasOwnProperty.call(message, "terminalResponse")) {
                object.terminalResponse = $root.hbb.TerminalResponse.toObject(message.terminalResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "terminalResponse";
            }
            return object;
        };

        Message.prototype.toJSON = function() {
            return Message.toObject(this, $protobuf.util.toJSONOptions);
        };

        return Message;
    })();

    hbb.RegisterPeer = (function() {

        const RegisterPeer = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        RegisterPeer.prototype.id = "";
        RegisterPeer.prototype.serial = 0;

        RegisterPeer.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(10).string(message.id);
            if (message.serial != null && $Object.hasOwnProperty.call(message, "serial") && message.serial !== 0)
                writer.uint32(16).int32(message.serial);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RegisterPeer.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.RegisterPeer(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.serial = value;
                        else
                            delete message.serial;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        RegisterPeer.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.RegisterPeer)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.RegisterPeer: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.RegisterPeer();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.serial != null)
                if ($Number(object.serial) !== 0)
                    message.serial = object.serial | 0;
            return message;
        };

        RegisterPeer.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.serial = 0;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.serial != null && $Object.hasOwnProperty.call(message, "serial"))
                object.serial = message.serial;
            return object;
        };

        RegisterPeer.prototype.toJSON = function() {
            return RegisterPeer.toObject(this, $protobuf.util.toJSONOptions);
        };

        return RegisterPeer;
    })();

    hbb.ConnType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "DEFAULT_CONN"] = 0;
        values[valuesById[1] = "FILE_TRANSFER"] = 1;
        values[valuesById[2] = "PORT_FORWARD"] = 2;
        values[valuesById[3] = "RDP"] = 3;
        values[valuesById[4] = "VIEW_CAMERA"] = 4;
        values[valuesById[5] = "TERMINAL"] = 5;
        return values;
    })();

    hbb.RegisterPeerResponse = (function() {

        const RegisterPeerResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        RegisterPeerResponse.prototype.requestPk = false;

        RegisterPeerResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.requestPk != null && $Object.hasOwnProperty.call(message, "requestPk") && message.requestPk !== false)
                writer.uint32(16).bool(message.requestPk);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RegisterPeerResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.RegisterPeerResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.requestPk = value;
                        else
                            delete message.requestPk;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        RegisterPeerResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.RegisterPeerResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.RegisterPeerResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.RegisterPeerResponse();
            if (object.requestPk != null)
                if (object.requestPk)
                    message.requestPk = $Boolean(object.requestPk);
            return message;
        };

        RegisterPeerResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.requestPk = false;
            if (message.requestPk != null && $Object.hasOwnProperty.call(message, "requestPk"))
                object.requestPk = message.requestPk;
            return object;
        };

        RegisterPeerResponse.prototype.toJSON = function() {
            return RegisterPeerResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return RegisterPeerResponse;
    })();

    hbb.PunchHoleRequest = (function() {

        const PunchHoleRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PunchHoleRequest.prototype.id = "";
        PunchHoleRequest.prototype.natType = 0;
        PunchHoleRequest.prototype.licenceKey = "";
        PunchHoleRequest.prototype.connType = 0;
        PunchHoleRequest.prototype.token = "";
        PunchHoleRequest.prototype.version = "";
        PunchHoleRequest.prototype.udpPort = 0;
        PunchHoleRequest.prototype.forceRelay = false;
        PunchHoleRequest.prototype.upnpPort = 0;
        PunchHoleRequest.prototype.socketAddrV6 = $util.newBuffer([]);
        PunchHoleRequest.prototype.switchCode = "";

        PunchHoleRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(10).string(message.id);
            if (message.natType != null && $Object.hasOwnProperty.call(message, "natType") && message.natType !== 0)
                writer.uint32(16).int32(message.natType);
            if (message.licenceKey != null && $Object.hasOwnProperty.call(message, "licenceKey") && message.licenceKey !== "")
                writer.uint32(26).string(message.licenceKey);
            if (message.connType != null && $Object.hasOwnProperty.call(message, "connType") && message.connType !== 0)
                writer.uint32(32).int32(message.connType);
            if (message.token != null && $Object.hasOwnProperty.call(message, "token") && message.token !== "")
                writer.uint32(42).string(message.token);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
                writer.uint32(50).string(message.version);
            if (message.udpPort != null && $Object.hasOwnProperty.call(message, "udpPort") && message.udpPort !== 0)
                writer.uint32(56).int32(message.udpPort);
            if (message.forceRelay != null && $Object.hasOwnProperty.call(message, "forceRelay") && message.forceRelay !== false)
                writer.uint32(64).bool(message.forceRelay);
            if (message.upnpPort != null && $Object.hasOwnProperty.call(message, "upnpPort") && message.upnpPort !== 0)
                writer.uint32(72).int32(message.upnpPort);
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6") && message.socketAddrV6.length)
                writer.uint32(82).bytes(message.socketAddrV6);
            if (message.switchCode != null && $Object.hasOwnProperty.call(message, "switchCode") && message.switchCode !== "")
                writer.uint32(90).string(message.switchCode);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PunchHoleRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PunchHoleRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.natType = value;
                        else
                            delete message.natType;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.licenceKey = value;
                        else
                            delete message.licenceKey;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.connType = value;
                        else
                            delete message.connType;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.udpPort = value;
                        else
                            delete message.udpPort;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.forceRelay = value;
                        else
                            delete message.forceRelay;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.upnpPort = value;
                        else
                            delete message.upnpPort;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddrV6 = value;
                        else
                            delete message.socketAddrV6;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.switchCode = value;
                        else
                            delete message.switchCode;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PunchHoleRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PunchHoleRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PunchHoleRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PunchHoleRequest();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.natType !== 0 && (typeof object.natType !== "string" || $root.hbb.NatType[object.natType] !== 0))
                switch (object.natType) {
                case "UNKNOWN_NAT":
                case 0:
                    message.natType = 0;
                    break;
                case "ASYMMETRIC":
                case 1:
                    message.natType = 1;
                    break;
                case "SYMMETRIC":
                case 2:
                    message.natType = 2;
                    break;
                default:
                    if (typeof object.natType === "number" && (object.natType | 0) === object.natType)
                        message.natType = object.natType;
                }
            if (object.licenceKey != null)
                if (typeof object.licenceKey !== "string" || object.licenceKey.length)
                    message.licenceKey = $String(object.licenceKey);
            if (object.connType !== 0 && (typeof object.connType !== "string" || $root.hbb.ConnType[object.connType] !== 0))
                switch (object.connType) {
                case "DEFAULT_CONN":
                case 0:
                    message.connType = 0;
                    break;
                case "FILE_TRANSFER":
                case 1:
                    message.connType = 1;
                    break;
                case "PORT_FORWARD":
                case 2:
                    message.connType = 2;
                    break;
                case "RDP":
                case 3:
                    message.connType = 3;
                    break;
                case "VIEW_CAMERA":
                case 4:
                    message.connType = 4;
                    break;
                case "TERMINAL":
                case 5:
                    message.connType = 5;
                    break;
                default:
                    if (typeof object.connType === "number" && (object.connType | 0) === object.connType)
                        message.connType = object.connType;
                }
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = $String(object.token);
            if (object.version != null)
                if (typeof object.version !== "string" || object.version.length)
                    message.version = $String(object.version);
            if (object.udpPort != null)
                if ($Number(object.udpPort) !== 0)
                    message.udpPort = object.udpPort | 0;
            if (object.forceRelay != null)
                if (object.forceRelay)
                    message.forceRelay = $Boolean(object.forceRelay);
            if (object.upnpPort != null)
                if ($Number(object.upnpPort) !== 0)
                    message.upnpPort = object.upnpPort | 0;
            if (object.socketAddrV6 != null)
                if (object.socketAddrV6.length)
                    if (typeof object.socketAddrV6 === "string")
                        $util.base64.decode(object.socketAddrV6, message.socketAddrV6 = $util.newBuffer($util.base64.length(object.socketAddrV6)), 0);
                    else if (object.socketAddrV6.length >= 0)
                        message.socketAddrV6 = object.socketAddrV6;
            if (object.switchCode != null)
                if (typeof object.switchCode !== "string" || object.switchCode.length)
                    message.switchCode = $String(object.switchCode);
            return message;
        };

        PunchHoleRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.natType = options.enums === $String ? "UNKNOWN_NAT" : 0;
                object.licenceKey = "";
                object.connType = options.enums === $String ? "DEFAULT_CONN" : 0;
                object.token = "";
                object.version = "";
                object.udpPort = 0;
                object.forceRelay = false;
                object.upnpPort = 0;
                if (options.bytes === $String)
                    object.socketAddrV6 = "";
                else {
                    object.socketAddrV6 = [];
                    if (options.bytes !== $Array)
                        object.socketAddrV6 = $util.newBuffer(object.socketAddrV6);
                }
                object.switchCode = "";
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.natType != null && $Object.hasOwnProperty.call(message, "natType"))
                object.natType = options.enums === $String ? $root.hbb.NatType[message.natType] === $undefined ? message.natType : $root.hbb.NatType[message.natType] : message.natType;
            if (message.licenceKey != null && $Object.hasOwnProperty.call(message, "licenceKey"))
                object.licenceKey = message.licenceKey;
            if (message.connType != null && $Object.hasOwnProperty.call(message, "connType"))
                object.connType = options.enums === $String ? $root.hbb.ConnType[message.connType] === $undefined ? message.connType : $root.hbb.ConnType[message.connType] : message.connType;
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = message.token;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.udpPort != null && $Object.hasOwnProperty.call(message, "udpPort"))
                object.udpPort = message.udpPort;
            if (message.forceRelay != null && $Object.hasOwnProperty.call(message, "forceRelay"))
                object.forceRelay = message.forceRelay;
            if (message.upnpPort != null && $Object.hasOwnProperty.call(message, "upnpPort"))
                object.upnpPort = message.upnpPort;
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6"))
                object.socketAddrV6 = options.bytes === $String ? $util.base64.encode(message.socketAddrV6, 0, message.socketAddrV6.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddrV6) : message.socketAddrV6;
            if (message.switchCode != null && $Object.hasOwnProperty.call(message, "switchCode"))
                object.switchCode = message.switchCode;
            return object;
        };

        PunchHoleRequest.prototype.toJSON = function() {
            return PunchHoleRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return PunchHoleRequest;
    })();

    hbb.ControlPermissions = (function() {

        const ControlPermissions = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ControlPermissions.prototype.permissions = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        ControlPermissions.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.permissions != null && $Object.hasOwnProperty.call(message, "permissions") && (typeof message.permissions === "object" ? message.permissions.low || message.permissions.high : message.permissions !== 0))
                writer.uint32(8).uint64(message.permissions);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ControlPermissions.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ControlPermissions(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                            message.permissions = value;
                        else
                            delete message.permissions;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ControlPermissions.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ControlPermissions)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ControlPermissions: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ControlPermissions();
            if (object.permissions != null)
                if (typeof object.permissions === "object" ? object.permissions.low || object.permissions.high : $Number(object.permissions) !== 0)
                    if ($util.Long)
                        message.permissions = $util.Long.fromValue(object.permissions, true);
                    else if (typeof object.permissions === "string")
                        message.permissions = $parseInt(object.permissions, 10);
                    else if (typeof object.permissions === "number")
                        message.permissions = object.permissions;
                    else if (typeof object.permissions === "object")
                        message.permissions = new $util.LongBits(object.permissions.low >>> 0, object.permissions.high >>> 0).toNumber(true);
            return message;
        };

        ControlPermissions.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.permissions = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
                } else
                    object.permissions = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
            if (message.permissions != null && $Object.hasOwnProperty.call(message, "permissions"))
                if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                    object.permissions = typeof message.permissions === "number" ? $BigInt(message.permissions) : $util.Long.fromBits(message.permissions.low >>> 0, message.permissions.high >>> 0, true).toBigInt();
                else if (typeof message.permissions === "number")
                    object.permissions = options.longs === $String ? $String(message.permissions) : message.permissions;
                else
                    object.permissions = options.longs === $String ? $util.Long.prototype.toString.call(message.permissions) : options.longs === $Number ? new $util.LongBits(message.permissions.low >>> 0, message.permissions.high >>> 0).toNumber(true) : message.permissions;
            return object;
        };

        ControlPermissions.prototype.toJSON = function() {
            return ControlPermissions.toObject(this, $protobuf.util.toJSONOptions);
        };

        ControlPermissions.Permission = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "keyboard"] = 0;
            values[valuesById[1] = "remote_printer"] = 1;
            values[valuesById[2] = "clipboard"] = 2;
            values[valuesById[3] = "file"] = 3;
            values[valuesById[4] = "audio"] = 4;
            values[valuesById[5] = "camera"] = 5;
            values[valuesById[6] = "terminal"] = 6;
            values[valuesById[7] = "tunnel"] = 7;
            values[valuesById[8] = "restart"] = 8;
            values[valuesById[9] = "recording"] = 9;
            values[valuesById[10] = "block_input"] = 10;
            values[valuesById[11] = "remote_modify"] = 11;
            values[valuesById[12] = "privacy_mode"] = 12;
            return values;
        })();

        return ControlPermissions;
    })();

    hbb.ControlledContext = (function() {

        const ControlledContext = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ControlledContext.prototype.connAuditRef = "";

        ControlledContext.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.connAuditRef != null && $Object.hasOwnProperty.call(message, "connAuditRef") && message.connAuditRef !== "")
                writer.uint32(10).string(message.connAuditRef);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ControlledContext.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ControlledContext(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.connAuditRef = value;
                        else
                            delete message.connAuditRef;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ControlledContext.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ControlledContext)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ControlledContext: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ControlledContext();
            if (object.connAuditRef != null)
                if (typeof object.connAuditRef !== "string" || object.connAuditRef.length)
                    message.connAuditRef = $String(object.connAuditRef);
            return message;
        };

        ControlledContext.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.connAuditRef = "";
            if (message.connAuditRef != null && $Object.hasOwnProperty.call(message, "connAuditRef"))
                object.connAuditRef = message.connAuditRef;
            return object;
        };

        ControlledContext.prototype.toJSON = function() {
            return ControlledContext.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ControlledContext;
    })();

    hbb.PunchHole = (function() {

        const PunchHole = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PunchHole.prototype.socketAddr = $util.newBuffer([]);
        PunchHole.prototype.relayServer = "";
        PunchHole.prototype.natType = 0;
        PunchHole.prototype.udpPort = 0;
        PunchHole.prototype.forceRelay = false;
        PunchHole.prototype.upnpPort = 0;
        PunchHole.prototype.socketAddrV6 = $util.newBuffer([]);
        PunchHole.prototype.controlPermissions = null;
        PunchHole.prototype.controlledContext = null;
        PunchHole.prototype.rendezvousToken = $util.newBuffer([]);

        PunchHole.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr") && message.socketAddr.length)
                writer.uint32(10).bytes(message.socketAddr);
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer") && message.relayServer !== "")
                writer.uint32(18).string(message.relayServer);
            if (message.natType != null && $Object.hasOwnProperty.call(message, "natType") && message.natType !== 0)
                writer.uint32(24).int32(message.natType);
            if (message.udpPort != null && $Object.hasOwnProperty.call(message, "udpPort") && message.udpPort !== 0)
                writer.uint32(32).int32(message.udpPort);
            if (message.forceRelay != null && $Object.hasOwnProperty.call(message, "forceRelay") && message.forceRelay !== false)
                writer.uint32(40).bool(message.forceRelay);
            if (message.upnpPort != null && $Object.hasOwnProperty.call(message, "upnpPort") && message.upnpPort !== 0)
                writer.uint32(48).int32(message.upnpPort);
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6") && message.socketAddrV6.length)
                writer.uint32(58).bytes(message.socketAddrV6);
            if (message.controlPermissions != null && $Object.hasOwnProperty.call(message, "controlPermissions"))
                $root.hbb.ControlPermissions.encode(message.controlPermissions, writer.uint32(66).fork(), _depth + 1).ldelim();
            if (message.controlledContext != null && $Object.hasOwnProperty.call(message, "controlledContext"))
                $root.hbb.ControlledContext.encode(message.controlledContext, writer.uint32(74).fork(), _depth + 1).ldelim();
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken") && message.rendezvousToken.length)
                writer.uint32(82).bytes(message.rendezvousToken);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PunchHole.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PunchHole(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddr = value;
                        else
                            delete message.socketAddr;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.relayServer = value;
                        else
                            delete message.relayServer;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.natType = value;
                        else
                            delete message.natType;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.udpPort = value;
                        else
                            delete message.udpPort;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.forceRelay = value;
                        else
                            delete message.forceRelay;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.upnpPort = value;
                        else
                            delete message.upnpPort;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddrV6 = value;
                        else
                            delete message.socketAddrV6;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.controlPermissions = $root.hbb.ControlPermissions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.controlPermissions);
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.controlledContext = $root.hbb.ControlledContext.decode(reader, reader.uint32(), $undefined, _depth + 1, message.controlledContext);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.rendezvousToken = value;
                        else
                            delete message.rendezvousToken;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PunchHole.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PunchHole)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PunchHole: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PunchHole();
            if (object.socketAddr != null)
                if (object.socketAddr.length)
                    if (typeof object.socketAddr === "string")
                        $util.base64.decode(object.socketAddr, message.socketAddr = $util.newBuffer($util.base64.length(object.socketAddr)), 0);
                    else if (object.socketAddr.length >= 0)
                        message.socketAddr = object.socketAddr;
            if (object.relayServer != null)
                if (typeof object.relayServer !== "string" || object.relayServer.length)
                    message.relayServer = $String(object.relayServer);
            if (object.natType !== 0 && (typeof object.natType !== "string" || $root.hbb.NatType[object.natType] !== 0))
                switch (object.natType) {
                case "UNKNOWN_NAT":
                case 0:
                    message.natType = 0;
                    break;
                case "ASYMMETRIC":
                case 1:
                    message.natType = 1;
                    break;
                case "SYMMETRIC":
                case 2:
                    message.natType = 2;
                    break;
                default:
                    if (typeof object.natType === "number" && (object.natType | 0) === object.natType)
                        message.natType = object.natType;
                }
            if (object.udpPort != null)
                if ($Number(object.udpPort) !== 0)
                    message.udpPort = object.udpPort | 0;
            if (object.forceRelay != null)
                if (object.forceRelay)
                    message.forceRelay = $Boolean(object.forceRelay);
            if (object.upnpPort != null)
                if ($Number(object.upnpPort) !== 0)
                    message.upnpPort = object.upnpPort | 0;
            if (object.socketAddrV6 != null)
                if (object.socketAddrV6.length)
                    if (typeof object.socketAddrV6 === "string")
                        $util.base64.decode(object.socketAddrV6, message.socketAddrV6 = $util.newBuffer($util.base64.length(object.socketAddrV6)), 0);
                    else if (object.socketAddrV6.length >= 0)
                        message.socketAddrV6 = object.socketAddrV6;
            if (object.controlPermissions != null) {
                if (!$util.isObject(object.controlPermissions))
                    throw $TypeError(".hbb.PunchHole.controlPermissions: object expected");
                message.controlPermissions = $root.hbb.ControlPermissions.fromObject(object.controlPermissions, _depth + 1);
            }
            if (object.controlledContext != null) {
                if (!$util.isObject(object.controlledContext))
                    throw $TypeError(".hbb.PunchHole.controlledContext: object expected");
                message.controlledContext = $root.hbb.ControlledContext.fromObject(object.controlledContext, _depth + 1);
            }
            if (object.rendezvousToken != null)
                if (object.rendezvousToken.length)
                    if (typeof object.rendezvousToken === "string")
                        $util.base64.decode(object.rendezvousToken, message.rendezvousToken = $util.newBuffer($util.base64.length(object.rendezvousToken)), 0);
                    else if (object.rendezvousToken.length >= 0)
                        message.rendezvousToken = object.rendezvousToken;
            return message;
        };

        PunchHole.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.socketAddr = "";
                else {
                    object.socketAddr = [];
                    if (options.bytes !== $Array)
                        object.socketAddr = $util.newBuffer(object.socketAddr);
                }
                object.relayServer = "";
                object.natType = options.enums === $String ? "UNKNOWN_NAT" : 0;
                object.udpPort = 0;
                object.forceRelay = false;
                object.upnpPort = 0;
                if (options.bytes === $String)
                    object.socketAddrV6 = "";
                else {
                    object.socketAddrV6 = [];
                    if (options.bytes !== $Array)
                        object.socketAddrV6 = $util.newBuffer(object.socketAddrV6);
                }
                object.controlPermissions = null;
                object.controlledContext = null;
                if (options.bytes === $String)
                    object.rendezvousToken = "";
                else {
                    object.rendezvousToken = [];
                    if (options.bytes !== $Array)
                        object.rendezvousToken = $util.newBuffer(object.rendezvousToken);
                }
            }
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr"))
                object.socketAddr = options.bytes === $String ? $util.base64.encode(message.socketAddr, 0, message.socketAddr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddr) : message.socketAddr;
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer"))
                object.relayServer = message.relayServer;
            if (message.natType != null && $Object.hasOwnProperty.call(message, "natType"))
                object.natType = options.enums === $String ? $root.hbb.NatType[message.natType] === $undefined ? message.natType : $root.hbb.NatType[message.natType] : message.natType;
            if (message.udpPort != null && $Object.hasOwnProperty.call(message, "udpPort"))
                object.udpPort = message.udpPort;
            if (message.forceRelay != null && $Object.hasOwnProperty.call(message, "forceRelay"))
                object.forceRelay = message.forceRelay;
            if (message.upnpPort != null && $Object.hasOwnProperty.call(message, "upnpPort"))
                object.upnpPort = message.upnpPort;
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6"))
                object.socketAddrV6 = options.bytes === $String ? $util.base64.encode(message.socketAddrV6, 0, message.socketAddrV6.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddrV6) : message.socketAddrV6;
            if (message.controlPermissions != null && $Object.hasOwnProperty.call(message, "controlPermissions"))
                object.controlPermissions = $root.hbb.ControlPermissions.toObject(message.controlPermissions, options, _depth + 1);
            if (message.controlledContext != null && $Object.hasOwnProperty.call(message, "controlledContext"))
                object.controlledContext = $root.hbb.ControlledContext.toObject(message.controlledContext, options, _depth + 1);
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken"))
                object.rendezvousToken = options.bytes === $String ? $util.base64.encode(message.rendezvousToken, 0, message.rendezvousToken.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.rendezvousToken) : message.rendezvousToken;
            return object;
        };

        PunchHole.prototype.toJSON = function() {
            return PunchHole.toObject(this, $protobuf.util.toJSONOptions);
        };

        return PunchHole;
    })();

    hbb.TestNatRequest = (function() {

        const TestNatRequest = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TestNatRequest.prototype.serial = 0;

        TestNatRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.serial != null && $Object.hasOwnProperty.call(message, "serial") && message.serial !== 0)
                writer.uint32(8).int32(message.serial);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TestNatRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TestNatRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.serial = value;
                        else
                            delete message.serial;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TestNatRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TestNatRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TestNatRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TestNatRequest();
            if (object.serial != null)
                if ($Number(object.serial) !== 0)
                    message.serial = object.serial | 0;
            return message;
        };

        TestNatRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.serial = 0;
            if (message.serial != null && $Object.hasOwnProperty.call(message, "serial"))
                object.serial = message.serial;
            return object;
        };

        TestNatRequest.prototype.toJSON = function() {
            return TestNatRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TestNatRequest;
    })();

    hbb.TestNatResponse = (function() {

        const TestNatResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        TestNatResponse.prototype.port = 0;
        TestNatResponse.prototype.cu = null;

        TestNatResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.port != null && $Object.hasOwnProperty.call(message, "port") && message.port !== 0)
                writer.uint32(8).int32(message.port);
            if (message.cu != null && $Object.hasOwnProperty.call(message, "cu"))
                $root.hbb.ConfigUpdate.encode(message.cu, writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        TestNatResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.TestNatResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.port = value;
                        else
                            delete message.port;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        message.cu = $root.hbb.ConfigUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.cu);
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        TestNatResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.TestNatResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.TestNatResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.TestNatResponse();
            if (object.port != null)
                if ($Number(object.port) !== 0)
                    message.port = object.port | 0;
            if (object.cu != null) {
                if (!$util.isObject(object.cu))
                    throw $TypeError(".hbb.TestNatResponse.cu: object expected");
                message.cu = $root.hbb.ConfigUpdate.fromObject(object.cu, _depth + 1);
            }
            return message;
        };

        TestNatResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.port = 0;
                object.cu = null;
            }
            if (message.port != null && $Object.hasOwnProperty.call(message, "port"))
                object.port = message.port;
            if (message.cu != null && $Object.hasOwnProperty.call(message, "cu"))
                object.cu = $root.hbb.ConfigUpdate.toObject(message.cu, options, _depth + 1);
            return object;
        };

        TestNatResponse.prototype.toJSON = function() {
            return TestNatResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return TestNatResponse;
    })();

    hbb.NatType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN_NAT"] = 0;
        values[valuesById[1] = "ASYMMETRIC"] = 1;
        values[valuesById[2] = "SYMMETRIC"] = 2;
        return values;
    })();

    hbb.PunchHoleSent = (function() {

        const PunchHoleSent = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PunchHoleSent.prototype.socketAddr = $util.newBuffer([]);
        PunchHoleSent.prototype.id = "";
        PunchHoleSent.prototype.relayServer = "";
        PunchHoleSent.prototype.natType = 0;
        PunchHoleSent.prototype.version = "";
        PunchHoleSent.prototype.upnpPort = 0;
        PunchHoleSent.prototype.socketAddrV6 = $util.newBuffer([]);
        PunchHoleSent.prototype.rendezvousToken = $util.newBuffer([]);

        PunchHoleSent.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr") && message.socketAddr.length)
                writer.uint32(10).bytes(message.socketAddr);
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(18).string(message.id);
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer") && message.relayServer !== "")
                writer.uint32(26).string(message.relayServer);
            if (message.natType != null && $Object.hasOwnProperty.call(message, "natType") && message.natType !== 0)
                writer.uint32(32).int32(message.natType);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
                writer.uint32(42).string(message.version);
            if (message.upnpPort != null && $Object.hasOwnProperty.call(message, "upnpPort") && message.upnpPort !== 0)
                writer.uint32(48).int32(message.upnpPort);
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6") && message.socketAddrV6.length)
                writer.uint32(58).bytes(message.socketAddrV6);
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken") && message.rendezvousToken.length)
                writer.uint32(66).bytes(message.rendezvousToken);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PunchHoleSent.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PunchHoleSent(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddr = value;
                        else
                            delete message.socketAddr;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.relayServer = value;
                        else
                            delete message.relayServer;
                        continue;
                    }
                case 4: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.natType = value;
                        else
                            delete message.natType;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.upnpPort = value;
                        else
                            delete message.upnpPort;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddrV6 = value;
                        else
                            delete message.socketAddrV6;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.rendezvousToken = value;
                        else
                            delete message.rendezvousToken;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PunchHoleSent.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PunchHoleSent)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PunchHoleSent: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PunchHoleSent();
            if (object.socketAddr != null)
                if (object.socketAddr.length)
                    if (typeof object.socketAddr === "string")
                        $util.base64.decode(object.socketAddr, message.socketAddr = $util.newBuffer($util.base64.length(object.socketAddr)), 0);
                    else if (object.socketAddr.length >= 0)
                        message.socketAddr = object.socketAddr;
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.relayServer != null)
                if (typeof object.relayServer !== "string" || object.relayServer.length)
                    message.relayServer = $String(object.relayServer);
            if (object.natType !== 0 && (typeof object.natType !== "string" || $root.hbb.NatType[object.natType] !== 0))
                switch (object.natType) {
                case "UNKNOWN_NAT":
                case 0:
                    message.natType = 0;
                    break;
                case "ASYMMETRIC":
                case 1:
                    message.natType = 1;
                    break;
                case "SYMMETRIC":
                case 2:
                    message.natType = 2;
                    break;
                default:
                    if (typeof object.natType === "number" && (object.natType | 0) === object.natType)
                        message.natType = object.natType;
                }
            if (object.version != null)
                if (typeof object.version !== "string" || object.version.length)
                    message.version = $String(object.version);
            if (object.upnpPort != null)
                if ($Number(object.upnpPort) !== 0)
                    message.upnpPort = object.upnpPort | 0;
            if (object.socketAddrV6 != null)
                if (object.socketAddrV6.length)
                    if (typeof object.socketAddrV6 === "string")
                        $util.base64.decode(object.socketAddrV6, message.socketAddrV6 = $util.newBuffer($util.base64.length(object.socketAddrV6)), 0);
                    else if (object.socketAddrV6.length >= 0)
                        message.socketAddrV6 = object.socketAddrV6;
            if (object.rendezvousToken != null)
                if (object.rendezvousToken.length)
                    if (typeof object.rendezvousToken === "string")
                        $util.base64.decode(object.rendezvousToken, message.rendezvousToken = $util.newBuffer($util.base64.length(object.rendezvousToken)), 0);
                    else if (object.rendezvousToken.length >= 0)
                        message.rendezvousToken = object.rendezvousToken;
            return message;
        };

        PunchHoleSent.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.socketAddr = "";
                else {
                    object.socketAddr = [];
                    if (options.bytes !== $Array)
                        object.socketAddr = $util.newBuffer(object.socketAddr);
                }
                object.id = "";
                object.relayServer = "";
                object.natType = options.enums === $String ? "UNKNOWN_NAT" : 0;
                object.version = "";
                object.upnpPort = 0;
                if (options.bytes === $String)
                    object.socketAddrV6 = "";
                else {
                    object.socketAddrV6 = [];
                    if (options.bytes !== $Array)
                        object.socketAddrV6 = $util.newBuffer(object.socketAddrV6);
                }
                if (options.bytes === $String)
                    object.rendezvousToken = "";
                else {
                    object.rendezvousToken = [];
                    if (options.bytes !== $Array)
                        object.rendezvousToken = $util.newBuffer(object.rendezvousToken);
                }
            }
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr"))
                object.socketAddr = options.bytes === $String ? $util.base64.encode(message.socketAddr, 0, message.socketAddr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddr) : message.socketAddr;
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer"))
                object.relayServer = message.relayServer;
            if (message.natType != null && $Object.hasOwnProperty.call(message, "natType"))
                object.natType = options.enums === $String ? $root.hbb.NatType[message.natType] === $undefined ? message.natType : $root.hbb.NatType[message.natType] : message.natType;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.upnpPort != null && $Object.hasOwnProperty.call(message, "upnpPort"))
                object.upnpPort = message.upnpPort;
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6"))
                object.socketAddrV6 = options.bytes === $String ? $util.base64.encode(message.socketAddrV6, 0, message.socketAddrV6.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddrV6) : message.socketAddrV6;
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken"))
                object.rendezvousToken = options.bytes === $String ? $util.base64.encode(message.rendezvousToken, 0, message.rendezvousToken.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.rendezvousToken) : message.rendezvousToken;
            return object;
        };

        PunchHoleSent.prototype.toJSON = function() {
            return PunchHoleSent.toObject(this, $protobuf.util.toJSONOptions);
        };

        return PunchHoleSent;
    })();

    hbb.RegisterPk = (function() {

        const RegisterPk = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        RegisterPk.prototype.id = "";
        RegisterPk.prototype.uuid = $util.newBuffer([]);
        RegisterPk.prototype.pk = $util.newBuffer([]);
        RegisterPk.prototype.oldId = "";
        RegisterPk.prototype.noRegisterDevice = false;

        RegisterPk.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(10).string(message.id);
            if (message.uuid != null && $Object.hasOwnProperty.call(message, "uuid") && message.uuid.length)
                writer.uint32(18).bytes(message.uuid);
            if (message.pk != null && $Object.hasOwnProperty.call(message, "pk") && message.pk.length)
                writer.uint32(26).bytes(message.pk);
            if (message.oldId != null && $Object.hasOwnProperty.call(message, "oldId") && message.oldId !== "")
                writer.uint32(34).string(message.oldId);
            if (message.noRegisterDevice != null && $Object.hasOwnProperty.call(message, "noRegisterDevice") && message.noRegisterDevice !== false)
                writer.uint32(40).bool(message.noRegisterDevice);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RegisterPk.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.RegisterPk(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pk = value;
                        else
                            delete message.pk;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.oldId = value;
                        else
                            delete message.oldId;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.noRegisterDevice = value;
                        else
                            delete message.noRegisterDevice;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        RegisterPk.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.RegisterPk)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.RegisterPk: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.RegisterPk();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.uuid != null)
                if (object.uuid.length)
                    if (typeof object.uuid === "string")
                        $util.base64.decode(object.uuid, message.uuid = $util.newBuffer($util.base64.length(object.uuid)), 0);
                    else if (object.uuid.length >= 0)
                        message.uuid = object.uuid;
            if (object.pk != null)
                if (object.pk.length)
                    if (typeof object.pk === "string")
                        $util.base64.decode(object.pk, message.pk = $util.newBuffer($util.base64.length(object.pk)), 0);
                    else if (object.pk.length >= 0)
                        message.pk = object.pk;
            if (object.oldId != null)
                if (typeof object.oldId !== "string" || object.oldId.length)
                    message.oldId = $String(object.oldId);
            if (object.noRegisterDevice != null)
                if (object.noRegisterDevice)
                    message.noRegisterDevice = $Boolean(object.noRegisterDevice);
            return message;
        };

        RegisterPk.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                if (options.bytes === $String)
                    object.uuid = "";
                else {
                    object.uuid = [];
                    if (options.bytes !== $Array)
                        object.uuid = $util.newBuffer(object.uuid);
                }
                if (options.bytes === $String)
                    object.pk = "";
                else {
                    object.pk = [];
                    if (options.bytes !== $Array)
                        object.pk = $util.newBuffer(object.pk);
                }
                object.oldId = "";
                object.noRegisterDevice = false;
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.uuid != null && $Object.hasOwnProperty.call(message, "uuid"))
                object.uuid = options.bytes === $String ? $util.base64.encode(message.uuid, 0, message.uuid.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.uuid) : message.uuid;
            if (message.pk != null && $Object.hasOwnProperty.call(message, "pk"))
                object.pk = options.bytes === $String ? $util.base64.encode(message.pk, 0, message.pk.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pk) : message.pk;
            if (message.oldId != null && $Object.hasOwnProperty.call(message, "oldId"))
                object.oldId = message.oldId;
            if (message.noRegisterDevice != null && $Object.hasOwnProperty.call(message, "noRegisterDevice"))
                object.noRegisterDevice = message.noRegisterDevice;
            return object;
        };

        RegisterPk.prototype.toJSON = function() {
            return RegisterPk.toObject(this, $protobuf.util.toJSONOptions);
        };

        return RegisterPk;
    })();

    hbb.RegisterPkResponse = (function() {

        const RegisterPkResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        RegisterPkResponse.prototype.result = 0;
        RegisterPkResponse.prototype.keepAlive = 0;

        RegisterPkResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.result != null && $Object.hasOwnProperty.call(message, "result") && message.result !== 0)
                writer.uint32(8).int32(message.result);
            if (message.keepAlive != null && $Object.hasOwnProperty.call(message, "keepAlive") && message.keepAlive !== 0)
                writer.uint32(16).int32(message.keepAlive);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RegisterPkResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.RegisterPkResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.result = value;
                        else
                            delete message.result;
                        continue;
                    }
                case 2: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.keepAlive = value;
                        else
                            delete message.keepAlive;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        RegisterPkResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.RegisterPkResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.RegisterPkResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.RegisterPkResponse();
            if (object.result !== 0 && (typeof object.result !== "string" || $root.hbb.RegisterPkResponse.Result[object.result] !== 0))
                switch (object.result) {
                case "OK":
                case 0:
                    message.result = 0;
                    break;
                case "UUID_MISMATCH":
                case 2:
                    message.result = 2;
                    break;
                case "ID_EXISTS":
                case 3:
                    message.result = 3;
                    break;
                case "TOO_FREQUENT":
                case 4:
                    message.result = 4;
                    break;
                case "INVALID_ID_FORMAT":
                case 5:
                    message.result = 5;
                    break;
                case "NOT_SUPPORT":
                case 6:
                    message.result = 6;
                    break;
                case "SERVER_ERROR":
                case 7:
                    message.result = 7;
                    break;
                case "NOT_DEPLOYED":
                case 8:
                    message.result = 8;
                    break;
                default:
                    if (typeof object.result === "number" && (object.result | 0) === object.result)
                        message.result = object.result;
                }
            if (object.keepAlive != null)
                if ($Number(object.keepAlive) !== 0)
                    message.keepAlive = object.keepAlive | 0;
            return message;
        };

        RegisterPkResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.result = options.enums === $String ? "OK" : 0;
                object.keepAlive = 0;
            }
            if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
                object.result = options.enums === $String ? $root.hbb.RegisterPkResponse.Result[message.result] === $undefined ? message.result : $root.hbb.RegisterPkResponse.Result[message.result] : message.result;
            if (message.keepAlive != null && $Object.hasOwnProperty.call(message, "keepAlive"))
                object.keepAlive = message.keepAlive;
            return object;
        };

        RegisterPkResponse.prototype.toJSON = function() {
            return RegisterPkResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        RegisterPkResponse.Result = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "OK"] = 0;
            values[valuesById[2] = "UUID_MISMATCH"] = 2;
            values[valuesById[3] = "ID_EXISTS"] = 3;
            values[valuesById[4] = "TOO_FREQUENT"] = 4;
            values[valuesById[5] = "INVALID_ID_FORMAT"] = 5;
            values[valuesById[6] = "NOT_SUPPORT"] = 6;
            values[valuesById[7] = "SERVER_ERROR"] = 7;
            values[valuesById[8] = "NOT_DEPLOYED"] = 8;
            return values;
        })();

        return RegisterPkResponse;
    })();

    hbb.PunchHoleResponse = (function() {

        const PunchHoleResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PunchHoleResponse.prototype.socketAddr = $util.newBuffer([]);
        PunchHoleResponse.prototype.pk = $util.newBuffer([]);
        PunchHoleResponse.prototype.failure = 0;
        PunchHoleResponse.prototype.relayServer = "";
        PunchHoleResponse.prototype.natType = null;
        PunchHoleResponse.prototype.isLocal = null;
        PunchHoleResponse.prototype.otherFailure = "";
        PunchHoleResponse.prototype.feedback = 0;
        PunchHoleResponse.prototype.isUdp = false;
        PunchHoleResponse.prototype.upnpPort = 0;
        PunchHoleResponse.prototype.socketAddrV6 = $util.newBuffer([]);

        let $oneOfFields;

        $Object.defineProperty(PunchHoleResponse.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["natType", "isLocal"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PunchHoleResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr") && message.socketAddr.length)
                writer.uint32(10).bytes(message.socketAddr);
            if (message.pk != null && $Object.hasOwnProperty.call(message, "pk") && message.pk.length)
                writer.uint32(18).bytes(message.pk);
            if (message.failure != null && $Object.hasOwnProperty.call(message, "failure") && message.failure !== 0)
                writer.uint32(24).int32(message.failure);
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer") && message.relayServer !== "")
                writer.uint32(34).string(message.relayServer);
            if (message.natType != null && $Object.hasOwnProperty.call(message, "natType"))
                writer.uint32(40).int32(message.natType);
            if (message.isLocal != null && $Object.hasOwnProperty.call(message, "isLocal"))
                writer.uint32(48).bool(message.isLocal);
            if (message.otherFailure != null && $Object.hasOwnProperty.call(message, "otherFailure") && message.otherFailure !== "")
                writer.uint32(58).string(message.otherFailure);
            if (message.feedback != null && $Object.hasOwnProperty.call(message, "feedback") && message.feedback !== 0)
                writer.uint32(64).int32(message.feedback);
            if (message.isUdp != null && $Object.hasOwnProperty.call(message, "isUdp") && message.isUdp !== false)
                writer.uint32(72).bool(message.isUdp);
            if (message.upnpPort != null && $Object.hasOwnProperty.call(message, "upnpPort") && message.upnpPort !== 0)
                writer.uint32(80).int32(message.upnpPort);
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6") && message.socketAddrV6.length)
                writer.uint32(90).bytes(message.socketAddrV6);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PunchHoleResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PunchHoleResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddr = value;
                        else
                            delete message.socketAddr;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.pk = value;
                        else
                            delete message.pk;
                        continue;
                    }
                case 3: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.failure = value;
                        else
                            delete message.failure;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.relayServer = value;
                        else
                            delete message.relayServer;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        message.natType = reader.int32();
                        message.union = "natType";
                        continue;
                    }
                case 6: {
                        if (wireType !== 0)
                            break;
                        message.isLocal = reader.bool();
                        message.union = "isLocal";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.otherFailure = value;
                        else
                            delete message.otherFailure;
                        continue;
                    }
                case 8: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.feedback = value;
                        else
                            delete message.feedback;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.isUdp = value;
                        else
                            delete message.isUdp;
                        continue;
                    }
                case 10: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.upnpPort = value;
                        else
                            delete message.upnpPort;
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddrV6 = value;
                        else
                            delete message.socketAddrV6;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PunchHoleResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PunchHoleResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PunchHoleResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PunchHoleResponse();
            if (object.socketAddr != null)
                if (object.socketAddr.length)
                    if (typeof object.socketAddr === "string")
                        $util.base64.decode(object.socketAddr, message.socketAddr = $util.newBuffer($util.base64.length(object.socketAddr)), 0);
                    else if (object.socketAddr.length >= 0)
                        message.socketAddr = object.socketAddr;
            if (object.pk != null)
                if (object.pk.length)
                    if (typeof object.pk === "string")
                        $util.base64.decode(object.pk, message.pk = $util.newBuffer($util.base64.length(object.pk)), 0);
                    else if (object.pk.length >= 0)
                        message.pk = object.pk;
            if (object.failure !== 0 && (typeof object.failure !== "string" || $root.hbb.PunchHoleResponse.Failure[object.failure] !== 0))
                switch (object.failure) {
                case "ID_NOT_EXIST":
                case 0:
                    message.failure = 0;
                    break;
                case "OFFLINE":
                case 2:
                    message.failure = 2;
                    break;
                case "LICENSE_MISMATCH":
                case 3:
                    message.failure = 3;
                    break;
                case "LICENSE_OVERUSE":
                case 4:
                    message.failure = 4;
                    break;
                default:
                    if (typeof object.failure === "number" && (object.failure | 0) === object.failure)
                        message.failure = object.failure;
                }
            if (object.relayServer != null)
                if (typeof object.relayServer !== "string" || object.relayServer.length)
                    message.relayServer = $String(object.relayServer);
            switch (object.natType) {
            case "UNKNOWN_NAT":
            case 0:
                message.natType = 0;
                break;
            case "ASYMMETRIC":
            case 1:
                message.natType = 1;
                break;
            case "SYMMETRIC":
            case 2:
                message.natType = 2;
                break;
            default:
                if (typeof object.natType === "number" && (object.natType | 0) === object.natType)
                    message.natType = object.natType;
            }
            if (object.isLocal != null)
                message.isLocal = $Boolean(object.isLocal);
            if (object.otherFailure != null)
                if (typeof object.otherFailure !== "string" || object.otherFailure.length)
                    message.otherFailure = $String(object.otherFailure);
            if (object.feedback != null)
                if ($Number(object.feedback) !== 0)
                    message.feedback = object.feedback | 0;
            if (object.isUdp != null)
                if (object.isUdp)
                    message.isUdp = $Boolean(object.isUdp);
            if (object.upnpPort != null)
                if ($Number(object.upnpPort) !== 0)
                    message.upnpPort = object.upnpPort | 0;
            if (object.socketAddrV6 != null)
                if (object.socketAddrV6.length)
                    if (typeof object.socketAddrV6 === "string")
                        $util.base64.decode(object.socketAddrV6, message.socketAddrV6 = $util.newBuffer($util.base64.length(object.socketAddrV6)), 0);
                    else if (object.socketAddrV6.length >= 0)
                        message.socketAddrV6 = object.socketAddrV6;
            return message;
        };

        PunchHoleResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.socketAddr = "";
                else {
                    object.socketAddr = [];
                    if (options.bytes !== $Array)
                        object.socketAddr = $util.newBuffer(object.socketAddr);
                }
                if (options.bytes === $String)
                    object.pk = "";
                else {
                    object.pk = [];
                    if (options.bytes !== $Array)
                        object.pk = $util.newBuffer(object.pk);
                }
                object.failure = options.enums === $String ? "ID_NOT_EXIST" : 0;
                object.relayServer = "";
                object.otherFailure = "";
                object.feedback = 0;
                object.isUdp = false;
                object.upnpPort = 0;
                if (options.bytes === $String)
                    object.socketAddrV6 = "";
                else {
                    object.socketAddrV6 = [];
                    if (options.bytes !== $Array)
                        object.socketAddrV6 = $util.newBuffer(object.socketAddrV6);
                }
            }
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr"))
                object.socketAddr = options.bytes === $String ? $util.base64.encode(message.socketAddr, 0, message.socketAddr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddr) : message.socketAddr;
            if (message.pk != null && $Object.hasOwnProperty.call(message, "pk"))
                object.pk = options.bytes === $String ? $util.base64.encode(message.pk, 0, message.pk.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pk) : message.pk;
            if (message.failure != null && $Object.hasOwnProperty.call(message, "failure"))
                object.failure = options.enums === $String ? $root.hbb.PunchHoleResponse.Failure[message.failure] === $undefined ? message.failure : $root.hbb.PunchHoleResponse.Failure[message.failure] : message.failure;
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer"))
                object.relayServer = message.relayServer;
            if (message.natType != null && $Object.hasOwnProperty.call(message, "natType")) {
                object.natType = options.enums === $String ? $root.hbb.NatType[message.natType] === $undefined ? message.natType : $root.hbb.NatType[message.natType] : message.natType;
                if (options.oneofs)
                    object.union = "natType";
            }
            if (message.isLocal != null && $Object.hasOwnProperty.call(message, "isLocal")) {
                object.isLocal = message.isLocal;
                if (options.oneofs)
                    object.union = "isLocal";
            }
            if (message.otherFailure != null && $Object.hasOwnProperty.call(message, "otherFailure"))
                object.otherFailure = message.otherFailure;
            if (message.feedback != null && $Object.hasOwnProperty.call(message, "feedback"))
                object.feedback = message.feedback;
            if (message.isUdp != null && $Object.hasOwnProperty.call(message, "isUdp"))
                object.isUdp = message.isUdp;
            if (message.upnpPort != null && $Object.hasOwnProperty.call(message, "upnpPort"))
                object.upnpPort = message.upnpPort;
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6"))
                object.socketAddrV6 = options.bytes === $String ? $util.base64.encode(message.socketAddrV6, 0, message.socketAddrV6.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddrV6) : message.socketAddrV6;
            return object;
        };

        PunchHoleResponse.prototype.toJSON = function() {
            return PunchHoleResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        PunchHoleResponse.Failure = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ID_NOT_EXIST"] = 0;
            values[valuesById[2] = "OFFLINE"] = 2;
            values[valuesById[3] = "LICENSE_MISMATCH"] = 3;
            values[valuesById[4] = "LICENSE_OVERUSE"] = 4;
            return values;
        })();

        return PunchHoleResponse;
    })();

    hbb.ConfigUpdate = (function() {

        const ConfigUpdate = function (properties) {
            this.rendezvousServers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        ConfigUpdate.prototype.serial = 0;
        ConfigUpdate.prototype.rendezvousServers = $util.emptyArray;

        ConfigUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.serial != null && $Object.hasOwnProperty.call(message, "serial") && message.serial !== 0)
                writer.uint32(8).int32(message.serial);
            if (message.rendezvousServers != null && message.rendezvousServers.length)
                for (let i = 0; i < message.rendezvousServers.length; ++i)
                    writer.uint32(18).string(message.rendezvousServers[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        ConfigUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.ConfigUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.serial = value;
                        else
                            delete message.serial;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.rendezvousServers && message.rendezvousServers.length))
                            message.rendezvousServers = [];
                        message.rendezvousServers.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        ConfigUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.ConfigUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.ConfigUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.ConfigUpdate();
            if (object.serial != null)
                if ($Number(object.serial) !== 0)
                    message.serial = object.serial | 0;
            if (object.rendezvousServers) {
                if (!$Array.isArray(object.rendezvousServers))
                    throw $TypeError(".hbb.ConfigUpdate.rendezvousServers: array expected");
                message.rendezvousServers = $Array(object.rendezvousServers.length);
                for (let i = 0; i < object.rendezvousServers.length; ++i)
                    message.rendezvousServers[i] = $String(object.rendezvousServers[i]);
            }
            return message;
        };

        ConfigUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.rendezvousServers = [];
            if (options.defaults)
                object.serial = 0;
            if (message.serial != null && $Object.hasOwnProperty.call(message, "serial"))
                object.serial = message.serial;
            if (message.rendezvousServers && message.rendezvousServers.length) {
                object.rendezvousServers = $Array(message.rendezvousServers.length);
                for (let j = 0; j < message.rendezvousServers.length; ++j)
                    object.rendezvousServers[j] = message.rendezvousServers[j];
            }
            return object;
        };

        ConfigUpdate.prototype.toJSON = function() {
            return ConfigUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        return ConfigUpdate;
    })();

    hbb.RequestRelay = (function() {

        const RequestRelay = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        RequestRelay.prototype.id = "";
        RequestRelay.prototype.uuid = "";
        RequestRelay.prototype.socketAddr = $util.newBuffer([]);
        RequestRelay.prototype.relayServer = "";
        RequestRelay.prototype.secure = false;
        RequestRelay.prototype.licenceKey = "";
        RequestRelay.prototype.connType = 0;
        RequestRelay.prototype.token = "";
        RequestRelay.prototype.controlPermissions = null;
        RequestRelay.prototype.controlledContext = null;
        RequestRelay.prototype.switchCode = "";
        RequestRelay.prototype.rendezvousToken = $util.newBuffer([]);

        RequestRelay.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(10).string(message.id);
            if (message.uuid != null && $Object.hasOwnProperty.call(message, "uuid") && message.uuid !== "")
                writer.uint32(18).string(message.uuid);
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr") && message.socketAddr.length)
                writer.uint32(26).bytes(message.socketAddr);
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer") && message.relayServer !== "")
                writer.uint32(34).string(message.relayServer);
            if (message.secure != null && $Object.hasOwnProperty.call(message, "secure") && message.secure !== false)
                writer.uint32(40).bool(message.secure);
            if (message.licenceKey != null && $Object.hasOwnProperty.call(message, "licenceKey") && message.licenceKey !== "")
                writer.uint32(50).string(message.licenceKey);
            if (message.connType != null && $Object.hasOwnProperty.call(message, "connType") && message.connType !== 0)
                writer.uint32(56).int32(message.connType);
            if (message.token != null && $Object.hasOwnProperty.call(message, "token") && message.token !== "")
                writer.uint32(66).string(message.token);
            if (message.controlPermissions != null && $Object.hasOwnProperty.call(message, "controlPermissions"))
                $root.hbb.ControlPermissions.encode(message.controlPermissions, writer.uint32(74).fork(), _depth + 1).ldelim();
            if (message.controlledContext != null && $Object.hasOwnProperty.call(message, "controlledContext"))
                $root.hbb.ControlledContext.encode(message.controlledContext, writer.uint32(82).fork(), _depth + 1).ldelim();
            if (message.switchCode != null && $Object.hasOwnProperty.call(message, "switchCode") && message.switchCode !== "")
                writer.uint32(90).string(message.switchCode);
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken") && message.rendezvousToken.length)
                writer.uint32(98).bytes(message.rendezvousToken);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RequestRelay.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.RequestRelay(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddr = value;
                        else
                            delete message.socketAddr;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.relayServer = value;
                        else
                            delete message.relayServer;
                        continue;
                    }
                case 5: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.bool())
                            message.secure = value;
                        else
                            delete message.secure;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.licenceKey = value;
                        else
                            delete message.licenceKey;
                        continue;
                    }
                case 7: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.connType = value;
                        else
                            delete message.connType;
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.controlPermissions = $root.hbb.ControlPermissions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.controlPermissions);
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.controlledContext = $root.hbb.ControlledContext.decode(reader, reader.uint32(), $undefined, _depth + 1, message.controlledContext);
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.switchCode = value;
                        else
                            delete message.switchCode;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.rendezvousToken = value;
                        else
                            delete message.rendezvousToken;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        RequestRelay.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.RequestRelay)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.RequestRelay: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.RequestRelay();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = $String(object.uuid);
            if (object.socketAddr != null)
                if (object.socketAddr.length)
                    if (typeof object.socketAddr === "string")
                        $util.base64.decode(object.socketAddr, message.socketAddr = $util.newBuffer($util.base64.length(object.socketAddr)), 0);
                    else if (object.socketAddr.length >= 0)
                        message.socketAddr = object.socketAddr;
            if (object.relayServer != null)
                if (typeof object.relayServer !== "string" || object.relayServer.length)
                    message.relayServer = $String(object.relayServer);
            if (object.secure != null)
                if (object.secure)
                    message.secure = $Boolean(object.secure);
            if (object.licenceKey != null)
                if (typeof object.licenceKey !== "string" || object.licenceKey.length)
                    message.licenceKey = $String(object.licenceKey);
            if (object.connType !== 0 && (typeof object.connType !== "string" || $root.hbb.ConnType[object.connType] !== 0))
                switch (object.connType) {
                case "DEFAULT_CONN":
                case 0:
                    message.connType = 0;
                    break;
                case "FILE_TRANSFER":
                case 1:
                    message.connType = 1;
                    break;
                case "PORT_FORWARD":
                case 2:
                    message.connType = 2;
                    break;
                case "RDP":
                case 3:
                    message.connType = 3;
                    break;
                case "VIEW_CAMERA":
                case 4:
                    message.connType = 4;
                    break;
                case "TERMINAL":
                case 5:
                    message.connType = 5;
                    break;
                default:
                    if (typeof object.connType === "number" && (object.connType | 0) === object.connType)
                        message.connType = object.connType;
                }
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = $String(object.token);
            if (object.controlPermissions != null) {
                if (!$util.isObject(object.controlPermissions))
                    throw $TypeError(".hbb.RequestRelay.controlPermissions: object expected");
                message.controlPermissions = $root.hbb.ControlPermissions.fromObject(object.controlPermissions, _depth + 1);
            }
            if (object.controlledContext != null) {
                if (!$util.isObject(object.controlledContext))
                    throw $TypeError(".hbb.RequestRelay.controlledContext: object expected");
                message.controlledContext = $root.hbb.ControlledContext.fromObject(object.controlledContext, _depth + 1);
            }
            if (object.switchCode != null)
                if (typeof object.switchCode !== "string" || object.switchCode.length)
                    message.switchCode = $String(object.switchCode);
            if (object.rendezvousToken != null)
                if (object.rendezvousToken.length)
                    if (typeof object.rendezvousToken === "string")
                        $util.base64.decode(object.rendezvousToken, message.rendezvousToken = $util.newBuffer($util.base64.length(object.rendezvousToken)), 0);
                    else if (object.rendezvousToken.length >= 0)
                        message.rendezvousToken = object.rendezvousToken;
            return message;
        };

        RequestRelay.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                object.uuid = "";
                if (options.bytes === $String)
                    object.socketAddr = "";
                else {
                    object.socketAddr = [];
                    if (options.bytes !== $Array)
                        object.socketAddr = $util.newBuffer(object.socketAddr);
                }
                object.relayServer = "";
                object.secure = false;
                object.licenceKey = "";
                object.connType = options.enums === $String ? "DEFAULT_CONN" : 0;
                object.token = "";
                object.controlPermissions = null;
                object.controlledContext = null;
                object.switchCode = "";
                if (options.bytes === $String)
                    object.rendezvousToken = "";
                else {
                    object.rendezvousToken = [];
                    if (options.bytes !== $Array)
                        object.rendezvousToken = $util.newBuffer(object.rendezvousToken);
                }
            }
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.uuid != null && $Object.hasOwnProperty.call(message, "uuid"))
                object.uuid = message.uuid;
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr"))
                object.socketAddr = options.bytes === $String ? $util.base64.encode(message.socketAddr, 0, message.socketAddr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddr) : message.socketAddr;
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer"))
                object.relayServer = message.relayServer;
            if (message.secure != null && $Object.hasOwnProperty.call(message, "secure"))
                object.secure = message.secure;
            if (message.licenceKey != null && $Object.hasOwnProperty.call(message, "licenceKey"))
                object.licenceKey = message.licenceKey;
            if (message.connType != null && $Object.hasOwnProperty.call(message, "connType"))
                object.connType = options.enums === $String ? $root.hbb.ConnType[message.connType] === $undefined ? message.connType : $root.hbb.ConnType[message.connType] : message.connType;
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = message.token;
            if (message.controlPermissions != null && $Object.hasOwnProperty.call(message, "controlPermissions"))
                object.controlPermissions = $root.hbb.ControlPermissions.toObject(message.controlPermissions, options, _depth + 1);
            if (message.controlledContext != null && $Object.hasOwnProperty.call(message, "controlledContext"))
                object.controlledContext = $root.hbb.ControlledContext.toObject(message.controlledContext, options, _depth + 1);
            if (message.switchCode != null && $Object.hasOwnProperty.call(message, "switchCode"))
                object.switchCode = message.switchCode;
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken"))
                object.rendezvousToken = options.bytes === $String ? $util.base64.encode(message.rendezvousToken, 0, message.rendezvousToken.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.rendezvousToken) : message.rendezvousToken;
            return object;
        };

        RequestRelay.prototype.toJSON = function() {
            return RequestRelay.toObject(this, $protobuf.util.toJSONOptions);
        };

        return RequestRelay;
    })();

    hbb.RelayResponse = (function() {

        const RelayResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        RelayResponse.prototype.socketAddr = $util.newBuffer([]);
        RelayResponse.prototype.uuid = "";
        RelayResponse.prototype.relayServer = "";
        RelayResponse.prototype.id = null;
        RelayResponse.prototype.pk = null;
        RelayResponse.prototype.refuseReason = "";
        RelayResponse.prototype.version = "";
        RelayResponse.prototype.feedback = 0;
        RelayResponse.prototype.socketAddrV6 = $util.newBuffer([]);
        RelayResponse.prototype.upnpPort = 0;
        RelayResponse.prototype.rendezvousToken = $util.newBuffer([]);

        let $oneOfFields;

        $Object.defineProperty(RelayResponse.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["id", "pk"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        RelayResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr") && message.socketAddr.length)
                writer.uint32(10).bytes(message.socketAddr);
            if (message.uuid != null && $Object.hasOwnProperty.call(message, "uuid") && message.uuid !== "")
                writer.uint32(18).string(message.uuid);
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer") && message.relayServer !== "")
                writer.uint32(26).string(message.relayServer);
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                writer.uint32(34).string(message.id);
            if (message.pk != null && $Object.hasOwnProperty.call(message, "pk"))
                writer.uint32(42).bytes(message.pk);
            if (message.refuseReason != null && $Object.hasOwnProperty.call(message, "refuseReason") && message.refuseReason !== "")
                writer.uint32(50).string(message.refuseReason);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
                writer.uint32(58).string(message.version);
            if (message.feedback != null && $Object.hasOwnProperty.call(message, "feedback") && message.feedback !== 0)
                writer.uint32(72).int32(message.feedback);
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6") && message.socketAddrV6.length)
                writer.uint32(82).bytes(message.socketAddrV6);
            if (message.upnpPort != null && $Object.hasOwnProperty.call(message, "upnpPort") && message.upnpPort !== 0)
                writer.uint32(88).int32(message.upnpPort);
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken") && message.rendezvousToken.length)
                writer.uint32(98).bytes(message.rendezvousToken);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RelayResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.RelayResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddr = value;
                        else
                            delete message.socketAddr;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.uuid = value;
                        else
                            delete message.uuid;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.relayServer = value;
                        else
                            delete message.relayServer;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.id = reader.stringVerify();
                        message.union = "id";
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.pk = reader.bytes();
                        message.union = "pk";
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.refuseReason = value;
                        else
                            delete message.refuseReason;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 9: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.feedback = value;
                        else
                            delete message.feedback;
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddrV6 = value;
                        else
                            delete message.socketAddrV6;
                        continue;
                    }
                case 11: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.upnpPort = value;
                        else
                            delete message.upnpPort;
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.rendezvousToken = value;
                        else
                            delete message.rendezvousToken;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        RelayResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.RelayResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.RelayResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.RelayResponse();
            if (object.socketAddr != null)
                if (object.socketAddr.length)
                    if (typeof object.socketAddr === "string")
                        $util.base64.decode(object.socketAddr, message.socketAddr = $util.newBuffer($util.base64.length(object.socketAddr)), 0);
                    else if (object.socketAddr.length >= 0)
                        message.socketAddr = object.socketAddr;
            if (object.uuid != null)
                if (typeof object.uuid !== "string" || object.uuid.length)
                    message.uuid = $String(object.uuid);
            if (object.relayServer != null)
                if (typeof object.relayServer !== "string" || object.relayServer.length)
                    message.relayServer = $String(object.relayServer);
            if (object.id != null)
                message.id = $String(object.id);
            if (object.pk != null)
                if (typeof object.pk === "string")
                    $util.base64.decode(object.pk, message.pk = $util.newBuffer($util.base64.length(object.pk)), 0);
                else if (object.pk.length >= 0)
                    message.pk = object.pk;
            if (object.refuseReason != null)
                if (typeof object.refuseReason !== "string" || object.refuseReason.length)
                    message.refuseReason = $String(object.refuseReason);
            if (object.version != null)
                if (typeof object.version !== "string" || object.version.length)
                    message.version = $String(object.version);
            if (object.feedback != null)
                if ($Number(object.feedback) !== 0)
                    message.feedback = object.feedback | 0;
            if (object.socketAddrV6 != null)
                if (object.socketAddrV6.length)
                    if (typeof object.socketAddrV6 === "string")
                        $util.base64.decode(object.socketAddrV6, message.socketAddrV6 = $util.newBuffer($util.base64.length(object.socketAddrV6)), 0);
                    else if (object.socketAddrV6.length >= 0)
                        message.socketAddrV6 = object.socketAddrV6;
            if (object.upnpPort != null)
                if ($Number(object.upnpPort) !== 0)
                    message.upnpPort = object.upnpPort | 0;
            if (object.rendezvousToken != null)
                if (object.rendezvousToken.length)
                    if (typeof object.rendezvousToken === "string")
                        $util.base64.decode(object.rendezvousToken, message.rendezvousToken = $util.newBuffer($util.base64.length(object.rendezvousToken)), 0);
                    else if (object.rendezvousToken.length >= 0)
                        message.rendezvousToken = object.rendezvousToken;
            return message;
        };

        RelayResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.socketAddr = "";
                else {
                    object.socketAddr = [];
                    if (options.bytes !== $Array)
                        object.socketAddr = $util.newBuffer(object.socketAddr);
                }
                object.uuid = "";
                object.relayServer = "";
                object.refuseReason = "";
                object.version = "";
                object.feedback = 0;
                if (options.bytes === $String)
                    object.socketAddrV6 = "";
                else {
                    object.socketAddrV6 = [];
                    if (options.bytes !== $Array)
                        object.socketAddrV6 = $util.newBuffer(object.socketAddrV6);
                }
                object.upnpPort = 0;
                if (options.bytes === $String)
                    object.rendezvousToken = "";
                else {
                    object.rendezvousToken = [];
                    if (options.bytes !== $Array)
                        object.rendezvousToken = $util.newBuffer(object.rendezvousToken);
                }
            }
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr"))
                object.socketAddr = options.bytes === $String ? $util.base64.encode(message.socketAddr, 0, message.socketAddr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddr) : message.socketAddr;
            if (message.uuid != null && $Object.hasOwnProperty.call(message, "uuid"))
                object.uuid = message.uuid;
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer"))
                object.relayServer = message.relayServer;
            if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
                object.id = message.id;
                if (options.oneofs)
                    object.union = "id";
            }
            if (message.pk != null && $Object.hasOwnProperty.call(message, "pk")) {
                object.pk = options.bytes === $String ? $util.base64.encode(message.pk, 0, message.pk.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.pk) : message.pk;
                if (options.oneofs)
                    object.union = "pk";
            }
            if (message.refuseReason != null && $Object.hasOwnProperty.call(message, "refuseReason"))
                object.refuseReason = message.refuseReason;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.feedback != null && $Object.hasOwnProperty.call(message, "feedback"))
                object.feedback = message.feedback;
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6"))
                object.socketAddrV6 = options.bytes === $String ? $util.base64.encode(message.socketAddrV6, 0, message.socketAddrV6.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddrV6) : message.socketAddrV6;
            if (message.upnpPort != null && $Object.hasOwnProperty.call(message, "upnpPort"))
                object.upnpPort = message.upnpPort;
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken"))
                object.rendezvousToken = options.bytes === $String ? $util.base64.encode(message.rendezvousToken, 0, message.rendezvousToken.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.rendezvousToken) : message.rendezvousToken;
            return object;
        };

        RelayResponse.prototype.toJSON = function() {
            return RelayResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return RelayResponse;
    })();

    hbb.SoftwareUpdate = (function() {

        const SoftwareUpdate = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        SoftwareUpdate.prototype.url = "";

        SoftwareUpdate.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.url != null && $Object.hasOwnProperty.call(message, "url") && message.url !== "")
                writer.uint32(10).string(message.url);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        SoftwareUpdate.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.SoftwareUpdate(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.url = value;
                        else
                            delete message.url;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        SoftwareUpdate.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.SoftwareUpdate)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.SoftwareUpdate: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.SoftwareUpdate();
            if (object.url != null)
                if (typeof object.url !== "string" || object.url.length)
                    message.url = $String(object.url);
            return message;
        };

        SoftwareUpdate.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.url = "";
            if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
                object.url = message.url;
            return object;
        };

        SoftwareUpdate.prototype.toJSON = function() {
            return SoftwareUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        return SoftwareUpdate;
    })();

    hbb.FetchLocalAddr = (function() {

        const FetchLocalAddr = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        FetchLocalAddr.prototype.socketAddr = $util.newBuffer([]);
        FetchLocalAddr.prototype.relayServer = "";
        FetchLocalAddr.prototype.socketAddrV6 = $util.newBuffer([]);
        FetchLocalAddr.prototype.controlPermissions = null;
        FetchLocalAddr.prototype.controlledContext = null;
        FetchLocalAddr.prototype.rendezvousToken = $util.newBuffer([]);

        FetchLocalAddr.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr") && message.socketAddr.length)
                writer.uint32(10).bytes(message.socketAddr);
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer") && message.relayServer !== "")
                writer.uint32(18).string(message.relayServer);
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6") && message.socketAddrV6.length)
                writer.uint32(26).bytes(message.socketAddrV6);
            if (message.controlPermissions != null && $Object.hasOwnProperty.call(message, "controlPermissions"))
                $root.hbb.ControlPermissions.encode(message.controlPermissions, writer.uint32(34).fork(), _depth + 1).ldelim();
            if (message.controlledContext != null && $Object.hasOwnProperty.call(message, "controlledContext"))
                $root.hbb.ControlledContext.encode(message.controlledContext, writer.uint32(42).fork(), _depth + 1).ldelim();
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken") && message.rendezvousToken.length)
                writer.uint32(50).bytes(message.rendezvousToken);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        FetchLocalAddr.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.FetchLocalAddr(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddr = value;
                        else
                            delete message.socketAddr;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.relayServer = value;
                        else
                            delete message.relayServer;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddrV6 = value;
                        else
                            delete message.socketAddrV6;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        message.controlPermissions = $root.hbb.ControlPermissions.decode(reader, reader.uint32(), $undefined, _depth + 1, message.controlPermissions);
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        message.controlledContext = $root.hbb.ControlledContext.decode(reader, reader.uint32(), $undefined, _depth + 1, message.controlledContext);
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.rendezvousToken = value;
                        else
                            delete message.rendezvousToken;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        FetchLocalAddr.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.FetchLocalAddr)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.FetchLocalAddr: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.FetchLocalAddr();
            if (object.socketAddr != null)
                if (object.socketAddr.length)
                    if (typeof object.socketAddr === "string")
                        $util.base64.decode(object.socketAddr, message.socketAddr = $util.newBuffer($util.base64.length(object.socketAddr)), 0);
                    else if (object.socketAddr.length >= 0)
                        message.socketAddr = object.socketAddr;
            if (object.relayServer != null)
                if (typeof object.relayServer !== "string" || object.relayServer.length)
                    message.relayServer = $String(object.relayServer);
            if (object.socketAddrV6 != null)
                if (object.socketAddrV6.length)
                    if (typeof object.socketAddrV6 === "string")
                        $util.base64.decode(object.socketAddrV6, message.socketAddrV6 = $util.newBuffer($util.base64.length(object.socketAddrV6)), 0);
                    else if (object.socketAddrV6.length >= 0)
                        message.socketAddrV6 = object.socketAddrV6;
            if (object.controlPermissions != null) {
                if (!$util.isObject(object.controlPermissions))
                    throw $TypeError(".hbb.FetchLocalAddr.controlPermissions: object expected");
                message.controlPermissions = $root.hbb.ControlPermissions.fromObject(object.controlPermissions, _depth + 1);
            }
            if (object.controlledContext != null) {
                if (!$util.isObject(object.controlledContext))
                    throw $TypeError(".hbb.FetchLocalAddr.controlledContext: object expected");
                message.controlledContext = $root.hbb.ControlledContext.fromObject(object.controlledContext, _depth + 1);
            }
            if (object.rendezvousToken != null)
                if (object.rendezvousToken.length)
                    if (typeof object.rendezvousToken === "string")
                        $util.base64.decode(object.rendezvousToken, message.rendezvousToken = $util.newBuffer($util.base64.length(object.rendezvousToken)), 0);
                    else if (object.rendezvousToken.length >= 0)
                        message.rendezvousToken = object.rendezvousToken;
            return message;
        };

        FetchLocalAddr.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.socketAddr = "";
                else {
                    object.socketAddr = [];
                    if (options.bytes !== $Array)
                        object.socketAddr = $util.newBuffer(object.socketAddr);
                }
                object.relayServer = "";
                if (options.bytes === $String)
                    object.socketAddrV6 = "";
                else {
                    object.socketAddrV6 = [];
                    if (options.bytes !== $Array)
                        object.socketAddrV6 = $util.newBuffer(object.socketAddrV6);
                }
                object.controlPermissions = null;
                object.controlledContext = null;
                if (options.bytes === $String)
                    object.rendezvousToken = "";
                else {
                    object.rendezvousToken = [];
                    if (options.bytes !== $Array)
                        object.rendezvousToken = $util.newBuffer(object.rendezvousToken);
                }
            }
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr"))
                object.socketAddr = options.bytes === $String ? $util.base64.encode(message.socketAddr, 0, message.socketAddr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddr) : message.socketAddr;
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer"))
                object.relayServer = message.relayServer;
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6"))
                object.socketAddrV6 = options.bytes === $String ? $util.base64.encode(message.socketAddrV6, 0, message.socketAddrV6.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddrV6) : message.socketAddrV6;
            if (message.controlPermissions != null && $Object.hasOwnProperty.call(message, "controlPermissions"))
                object.controlPermissions = $root.hbb.ControlPermissions.toObject(message.controlPermissions, options, _depth + 1);
            if (message.controlledContext != null && $Object.hasOwnProperty.call(message, "controlledContext"))
                object.controlledContext = $root.hbb.ControlledContext.toObject(message.controlledContext, options, _depth + 1);
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken"))
                object.rendezvousToken = options.bytes === $String ? $util.base64.encode(message.rendezvousToken, 0, message.rendezvousToken.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.rendezvousToken) : message.rendezvousToken;
            return object;
        };

        FetchLocalAddr.prototype.toJSON = function() {
            return FetchLocalAddr.toObject(this, $protobuf.util.toJSONOptions);
        };

        return FetchLocalAddr;
    })();

    hbb.LocalAddr = (function() {

        const LocalAddr = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        LocalAddr.prototype.socketAddr = $util.newBuffer([]);
        LocalAddr.prototype.localAddr = $util.newBuffer([]);
        LocalAddr.prototype.relayServer = "";
        LocalAddr.prototype.id = "";
        LocalAddr.prototype.version = "";
        LocalAddr.prototype.socketAddrV6 = $util.newBuffer([]);
        LocalAddr.prototype.rendezvousToken = $util.newBuffer([]);

        LocalAddr.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr") && message.socketAddr.length)
                writer.uint32(10).bytes(message.socketAddr);
            if (message.localAddr != null && $Object.hasOwnProperty.call(message, "localAddr") && message.localAddr.length)
                writer.uint32(18).bytes(message.localAddr);
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer") && message.relayServer !== "")
                writer.uint32(26).string(message.relayServer);
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(34).string(message.id);
            if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
                writer.uint32(42).string(message.version);
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6") && message.socketAddrV6.length)
                writer.uint32(50).bytes(message.socketAddrV6);
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken") && message.rendezvousToken.length)
                writer.uint32(58).bytes(message.rendezvousToken);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        LocalAddr.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.LocalAddr(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddr = value;
                        else
                            delete message.socketAddr;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.localAddr = value;
                        else
                            delete message.localAddr;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.relayServer = value;
                        else
                            delete message.relayServer;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.version = value;
                        else
                            delete message.version;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.socketAddrV6 = value;
                        else
                            delete message.socketAddrV6;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.rendezvousToken = value;
                        else
                            delete message.rendezvousToken;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        LocalAddr.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.LocalAddr)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.LocalAddr: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.LocalAddr();
            if (object.socketAddr != null)
                if (object.socketAddr.length)
                    if (typeof object.socketAddr === "string")
                        $util.base64.decode(object.socketAddr, message.socketAddr = $util.newBuffer($util.base64.length(object.socketAddr)), 0);
                    else if (object.socketAddr.length >= 0)
                        message.socketAddr = object.socketAddr;
            if (object.localAddr != null)
                if (object.localAddr.length)
                    if (typeof object.localAddr === "string")
                        $util.base64.decode(object.localAddr, message.localAddr = $util.newBuffer($util.base64.length(object.localAddr)), 0);
                    else if (object.localAddr.length >= 0)
                        message.localAddr = object.localAddr;
            if (object.relayServer != null)
                if (typeof object.relayServer !== "string" || object.relayServer.length)
                    message.relayServer = $String(object.relayServer);
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.version != null)
                if (typeof object.version !== "string" || object.version.length)
                    message.version = $String(object.version);
            if (object.socketAddrV6 != null)
                if (object.socketAddrV6.length)
                    if (typeof object.socketAddrV6 === "string")
                        $util.base64.decode(object.socketAddrV6, message.socketAddrV6 = $util.newBuffer($util.base64.length(object.socketAddrV6)), 0);
                    else if (object.socketAddrV6.length >= 0)
                        message.socketAddrV6 = object.socketAddrV6;
            if (object.rendezvousToken != null)
                if (object.rendezvousToken.length)
                    if (typeof object.rendezvousToken === "string")
                        $util.base64.decode(object.rendezvousToken, message.rendezvousToken = $util.newBuffer($util.base64.length(object.rendezvousToken)), 0);
                    else if (object.rendezvousToken.length >= 0)
                        message.rendezvousToken = object.rendezvousToken;
            return message;
        };

        LocalAddr.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === $String)
                    object.socketAddr = "";
                else {
                    object.socketAddr = [];
                    if (options.bytes !== $Array)
                        object.socketAddr = $util.newBuffer(object.socketAddr);
                }
                if (options.bytes === $String)
                    object.localAddr = "";
                else {
                    object.localAddr = [];
                    if (options.bytes !== $Array)
                        object.localAddr = $util.newBuffer(object.localAddr);
                }
                object.relayServer = "";
                object.id = "";
                object.version = "";
                if (options.bytes === $String)
                    object.socketAddrV6 = "";
                else {
                    object.socketAddrV6 = [];
                    if (options.bytes !== $Array)
                        object.socketAddrV6 = $util.newBuffer(object.socketAddrV6);
                }
                if (options.bytes === $String)
                    object.rendezvousToken = "";
                else {
                    object.rendezvousToken = [];
                    if (options.bytes !== $Array)
                        object.rendezvousToken = $util.newBuffer(object.rendezvousToken);
                }
            }
            if (message.socketAddr != null && $Object.hasOwnProperty.call(message, "socketAddr"))
                object.socketAddr = options.bytes === $String ? $util.base64.encode(message.socketAddr, 0, message.socketAddr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddr) : message.socketAddr;
            if (message.localAddr != null && $Object.hasOwnProperty.call(message, "localAddr"))
                object.localAddr = options.bytes === $String ? $util.base64.encode(message.localAddr, 0, message.localAddr.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.localAddr) : message.localAddr;
            if (message.relayServer != null && $Object.hasOwnProperty.call(message, "relayServer"))
                object.relayServer = message.relayServer;
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.socketAddrV6 != null && $Object.hasOwnProperty.call(message, "socketAddrV6"))
                object.socketAddrV6 = options.bytes === $String ? $util.base64.encode(message.socketAddrV6, 0, message.socketAddrV6.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.socketAddrV6) : message.socketAddrV6;
            if (message.rendezvousToken != null && $Object.hasOwnProperty.call(message, "rendezvousToken"))
                object.rendezvousToken = options.bytes === $String ? $util.base64.encode(message.rendezvousToken, 0, message.rendezvousToken.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.rendezvousToken) : message.rendezvousToken;
            return object;
        };

        LocalAddr.prototype.toJSON = function() {
            return LocalAddr.toObject(this, $protobuf.util.toJSONOptions);
        };

        return LocalAddr;
    })();

    hbb.PeerDiscovery = (function() {

        const PeerDiscovery = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        PeerDiscovery.prototype.cmd = "";
        PeerDiscovery.prototype.mac = "";
        PeerDiscovery.prototype.id = "";
        PeerDiscovery.prototype.username = "";
        PeerDiscovery.prototype.hostname = "";
        PeerDiscovery.prototype.platform = "";
        PeerDiscovery.prototype.misc = "";

        PeerDiscovery.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.cmd != null && $Object.hasOwnProperty.call(message, "cmd") && message.cmd !== "")
                writer.uint32(10).string(message.cmd);
            if (message.mac != null && $Object.hasOwnProperty.call(message, "mac") && message.mac !== "")
                writer.uint32(18).string(message.mac);
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(26).string(message.id);
            if (message.username != null && $Object.hasOwnProperty.call(message, "username") && message.username !== "")
                writer.uint32(34).string(message.username);
            if (message.hostname != null && $Object.hasOwnProperty.call(message, "hostname") && message.hostname !== "")
                writer.uint32(42).string(message.hostname);
            if (message.platform != null && $Object.hasOwnProperty.call(message, "platform") && message.platform !== "")
                writer.uint32(50).string(message.platform);
            if (message.misc != null && $Object.hasOwnProperty.call(message, "misc") && message.misc !== "")
                writer.uint32(58).string(message.misc);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        PeerDiscovery.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.PeerDiscovery(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.cmd = value;
                        else
                            delete message.cmd;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.mac = value;
                        else
                            delete message.mac;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.username = value;
                        else
                            delete message.username;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.hostname = value;
                        else
                            delete message.hostname;
                        continue;
                    }
                case 6: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.platform = value;
                        else
                            delete message.platform;
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.misc = value;
                        else
                            delete message.misc;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        PeerDiscovery.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.PeerDiscovery)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.PeerDiscovery: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.PeerDiscovery();
            if (object.cmd != null)
                if (typeof object.cmd !== "string" || object.cmd.length)
                    message.cmd = $String(object.cmd);
            if (object.mac != null)
                if (typeof object.mac !== "string" || object.mac.length)
                    message.mac = $String(object.mac);
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.username != null)
                if (typeof object.username !== "string" || object.username.length)
                    message.username = $String(object.username);
            if (object.hostname != null)
                if (typeof object.hostname !== "string" || object.hostname.length)
                    message.hostname = $String(object.hostname);
            if (object.platform != null)
                if (typeof object.platform !== "string" || object.platform.length)
                    message.platform = $String(object.platform);
            if (object.misc != null)
                if (typeof object.misc !== "string" || object.misc.length)
                    message.misc = $String(object.misc);
            return message;
        };

        PeerDiscovery.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.cmd = "";
                object.mac = "";
                object.id = "";
                object.username = "";
                object.hostname = "";
                object.platform = "";
                object.misc = "";
            }
            if (message.cmd != null && $Object.hasOwnProperty.call(message, "cmd"))
                object.cmd = message.cmd;
            if (message.mac != null && $Object.hasOwnProperty.call(message, "mac"))
                object.mac = message.mac;
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.username != null && $Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.hostname != null && $Object.hasOwnProperty.call(message, "hostname"))
                object.hostname = message.hostname;
            if (message.platform != null && $Object.hasOwnProperty.call(message, "platform"))
                object.platform = message.platform;
            if (message.misc != null && $Object.hasOwnProperty.call(message, "misc"))
                object.misc = message.misc;
            return object;
        };

        PeerDiscovery.prototype.toJSON = function() {
            return PeerDiscovery.toObject(this, $protobuf.util.toJSONOptions);
        };

        return PeerDiscovery;
    })();

    hbb.OnlineRequest = (function() {

        const OnlineRequest = function (properties) {
            this.peers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        OnlineRequest.prototype.id = "";
        OnlineRequest.prototype.peers = $util.emptyArray;

        OnlineRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
                writer.uint32(10).string(message.id);
            if (message.peers != null && message.peers.length)
                for (let i = 0; i < message.peers.length; ++i)
                    writer.uint32(18).string(message.peers[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        OnlineRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.OnlineRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.id = value;
                        else
                            delete message.id;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.peers && message.peers.length))
                            message.peers = [];
                        message.peers.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        OnlineRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.OnlineRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.OnlineRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.OnlineRequest();
            if (object.id != null)
                if (typeof object.id !== "string" || object.id.length)
                    message.id = $String(object.id);
            if (object.peers) {
                if (!$Array.isArray(object.peers))
                    throw $TypeError(".hbb.OnlineRequest.peers: array expected");
                message.peers = $Array(object.peers.length);
                for (let i = 0; i < object.peers.length; ++i)
                    message.peers[i] = $String(object.peers[i]);
            }
            return message;
        };

        OnlineRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.peers = [];
            if (options.defaults)
                object.id = "";
            if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.peers && message.peers.length) {
                object.peers = $Array(message.peers.length);
                for (let j = 0; j < message.peers.length; ++j)
                    object.peers[j] = message.peers[j];
            }
            return object;
        };

        OnlineRequest.prototype.toJSON = function() {
            return OnlineRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return OnlineRequest;
    })();

    hbb.OnlineResponse = (function() {

        const OnlineResponse = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        OnlineResponse.prototype.states = $util.newBuffer([]);

        OnlineResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.states != null && $Object.hasOwnProperty.call(message, "states") && message.states.length)
                writer.uint32(10).bytes(message.states);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        OnlineResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.OnlineResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.states = value;
                        else
                            delete message.states;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        OnlineResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.OnlineResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.OnlineResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.OnlineResponse();
            if (object.states != null)
                if (object.states.length)
                    if (typeof object.states === "string")
                        $util.base64.decode(object.states, message.states = $util.newBuffer($util.base64.length(object.states)), 0);
                    else if (object.states.length >= 0)
                        message.states = object.states;
            return message;
        };

        OnlineResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === $String)
                    object.states = "";
                else {
                    object.states = [];
                    if (options.bytes !== $Array)
                        object.states = $util.newBuffer(object.states);
                }
            if (message.states != null && $Object.hasOwnProperty.call(message, "states"))
                object.states = options.bytes === $String ? $util.base64.encode(message.states, 0, message.states.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.states) : message.states;
            return object;
        };

        OnlineResponse.prototype.toJSON = function() {
            return OnlineResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return OnlineResponse;
    })();

    hbb.KeyExchange = (function() {

        const KeyExchange = function (properties) {
            this.keys = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        KeyExchange.prototype.keys = $util.emptyArray;

        KeyExchange.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.keys != null && message.keys.length)
                for (let i = 0; i < message.keys.length; ++i)
                    writer.uint32(10).bytes(message.keys[i]);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        KeyExchange.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.KeyExchange();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if (!(message.keys && message.keys.length))
                            message.keys = [];
                        message.keys.push(reader.bytes());
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        KeyExchange.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.KeyExchange)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.KeyExchange: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.KeyExchange();
            if (object.keys) {
                if (!$Array.isArray(object.keys))
                    throw $TypeError(".hbb.KeyExchange.keys: array expected");
                message.keys = $Array(object.keys.length);
                for (let i = 0; i < object.keys.length; ++i)
                    if (typeof object.keys[i] === "string")
                        $util.base64.decode(object.keys[i], message.keys[i] = $util.newBuffer($util.base64.length(object.keys[i])), 0);
                    else if (object.keys[i].length >= 0)
                        message.keys[i] = object.keys[i];
            }
            return message;
        };

        KeyExchange.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.keys = [];
            if (message.keys && message.keys.length) {
                object.keys = $Array(message.keys.length);
                for (let j = 0; j < message.keys.length; ++j)
                    object.keys[j] = options.bytes === $String ? $util.base64.encode(message.keys[j], 0, message.keys[j].length) : options.bytes === $Array ? $Array.prototype.slice.call(message.keys[j]) : message.keys[j];
            }
            return object;
        };

        KeyExchange.prototype.toJSON = function() {
            return KeyExchange.toObject(this, $protobuf.util.toJSONOptions);
        };

        return KeyExchange;
    })();

    hbb.HealthCheck = (function() {

        const HealthCheck = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        HealthCheck.prototype.token = "";

        HealthCheck.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.token != null && $Object.hasOwnProperty.call(message, "token") && message.token !== "")
                writer.uint32(10).string(message.token);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        HealthCheck.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.HealthCheck(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.token = value;
                        else
                            delete message.token;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        HealthCheck.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.HealthCheck)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.HealthCheck: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.HealthCheck();
            if (object.token != null)
                if (typeof object.token !== "string" || object.token.length)
                    message.token = $String(object.token);
            return message;
        };

        HealthCheck.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.token = "";
            if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
                object.token = message.token;
            return object;
        };

        HealthCheck.prototype.toJSON = function() {
            return HealthCheck.toObject(this, $protobuf.util.toJSONOptions);
        };

        return HealthCheck;
    })();

    hbb.HeaderEntry = (function() {

        const HeaderEntry = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        HeaderEntry.prototype.name = "";
        HeaderEntry.prototype.value = "";

        HeaderEntry.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
                writer.uint32(10).string(message.name);
            if (message.value != null && $Object.hasOwnProperty.call(message, "value") && message.value !== "")
                writer.uint32(18).string(message.value);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        HeaderEntry.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.HeaderEntry(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.name = value;
                        else
                            delete message.name;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.value = value;
                        else
                            delete message.value;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        HeaderEntry.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.HeaderEntry)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.HeaderEntry: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.HeaderEntry();
            if (object.name != null)
                if (typeof object.name !== "string" || object.name.length)
                    message.name = $String(object.name);
            if (object.value != null)
                if (typeof object.value !== "string" || object.value.length)
                    message.value = $String(object.value);
            return message;
        };

        HeaderEntry.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.value = "";
            }
            if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        HeaderEntry.prototype.toJSON = function() {
            return HeaderEntry.toObject(this, $protobuf.util.toJSONOptions);
        };

        return HeaderEntry;
    })();

    hbb.HttpProxyRequest = (function() {

        const HttpProxyRequest = function (properties) {
            this.headers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        HttpProxyRequest.prototype.method = "";
        HttpProxyRequest.prototype.path = "";
        HttpProxyRequest.prototype.headers = $util.emptyArray;
        HttpProxyRequest.prototype.body = $util.newBuffer([]);
        HttpProxyRequest.prototype.licenceKey = "";

        HttpProxyRequest.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.method != null && $Object.hasOwnProperty.call(message, "method") && message.method !== "")
                writer.uint32(10).string(message.method);
            if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
                writer.uint32(18).string(message.path);
            if (message.headers != null && message.headers.length)
                for (let i = 0; i < message.headers.length; ++i)
                    $root.hbb.HeaderEntry.encode(message.headers[i], writer.uint32(26).fork(), _depth + 1).ldelim();
            if (message.body != null && $Object.hasOwnProperty.call(message, "body") && message.body.length)
                writer.uint32(34).bytes(message.body);
            if (message.licenceKey != null && $Object.hasOwnProperty.call(message, "licenceKey") && message.licenceKey !== "")
                writer.uint32(42).string(message.licenceKey);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        HttpProxyRequest.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.HttpProxyRequest(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.method = value;
                        else
                            delete message.method;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.path = value;
                        else
                            delete message.path;
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if (!(message.headers && message.headers.length))
                            message.headers = [];
                        message.headers.push($root.hbb.HeaderEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.body = value;
                        else
                            delete message.body;
                        continue;
                    }
                case 5: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.licenceKey = value;
                        else
                            delete message.licenceKey;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        HttpProxyRequest.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.HttpProxyRequest)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.HttpProxyRequest: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.HttpProxyRequest();
            if (object.method != null)
                if (typeof object.method !== "string" || object.method.length)
                    message.method = $String(object.method);
            if (object.path != null)
                if (typeof object.path !== "string" || object.path.length)
                    message.path = $String(object.path);
            if (object.headers) {
                if (!$Array.isArray(object.headers))
                    throw $TypeError(".hbb.HttpProxyRequest.headers: array expected");
                message.headers = $Array(object.headers.length);
                for (let i = 0; i < object.headers.length; ++i) {
                    if (!$util.isObject(object.headers[i]))
                        throw $TypeError(".hbb.HttpProxyRequest.headers: object expected");
                    message.headers[i] = $root.hbb.HeaderEntry.fromObject(object.headers[i], _depth + 1);
                }
            }
            if (object.body != null)
                if (object.body.length)
                    if (typeof object.body === "string")
                        $util.base64.decode(object.body, message.body = $util.newBuffer($util.base64.length(object.body)), 0);
                    else if (object.body.length >= 0)
                        message.body = object.body;
            if (object.licenceKey != null)
                if (typeof object.licenceKey !== "string" || object.licenceKey.length)
                    message.licenceKey = $String(object.licenceKey);
            return message;
        };

        HttpProxyRequest.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.headers = [];
            if (options.defaults) {
                object.method = "";
                object.path = "";
                if (options.bytes === $String)
                    object.body = "";
                else {
                    object.body = [];
                    if (options.bytes !== $Array)
                        object.body = $util.newBuffer(object.body);
                }
                object.licenceKey = "";
            }
            if (message.method != null && $Object.hasOwnProperty.call(message, "method"))
                object.method = message.method;
            if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
                object.path = message.path;
            if (message.headers && message.headers.length) {
                object.headers = $Array(message.headers.length);
                for (let j = 0; j < message.headers.length; ++j)
                    object.headers[j] = $root.hbb.HeaderEntry.toObject(message.headers[j], options, _depth + 1);
            }
            if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                object.body = options.bytes === $String ? $util.base64.encode(message.body, 0, message.body.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.body) : message.body;
            if (message.licenceKey != null && $Object.hasOwnProperty.call(message, "licenceKey"))
                object.licenceKey = message.licenceKey;
            return object;
        };

        HttpProxyRequest.prototype.toJSON = function() {
            return HttpProxyRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        return HttpProxyRequest;
    })();

    hbb.HttpProxyResponse = (function() {

        const HttpProxyResponse = function (properties) {
            this.headers = [];
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        HttpProxyResponse.prototype.status = 0;
        HttpProxyResponse.prototype.headers = $util.emptyArray;
        HttpProxyResponse.prototype.body = $util.newBuffer([]);
        HttpProxyResponse.prototype.error = "";

        HttpProxyResponse.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== 0)
                writer.uint32(8).int32(message.status);
            if (message.headers != null && message.headers.length)
                for (let i = 0; i < message.headers.length; ++i)
                    $root.hbb.HeaderEntry.encode(message.headers[i], writer.uint32(18).fork(), _depth + 1).ldelim();
            if (message.body != null && $Object.hasOwnProperty.call(message, "body") && message.body.length)
                writer.uint32(26).bytes(message.body);
            if (message.error != null && $Object.hasOwnProperty.call(message, "error") && message.error !== "")
                writer.uint32(34).string(message.error);
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        HttpProxyResponse.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.HttpProxyResponse(), value;
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (wireType !== 0)
                            break;
                        if (value = reader.int32())
                            message.status = value;
                        else
                            delete message.status;
                        continue;
                    }
                case 2: {
                        if (wireType !== 2)
                            break;
                        if (!(message.headers && message.headers.length))
                            message.headers = [];
                        message.headers.push($root.hbb.HeaderEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
                        continue;
                    }
                case 3: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.bytes()).length)
                            message.body = value;
                        else
                            delete message.body;
                        continue;
                    }
                case 4: {
                        if (wireType !== 2)
                            break;
                        if ((value = reader.stringVerify()).length)
                            message.error = value;
                        else
                            delete message.error;
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        HttpProxyResponse.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.HttpProxyResponse)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.HttpProxyResponse: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.HttpProxyResponse();
            if (object.status != null)
                if ($Number(object.status) !== 0)
                    message.status = object.status | 0;
            if (object.headers) {
                if (!$Array.isArray(object.headers))
                    throw $TypeError(".hbb.HttpProxyResponse.headers: array expected");
                message.headers = $Array(object.headers.length);
                for (let i = 0; i < object.headers.length; ++i) {
                    if (!$util.isObject(object.headers[i]))
                        throw $TypeError(".hbb.HttpProxyResponse.headers: object expected");
                    message.headers[i] = $root.hbb.HeaderEntry.fromObject(object.headers[i], _depth + 1);
                }
            }
            if (object.body != null)
                if (object.body.length)
                    if (typeof object.body === "string")
                        $util.base64.decode(object.body, message.body = $util.newBuffer($util.base64.length(object.body)), 0);
                    else if (object.body.length >= 0)
                        message.body = object.body;
            if (object.error != null)
                if (typeof object.error !== "string" || object.error.length)
                    message.error = $String(object.error);
            return message;
        };

        HttpProxyResponse.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.headers = [];
            if (options.defaults) {
                object.status = 0;
                if (options.bytes === $String)
                    object.body = "";
                else {
                    object.body = [];
                    if (options.bytes !== $Array)
                        object.body = $util.newBuffer(object.body);
                }
                object.error = "";
            }
            if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            if (message.headers && message.headers.length) {
                object.headers = $Array(message.headers.length);
                for (let j = 0; j < message.headers.length; ++j)
                    object.headers[j] = $root.hbb.HeaderEntry.toObject(message.headers[j], options, _depth + 1);
            }
            if (message.body != null && $Object.hasOwnProperty.call(message, "body"))
                object.body = options.bytes === $String ? $util.base64.encode(message.body, 0, message.body.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.body) : message.body;
            if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
                object.error = message.error;
            return object;
        };

        HttpProxyResponse.prototype.toJSON = function() {
            return HttpProxyResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        return HttpProxyResponse;
    })();

    hbb.RendezvousMessage = (function() {

        const RendezvousMessage = function (properties) {
            if (properties)
                for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        };

        RendezvousMessage.prototype.registerPeer = null;
        RendezvousMessage.prototype.registerPeerResponse = null;
        RendezvousMessage.prototype.punchHoleRequest = null;
        RendezvousMessage.prototype.punchHole = null;
        RendezvousMessage.prototype.punchHoleSent = null;
        RendezvousMessage.prototype.punchHoleResponse = null;
        RendezvousMessage.prototype.fetchLocalAddr = null;
        RendezvousMessage.prototype.localAddr = null;
        RendezvousMessage.prototype.configureUpdate = null;
        RendezvousMessage.prototype.registerPk = null;
        RendezvousMessage.prototype.registerPkResponse = null;
        RendezvousMessage.prototype.softwareUpdate = null;
        RendezvousMessage.prototype.requestRelay = null;
        RendezvousMessage.prototype.relayResponse = null;
        RendezvousMessage.prototype.testNatRequest = null;
        RendezvousMessage.prototype.testNatResponse = null;
        RendezvousMessage.prototype.peerDiscovery = null;
        RendezvousMessage.prototype.onlineRequest = null;
        RendezvousMessage.prototype.onlineResponse = null;
        RendezvousMessage.prototype.keyExchange = null;
        RendezvousMessage.prototype.hc = null;
        RendezvousMessage.prototype.httpProxyRequest = null;
        RendezvousMessage.prototype.httpProxyResponse = null;

        let $oneOfFields;

        $Object.defineProperty(RendezvousMessage.prototype, "union", {
            get: $util.oneOfGetter($oneOfFields = ["registerPeer", "registerPeerResponse", "punchHoleRequest", "punchHole", "punchHoleSent", "punchHoleResponse", "fetchLocalAddr", "localAddr", "configureUpdate", "registerPk", "registerPkResponse", "softwareUpdate", "requestRelay", "relayResponse", "testNatRequest", "testNatResponse", "peerDiscovery", "onlineRequest", "onlineResponse", "keyExchange", "hc", "httpProxyRequest", "httpProxyResponse"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        RendezvousMessage.encode = function (message, writer, _depth) {
            if (!writer)
                writer = $Writer.create();
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (message.registerPeer != null && $Object.hasOwnProperty.call(message, "registerPeer"))
                $root.hbb.RegisterPeer.encode(message.registerPeer, writer.uint32(50).fork(), _depth + 1).ldelim();
            if (message.registerPeerResponse != null && $Object.hasOwnProperty.call(message, "registerPeerResponse"))
                $root.hbb.RegisterPeerResponse.encode(message.registerPeerResponse, writer.uint32(58).fork(), _depth + 1).ldelim();
            if (message.punchHoleRequest != null && $Object.hasOwnProperty.call(message, "punchHoleRequest"))
                $root.hbb.PunchHoleRequest.encode(message.punchHoleRequest, writer.uint32(66).fork(), _depth + 1).ldelim();
            if (message.punchHole != null && $Object.hasOwnProperty.call(message, "punchHole"))
                $root.hbb.PunchHole.encode(message.punchHole, writer.uint32(74).fork(), _depth + 1).ldelim();
            if (message.punchHoleSent != null && $Object.hasOwnProperty.call(message, "punchHoleSent"))
                $root.hbb.PunchHoleSent.encode(message.punchHoleSent, writer.uint32(82).fork(), _depth + 1).ldelim();
            if (message.punchHoleResponse != null && $Object.hasOwnProperty.call(message, "punchHoleResponse"))
                $root.hbb.PunchHoleResponse.encode(message.punchHoleResponse, writer.uint32(90).fork(), _depth + 1).ldelim();
            if (message.fetchLocalAddr != null && $Object.hasOwnProperty.call(message, "fetchLocalAddr"))
                $root.hbb.FetchLocalAddr.encode(message.fetchLocalAddr, writer.uint32(98).fork(), _depth + 1).ldelim();
            if (message.localAddr != null && $Object.hasOwnProperty.call(message, "localAddr"))
                $root.hbb.LocalAddr.encode(message.localAddr, writer.uint32(106).fork(), _depth + 1).ldelim();
            if (message.configureUpdate != null && $Object.hasOwnProperty.call(message, "configureUpdate"))
                $root.hbb.ConfigUpdate.encode(message.configureUpdate, writer.uint32(114).fork(), _depth + 1).ldelim();
            if (message.registerPk != null && $Object.hasOwnProperty.call(message, "registerPk"))
                $root.hbb.RegisterPk.encode(message.registerPk, writer.uint32(122).fork(), _depth + 1).ldelim();
            if (message.registerPkResponse != null && $Object.hasOwnProperty.call(message, "registerPkResponse"))
                $root.hbb.RegisterPkResponse.encode(message.registerPkResponse, writer.uint32(130).fork(), _depth + 1).ldelim();
            if (message.softwareUpdate != null && $Object.hasOwnProperty.call(message, "softwareUpdate"))
                $root.hbb.SoftwareUpdate.encode(message.softwareUpdate, writer.uint32(138).fork(), _depth + 1).ldelim();
            if (message.requestRelay != null && $Object.hasOwnProperty.call(message, "requestRelay"))
                $root.hbb.RequestRelay.encode(message.requestRelay, writer.uint32(146).fork(), _depth + 1).ldelim();
            if (message.relayResponse != null && $Object.hasOwnProperty.call(message, "relayResponse"))
                $root.hbb.RelayResponse.encode(message.relayResponse, writer.uint32(154).fork(), _depth + 1).ldelim();
            if (message.testNatRequest != null && $Object.hasOwnProperty.call(message, "testNatRequest"))
                $root.hbb.TestNatRequest.encode(message.testNatRequest, writer.uint32(162).fork(), _depth + 1).ldelim();
            if (message.testNatResponse != null && $Object.hasOwnProperty.call(message, "testNatResponse"))
                $root.hbb.TestNatResponse.encode(message.testNatResponse, writer.uint32(170).fork(), _depth + 1).ldelim();
            if (message.peerDiscovery != null && $Object.hasOwnProperty.call(message, "peerDiscovery"))
                $root.hbb.PeerDiscovery.encode(message.peerDiscovery, writer.uint32(178).fork(), _depth + 1).ldelim();
            if (message.onlineRequest != null && $Object.hasOwnProperty.call(message, "onlineRequest"))
                $root.hbb.OnlineRequest.encode(message.onlineRequest, writer.uint32(186).fork(), _depth + 1).ldelim();
            if (message.onlineResponse != null && $Object.hasOwnProperty.call(message, "onlineResponse"))
                $root.hbb.OnlineResponse.encode(message.onlineResponse, writer.uint32(194).fork(), _depth + 1).ldelim();
            if (message.keyExchange != null && $Object.hasOwnProperty.call(message, "keyExchange"))
                $root.hbb.KeyExchange.encode(message.keyExchange, writer.uint32(202).fork(), _depth + 1).ldelim();
            if (message.hc != null && $Object.hasOwnProperty.call(message, "hc"))
                $root.hbb.HealthCheck.encode(message.hc, writer.uint32(210).fork(), _depth + 1).ldelim();
            if (message.httpProxyRequest != null && $Object.hasOwnProperty.call(message, "httpProxyRequest"))
                $root.hbb.HttpProxyRequest.encode(message.httpProxyRequest, writer.uint32(218).fork(), _depth + 1).ldelim();
            if (message.httpProxyResponse != null && $Object.hasOwnProperty.call(message, "httpProxyResponse"))
                $root.hbb.HttpProxyResponse.encode(message.httpProxyResponse, writer.uint32(226).fork(), _depth + 1).ldelim();
            if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
                for (let i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        RendezvousMessage.decode = function (reader, length, _end, _depth, _target) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.hbb.RendezvousMessage();
            while (reader.pos < end) {
                let start = reader.pos;
                let tag = reader.tag();
                if (tag === _end) {
                    _end = $undefined;
                    break;
                }
                let wireType = tag & 7;
                switch (tag >>>= 3) {
                case 6: {
                        if (wireType !== 2)
                            break;
                        message.registerPeer = $root.hbb.RegisterPeer.decode(reader, reader.uint32(), $undefined, _depth + 1, message.registerPeer);
                        message.union = "registerPeer";
                        continue;
                    }
                case 7: {
                        if (wireType !== 2)
                            break;
                        message.registerPeerResponse = $root.hbb.RegisterPeerResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.registerPeerResponse);
                        message.union = "registerPeerResponse";
                        continue;
                    }
                case 8: {
                        if (wireType !== 2)
                            break;
                        message.punchHoleRequest = $root.hbb.PunchHoleRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.punchHoleRequest);
                        message.union = "punchHoleRequest";
                        continue;
                    }
                case 9: {
                        if (wireType !== 2)
                            break;
                        message.punchHole = $root.hbb.PunchHole.decode(reader, reader.uint32(), $undefined, _depth + 1, message.punchHole);
                        message.union = "punchHole";
                        continue;
                    }
                case 10: {
                        if (wireType !== 2)
                            break;
                        message.punchHoleSent = $root.hbb.PunchHoleSent.decode(reader, reader.uint32(), $undefined, _depth + 1, message.punchHoleSent);
                        message.union = "punchHoleSent";
                        continue;
                    }
                case 11: {
                        if (wireType !== 2)
                            break;
                        message.punchHoleResponse = $root.hbb.PunchHoleResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.punchHoleResponse);
                        message.union = "punchHoleResponse";
                        continue;
                    }
                case 12: {
                        if (wireType !== 2)
                            break;
                        message.fetchLocalAddr = $root.hbb.FetchLocalAddr.decode(reader, reader.uint32(), $undefined, _depth + 1, message.fetchLocalAddr);
                        message.union = "fetchLocalAddr";
                        continue;
                    }
                case 13: {
                        if (wireType !== 2)
                            break;
                        message.localAddr = $root.hbb.LocalAddr.decode(reader, reader.uint32(), $undefined, _depth + 1, message.localAddr);
                        message.union = "localAddr";
                        continue;
                    }
                case 14: {
                        if (wireType !== 2)
                            break;
                        message.configureUpdate = $root.hbb.ConfigUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.configureUpdate);
                        message.union = "configureUpdate";
                        continue;
                    }
                case 15: {
                        if (wireType !== 2)
                            break;
                        message.registerPk = $root.hbb.RegisterPk.decode(reader, reader.uint32(), $undefined, _depth + 1, message.registerPk);
                        message.union = "registerPk";
                        continue;
                    }
                case 16: {
                        if (wireType !== 2)
                            break;
                        message.registerPkResponse = $root.hbb.RegisterPkResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.registerPkResponse);
                        message.union = "registerPkResponse";
                        continue;
                    }
                case 17: {
                        if (wireType !== 2)
                            break;
                        message.softwareUpdate = $root.hbb.SoftwareUpdate.decode(reader, reader.uint32(), $undefined, _depth + 1, message.softwareUpdate);
                        message.union = "softwareUpdate";
                        continue;
                    }
                case 18: {
                        if (wireType !== 2)
                            break;
                        message.requestRelay = $root.hbb.RequestRelay.decode(reader, reader.uint32(), $undefined, _depth + 1, message.requestRelay);
                        message.union = "requestRelay";
                        continue;
                    }
                case 19: {
                        if (wireType !== 2)
                            break;
                        message.relayResponse = $root.hbb.RelayResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.relayResponse);
                        message.union = "relayResponse";
                        continue;
                    }
                case 20: {
                        if (wireType !== 2)
                            break;
                        message.testNatRequest = $root.hbb.TestNatRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.testNatRequest);
                        message.union = "testNatRequest";
                        continue;
                    }
                case 21: {
                        if (wireType !== 2)
                            break;
                        message.testNatResponse = $root.hbb.TestNatResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.testNatResponse);
                        message.union = "testNatResponse";
                        continue;
                    }
                case 22: {
                        if (wireType !== 2)
                            break;
                        message.peerDiscovery = $root.hbb.PeerDiscovery.decode(reader, reader.uint32(), $undefined, _depth + 1, message.peerDiscovery);
                        message.union = "peerDiscovery";
                        continue;
                    }
                case 23: {
                        if (wireType !== 2)
                            break;
                        message.onlineRequest = $root.hbb.OnlineRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.onlineRequest);
                        message.union = "onlineRequest";
                        continue;
                    }
                case 24: {
                        if (wireType !== 2)
                            break;
                        message.onlineResponse = $root.hbb.OnlineResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.onlineResponse);
                        message.union = "onlineResponse";
                        continue;
                    }
                case 25: {
                        if (wireType !== 2)
                            break;
                        message.keyExchange = $root.hbb.KeyExchange.decode(reader, reader.uint32(), $undefined, _depth + 1, message.keyExchange);
                        message.union = "keyExchange";
                        continue;
                    }
                case 26: {
                        if (wireType !== 2)
                            break;
                        message.hc = $root.hbb.HealthCheck.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hc);
                        message.union = "hc";
                        continue;
                    }
                case 27: {
                        if (wireType !== 2)
                            break;
                        message.httpProxyRequest = $root.hbb.HttpProxyRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.httpProxyRequest);
                        message.union = "httpProxyRequest";
                        continue;
                    }
                case 28: {
                        if (wireType !== 2)
                            break;
                        message.httpProxyResponse = $root.hbb.HttpProxyResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.httpProxyResponse);
                        message.union = "httpProxyResponse";
                        continue;
                    }
                }
                reader.skipType(wireType, _depth, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
                }
            }
            if (_end !== $undefined)
                throw $Error("missing end group");
            return message;
        };

        RendezvousMessage.fromObject = function (object, _depth) {
            if (object instanceof $root.hbb.RendezvousMessage)
                return object;
            if (!$util.isObject(object))
                throw $TypeError(".hbb.RendezvousMessage: object expected");
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let message = new $root.hbb.RendezvousMessage();
            if (object.registerPeer != null) {
                if (!$util.isObject(object.registerPeer))
                    throw $TypeError(".hbb.RendezvousMessage.registerPeer: object expected");
                message.registerPeer = $root.hbb.RegisterPeer.fromObject(object.registerPeer, _depth + 1);
            }
            if (object.registerPeerResponse != null) {
                if (!$util.isObject(object.registerPeerResponse))
                    throw $TypeError(".hbb.RendezvousMessage.registerPeerResponse: object expected");
                message.registerPeerResponse = $root.hbb.RegisterPeerResponse.fromObject(object.registerPeerResponse, _depth + 1);
            }
            if (object.punchHoleRequest != null) {
                if (!$util.isObject(object.punchHoleRequest))
                    throw $TypeError(".hbb.RendezvousMessage.punchHoleRequest: object expected");
                message.punchHoleRequest = $root.hbb.PunchHoleRequest.fromObject(object.punchHoleRequest, _depth + 1);
            }
            if (object.punchHole != null) {
                if (!$util.isObject(object.punchHole))
                    throw $TypeError(".hbb.RendezvousMessage.punchHole: object expected");
                message.punchHole = $root.hbb.PunchHole.fromObject(object.punchHole, _depth + 1);
            }
            if (object.punchHoleSent != null) {
                if (!$util.isObject(object.punchHoleSent))
                    throw $TypeError(".hbb.RendezvousMessage.punchHoleSent: object expected");
                message.punchHoleSent = $root.hbb.PunchHoleSent.fromObject(object.punchHoleSent, _depth + 1);
            }
            if (object.punchHoleResponse != null) {
                if (!$util.isObject(object.punchHoleResponse))
                    throw $TypeError(".hbb.RendezvousMessage.punchHoleResponse: object expected");
                message.punchHoleResponse = $root.hbb.PunchHoleResponse.fromObject(object.punchHoleResponse, _depth + 1);
            }
            if (object.fetchLocalAddr != null) {
                if (!$util.isObject(object.fetchLocalAddr))
                    throw $TypeError(".hbb.RendezvousMessage.fetchLocalAddr: object expected");
                message.fetchLocalAddr = $root.hbb.FetchLocalAddr.fromObject(object.fetchLocalAddr, _depth + 1);
            }
            if (object.localAddr != null) {
                if (!$util.isObject(object.localAddr))
                    throw $TypeError(".hbb.RendezvousMessage.localAddr: object expected");
                message.localAddr = $root.hbb.LocalAddr.fromObject(object.localAddr, _depth + 1);
            }
            if (object.configureUpdate != null) {
                if (!$util.isObject(object.configureUpdate))
                    throw $TypeError(".hbb.RendezvousMessage.configureUpdate: object expected");
                message.configureUpdate = $root.hbb.ConfigUpdate.fromObject(object.configureUpdate, _depth + 1);
            }
            if (object.registerPk != null) {
                if (!$util.isObject(object.registerPk))
                    throw $TypeError(".hbb.RendezvousMessage.registerPk: object expected");
                message.registerPk = $root.hbb.RegisterPk.fromObject(object.registerPk, _depth + 1);
            }
            if (object.registerPkResponse != null) {
                if (!$util.isObject(object.registerPkResponse))
                    throw $TypeError(".hbb.RendezvousMessage.registerPkResponse: object expected");
                message.registerPkResponse = $root.hbb.RegisterPkResponse.fromObject(object.registerPkResponse, _depth + 1);
            }
            if (object.softwareUpdate != null) {
                if (!$util.isObject(object.softwareUpdate))
                    throw $TypeError(".hbb.RendezvousMessage.softwareUpdate: object expected");
                message.softwareUpdate = $root.hbb.SoftwareUpdate.fromObject(object.softwareUpdate, _depth + 1);
            }
            if (object.requestRelay != null) {
                if (!$util.isObject(object.requestRelay))
                    throw $TypeError(".hbb.RendezvousMessage.requestRelay: object expected");
                message.requestRelay = $root.hbb.RequestRelay.fromObject(object.requestRelay, _depth + 1);
            }
            if (object.relayResponse != null) {
                if (!$util.isObject(object.relayResponse))
                    throw $TypeError(".hbb.RendezvousMessage.relayResponse: object expected");
                message.relayResponse = $root.hbb.RelayResponse.fromObject(object.relayResponse, _depth + 1);
            }
            if (object.testNatRequest != null) {
                if (!$util.isObject(object.testNatRequest))
                    throw $TypeError(".hbb.RendezvousMessage.testNatRequest: object expected");
                message.testNatRequest = $root.hbb.TestNatRequest.fromObject(object.testNatRequest, _depth + 1);
            }
            if (object.testNatResponse != null) {
                if (!$util.isObject(object.testNatResponse))
                    throw $TypeError(".hbb.RendezvousMessage.testNatResponse: object expected");
                message.testNatResponse = $root.hbb.TestNatResponse.fromObject(object.testNatResponse, _depth + 1);
            }
            if (object.peerDiscovery != null) {
                if (!$util.isObject(object.peerDiscovery))
                    throw $TypeError(".hbb.RendezvousMessage.peerDiscovery: object expected");
                message.peerDiscovery = $root.hbb.PeerDiscovery.fromObject(object.peerDiscovery, _depth + 1);
            }
            if (object.onlineRequest != null) {
                if (!$util.isObject(object.onlineRequest))
                    throw $TypeError(".hbb.RendezvousMessage.onlineRequest: object expected");
                message.onlineRequest = $root.hbb.OnlineRequest.fromObject(object.onlineRequest, _depth + 1);
            }
            if (object.onlineResponse != null) {
                if (!$util.isObject(object.onlineResponse))
                    throw $TypeError(".hbb.RendezvousMessage.onlineResponse: object expected");
                message.onlineResponse = $root.hbb.OnlineResponse.fromObject(object.onlineResponse, _depth + 1);
            }
            if (object.keyExchange != null) {
                if (!$util.isObject(object.keyExchange))
                    throw $TypeError(".hbb.RendezvousMessage.keyExchange: object expected");
                message.keyExchange = $root.hbb.KeyExchange.fromObject(object.keyExchange, _depth + 1);
            }
            if (object.hc != null) {
                if (!$util.isObject(object.hc))
                    throw $TypeError(".hbb.RendezvousMessage.hc: object expected");
                message.hc = $root.hbb.HealthCheck.fromObject(object.hc, _depth + 1);
            }
            if (object.httpProxyRequest != null) {
                if (!$util.isObject(object.httpProxyRequest))
                    throw $TypeError(".hbb.RendezvousMessage.httpProxyRequest: object expected");
                message.httpProxyRequest = $root.hbb.HttpProxyRequest.fromObject(object.httpProxyRequest, _depth + 1);
            }
            if (object.httpProxyResponse != null) {
                if (!$util.isObject(object.httpProxyResponse))
                    throw $TypeError(".hbb.RendezvousMessage.httpProxyResponse: object expected");
                message.httpProxyResponse = $root.hbb.HttpProxyResponse.fromObject(object.httpProxyResponse, _depth + 1);
            }
            return message;
        };

        RendezvousMessage.toObject = function (message, options, _depth) {
            if (!options)
                options = {};
            if (_depth === $undefined)
                _depth = 0;
            if (_depth > $util.recursionLimit)
                throw $Error("max depth exceeded");
            let object = {};
            if (message.registerPeer != null && $Object.hasOwnProperty.call(message, "registerPeer")) {
                object.registerPeer = $root.hbb.RegisterPeer.toObject(message.registerPeer, options, _depth + 1);
                if (options.oneofs)
                    object.union = "registerPeer";
            }
            if (message.registerPeerResponse != null && $Object.hasOwnProperty.call(message, "registerPeerResponse")) {
                object.registerPeerResponse = $root.hbb.RegisterPeerResponse.toObject(message.registerPeerResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "registerPeerResponse";
            }
            if (message.punchHoleRequest != null && $Object.hasOwnProperty.call(message, "punchHoleRequest")) {
                object.punchHoleRequest = $root.hbb.PunchHoleRequest.toObject(message.punchHoleRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "punchHoleRequest";
            }
            if (message.punchHole != null && $Object.hasOwnProperty.call(message, "punchHole")) {
                object.punchHole = $root.hbb.PunchHole.toObject(message.punchHole, options, _depth + 1);
                if (options.oneofs)
                    object.union = "punchHole";
            }
            if (message.punchHoleSent != null && $Object.hasOwnProperty.call(message, "punchHoleSent")) {
                object.punchHoleSent = $root.hbb.PunchHoleSent.toObject(message.punchHoleSent, options, _depth + 1);
                if (options.oneofs)
                    object.union = "punchHoleSent";
            }
            if (message.punchHoleResponse != null && $Object.hasOwnProperty.call(message, "punchHoleResponse")) {
                object.punchHoleResponse = $root.hbb.PunchHoleResponse.toObject(message.punchHoleResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "punchHoleResponse";
            }
            if (message.fetchLocalAddr != null && $Object.hasOwnProperty.call(message, "fetchLocalAddr")) {
                object.fetchLocalAddr = $root.hbb.FetchLocalAddr.toObject(message.fetchLocalAddr, options, _depth + 1);
                if (options.oneofs)
                    object.union = "fetchLocalAddr";
            }
            if (message.localAddr != null && $Object.hasOwnProperty.call(message, "localAddr")) {
                object.localAddr = $root.hbb.LocalAddr.toObject(message.localAddr, options, _depth + 1);
                if (options.oneofs)
                    object.union = "localAddr";
            }
            if (message.configureUpdate != null && $Object.hasOwnProperty.call(message, "configureUpdate")) {
                object.configureUpdate = $root.hbb.ConfigUpdate.toObject(message.configureUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.union = "configureUpdate";
            }
            if (message.registerPk != null && $Object.hasOwnProperty.call(message, "registerPk")) {
                object.registerPk = $root.hbb.RegisterPk.toObject(message.registerPk, options, _depth + 1);
                if (options.oneofs)
                    object.union = "registerPk";
            }
            if (message.registerPkResponse != null && $Object.hasOwnProperty.call(message, "registerPkResponse")) {
                object.registerPkResponse = $root.hbb.RegisterPkResponse.toObject(message.registerPkResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "registerPkResponse";
            }
            if (message.softwareUpdate != null && $Object.hasOwnProperty.call(message, "softwareUpdate")) {
                object.softwareUpdate = $root.hbb.SoftwareUpdate.toObject(message.softwareUpdate, options, _depth + 1);
                if (options.oneofs)
                    object.union = "softwareUpdate";
            }
            if (message.requestRelay != null && $Object.hasOwnProperty.call(message, "requestRelay")) {
                object.requestRelay = $root.hbb.RequestRelay.toObject(message.requestRelay, options, _depth + 1);
                if (options.oneofs)
                    object.union = "requestRelay";
            }
            if (message.relayResponse != null && $Object.hasOwnProperty.call(message, "relayResponse")) {
                object.relayResponse = $root.hbb.RelayResponse.toObject(message.relayResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "relayResponse";
            }
            if (message.testNatRequest != null && $Object.hasOwnProperty.call(message, "testNatRequest")) {
                object.testNatRequest = $root.hbb.TestNatRequest.toObject(message.testNatRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "testNatRequest";
            }
            if (message.testNatResponse != null && $Object.hasOwnProperty.call(message, "testNatResponse")) {
                object.testNatResponse = $root.hbb.TestNatResponse.toObject(message.testNatResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "testNatResponse";
            }
            if (message.peerDiscovery != null && $Object.hasOwnProperty.call(message, "peerDiscovery")) {
                object.peerDiscovery = $root.hbb.PeerDiscovery.toObject(message.peerDiscovery, options, _depth + 1);
                if (options.oneofs)
                    object.union = "peerDiscovery";
            }
            if (message.onlineRequest != null && $Object.hasOwnProperty.call(message, "onlineRequest")) {
                object.onlineRequest = $root.hbb.OnlineRequest.toObject(message.onlineRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "onlineRequest";
            }
            if (message.onlineResponse != null && $Object.hasOwnProperty.call(message, "onlineResponse")) {
                object.onlineResponse = $root.hbb.OnlineResponse.toObject(message.onlineResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "onlineResponse";
            }
            if (message.keyExchange != null && $Object.hasOwnProperty.call(message, "keyExchange")) {
                object.keyExchange = $root.hbb.KeyExchange.toObject(message.keyExchange, options, _depth + 1);
                if (options.oneofs)
                    object.union = "keyExchange";
            }
            if (message.hc != null && $Object.hasOwnProperty.call(message, "hc")) {
                object.hc = $root.hbb.HealthCheck.toObject(message.hc, options, _depth + 1);
                if (options.oneofs)
                    object.union = "hc";
            }
            if (message.httpProxyRequest != null && $Object.hasOwnProperty.call(message, "httpProxyRequest")) {
                object.httpProxyRequest = $root.hbb.HttpProxyRequest.toObject(message.httpProxyRequest, options, _depth + 1);
                if (options.oneofs)
                    object.union = "httpProxyRequest";
            }
            if (message.httpProxyResponse != null && $Object.hasOwnProperty.call(message, "httpProxyResponse")) {
                object.httpProxyResponse = $root.hbb.HttpProxyResponse.toObject(message.httpProxyResponse, options, _depth + 1);
                if (options.oneofs)
                    object.union = "httpProxyResponse";
            }
            return object;
        };

        RendezvousMessage.prototype.toJSON = function() {
            return RendezvousMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        return RendezvousMessage;
    })();

    return hbb;
})();

export {
  $root as default
};
